agentic ai 3회차 필기 8/10

tool calling 함
모델이 함수 실행 안한다고?? 근데 그럼 뭐하는거임
아 json 뱉는거구나 실행은 내가
메뉴판 비유 -> 주문은 모델 요리는 나 (이거 좋음)

=== 스키마 ===
tools = [{
    "type": "function",
    "function": {
        "name": "extract_tasks",
        "description": "회의록에서 할 일과 담당자를 추출한다",
        "parameters": {
            "type": "object",
            "properties": {
                "tasks": {
                    "type": "array",
                    "items": {
                        "type": "object",
                        "propertie
(뒤에 못따라감 자료 5p 참고)

required 없으면 모델이 도구 안부름????? 왜???
-> 이거 시험나올듯 강사님이 두번 말함
description 구체적으로 = 호출률 올라감

[스샷 붙임 - 스키마 전체 화면]


ReAct
Thought -> Action -> Observation -> 다시 Thought
반복하는거

Observation이 뭔데 ????? 
별도 함수인가? 아니라고 하신것같은데
role tool 어쩌구...  ????

** 이거 안하면 무한루프 난다고 강조하심 **


루프코드
for turn in range(MAX_TURNS):
    resp = client.chat.completions.create(...)
    msg = resp.choices[0].message
    if not msg.tool_calls:
        return msg.content
    messages.append(msg)
    for call in msg.tool_calls:
        result = TOOLS[...](...)
        messages.append({"role":"tool", ...})

-> if not msg.tool_calls 이게 끝났다는 신호?? 
왜 도구 안부르면 끝난거지 ???


종료조건 4개
1 자연종료
2 max turns  개발중엔 3~5로
3 반복감지 같은도구 같은인자
4 목표검증
-> 2번 안넣어서 사고 제일 많이 난다고

[스샷 붙임 - 종료조건 표]


실습
회의록 800자 -> task owner due json
논의만 한거 제외해야됨
담당자 없으면 지어내면 안됨
집에서 다시 돌려보기 (코드 못따라감)

MAX_TURNS = 5
def extract_tasks(tasks):
    return {"count": len(tasks), "tasks": tasks}


---
아 그리고 담당자 없을때 어떻게 처리하라고 했더라
못들음
