# Day 1 · 덱 A (1~19장) — 설명란

> **업로드할 파일 2개**
> ① `00_디자인기준판\디자인기준판.pdf`
> ② `Day1_덱A_[첨부]원고.md` ← **덱 A 범위만 담긴 원고. 다른 덱 원고를 같이 올리지 마라.**
>
> ※ **NotebookLM이 받는 형식** — PDF · txt · **Markdown(.md)**. HTML·PNG는 안 된다.
> **맞춤설정** — 언어 한국어 / 길이 기본값
> **설명 칸** — 아래 ▼부터 ▲까지 전부 복사해 붙여넣기
> **생성 결과** — 19장 → PPT로 내려받아 확정. 머리글·페이지 번호는 PPT 슬라이드 마스터에서 일괄 처리한다.

---

▼ 복붙 시작 ▼

**정확히 19장**의 슬라이드를 만들어라. 아래 1~19페이지 지정을 **한 지정 = 한 장**으로 따르고 합치거나 나누지 마라.

## 이 자료에는 발표 대본이 없다

강사가 읽을 대본이 따로 없다. **슬라이드만 보고도 뜻이 통해야 한다.**

그래서 이렇게 만든다.

- **글로 다 쓰지 마라.** 화면을 글자로 채우면 읽느라 강의를 못 듣는다.
- **그렇다고 비우지도 마라.** 낱말만 덩그러니 있으면 대본 없이는 아무 뜻도 안 통한다.
- **답은 삽화다.** 그림이 뜻을 지고, 글은 그림이 못 하는 말만 한다.
- **그림만으로 뜻이 안 통하면 그때 라벨을 박아라.** 보면 아는 물건에는 붙이지 않는다. 라벨은 설명이지 되풀이가 아니다.

## 범위 — 이걸 어기면 실패다

이 자료는 **55분 강의의 앞 19장**이다. 뒤에 덱 B(20~38장)가 이어진다.

- 이 덱은 **「4일 지도」와 「그런데 그 판단을 LLM에게 그냥 물어보면 될까요?」라는 질문에서 끝난다.**
- **그 질문에 답하지 마라.** 에이전트가 무엇인지, 뇌·도구·루프·자율성, 챗봇·코파일럿 비교, ReAct, 디자인 패턴, 시뮬레이터 무대 소개, 실습 예고 — **전부 덱 B 몫이다. 한 글자도 꺼내지 마라.**
- 업로드한 원고에도 그 내용은 없다. **없는 것을 만들어 넣지 마라.**
- **19장을 넘겨 만들지 마라.** 19페이지가 마지막이다.
- 19장 안에 덱 B 내용을 욱여넣느라 **아래 지정된 장을 빼먹는 일이 절대 없어야 한다.** 지정된 19장을 전부 만들어라.

## 지시문 읽는 법

각 페이지의 **「화면 글자」** 에 적힌 문장은 **최종 문구**다.

- **그대로 옮겨 써라.** 다듬지 말고, 요약하지 말고, 다른 말로 바꾸지 마라.
- **적힌 것 외의 문장을 추가하지 마라.**
- 백틱(`) 안이 화면에 나갈 글자다. 백틱 자체는 쓰지 마라.
- **「레이아웃」** 은 기준판 PDF의 몇 번째 장 형식인지 지정한 것이다.
- **「삽화」** 는 **무엇이 보여야 하는지**와 **그림 안에 박을 라벨**을 적은 것이다. 그림의 모양은 당신이 정하라.
- **「금지」** 를 반드시 지켜라. 특히 이야기를 미리 흘리지 마라.
- **한 페이지 지정을 두 페이지로 쪼개거나, 두 지정을 한 페이지에 합치지 마라.**

# 기준판 8종 — 레이아웃 번호

| 번호 | 형식 | 생김새 |
|---|---|---|
| **1** | 표지형 | 화면 전체 짙은 남색, 왼쪽에 흰 제목, 제목 아래 짧은 주황 가로선, 오른쪽에 옅은 동심원 |
| **2** | 구간 표지형 | **순백 바탕.** 왼쪽에 큰 삽화, 그 옆에 큰 낱말 하나. 화면 아래 가는 선 위에 결론 한 줄 |
| **3** | 큰 그림 + 짧은 글 | **왼쪽 절반이 큰 삽화**, 오른쪽에 문장 두세 줄. 아래 가는 선 위에 결론 한 줄 |
| **4** | 번호 레일형 | 항목을 가로선으로 나누고 **각 항목 왼쪽에 작은 그림 하나씩**, 그 옆에 번호 |
| **5** | 나란히 비교형 | **배경을 나누지 마라.** 순백 바탕에 **가는 세로선 하나**로만 좌우를 가른다. 양쪽에 그림 하나씩 |
| **6** | 표형 | 머리행만 남색 배경 흰 글씨, 가로선만, 세로줄 없음 |
| **7** | 낱말 나열형 | 낱말 3~4개를 가로로, **각 낱말 위에 큰 그림 하나씩.** 아래 한 줄 풀이 |
| **8** | 가는 선 그래프형 | 얇은 주황 곡선 하나 + 남색 점선 기준선, 축 눈금·격자 없음 |

# 삽화 — 이 자료의 주인공이다

**화면의 절반쯤을 삽화가 차지해야 한다.** 글이 화면을 채우면 실패다.

- **그림만으로 뜻이 안 통하는 자리에는 라벨을 박아라.** 화살표로 가리키고 **낱말로 짧게** 붙인다 — `무인으로 도는 16시간`, `임계값 80℃` 같은 것이다. 대본이 없으므로 **그림이 스스로 설명해야 한다.**
- **한 장에 큰 그림 하나.** 작은 것 여럿보다 큰 것 하나가 낫다.
- 색은 **남색 계열과 주황**으로 맞춘다. 강조하고 싶은 한 곳만 주황.
- **설명하는 그림을 그려도 된다.** 단계를 늘어놓거나, 흐름을 보여 주거나, 두 상태를 견주는 그림 전부 괜찮다. 입체감이나 옅은 음영도 괜찮다.
- **읽는 데 방해가 되면 뺀다.** 글자 위에 겹치면 안 된다.
- 사진은 쓰지 마라. 제품 로고와 회사 이름도 넣지 마라.
- **그림 안에도 격자·모눈·청사진 무늬를 깔지 마라.** 그림 뒤에 네모난 색 판을 두지 마라. 그림은 흰 바탕 위에 선으로만 선다.
- **뜻 없는 숫자나 글자를 배경처럼 채우지 마라.** 0과 1을 흩뿌리거나 알 수 없는 글자를 늘어놓으면 읽기만 방해한다. 그림에 들어가는 글자는 **읽으라고 넣는 라벨**뿐이다.
- **그림과 라벨을 굵은 화살표로 잇지 마라.** 가는 선으로 가리킨다.

**판단 기준** — 그 장에서 말하려는 것이 **그림만 보고도 짐작되면 성공**이고, 눈길만 끌고 뜻을 안 담으면 실패다.

# 한 가지 사실은 화면에 한 번만

같은 말을 두 번 하면 화면만 차지하고 뜻은 늘지 않는다. 대본이 없다고 해서 되풀이하는 것이 아니다.

- **제목·설명문·본문·결론·라벨이 서로 다른 말을 해야 한다.** 앞에서 한 말을 뒤에서 다시 하지 마라.
- **라벨은 「그림만으로는 알 수 없는 것」에만 붙인다.** 시계에 칠해진 구역은 라벨이 없으면 뜻을 모르니 붙이고, 에어컨은 보면 아는 물건이니 붙이지 않는다.
- **본문에 이미 있는 문장을 라벨로 옮겨 적지 마라.** 그건 설명이 아니라 반복이다.
- 라벨을 붙일 때는 **문장이 아니라 낱말**로 짧게 쓴다.
- 결론 한 줄은 본문의 요약이 아니라 **본문이 하지 않은 한 걸음**이어야 한다.
- **같은 문장을 화면에 두 번 찍지 마라.** 지난번에 마무리 줄이 똑같이 두 줄로 나왔다. 한 문장은 한 번만 나온다.

# 글자 크기 — 비율표를 그대로 지켜라

**본문 글자 크기를 1이라 하고, 나머지를 그 배수로 맞춰라.** 아래 표를 어림잡지 말고 그대로 따르라.

| 요소 | 본문 대비 배수 | 화면 높이에서 차지하는 몫 | 참고 pt (16:9 기준) |
|---|---|---|---|
| 표지 제목 (1번 형식만) | **2.1배** | 12분의 1 이하 | 37pt |
| **제목** | **1.65배** | **16분의 1 이하** | 29pt |
| 큰 낱말 (폐루프 · LLM · 50년) | **2.0배** | **12분의 1 이하** | 35pt |
| 낱말 나열형의 낱말 | 1.45배 | 20분의 1 이하 | 25pt |
| 소제목 · 표 항목명 | 1.1배 | — | 19pt |
| 제목 아래 설명문 | **1.0배** | — | 18pt |
| **본문** | **1 (기준)** | — | **18pt** |
| 삽화 안 라벨 | 0.8배 | — | 14pt |

- **제목은 크기가 아니라 굵기로 눈에 띈다.** 본문의 **1.65배를 넘기지 마라.** 제목이 화면을 잡아먹으면 그림 자리가 없어진다.
- **큰 낱말도 크게 만들지 마라.** 「폐루프」·「LLM」·「50년」은 **제목보다 조금만 크다.** 화면 높이의 **12분의 1**을 넘지 마라.
  → 지난번에 「볼 사람이 없었을 뿐이다」가 **화면 절반**을 차지해 다른 것이 다 밀려났다. 그 장은 실패다.
  → 큰 낱말이 **두 줄로 넘어가면 이미 너무 큰 것이다.** 한 줄에 들어가게 줄여라.
- **숫자를 키우지 마라.** 숫자는 문장 안이나 그림 라벨 안에서 다룬다. 숫자 하나가 주인공인 장을 만들지 마라.
- **본문을 작게 만들지 마라.** 강의실 뒷자리에서 읽혀야 한다. **줄이는 것은 큰 글자지 본문이 아니다.**
- 제목 아래에는 짧은 부제가 아니라 **본문과 같은 크기의 설명문 한두 줄**을 둔다.

# 줄간격

- **문단과 문단 사이를 한 줄 띄우지 마라.** 문단 간격이 글자 크기의 두 배를 넘으면 빈 줄이 들어간 것처럼 보인다.
- **한 호흡으로 읽히는 두 문장은 줄바꿈으로 잇는다.** 문단을 나누지 마라.
- 줄 사이는 글자 크기의 1.5배쯤이 적당하다.

# 화면 여백

- 글자가 화면 끝까지 가지 않게 여백을 둔다.
- **화면 아래를 비워 두지 마라.** 아래 3분의 1이 텅 비면 잘못 만든 것이다. 그 자리는 삽화를 키우거나 결론 한 줄로 채운다.
- 제목·본문·삽화가 **서로 겹치지 않게** 배치한다.
- 왼쪽 시작선을 장마다 같게 맞춰라. 제목과 본문이 같은 세로선에서 시작해야 한다.

# 배경과 색

- **모든 장의 바탕은 순백 #FFFFFF다.** 표지 형식(1번) 한 종류만 화면 전체가 짙은 남색이다. 그 둘뿐이고 세 번째 바탕은 없다.
- 화면을 좌우나 상하로 갈라 한쪽만 다른 색으로 칠하는 일이 없어야 한다. 나누는 것은 **가는 회색 선 하나**다.
- 화면 아래에 색 띠를 두르지 마라.
- 배경에 격자·무늬·워터마크를 깔지 마라. 배경에 옅게 깔린 큰 숫자도 넣지 마라.
- 주조색 **남색 #12305A**, 강조색 **주황 #D4541E**, 부연은 회색 #6B7280. 이 셋과 검정·흰색뿐이다.
- 강조는 **아주 연한 주황 밑칠 #FBE3D6**이다. **형광펜처럼 진한 주황을 쓰지 마라.** 핵심 어절 몇 글자에만, 한 장에 한 곳.
- **한 가지 예외** — 표(6번 형식) 안에서 한 행만 **아주 연한 남색 #EEF3F9**으로 칠하는 것은 허용한다. 표 안에서만이고, 슬라이드 바탕과는 무관하다.

## 무대 설정 (4일 내내 고정 — 여기 없는 사실을 지어내지 마라)
- 무대는 경남 창원의 **가상** 정밀기계 부품 제조사 **「K-정밀」**. 실재 회사가 아니다.
- **CNC 설비 6대**(EQ-01 ~ EQ-06)와 **자율이동로봇(AMR) 2대**가 있다.
- CNC가 내보내는 값 — **온도 · 진동 · rpm · 가동상태**. AMR — **위치 · 배터리 · 적재상태**. 값은 **1초마다** 바뀐다.
- 이 공장은 전부 소프트웨어로 만든 가짜이며, 강사가 미리 만들어 둔 **팩토리 시뮬레이터** 안에서만 존재한다.
- **K-정밀은 주간 근무만 하고 야간에는 무인으로 돌아간다.** 설비는 스물네 시간 돌지만 사람은 주간에만 있다.
- 4일은 **「보기 → 알기 → 움직이기」**. 오늘은 **보기**다.
- 이 사업에는 **참여기업 15개사**, **채용약정 4개사**가 붙어 있다.
- **되돌아오는 사건** — 새벽 3시, EQ-03의 온도가 시간당 0.5도씩 네 시간에 걸쳐 62도에서 64도로 올랐고 아무도 몰랐다. 그날 밤 깎은 부품은 전량 폐기됐다. 임계값 80도는 한 번도 넘지 않았다.
- 위에 없는 **숫자·회사명·제품명·사건을 지어내지 마라.** "혁신적인", "놀라운" 같은 홍보성 표현도 쓰지 마라.
- 듣는 사람은 컴퓨터공학 전공이지만 **코딩은 거의 모른다고 가정**한다. 낯선 말은 처음 나오는 자리에서 풀고 나서 써라.

---

# 1페이지

**레이아웃** 1번 (표지형)

**화면 글자**
- 윗줄: `DAY 1 · 보기`
- 제목: `공장을 보는 눈을 만든다`
- 부제: `경남대 RISE 피지컬AI 사관학교 · 8월 Agentic AI 특강`
- 하단 1행: `피지컬 AI란 무엇인가 · 왜 하필 지금인가 · 에이전트란 무엇인가`
- 하단 2행(굵게): `오늘, 코드는 한 줄도 치지 않습니다.`

**삽화** 오른쪽에 옅은 동심원 몇 개. 그 외 장식은 넣지 마라.
**금지** 흰 배경으로 만들지 마라 — **화면 전체가 짙은 남색이어야 한다.** 공장·로봇 그림 금지.

---

# 2페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `새벽 3시, 아무도 없는 공장에서`
- 설명문: `창원 K-정밀. 밤에도 공장은 돌고, 사람은 없다.`
- 오른쪽 본문 3행:
  - `새벽 3시, EQ-03이라는 CNC 한 대의 온도가 조금씩 오르기 시작했다.`
  - `CNC는 컴퓨터 수치제어 공작기계. 창원 공장에 가장 흔하게 깔린 장비다.`
  - `아침 7시, EQ-03은 여전히 돌고 있었다. 멈추지도, 불이 나지도, 경고음이 울리지도 않았다.`
- 아래 가는 선 위 결론 1행: `사람이 옆에 서 있었더라도 몰랐을 속도다.`

**삽화** 왼쪽 절반에 **밤의 시간을 알려 주는 큰 그림** — 시계와 달 같은 것. 새벽 3시라는 것이 보여야 한다.
그림 안에 라벨 하나만 박아라 — `한 시간에 0.5도씩`. 시각은 시계 바늘이 말하므로 따로 적지 마라.
**금지** 온도 그래프. **부품 폐기 이야기를 여기서 하지 마라 — 3페이지 몫이다.** 숫자 `0.5`를 화면 절반만 하게 키우지 마라.

---

# 3페이지

**레이아웃** 8번 (가는 선 그래프형)

**화면 글자**
- 제목: `문제는 그날 오후에 드러났습니다`
- 설명문: `밤새 아무도 몰랐고, 아무 일도 없어 보였습니다.`
- 본문 2행:
  - `밤새 깎아 낸 부품의 치수가 미세하게 틀어져 있었다. 하루치 생산분을 통째로 버렸다.`
  - `설비가 고장 난 것도, 알람이 울린 것도 아니다. 그냥 조용히 나빠졌다.`
- 하단 강조(핵심 어절에만 아주 연한 주황 밑칠): `오늘 물을 것은 "왜 온도가 올랐나"가 아니다. 왜 네 시간 동안 아무도 몰랐나 — 이것이다.`

**삽화** 화면 오른쪽 절반에 62에서 64로 완만히 오르는 얇은 주황 곡선 하나. **곡선을 크게 그려라.**
곡선 안에 라벨을 박아라 — 왼쪽 끝에 `62℃ · 새벽 3시`, 오른쪽 끝에 `64℃ · 아침 7시`.
**금지** 축 눈금, 격자선, **임계선** — 임계선은 9페이지에서 처음 나온다.

---

# 4페이지

**레이아웃** 2번 (구간 표지형) — **바탕은 순백이다**

**화면 글자**
- 제목: `센서가 없어서 생긴 사고가 아닙니다`
- 설명문: `EQ-03에는 온도계가 달려 있었고, 값은 계속 기록되고 있었다.`
- 큰 낱말: `볼 사람이 없었을 뿐이다`
- 큰 낱말 아래 1행: `데이터는 1초마다 꼬박꼬박 쌓이고 있었다`
- 아래 가는 선 위 결론 1행: `이유는 셋입니다 — ① 볼 시간이 없다 ② 봐도 알아채지 못한다 ③ 볼 것이 너무 많다`

**삽화** 왼쪽에 **온도계나 계기판 큰 그림.** 「재고는 있었다」가 보여야 한다.
그림 안에 라벨을 박아라 — `1초마다 기록됨`.
**금지** 사람 그림. 세 이유를 여기서 상세히 설명하지 마라 — 다음 세 장이 하나씩 맡는다.

---

# 5페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 좌상단 번호(옅은 남색, 작게): `01`
- 제목: `사람은 볼 시간이 없습니다`
- 설명문: `하루 스물네 시간 가운데, 사람이 지키고 있는 시간은 얼마나 될까요.`
- 오른쪽 본문 2행:
  - `K-정밀은 주간 근무만 하고, 야간에는 무인으로 돌아간다. 새벽 3시가 정확히 그 안에 있다.`
  - `3교대로 사람을 붙여도 야간에는 인원이 줄고 집중도가 떨어진다. 사람을 더 뽑는 것으로는 안 풀린다.`
- 아래 가는 선 위 결론 1행: `사고는 사람이 가장 적은 시간에 납니다.`

**삽화** 왼쪽 절반에 **큰 시계.** 하루 중 무인으로 도는 시간이 얼마나 되는지 **원의 3분의 2가 옅게 칠해져** 눈에 보이게 하라.
그림 안에 라벨 두 개를 박아라 — 칠해진 쪽에 `무인으로 도는 16시간`, 나머지에 `주간 근무 8시간`.
**금지** 숫자 `16`을 화면 절반만 하게 키우지 마라. **뜻이 통하지 않는 문장을 절대 넣지 마라.** 지정한 두 문장 외의 본문을 만들지 마라.

---

# 6페이지

**레이아웃** 3번 (큰 그림 + 짧은 글) — 다만 **그림 자리에 두 숫자를 나란히** 놓는다

**화면 글자**
- 좌상단 번호(옅은 남색, 작게): `02`
- 제목: `봐도 알아채지 못합니다`
- 설명문: `62.1이 62.6으로 바뀌는 것을 보고 "이상하다"고 말할 사람은 없다.`
- 화면 가운데 좌측 수치: `62.1` / 아래 라벨(회색): `새벽 3시`
- 화면 가운데 우측 수치: `62.6` / 아래 라벨(회색): `새벽 4시`
- 두 수치 사이(주황, 수치의 절반 크기): `?`
- 하단 본문 2행:
  - `급격한 변화라면 누구나 안다. 문제는 이 사건이 급격하지 않았다는 것이다.`
  - `화면을 계속 보고 있었더라도 결과는 같았을 것이다.`

**삽화** 이 장은 **두 숫자 자체가 삽화**다. 두 숫자를 크게 놓고 그 사이에 주황 물음표를 둔다. 다른 그림은 넣지 마라.
**금지** 그래프, 화살표. 두 숫자 말고 **다른 그림을 넣지 마라.** 숫자가 화면 높이의 4분의 1을 넘지 않게 하라.

---

# 7페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 좌상단 번호(옅은 남색, 작게): `03`
- 제목: `볼 것이 너무 많습니다`
- 설명문: `한 대만 보면 되는 게 아닙니다. 설비 6대 × 값 4가지 — 숫자 24개가 1초마다 바뀝니다.`
- 오른쪽 본문 2행:
  - `여기에 자율이동로봇 2대의 값까지 더해진다.`
  - `여섯 대를 동시에, 쉬지 않고 봐야 한다. 사람 눈이 따라갈 양이 아니다.`
- 아래 가는 선 위 결론 1행: `한 사람이 감당할 수 있는 양이 아니다.`

**삽화** **설비 6대가 나란히 있고 각 설비에서 값이 네 개씩 흘러나오는 그림.** 숫자가 쏟아진다는 느낌이 보여야 한다.
그림 안에 라벨을 박아라 — 설비에 `EQ-01` … `EQ-06`, 그리고 한쪽에 `온도 · 진동 · rpm · 가동상태`.
**금지** 공장 건물 그림, 로봇 그림. 숫자 `24`를 화면 절반만 하게 키우지 마라. 설비를 **빈 네모와 점**으로만 그리지 마라 — 기계로 보여야 한다.

---

# 8페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `여러분이라면 어떻게 하시겠습니까?`
- 설명문: `아마 가장 먼저 떠오르는 답은 이것일 겁니다 — "온도가 80도를 넘으면 경고음을 울리자."`
- 오른쪽 본문 2행:
  - `공장에서 수십 년째 쓰는 방법이고, 실제로 쓸모도 있다. 갑자기 과열되는 상황은 이걸로 잡힌다.`
  - `잠도 안 자고, 0.5도도 정확히 재고, 여섯 대를 동시에 본다.`
- 아래 가는 선 위 결론 1행: `앞의 세 문제를 한 번에 푸는 것처럼 보입니다.`

**삽화** 왼쪽 절반에 **경고 벨이나 알람 장치의 큰 그림.** 「선을 넘으면 울린다」가 보여야 한다.
그림 안에 라벨을 박아라 — `고정 임계값 80℃`, 그리고 `넘으면 울린다`.
**이 장에 삽화가 없으면 실패다.** 반드시 그려라.

**금지 — 이 장에서 가장 중요하다**
- **주황색을 쓰지 마라.** 남색과 검정만. 라벨도 남색으로.
- **"80도를 넘은 적이 없다"는 말을 절대 쓰지 마라.** 그것은 다음 장의 반전이다.
- **"그런데", "하지만" 으로 시작하는 반전 문장을 넣지 마라.**
- 숫자 `80℃`를 화면 절반만 하게 키우지 마라.
- 이 장은 **"이걸로 풀릴 것 같다"는 기대**로 끝나야 한다.

---

# 9페이지 ★ 반전

**레이아웃** 8번 (가는 선 그래프형) — **화면의 절반 이상을 그래프에 쓴다**

**화면 글자**
- 제목(핵심 어절에만 아주 연한 주황 밑칠): `그런데 80도를 넘은 적이 없습니다`
- 설명문: `선을 넘지 않고도 부품은 망가졌습니다.`
- 본문 3행:
  - `그럼 선을 64도로 낮추면 되지 않나. 그러면 여름철에 정상적으로 돌던 다른 설비들이 하루 종일 울린다.`
  - `경고가 자주 울리면 사람은 그것을 꺼 버린다. 그러면 처음 상태로 돌아간다.`
  - `선을 높이면 못 잡고, 낮추면 다 울린다. 선 하나로는 빠져나갈 길이 없다.`
- 하단 강조(아주 연한 주황 밑칠): `"값이 얼마인가"만 봐서는 부족하다. "값이 어떻게 변해 왔는가"를 봐야 한다.`

**삽화** 화면 **오른쪽 절반**에 그린다. 위쪽에 **남색 점선 가로선**(임계값), 아래에 62→64로 오르는 얇은 주황 곡선.
그림 안에 라벨 셋을 박아라 — 점선 위에 `임계값 80℃`, 곡선 양 끝에 `62℃` `64℃`.
**그리고 두 선 사이의 빈 공간에 세로 화살표를 긋고 `16℃ 남음`이라고 적어라.**
→ 이 빈 공간이 이 장의 핵심이다. **비워 두면 고장 난 그래프로 보이고, 이름을 붙이면 「이만큼 남아서 아무 일도 안 일어났다」는 뜻이 된다.**
→ 세로 눈금을 60부터 82까지로 잡아 **그래프 안이 3분의 1 넘게 비지 않게** 하라.
**본문 글자와 그래프가 절대 겹치지 않게 하라.**

**금지**
- **좌우로 배경을 나누지 마라.** 반드시 그래프가 있어야 한다.
- 같은 문장을 두 번 쓰지 마라.
- **해결책을 말하지 마라.** 인지·판단·행동, 폐루프, AI 같은 말을 한 글자도 쓰지 마라.

---

# 10페이지

**레이아웃** 7번 (낱말 나열형)

**화면 글자**
- 제목: `값이 아니라, 값이 걸어온 길`
- 설명문: `62.1 → 62.6 → 63.1 … 각각은 전부 정상입니다. 그런데 네 시간 동안 한 번도 내려가지 않은 것은 정상이 아닙니다.`
- 낱말 3개와 풀이:
  - `인지` — `잠들지 않고 계속 본다`
  - `판단` — `흐름을 보고 정상인지 가른다`
  - `행동` — `속도를 낮추고 정비를 부른다`
- 하단 1행: `셋 다 있어야 한다. 하나라도 빠지면 새벽 3시가 되풀이된다.`

**삽화** 세 낱말 **위에 각각 큰 그림 하나씩** — 인지는 눈, 판단은 저울, 행동은 손 같은 것. 셋이 한 세트로 보이게.
**세 그림을 반드시 그려라. 이 장은 그림이 핵심이다.**
**금지 — 이 장에서 가장 중요하다**
- **낱말 위에 굵은 가로줄을 긋고 끝내지 마라.** 거기 들어갈 것은 줄이 아니라 **그림**이다. 줄만 긋고 그림을 빼면 실패다.
- **낱말을 넷으로 늘리지 마라.** 「다시 인지」와 「폐루프」는 다음 장 몫이다.
- 낱말을 상자로 감싸거나 화살표로 잇지 마라.

---

# 11페이지

**레이아웃** 2번 (구간 표지형) — **바탕은 순백이다**

**화면 글자**
- 제목: `그리고 결정적인 네 번째`
- 설명문: `행동한 결과를 다시 본다 — 그래야 고리가 닫힌다.`
- 큰 낱말: `폐루프`
- 큰 낱말 아래 1행: `닫힌 고리 · closed loop`
- 본문 3행:
  - `인지 → 판단 → 행동까지는 직선이다. 여기서 끝내면 시킨 것을 한 번 하고 마는 셈이다.`
  - `속도를 낮췄으면 물어야 한다 — 온도가 정말 떨어졌는가? 안 떨어졌으면 더 낮추고, 떨어졌으면 그만한다.`
- 아래 가는 선 위 결론 1행: `결과를 안 보고 시킨 대로만 하는 구조는 고리가 열려 있다고 해서 개루프라 한다`

**삽화** 왼쪽에 **고리가 닫혀 있다는 것이 보이는 큰 그림.** 맞물린 고리 두 개 같은 것.
그림 안에 라벨 두 개를 박아라 — 낱말만 짧게. 한쪽 고리에 `행동`, 다른 고리에 주황으로 `확인`.
**금지** 순환 화살표 도해. 에어컨·전자레인지 언급 — 그것은 13페이지 몫이다.

---

# 12페이지

**레이아웃** 7번 (낱말 나열형)

**화면 글자**
- 제목: `이것이 피지컬 AI입니다`
- 설명문: `물리 세계를 센서로 읽고, 판단하고, 실제로 무언가를 움직이고, 그 결과를 다시 읽는 AI.`
- 낱말 3개와 풀이:
  - `자율주행` — `읽는 것은 카메라, 움직이는 것은 바퀴`
  - `휴머노이드` — `읽는 것은 카메라와 관절, 움직이는 것은 팔다리`
  - `스마트팩토리` — `읽는 것은 온도계, 움직이는 것은 설비 모터`
- 하단 강조(핵심 어절에만 아주 연한 주황 밑칠): `읽는 것과 움직이는 것만 다를 뿐, 고리는 같습니다. 4일 뒤 이 고리를 돌리는 것은 여러분이 만든 프로그램입니다.`

**삽화** 세 낱말 위에 각각 **자동차 · 사람 형태 로봇 · 공장** 그림을 크게 넣어라.
**이 장은 그림이 핵심이다. 글자만 두면 실패다.**
**금지 — 이 장에서 가장 중요하다**
- **낱말 위에 굵은 가로줄을 긋고 끝내지 마라.** 지난번에 그림 대신 검은 줄만 나왔다. **줄이 아니라 자동차·로봇·공장 그림이다.**
- **그림을 생략하지 마라.** 세 개 다 그려라.
- 낱말을 「읽고·판단하고·움직인다」로 바꾸지 마라. **반드시 자율주행·휴머노이드·스마트팩토리 세 낱말이어야 한다.**
- 치수선·도면 선을 넣지 마라.

---

# 13페이지

**레이아웃** 5번 (나란히 비교형)

**화면 글자**
- 제목: `집에도 있습니다 — 에어컨과 전자레인지`
- 설명문: `고리가 닫힌다는 게 무슨 뜻인지, 집에 있는 물건 두 개로 확인합니다.`
- 좌측 태그(주황): `닫힌 고리` / 소제목: `에어컨은 결과를 다시 봅니다`
- 좌측 본문 2행:
  - `온도를 잰다 → 24도보다 높다고 판단한다 → 압축기를 돌린다.`
  - `그리고 다시 온도를 잰다. 24도가 되면 멈추고, 오르면 또 돈다.`
- 우측 태그(회색): `열린 고리` / 소제목: `전자레인지는 결과를 보지 않습니다`
- 우측 본문 2행:
  - `3분을 눌렀다. 음식이 데워졌든 이미 타고 있든 3분을 채우고 멈춘다.`
  - `결과를 보지 않는다.`
- 아래 가는 선 위 결론 2행:
  - `다시 공장으로 — 설비에 "속도를 20% 낮춰"라고 한 번 시키고 끝내면 그건 전자레인지다.`
  - (아주 연한 주황 밑칠) `우리가 4일 동안 만들 것은 에어컨 쪽이다.`

**삽화** 왼쪽에 에어컨, 오른쪽에 전자레인지 그림. **둘 다 크고 한눈에 알아볼 수 있게.**
**라벨은 붙이지 마라** — 보면 아는 물건이고, 무슨 일을 하는지는 아래 본문이 말한다.
**이 장에 그림이 없으면 실패다.** 지난번에 두 그림이 다 빠졌다.

**금지 — 이 장에서 가장 중요하다**
- 제목이 두 구역에 걸쳐 글자색이 반씩 달라지는 일이 절대 없어야 한다. **바탕은 양쪽 다 순백이고, 가는 세로선 하나로만 나눈다.**
- 하단 두 줄을 빠뜨리지 마라. **이 장은 비유로 끝나면 안 되고 반드시 공장으로 돌아와야 한다.**

---

# 14페이지 ★ 반전

**레이아웃** 2번 (구간 표지형) — **바탕은 순백이다**

**화면 글자**
- 제목: `그런데 이 고리는 50년 전에도 있었습니다`
- 설명문: `공장 자동화 장치는 1960년대부터 센서로 값을 읽고, 조건을 따지고, 밸브를 열고 닫았습니다.`
- 큰 낱말: `50년`
- 큰 낱말 아래 1행: `이 구조가 존재해 온 기간`
- 아래 가는 선 위 결론 1행(크게): `그렇게 오래된 구조인데, 왜 아직도 새벽 3시의 EQ-03을 아무도 몰랐나`

**삽화** 왼쪽에 **오래된 공장 제어 장치의 큰 그림** — 다이얼과 계기가 달린 낡은 제어반 같은 것. 「오래됐다」가 보여야 한다.
그림 안에 라벨 하나만 박아라 — `50년 전에도 있던 것`. 설명문이 이미 하는 일을 말했으므로 되풀이하지 마라.

**금지 — 이 장에서 가장 중요하다**
- **답을 말하지 마라.** "병목은 판단 칸이었다"는 다음 장 몫이다.
- 「사람」이나 「규칙」이라는 낱말을 쓰지 마라.
- 본문을 추가하지 마라. **이 장은 질문 하나만 남기고 넘어간다.**
- 화면 가운데를 비워 두지 마라. 그림이 그 자리를 채운다.

---

# 15페이지

**레이아웃** 5번 (나란히 비교형)

**화면 글자**
- 제목: `막혀 있던 것은 가운데 「판단」이었습니다`
- 설명문: `온도를 재는 일과 모터를 돌리는 일은 기계가 잘한다. 문제는 그 사이다.`
- 좌측 소제목: `사람`
- 좌측 본문 2행:
  - `정확하다. 그러나 잠을 자야 하고, 여섯 대를 동시에 못 본다.`
  - `그리고 새벽 3시에는 없다.`
- 우측 소제목: `규칙`
- 우측 본문 2행:
  - `사람이 미리 적어 둔 것. 지치지 않고 빠르지만, 적어 둔 것만 잡는다.`
  - `"한 시간에 0.5도씩 오르는 경우"를 적어 두지 않았으면 그냥 지나간다.`
- 아래 가는 선 위 결론 2행:
  - `그래서 규칙을 계속 늘리게 된다. 100개, 500개. 서로 부딪히다 어느 순간 아무도 전체를 이해하지 못하는 덩어리가 된다.`
  - (아주 연한 주황 밑칠) `50년 동안 이 칸에 넣을 수 있는 재료가 둘뿐이었다 — 이것이 병목의 정체다.`

**삽화** 왼쪽에 사람 그림, 오른쪽에 규칙이 빼곡히 적힌 문서 그림. **둘 다 크게.**
**라벨은 붙이지 마라** — 무엇이 부족한지는 아래 본문이 말한다.
**이 장에 그림이 없으면 실패다.** 지난번에 두 그림이 다 빠졌다.

**금지**
- **좌측을 「지금까지의 규칙」으로 바꾸지 마라. 반드시 「사람」과 「규칙」 둘이어야 한다.**
- LLM을 여기서 꺼내지 마라 — 다음 장 몫이다.

---

# 16페이지 ★ 아하

**레이아웃** 2번 (구간 표지형) — **바탕은 순백이다**

**화면 글자**
- 제목: `그 칸에 넣을 세 번째 재료가 생겼습니다`
- 설명문: `LLM — Large Language Model, 대규모 언어 모델. 엄청난 양의 글을 읽고 다음에 올 말을 알아맞히도록 훈련된 프로그램.`
- 큰 낱말: `LLM`
- 큰 낱말 아래 1행: `범용 판단 엔진 · 특정 분야에 묶여 있지 않다`
- 본문 4행:
  - `글을 다루려고 만들었는데, 훈련 과정에서 맥락을 보고 판단을 내리는 능력이 함께 생겼다.`
  - `규칙을 500개 적어 두지 않아도 "이 온도 흐름이 평소와 다른가"를 맥락으로 본다.`
  - `미리 예상하지 못한 상황에도 답을 낸다. 규칙은 침묵하지만 이쪽은 말을 한다.`
  - `그리고 판단한 이유를 사람의 말로 설명한다. 규칙은 "조건 37번 걸림"이라고만 한다.`
- 아래 가는 선 위 결론 1행(아주 연한 주황 밑칠): `이것이 "왜 하필 지금인가"의 답이다. 센서가 좋아져서도, 로봇이 싸져서도 아니다. 50년 동안 막혀 있던 칸이 뚫렸기 때문이다.`

**삽화** 왼쪽에 **막혀 있던 칸이 뚫렸다는 것이 보이는 그림** — 세 칸 중 가운데 칸이 이제 채워졌다는 식.
그림 안에 라벨 세 개를 박아라 — `인지`, 가운데에 주황으로 `판단 — 세 번째 재료`, `행동`.
**금지** 뇌 그림, 신경망 그림, 로봇 그림. **"LLM은 눈이 없다·손이 없다"는 말을 쓰지 마라 — 다음 덱 몫이다.**

---

# 17페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `다만, AI는 판단만 합니다`
- 설명문: `"AI가 다 한다"는 말은 틀렸다. AI는 고리의 가운데 한 칸을 맡는다.`
- 표 머리행: `누가` · `무엇을 하는가`
- 표 4행:
  - `센서` — `온도·진동을 잰다`
  - `프로그램(코드)` — `값을 모으고 형식을 맞추고 저장한다`
  - `제어 장치` — `설비 속도를 실제로 바꾸고 로봇을 움직인다`
  - `AI (LLM)` — `모인 값을 보고 "이건 이상하다, 이렇게 하자"고 정하는 일만 한다`
- 표 아래 1행: `4일 내내 이 경계를 지킨다. AI가 로그를 전부 읽거나 스스로 설비를 세운다고 말하지 않는다.`

**삽화** 표의 각 행 왼쪽에 작은 그림 하나씩 — 센서·코드·제어 장치·AI를 구분해 주는 정도로.
**강조** 표의 `AI (LLM)` 행 하나만 아주 연한 남색(#EEF3F9)으로 칠하고 글자를 남색 굵게. 표 안에서만이다.
**금지** 세로줄, 표 테두리. **이 표를 「눈이 없다·손이 없다·심장이 없다」로 바꾸지 마라.**

---

# 18페이지

**레이아웃** 5번 (나란히 비교형) — 다만 **가운데를 비워 둔다**

**화면 글자**
- 제목: `기업이 찾는 사람은 AI 전문가가 아닙니다`
- 설명문: `로봇도 센서도 공장에는 이미 깔려 있습니다. 장비가 없어서 못 하는 것이 아닙니다.`
- 좌측: `하드웨어` / 그 아래 `로봇 · 센서`
- 우측: `소프트웨어` / 그 아래 `LLM · AI`
- 두 그림 사이 한가운데(주황): `이 사이를 잇는 사람이 없다`
- 본문 2행:
  - `기업이 실제로 호소하는 것은 이것이다 — 새 AI를 공장에 이미 깔려 있는 낡은 시스템과 이어 붙일 사람이 없다.`
  - `그 다리를 놓을 수 있는 사람 — 이것이 참여기업이 원하는 인재상이다. 모델을 새로 만드는 연구자가 아니다.`
- 용어 3행(작게):
  - `MES — Manufacturing Execution System, 제조실행시스템. 어떤 설비가 무엇을 얼마나 만들었는지 관리하는 프로그램`
  - `PLC — Programmable Logic Controller. 설비를 직접 켜고 끄는 제어 장치`
  - `SOP — Standard Operating Procedure. 표준 작업 절차서`
- 아래 가는 선 위 결론 1행(아주 연한 주황 밑칠): `오늘 만드는 관제 대시보드는 그 기업들이 요청한 과제 목록의 첫 줄과 같은 종류의 일이다. 수요조사 참여기업 15개사, 채용약정 4개사.`

**삽화** 왼쪽 끝에 로봇이나 설비 그림, 오른쪽 끝에 화면이나 코드 그림.
**그 사이를 그냥 비워 두지 마라. 「끊어져 있다」가 눈에 보여야 한다.**
양쪽에서 선이 가운데를 향해 오다가 **닿기 전에 끊긴다.** 끊긴 두 끝을 마주 보게 두고, **그 자리 한가운데에 위의 주황 문구를 놓아라.** 화면 아래로 내리지 마라.
→ 9페이지 그래프와 같은 원칙이다. **빈 곳을 그냥 두면 미완성으로 보이고, 이름을 붙이면 「여기가 문제다」라는 뜻이 된다.**
**금지**
- **두 그림을 이어 붙이지 마라.** 지난번에 선으로 이어 놓아 「끊겨 있다」가 「이어져 있다」로 뒤집혔다. 선은 **닿기 전에 끊겨야** 한다.
- 다리 그림, 절벽 그림, 회사 이름 금지. 숫자 `15`와 `4`를 크게 키우지 마라.
- **이 장을 빠뜨리지 마라.**

---

# 19페이지 — 이 덱의 마지막 장

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `그래서 4일 동안 이 고리를 한 토막씩 붙입니다`
- 설명문: `오늘은 첫 토막 — 보는 눈을 만든다.`
- 표 머리행: `일자` · `고리의 어느 부분` · `만드는 것` · `남는 것`
- 표 4행:
  - `Day 1` · `인지 — 보기` · `설비·로봇 실시간 관제 대시보드` · `관제화면`
  - `Day 2` · `인지의 구조화` · `스펙 기반 관제 시스템, 알람 규칙과 작업지시` · `DB 백엔드`
  - `Day 3` · `판단 — 알기` · `센서 이상감지 + 진단 에이전트` · `이상감지·진단`
  - `Day 4` · `행동 — 움직이기` · `설비 제어와 로봇 파견 폐루프` · `완성된 폐루프`
- 표 아래 1행: `넷째 날, 여러분이 만든 프로그램이 설비에 진짜로 명령을 내리면서 고리가 닫힌다.`
- 하단 강조(아주 연한 주황 밑칠, 크게): `그런데 그 판단을 LLM에게 그냥 물어보면 될까요?`

**삽화** 이 장은 표가 주인공이다. 그림은 넣지 않아도 된다.
**강조** 표의 `Day 1` 행 하나만 아주 연한 남색(#EEF3F9)으로 칠하고 글자를 남색 굵게. 표 안에서만이다.
**금지** 세로줄, 표 테두리. **마지막 질문에 답하지 마라.** 에이전트라는 말을 쓰지 마라. 이 덱은 여기서 끝난다.

▲ 복붙 끝 ▲
