# Day 4 · 강의 4 (16장) — 설명란

> **업로드할 파일 2개**
> ① `00_디자인기준판\디자인기준판.pdf`
> ② `Day4_[첨부]원고.md` ← **Day 4 범위만 담긴 원고. 다른 날 원고를 같이 올리지 마라.**
>
> ※ **NotebookLM이 받는 형식** — PDF · txt · **Markdown(.md)**. HTML·PNG는 안 된다.
> **맞춤설정** — 언어 한국어 / 길이 기본값
> **설명 칸** — 아래 ▼부터 ▲까지 전부 복사해 붙여넣기
> **생성 결과** — 16장 → PPT로 내려받아 확정.

---

▼ 복붙 시작 ▼

**정확히 16장**의 슬라이드를 만들어라. 아래 1~16페이지 지정을 **한 지정 = 한 장**으로 따르고 합치거나 나누지 마라.

## 이 자료에는 발표 대본이 없다

강사가 읽을 대본이 따로 없다. **슬라이드만 보고도 뜻이 통해야 한다.**

그래서 이렇게 만든다.

- **글로 다 쓰지 마라.** 화면을 글자로 채우면 읽느라 강의를 못 듣는다.
- **그렇다고 비우지도 마라.** 낱말만 덩그러니 있으면 대본 없이는 아무 뜻도 안 통한다.
- **답은 삽화다.** 그림이 뜻을 지고, 글은 그림이 못 하는 말만 한다.
- **그림만으로 뜻이 안 통하면 그때 라벨을 박아라.** 보면 아는 물건에는 붙이지 않는다. 라벨은 설명이지 되풀이가 아니다.

## 사흘 동안 끝난 것 — 다시 설명하지 말고, 안다고 전제하고 써라

첫날 공장을 봤고(대시보드), 둘째 날 설계했고(스펙 → 백엔드), 셋째 날 이상을 알아냈다(이상감지 + 진단 에이전트).
그런데 어제 에이전트는 **제안까지만** 했다. 리포트가 "속도를 낮추라"고 권고해도 공장은 그대로 돌았다.
첫날 배운 패턴 중 **총괄-담당(Orchestrator-Worker)** 을 오늘 손으로 만든다.

**오늘 고리의 마지막 변을 잇는다.**

## 범위 — 이걸 어기면 실패다

- 이 덱은 **Day 4 강의 4 + 라이브 시연(50분) 16장 전부**다.
- **11~13페이지는 라이브 시연 자리다.** 실제 화면은 강사가 띄운다. **대시보드 목업이나 시연 결과 그림을 그리지 마라.**
- **16장을 넘겨 만들지 마라. 지정된 16장을 하나도 빼먹지 마라.**

## 지시문 읽는 법

각 페이지의 **「화면 글자」** 에 적힌 문장은 **최종 문구**다.

- **그대로 옮겨 써라.** 다듬지 말고, 요약하지 말고, 다른 말로 바꾸지 마라.
- **적힌 것 외의 문장을 추가하지 마라.**
- 백틱(`) 안이 화면에 나갈 글자다. 백틱 자체는 쓰지 마라.
- **「레이아웃」** 은 기준판 PDF의 몇 번째 장 형식인지 지정한 것이다.
- **「삽화」** 는 **무엇이 보여야 하는지**와 **그림 안에 박을 라벨**을 적은 것이다. 그림의 모양은 당신이 정하라.
- **「금지」** 를 반드시 지켜라. 특히 이야기를 미리 흘리지 마라.
- **한 페이지 지정을 두 페이지로 쪼개거나, 두 지정을 한 페이지에 합치지 마라.**

# 기준판 8종 — 레이아웃 번호

| 번호 | 형식 | 생김새 |
|---|---|---|
| **1** | 표지형 | 화면 전체 짙은 남색, 왼쪽에 흰 제목, 제목 아래 짧은 주황 가로선, 오른쪽에 옅은 동심원 |
| **2** | 구간 표지형 | **순백 바탕.** 왼쪽에 큰 삽화, 그 옆에 큰 낱말 하나. 화면 아래 가는 선 위에 결론 한 줄 |
| **3** | 큰 그림 + 짧은 글 | **왼쪽 절반이 큰 삽화**, 오른쪽에 문장 두세 줄. 아래 가는 선 위에 결론 한 줄 |
| **4** | 번호 레일형 | 항목을 가로선으로 나누고 **각 항목 왼쪽에 작은 그림 하나씩**, 그 옆에 번호 |
| **5** | 나란히 비교형 | **배경을 나누지 마라.** 순백 바탕에 **가는 세로선 하나**로만 좌우를 가른다. 양쪽에 그림 하나씩 |
| **6** | 표형 | 머리행만 남색 배경 흰 글씨, 가로선만, 세로줄 없음 |
| **7** | 낱말 나열형 | 낱말 3~4개를 가로로, **각 낱말 위에 큰 그림 하나씩.** 아래 한 줄 풀이 |
| **8** | 가는 선 그래프형 | 얇은 주황 곡선 하나 + 남색 점선 기준선, 축 눈금·격자 없음 |

# 삽화 — 이 자료의 주인공이다

**화면의 절반쯤을 삽화가 차지해야 한다.** 글이 화면을 채우면 실패다.

- **그림만으로 뜻이 안 통하는 자리에는 라벨을 박아라.** 화살표로 가리키고 **낱말로 짧게** 붙인다 — `무인으로 도는 16시간`, `임계값 80℃` 같은 것이다. 대본이 없으므로 **그림이 스스로 설명해야 한다.**
- **한 장에 큰 그림 하나.** 작은 것 여럿보다 큰 것 하나가 낫다.
- 색은 **남색 계열과 주황**으로 맞춘다. 강조하고 싶은 한 곳만 주황.
- **설명하는 그림을 그려도 된다.** 단계를 늘어놓거나, 흐름을 보여 주거나, 두 상태를 견주는 그림 전부 괜찮다. 입체감이나 옅은 음영도 괜찮다.
- **읽는 데 방해가 되면 뺀다.** 글자 위에 겹치면 안 된다.
- 사진은 쓰지 마라. 제품 로고와 회사 이름도 넣지 마라.
- **그림 안에도 격자·모눈·청사진 무늬를 깔지 마라.** 그림 뒤에 네모난 색 판을 두지 마라. 그림은 흰 바탕 위에 선으로만 선다.
- **뜻 없는 숫자나 글자를 배경처럼 채우지 마라.** 0과 1을 흩뿌리거나 알 수 없는 글자를 늘어놓으면 읽기만 방해한다. 그림에 들어가는 글자는 **읽으라고 넣는 라벨**뿐이다.
- **그림과 라벨을 굵은 화살표로 잇지 마라.** 가는 선으로 가리킨다.

**판단 기준** — 그 장에서 말하려는 것이 **그림만 보고도 짐작되면 성공**이고, 눈길만 끌고 뜻을 안 담으면 실패다.

# 한 가지 사실은 화면에 한 번만

같은 말을 두 번 하면 화면만 차지하고 뜻은 늘지 않는다. 대본이 없다고 해서 되풀이하는 것이 아니다.

- **제목·설명문·본문·결론·라벨이 서로 다른 말을 해야 한다.** 앞에서 한 말을 뒤에서 다시 하지 마라.
- **라벨은 「그림만으로는 알 수 없는 것」에만 붙인다.** 시계에 칠해진 구역은 라벨이 없으면 뜻을 모르니 붙이고, 에어컨은 보면 아는 물건이니 붙이지 않는다.
- **본문에 이미 있는 문장을 라벨로 옮겨 적지 마라.** 그건 설명이 아니라 반복이다.
- 라벨을 붙일 때는 **문장이 아니라 낱말**로 짧게 쓴다.
- 결론 한 줄은 본문의 요약이 아니라 **본문이 하지 않은 한 걸음**이어야 한다.
- **같은 문장을 화면에 두 번 찍지 마라.** 지난번에 마무리 줄이 똑같이 두 줄로 나왔다. 한 문장은 한 번만 나온다.

# 글자 크기 — 비율표를 그대로 지켜라

**본문 글자 크기를 1이라 하고, 나머지를 그 배수로 맞춰라.** 아래 표를 어림잡지 말고 그대로 따르라.

| 요소 | 본문 대비 배수 | 화면 높이에서 차지하는 몫 | 참고 pt (16:9 기준) |
|---|---|---|---|
| 표지 제목 (1번 형식만) | **2.1배** | 12분의 1 이하 | 37pt |
| **제목** | **1.65배** | **16분의 1 이하** | 29pt |
| 큰 낱말 (폐루프 · LLM · 50년) | **2.0배** | **12분의 1 이하** | 35pt |
| 낱말 나열형의 낱말 | 1.45배 | 20분의 1 이하 | 25pt |
| 소제목 · 표 항목명 | 1.1배 | — | 19pt |
| 제목 아래 설명문 | **1.0배** | — | 18pt |
| **본문** | **1 (기준)** | — | **18pt** |
| 삽화 안 라벨 | 0.8배 | — | 14pt |

- **제목은 크기가 아니라 굵기로 눈에 띈다.** 본문의 **1.65배를 넘기지 마라.** 제목이 화면을 잡아먹으면 그림 자리가 없어진다.
- **큰 낱말도 크게 만들지 마라.** 「폐루프」·「LLM」·「50년」은 **제목보다 조금만 크다.** 화면 높이의 **12분의 1**을 넘지 마라.
  → 지난번에 「볼 사람이 없었을 뿐이다」가 **화면 절반**을 차지해 다른 것이 다 밀려났다. 그 장은 실패다.
  → 큰 낱말이 **두 줄로 넘어가면 이미 너무 큰 것이다.** 한 줄에 들어가게 줄여라.
- **숫자를 키우지 마라.** 숫자는 문장 안이나 그림 라벨 안에서 다룬다. 숫자 하나가 주인공인 장을 만들지 마라.
- **본문을 작게 만들지 마라.** 강의실 뒷자리에서 읽혀야 한다. **줄이는 것은 큰 글자지 본문이 아니다.**
- 제목 아래에는 짧은 부제가 아니라 **본문과 같은 크기의 설명문 한두 줄**을 둔다.

# 줄간격

- **문단과 문단 사이를 한 줄 띄우지 마라.** 문단 간격이 글자 크기의 두 배를 넘으면 빈 줄이 들어간 것처럼 보인다.
- **한 호흡으로 읽히는 두 문장은 줄바꿈으로 잇는다.** 문단을 나누지 마라.
- 줄 사이는 글자 크기의 1.5배쯤이 적당하다.

# 화면 여백

- 글자가 화면 끝까지 가지 않게 여백을 둔다.
- **화면 아래를 비워 두지 마라.** 아래 3분의 1이 텅 비면 잘못 만든 것이다. 그 자리는 삽화를 키우거나 결론 한 줄로 채운다.
- 제목·본문·삽화가 **서로 겹치지 않게** 배치한다.
- 왼쪽 시작선을 장마다 같게 맞춰라. 제목과 본문이 같은 세로선에서 시작해야 한다.

# 배경과 색

- **모든 장의 바탕은 순백 #FFFFFF다.** 표지 형식(1번) 한 종류만 화면 전체가 짙은 남색이다. 그 둘뿐이고 세 번째 바탕은 없다.
- 화면을 좌우나 상하로 갈라 한쪽만 다른 색으로 칠하는 일이 없어야 한다. 나누는 것은 **가는 회색 선 하나**다.
- 화면 아래에 색 띠를 두르지 마라.
- 배경에 격자·무늬·워터마크를 깔지 마라. 배경에 옅게 깔린 큰 숫자도 넣지 마라.
- 주조색 **남색 #12305A**, 강조색 **주황 #D4541E**, 부연은 회색 #6B7280. 이 셋과 검정·흰색뿐이다.
- 강조는 **아주 연한 주황 밑칠 #FBE3D6**이다. **형광펜처럼 진한 주황을 쓰지 마라.** 핵심 어절 몇 글자에만, 한 장에 한 곳.
- **한 가지 예외** — 표(6번 형식) 안에서 한 행만 **아주 연한 남색 #EEF3F9**으로 칠하는 것은 허용한다. 표 안에서만이고, 슬라이드 바탕과는 무관하다.

## 무대 설정 (4일 내내 고정 — 여기 없는 사실을 지어내지 마라)
- 무대는 경남 창원의 **가상** 정밀기계 부품 제조사 **「K-정밀」**. 실재 회사가 아니다.
- **CNC 설비 6대**(EQ-01 ~ EQ-06)와 **자율이동로봇(AMR) 2대**가 있다.
- CNC가 내보내는 값 — **온도 · 진동 · rpm · 가동상태**. AMR — **위치 · 배터리 · 적재상태**. 값은 **1초마다** 바뀐다.
- 이 공장은 전부 소프트웨어로 만든 가짜이며, 강사가 미리 만들어 둔 **팩토리 시뮬레이터** 안에서만 존재한다.
- **K-정밀은 주간 근무만 하고 야간에는 무인으로 돌아간다.** 설비는 스물네 시간 돌지만 사람은 주간에만 있다.
- 4일은 **「보기 → 알기 → 움직이기」**. 오늘은 **보기**다.
- 이 사업에는 **참여기업 15개사**, **채용약정 4개사**가 붙어 있다.
- **되돌아오는 사건** — 새벽 3시, EQ-03의 온도가 시간당 0.5도씩 네 시간에 걸쳐 62도에서 64도로 올랐고 아무도 몰랐다. 그날 밤 깎은 부품은 전량 폐기됐다. 임계값 80도는 한 번도 넘지 않았다.
- **넷째 날 제어 도구 4개** — `set_equipment_speed`(감속) · `ack_alarm`(알람 확인)은 **자동**, `stop_equipment`(정지) · `dispatch_robot`(로봇 파견)은 **사람 승인 필수**. 이 구분을 바꾸지 마라.
- **라이브 시연 실측값** — 주입 → 감지까지 **1~3분** · 진단이 인용하는 정비 작업지시는 **WO-2026-0801「부품 입고 지연으로 보류」** · 승인 후 온도는 **63℃에서 53℃대로** 내려간다. 이 수치를 바꾸지 마라.
- 위에 없는 **숫자·회사명·제품명·사건을 지어내지 마라.** "혁신적인", "놀라운" 같은 홍보성 표현도 쓰지 마라.
- 듣는 사람은 컴퓨터공학 전공이지만 **코딩은 거의 모른다고 가정**한다. 낯선 말은 처음 나오는 자리에서 풀고 나서 써라.

---

# 1페이지

**레이아웃** 1번 (표지형)

**화면 글자**
- 윗줄: `DAY 4 · 움직이기`
- 제목: `폐루프를 닫다`
- 부제: `경남대 RISE 피지컬AI 사관학교 · 8월 Agentic AI 특강 — 멀티에이전트와 팀 챌린지`
- 하단 1행: `어제 에이전트 부검 · 책임을 쪼개기 · 움직이는 손 · 사람의 승인`
- 하단 2행(굵게): `오늘, 여러분의 AI가 공장을 실제로 움직입니다.`

**삽화** 오른쪽에 옅은 동심원 몇 개. 그 외 장식은 넣지 마라.
**금지** 흰 배경으로 만들지 마라 — **화면 전체가 짙은 남색이어야 한다.** 공장·로봇 그림 금지.

---

# 2페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `사흘 동안 고리는 ㄷ자로 열려 있었습니다`
- 설명문: `보고, 설계하고, 알았지만 — 에이전트는 제안까지만 했습니다.`
- 오른쪽 본문 3행:
  - `첫날 공장을 봤다. 둘째 날 그 뒤에 구조를 붙였다. 셋째 날 이상을 알아냈다.`
  - `그런데 어제 진단 리포트가 "속도를 낮추라"고 권고했을 때, 공장은 그대로 돌았다.`
  - `읽는 손까지는 달았고, 움직이는 손이 없었다. 오늘 마지막 변을 잇는다.`
- 아래 가는 선 위 결론 1행(핵심 어절에만 아주 연한 주황 밑칠): `오늘 여러분 팀의 에이전트가 공장을 직접 움직입니다.`

**삽화** 왼쪽 절반에 **세 변만 그려진 ㄷ자 고리 그림.** 마지막 한 변이 비어 있는 것이 보여야 한다.
그림 안에 라벨 세 개를 박아라 — 세 변에 `보기` `설계` `알기`, 빈 자리에 주황으로 `?`.
**금지** 완성된 고리를 그리지 마라 — 아직 닫히지 않았다.

---

# 3페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `어제 만든 에이전트를 열어 봅시다`
- 설명문: `지시 한 문장 안에 역할이 몇 개나 들어 있었을까요.`
- 오른쪽 인용(회색, 본문과 같은 크기): `"지난 주 설비 이상을 점검하고, 이상이 있으면 해당 설비의 정비 이력을 조회해 원인 추정과 권고 조치를 담은 진단 리포트를 작성하라."`
- 오른쪽 본문 2행:
  - `한 문장에 네 가지 일이 들어 있다 — 점검하고, 뒤지고, 추정하고, 쓴다.`
  - `어제는 잘 돌았다. 데이터가 얌전했고 할 일이 적었으니까.`
- 아래 가는 선 위 결론 1행: `그런데 여기에 「움직이기」까지 얹으면 어떻게 될까.`

**삽화** 왼쪽 절반에 **한 사람이 모자 네 개를 한꺼번에 쓰고 있는 그림.**
그림 안에 라벨 하나만 박아라 — 주황으로 `역할 4개`.
**금지** 인용문을 크게 키우지 마라. 본문과 같은 크기다.

---

# 4페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `한 몸에 다 시키면 생기는 일`
- 설명문: `역할 과부하 — 첫날 패턴 이야기에서 봤던 그 문제입니다.`
- 세 항목:
  - `지시가 길어진다` — `역할이 늘 때마다 프롬프트에 문장이 붙는다. 길어질수록 뒤쪽 요구를 흘린다.`
  - `어디서 틀렸는지 모른다` — `진단이 이상한 건지 조치 판단이 이상한 건지, 통째로 한 번에 하니 중간이 안 보인다.`
  - `하나만 고칠 수 없다` — `조치 기준만 바꾸고 싶어도 전체 지시를 다시 써야 하고, 그러면 진단까지 흔들린다.`
- 아래 가는 선 위 결론 1행: `그리고 오늘은 이 판단이 진짜 설비를 움직인다. 흔들리면 안 되는 이유가 하나 더 생겼다.`

**삽화** 각 항목 왼쪽에 작은 그림 하나씩 — 늘어지는 두루마리, 안이 안 보이는 상자, 얽힌 실타래 같은 것.
**금지** 지시와 이유를 화살표로 잇지 마라. 가는 가로선으로만 구분한다.

---

# 5페이지

**레이아웃** 7번 (낱말 나열형)

**화면 글자**
- 제목: `그래서 셋으로 쪼갭니다`
- 설명문: `첫날 배운 총괄-담당 구조를 오늘 손으로 만듭니다.`
- 낱말 3개와 풀이:
  - `감지` — `온도 이력을 보고 이상 구간을 찾는다`
  - `진단` — `정비 이력을 뒤져 원인을 추정한다`
  - `조치` — `무엇을 할지 정하고 도구를 부른다`
- 아래 가는 선 위 결론 1행(핵심 어절에만 아주 연한 주황 밑칠): `각자 하나씩만 맡으면 지시가 짧아지고, 틀린 자리가 보이고, 그 자리만 고칠 수 있다.`

**삽화** 세 낱말 **위에 각각 큰 그림 하나씩** — 찾는 눈, 살피는 돋보기, 조작하는 손 같은 것.
**세 그림을 반드시 그려라. 이 장은 그림이 핵심이다.**
**금지** **낱말 위에 굵은 가로줄을 긋고 끝내지 마라.** 낱말을 상자로 감싸거나 화살표로 잇지 마라.

---

# 6페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `그리고 지휘하는 하나 — 오케스트레이터`
- 설명문: `첫날 「총괄이 나누고 담당이 처리한다」로 배운 그 패턴의 회수입니다.`
- 오른쪽 본문 3행:
  - `오케스트레이터는 직접 일하지 않는다. 감지에게 묻고, 결과를 진단에게 넘기고, 진단 결과를 조치에게 넘긴다.`
  - `그리고 이 순환을 계속 돌린다 — 조치한 뒤 다시 감지로 돌아가는 것이 폐루프다.`
  - `오늘 받는 템플릿에서 이 지휘자는 이미 완성돼 있다. 여러분은 담당 셋의 속만 채운다.`
- 아래 가는 선 위 결론 1행: `첫날 이름만 알았던 패턴이, 오늘 여러분 손에서 돈다.`

**삽화** 왼쪽 절반에 **가운데 지휘자와 둘레의 담당 셋이 순환하는 그림.** 지휘자가 직접 일하지 않는 것이 보여야 한다.
그림 안에 라벨 하나만 박아라 — 가운데에 `총괄`.
**금지** 조직도. 상자를 선으로 잇는 그림.

---

# 7페이지

**레이아웃** 2번 (구간 표지형) — **바탕은 순백이다**

**화면 글자**
- 제목: `오늘 아침, 제어 권한이 열렸습니다`
- 설명문: `어제까지 읽기만 됐습니다. 오늘부터 여러분의 코드가 공장에 명령을 내립니다.`
- 큰 낱말: `움직이는 손`
- 큰 낱말 아래 1행: `제어 MCP 도구 4개 공개`
- 본문 2행:
  - `첫날 무대 소개에서 "명령을 넣는 통로가 네 개 준비돼 있다. 그 네 개는 오늘 열지 않는다"고 했다.`
  - `그 네 개를 오늘 연다.`
- 아래 가는 선 위 결론 1행: `읽는 손에서 움직이는 손으로 — 무게가 달라진다.`

**삽화** 왼쪽에 **잠겨 있던 문이 열리는 그림.** 안쪽에 도구 넷이 걸려 있는 것이 보이면 좋다.
그림 안에 라벨 하나만 박아라 — 주황으로 `오늘 열림`.
**금지** 네 도구의 이름을 여기서 나열하지 마라 — 다음 장 몫이다.

---

# 8페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `열린 도구 넷 — 그리고 승인이 갈립니다`
- 설명문: `무엇이 자동이고 무엇이 사람 승인인지, 여기서 갈라집니다.`
- 표 머리행: `도구` · `무엇을 하나` · `승인`
- 표 4행:
  - `set_equipment_speed` · `설비 회전수를 바꾼다 (감속)` · `자동`
  - `ack_alarm` · `알람을 확인 처리한다` · `자동`
  - `stop_equipment` · `설비를 정지시킨다` · `사람 승인 필수`
  - `dispatch_robot` · `정비 로봇(AMR)을 파견한다` · `사람 승인 필수`
- 표 아래 1행: `왜 둘만 승인이 필요한가 — 다음 장.`

**삽화** 이 장은 표가 주인공이다. 그림은 넣지 않아도 된다.
**강조** 표의 `dispatch_robot` 행 하나만 아주 연한 남색(#EEF3F9)으로 칠하고 글자를 남색 굵게.
**금지** 세로줄, 표 테두리. 도구 이름을 한국어로 바꾸지 마라 — 실습에서 그대로 쓰는 이름이다.

---

# 9페이지

**레이아웃** 5번 (나란히 비교형)

**화면 글자**
- 제목: `되돌릴 수 있는가 — 이것이 갈림선입니다`
- 설명문: `물리 세계에는 한 번 하면 되돌릴 수 없는 행동이 있습니다.`
- 좌측 태그(주황): `자동`
- 좌측 소제목: `되돌릴 수 있다`
- 좌측 본문 2행:
  - `감속은 잘못돼도 다시 올리면 된다.`
  - `알람 확인도 다시 열면 된다. 그래서 에이전트가 스스로 실행한다.`
- 우측 태그(회색): `승인 필수`
- 우측 소제목: `되돌릴 수 없다`
- 우측 본문 2행:
  - `정지는 라인 전체를 멈춘다. 다시 켜도 그 시간은 돌아오지 않는다.`
  - `로봇은 이미 움직였다. 사람과 부딪히면 되돌릴 방법이 없다.`
- 아래 가는 선 위 결론 1행(핵심 어절에만 아주 연한 주황 밑칠): `되돌릴 수 없는 행동 앞에 사람을 세운다 — 이것이 HITL이다.`

**삽화** 왼쪽에 되돌아오는 화살표, 오른쪽에 한 방향으로만 가는 화살표 그림. **둘 다 크게.**
**라벨은 붙이지 마라** — 태그와 소제목이 말한다.
**금지** 제목이 두 구역에 걸쳐 글자색이 반씩 달라지는 일이 절대 없어야 한다. **바탕은 양쪽 다 순백, 가는 세로선 하나.**

---

# 10페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `HITL — 사람이 지키는 관문`
- 설명문: `Human In The Loop. 고리 안에 사람이 한 자리 있는 구조입니다.`
- 오른쪽 본문 3행:
  - `에이전트가 정지나 파견을 하려 하면, 실행 대신 승인 요청이 뜬다. 사람이 승인해야 도구가 불린다.`
  - `이 구분은 템플릿의 승인 관문이 강제한다 — 여러분 코드가 실수로 정지를 자동 실행하는 일은 구조적으로 불가능하다.`
  - `여러분이 못 건드리는 계층으로 만들어 둔 이유다. 안전은 조심해서 지키는 게 아니라 구조로 지킨다.`
- 아래 가는 선 위 결론 1행: `AI에게 다 맡기는 것도, 사람이 다 하는 것도 아니다 — 되돌릴 수 없는 곳에만 사람을 둔다.`

**삽화** 왼쪽 절반에 **고리 위에 관문이 하나 서 있고 그 앞에 사람이 선 그림.**
그림 안에 라벨 하나만 박아라 — 관문에 주황으로 `승인`.
**금지** 신호등·경비원 그림으로 흐르지 마라. 고리와 관문이 보여야 한다.

---

# 11페이지

**레이아웃** 2번 (구간 표지형) — **바탕은 순백이다**

**화면 글자**
- 제목: `이제 보여 드리겠습니다`
- 설명문: `강사 화면에서 실제로 폐루프를 돌립니다. 아래 순서로 진행됩니다.`
- 큰 낱말: `라이브 시연`
- 본문 3행:
  - `제가 콘솔에서 EQ-03에 온도 드리프트를 주입합니다. 첫날 새벽 3시에 벌어졌던 그 일입니다.`
  - `그다음은 아무도 손대지 않습니다. 승인 한 번을 빼고는.`
  - `감지까지 1분에서 3분 걸립니다. 조용해도 고장 난 게 아닙니다 — 설계된 대기입니다.`
- 아래 가는 선 위 결론 1행: `지금부터 화면을 보십시오.`

**삽화** 왼쪽에 **콘솔에서 손가락 하나로 주입 버튼을 누르는 그림.**
그림 안에 라벨 하나만 박아라 — 주황으로 `드리프트 주입`.
**금지** 대시보드 목업 — **실제 화면은 강사가 띄운다.** 시연 결과를 미리 그리지 마라.

---

# 12페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `방금 무슨 일이 있었나 — 앞의 네 걸음`
- 설명문: `주입에서 감속까지, 사람은 아무것도 하지 않았습니다.`
- 네 항목:
  - `01 · 주입` — `강사 콘솔에서 EQ-03에 온도 드리프트. 시간당 0.5도씩, 62도에서 시작한다.`
  - `02 · 감지` — `1~3분 뒤 감지 에이전트가 포착. z-score가 아니라, 앞 절반과 뒤 절반의 평균을 견주는 방식이다.`
  - `03 · 진단` — `진단 에이전트가 정비 이력을 뒤져 작업지시 WO-2026-0801 — 「부품 입고 지연으로 보류」를 찾아낸다.`
  - `04 · 감속` — `조치 에이전트가 회전수를 낮춘다. 되돌릴 수 있는 행동이라 승인 없이 바로 실행된다.`
- 아래 가는 선 위 결론 1행: `여기까지는 자동이다. 그런데 진단은 감속만으로 부족하다고 판단했다.`

**삽화** 각 항목 왼쪽에 작은 그림 하나씩 — 주입 버튼, 오르는 선, 뒤지는 서류, 내려가는 회전수 같은 것.
**금지** 네 단계를 화살표로 잇지 마라. 가는 가로선으로만 구분한다. **5~7번째 걸음을 여기서 말하지 마라.**

---

# 13페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `그리고 사람에게 물었습니다 — 뒤의 세 걸음`
- 설명문: `되돌릴 수 없는 행동 앞에서 루프가 멈춰 섭니다.`
- 세 항목:
  - `05 · 승인 요청` — `터미널에 뜬다 — "승인 요청 — 되돌릴 수 없는 행동입니다. 승인하시겠습니까?" 여기서 루프가 기다린다.`
  - `06 · 로봇 이동` — `승인하면 정비 로봇이 EQ-03으로 출발한다. 2D 공장 화면에서 실제로 움직인다.`
  - `07 · 온도 하강` — `잠시 뒤 온도가 내려간다. 실측으로 63도에서 53도대까지 떨어졌다.`
- 아래 가는 선 위 결론 1행(핵심 어절에만 아주 연한 주황 밑칠): `그리고 다시 감지로 돌아간다 — 이번엔 이상 없음. 고리가 닫혔다.`

**삽화** 각 항목 왼쪽에 작은 그림 하나씩 — 물음표 관문, 움직이는 로봇, 내려가는 온도 곡선 같은 것.
**금지** 네 단계를 화살표로 잇지 마라. 가는 가로선으로만 구분한다.

---

# 14페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `여러분이 채울 곳은 세 군데입니다`
- 설명문: `연결·안전장치·실행은 전부 완성돼 있습니다.`
- 오른쪽 본문 3행:
  - `감지 — 어제 z-score가 드리프트를 왜 못 잡았는지 기억하십시오. 최근 구간을 앞 절반과 뒤 절반으로 나눠 평균을 견주면, 천천히 올라도 뒤가 앞보다 높습니다.`
  - `진단 — 프롬프트에 두 가지를 꼭 넣으십시오. "드리프트는 지금 작아도 계속 오른다", 그리고 "원인이 정비 보류·지연이면 감속과 함께 파견 요청까지 낼 것".`
  - `조치 — 감속부터. 심각하면 정지·파견을 요청합니다. 승인은 사람이 합니다.`
- 아래 가는 선 위 결론 1행: `두 번째 문장을 빼면 감속만 하고 끝난다 — 승인 장면이 안 나온다. 실측으로 확인된 함정이다.`

**삽화** 왼쪽 절반에 **세 칸짜리 틀에서 세 자리만 비어 있는 그림.** 나머지는 채워져 있는 것이 보여야 한다.
그림 안에 라벨 하나만 박아라 — 빈 세 칸에 주황으로 `★ 3곳`.
**금지** 코드 화면. 파일 이름을 크게 쓰지 마라.

---

# 15페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `챌린지 규칙`
- 설명문: `4~5명씩 8팀. 90분 개발 + 30분 데모 준비, 팀당 5분 발표.`
- 표 머리행: `항목` · `내용`
- 표 5행:
  - `기본 미션` · `강사 시연과 같은 폐루프를 자기 팀 공장에서 돌린다 (60분 목표)`
  - `확장 미션` · `남는 시간에 새 에이전트 1개 추가 — 예방정비·AMR 배차·안전 인터록·교대 리포트·자유`
  - `막힘 티켓` · `팀당 2장. 스태프를 집중적으로 붙잡아 쓰는 권한 — 아껴 쓸 것`
  - `안전망` · `60분에 기본 미션이 안 끝나면 「동작 보장 최소 경로」 카드를 받는다`
  - `평가` · `폐루프가 실제로 도는가 40 / 스펙 문서화 30 / 창의성 30 + 즉석 투표`
- 표 아래 1행: `최우선은 전원이 데모에 성공하는 것이다. 확장은 가점이다.`

**삽화** 이 장은 표가 주인공이다. 그림은 넣지 않아도 된다.
**강조** 표의 `평가` 행 하나만 아주 연한 남색(#EEF3F9)으로 칠하고 글자를 남색 굵게.
**금지** 세로줄, 표 테두리.

---

# 16페이지 — Day 4 강의의 마지막 장

**레이아웃** 1번 (표지형) — **화면 전체 짙은 남색**

**화면 글자**
- 윗줄: `Day 4 · 움직이기 — 팀 챌린지 시작`
- 제목(흰 글씨): `시뮬레이터의 공장을 움직였습니다. 9월부터는 진짜를 움직입니다.`
- 하단 3행(옅은 파랑):
  - `발표에서 보여 줄 것 — 감지 로그, 진단 리포트, 승인하는 순간, 그리고 움직이는 로봇.`
  - `화려함보다 실제로 도는가가 최대 배점이다. 전원이 데모에 성공하는 것이 오늘의 최우선이다.`
  - `4일 전에 그린 고리를, 오늘 여러분 손으로 닫습니다.`

**삽화** 제목 아래 짧은 주황 가로선. 그 외 없음.
**금지** 흰 배경으로 만들지 마라 — **화면 전체가 짙은 남색이어야 한다.** 표, 그래프, 그림 금지.

▲ 복붙 끝 ▲
