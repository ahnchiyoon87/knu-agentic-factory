# 2일차 진행 — 이 문서 하나면 됩니다

**오늘 화면 세 개**: ① 슬라이드(`이론/슬라이드.pptx`, 42장) ② 터미널(`특강/시뮬레이터` 에서 연 것) ③ 이 문서.
대본은 없습니다 — 슬라이드에 적힌 문장을 그대로 읽으면 뜻이 통합니다.
아래 「강사가 하는 것」은 슬라이드에 없어서 사람이 해야 하는 것만 적었습니다.

## 오늘 하루 (240분)

| 분 | 무엇 | 자료 |
|---|---|---|
| **60** | **1교시 — 노션 자산화.** 학습 템플릿을 맨땅에서 만들고 웹에 게시 | `이론/노션_자산화_강의자료.pptx` 24장 |
| 20 | 셋업 — 저장소 받기 · 첫 실행 (처음 한 번 1~2분) | — |
| 20 | 새벽 3시 사건 → 사람이 못 본 이유 셋 → **★ 80도를 넘은 적이 없다** | 슬라이드 01 ~ 11 |
| 10 | 무대 K-정밀 · 온도는 회전수의 함수 · 오늘 세 도막 | 12 ~ 17 |
| 20 | **실습 1 — 내 공장 열어보기** (21장에서 고장 주입) | 18 ~ 22 |
| 20 | 이상을 재는 자 셋 · 손계산 · k 와 W · 오탐과 미탐 | 23 ~ 31 |
| 50 | **실습 2 — 이상감지 직접 구현** (37장에서 "다 같이 엔터") | 32 ~ 37 |
| 10 | 닫기 — *"그럼 누가 판단하죠?"* | 38 ~ 42 |
| 20 | 1교시에 만든 템플릿에 오늘 걸 처음 채운다 | — |

**강사가 손대는 자리는 네 곳** — 11장 반전에서 한 박자 · 18장 `내번호.py` 안내 ·
21장 고장 주입 · 37장 "다 같이 엔터". 나머지는 넘기기만.

---

## 아침 (수업 30분 전) — 순서대로

**① 서버(클라우드)를 켭니다** — 수업은 항상 클라우드 서버로 합니다.

```bash
gcloud compute instances start knu-factory --zone=asia-northeast3-a
```

약 1분 뒤 자동으로 서버가 뜹니다. **강사 PC 에서 서버를 켜지 마세요.**

**② 살아 있는지 확인**

```bash
cd 특강/시뮬레이터
uv run tools/제어개방.py --status        # 「공장 39개 · 경고 없음」 이 나와야 함
```

첫 줄에 **`대상 http://34.64.94.16:8000`** 이 찍히는지 보세요. 이게 클라우드를 향한다는 뜻입니다.
(주소는 `특강\시뮬레이터\.env` 의 `BASE_URL` 한 줄로 정해져 있어 명령마다 붙일 것이 없습니다.)

**학생에게 알려 줄 주소** — `http://34.64.94.16:8000`
강사 콘솔은 `http://34.64.94.16:8000/console` 입니다.

**③ 배정을 비웁니다 — 오늘(2일차) 아침에만!**

```bash
uv run tools/자리배정.py --전부풀기 --예
```
리허설·테스트로 나간 배정을 지웁니다. **3일차 아침에는 절대 하지 마세요** — 39명이 어제 공장을 잃습니다.

**④ 단톡방에 서버 주소 공지** — 아래를 그대로 붙여넣습니다.

> 오늘 쓸 서버 주소입니다.
>
> **http://34.64.94.16:8000**
>
> 1교시(노션) 끝나고 실습 시작할 때 씁니다. 지금 적어 두시거나 이 메시지를 남겨 두세요.

**⑤ USB 를 챙깁니다** — 안에 실습 저장소 폴더를 그대로 넣어 둡니다(드라이브·clone 이 다 막힌 학생용 예비).

---

## 1교시 — 노션 자산화 (60분, `이론/노션_자산화_강의자료.pptx` 24장)

앞으로 이틀 동안 쌓을 **그릇을 먼저 만드는 시간**입니다. 산출물은 학생 각자의 학습 템플릿 하나.

| 무엇 | 왜 |
|---|---|
| 데이터베이스 하나 | 수업마다 페이지를 새로 만들지 않는다. **표의 한 줄이 하루** |
| 속성 두 개 (분야·이해도) | 늘리지 않는다. 분야가 늘어도 옵션만 추가 |
| **카드 템플릿** | 새 카드가 항상 같은 모양으로 열린다 |
| 뷰 네 개 (표·갤러리·보드·남은 것) | 데이터는 하나, 보는 창만 늘린다 |
| **웹 게시** | 링크 하나가 나온다. **이틀 뒤 이 링크 제출이 수료** |

강사가 할 말 두 가지 — **"먼저 쓰고, 그다음 AI에게 교정받으세요."** / **"오늘 저녁과 내일 저녁에 여기에 실제로 채웁니다."**
끝나기 전 확인 셋(자료 19장) — 홈에 아이콘·커버 / 새로 만들기에 양식 / 뷰 탭 네 개.

## 학생이 실습 코드를 받는 법 (이미 사전안내문으로 안내됨)

git clone(`https://github.com/ahnchiyoon87/k-precision-lab.git`)과 드라이브 ZIP — **내용이 같습니다.**
폴더 안에 `2일차` `3일차` `데이터` 셋이 보이면 정상. **압축을 안 푼 학생이 꼭 나옵니다** — 첫 실습에서 이상하면 이것부터.
둘 다 막히면 USB 로 복사해 줍니다.

## 번호는 학생이 직접 받습니다 (18장, `uv run 내번호.py`)

서버가 안 나간 공장 하나를 그 PC 에 붙여 주고, **3일차 설정 파일까지 자동으로 채웁니다.** 나눠 줄 것 없음.

| 학생 상황 | 시킬 말 |
|---|---|
| 번호를 잊었다 | `uv run 내번호.py` 다시 — **같은 번호**가 나옵니다 |
| 자리·컴퓨터를 바꿨다 | `uv run 내번호.py S07` — 그 공장을 그대로 가져옵니다 |
| 「남은 공장이 없습니다」 | 강사: `uv run tools/자리배정.py --풀기 S07` |

---

## 슬라이드 진행 (42장) — 번호를 보세요, 표시 없는 장은 넘기기만

| 장 | 화면 | 강사가 하는 것 |
|---|---|---|
| 1~10 | 사건 → 사람이 못 본 이유 셋 → "80도 알람 걸면 되잖아" | 넘기기만 |
| **11** | **★ 반전 — 80도를 넘은 적이 없습니다** | **한 박자 쉬세요.** 학생이 스스로 "어?" 하는 자리 |
| 12~17 | 무대 K-정밀 · 온도는 회전수의 함수 · 공장은 한 사람에 하나씩 | 넘기기만 |
| **18** | **실습 1 — 먼저 내 공장을 엽니다** | **"`2일차/실습` 에서 `uv run 내번호.py`"** 서버 주소 띄워 두기. 20분 |
| 19~20 | 화면에 세 가지 / 기계마다 평소 온도가 다름 | 학생이 보는 동안 넘김 |
| **21** | **일부러 고장 낼 수 있습니다** | ★ **주입을 누릅니다** (아래 「고장 주입」) |
| **22** | **무엇이 달라졌습니까** | **"맞히지 않아도 됩니다."** 정답을 말하지 마세요 |
| 23~31 | 이상을 재는 자 셋 · 손계산 · k와 W · 오탐/미탐 | 넘기기만 |
| **32** | **실습 2 — 알고리즘을 직접 짭니다** | **"2일차/실습 폴더를 여세요."** 50분 |
| 33~36 | 채울 곳 셋 · 돌려 보는 법 · 50분 쓰는 법 | 학생이 짜는 동안 띄워 둠 |
| — | 실습 시작 15 / 25 / 35분 뒤 | **힌트 1 / 2 / 3 공개** (아래 복붙 블록) |
| **37** | **다 채웠으면 기다리세요** | ★ **"다 같이 엔터"** — 전원 확인 후 한 번에 `uv run run.py` |
| 38~39 | 오늘 세 가지 · 오늘 잰 숫자 버리지 마세요 | 결과를 각자 보게 두고 넘김 |
| **40** | **★ 규칙으로는 여기까지입니다** | **"그럼 누가 판단하죠?" 로 끝냅니다.** 답하지 마세요 — 내일 것 |
| 41 | 내일 이 자리에서 | 넘기기만 |
| **42** | 남은 20분은 노션 | **"내 번호 적어 두세요 · 2일차 폴더 지우지 마세요"** 를 여기서 |

### "다 같이 엔터" (37장)
먼저 끝낸 학생이 혼자 돌리면 옆에서 보고 김이 샙니다. **전원이 채운 것을 확인한 뒤** 한 번에.
못 끝낸 학생이 3명 이하면 그대로 진행하고 따로 붙습니다.

### 오늘 절대 말하면 안 되는 것
- 어떤 이상이 잡히고 어떤 것이 **안 잡히는지** — 학생이 돌려서 발견해야 합니다.
- 가이드 §2-2 의 접힌 부분을 미리 펴게 하지 마세요.
- 오탐 수치(하루 81건 → 911건)는 이론이라 말해도 됩니다.

---

## 고장 주입 (21장) — 39명 동시

```bash
uv run tools/제어개방.py --inject-all --stagger 2
```
- 2초씩 벌려 전체 약 81초. **"지금 넣습니다" 라고 말한 뒤** 돌리세요.
- 한 명만 다시: `--inject S07` · 되돌리기: `--end`
- 이 명령은 **온도 드리프트만** 넣습니다(오늘은 그걸 권장 — 눈으로 못 알아채는 것이 출발점).
  스파이크·결측은 강사 콘솔(`http://<주소>:8000/console`)에서 종류를 골라.

## 힌트 카드 — 3장, 단톡방에 복붙 (시작 15 / 25 / 35분 뒤)

붙여넣기 전 매번 **"막혀 있는 게 정상 범위입니다"** 를 먼저 말합니다. 한 장만 주고 끝내지 마세요 —
막히는 데가 세 군데라 카드도 셋입니다.

**힌트 1 (15분 · TODO 1 윈도 통계)**
> 📌 힌트 1 — TODO 1 (윈도 통계)
>
> 막히는 곳은 대부분 "지금 값을 통계에 넣느냐"와 슬라이싱 범위입니다.
>
> · 통계는 지금 값 바로 앞 W개로만 냅니다. 지금 값을 넣으면 튀는 값이 자기 평균을 끌어올려 자신을 숨깁니다
> · values[i-window:i] — 파이썬 슬라이싱은 뒤끝(i)을 뺍니다
> · 앞이 모자라면(i < window) None 을 돌려줍니다
> · 표본 표준편차는 (개수−1)로 나눕니다
>
> seg = values[i - window:i]
> mean = sum(seg) / len(seg)
> var = sum((x-mean)**2 for x in seg) / (len(seg)-1)

**힌트 2 (25분 · TODO 2 임계 판정)**
> 📌 힌트 2 — TODO 2 (임계 판정)
>
> ZeroDivisionError 가 나면 표준편차가 0인 구간입니다.
> 최근 W개가 완전히 똑같았다는 뜻이고, 실제 데이터에서 생깁니다.
>
> 정답이 하나가 아닙니다. 하나를 고르고 이유를 말할 수 있으면 됩니다:
> · False 를 돌려준다 — 판단 불가 구간은 알람을 안 울린다
> · 값이 평균과 다르면 True — 계속 같다가 달라졌으니 이상이다
> · 아주 작은 수를 더해 나눈다 — 편법. 왜 위험한지 설명할 수 있어야 합니다
>
> z = (value - mean) / std
> return abs(z) > k

**힌트 3 (35분 · TODO 3 결측 처리)**
> 📌 힌트 3 — TODO 3 (결측 처리)
>
> TypeError: unsupported operand ... 가 나면 None 을 계산에 넣은 것입니다.
> EQ-01 에 2시간(120개)짜리 결측이 들어 있습니다.
> 값이 없는지 확인은 if v is None: 입니다 (== None 이 아닙니다).
>
> 선택지와 각각의 대가 — 이게 Step 3 비교의 재료입니다:
> · 그냥 둔다 (그 구간은 판정 안 함) — 결측 자체는 못 잡는다
> · 직전 값으로 메운다 (ffill) — 2시간 내내 같은 값 → 표준편차 0 → 값이 돌아오는 순간 없던 이상이 생긴다
> · 앞뒤 평균으로 보간 — 없는 값을 지어낸다. 그 구간 z-score 는 거짓말이 된다
> · 결측 자체를 이상으로 본다 — 센서 고장을 잡는다. 단, 온도·진동 이상과는 다른 종류

---

## 마칠 때

**말로 두 문장** — "내 번호 적어 두세요" · "`2일차` 폴더 지우지 마세요"(내일이 오늘 코드를 그대로 씁니다).
**단톡방에는 이걸** 붙여넣습니다:

> 오늘 수고하셨습니다. 내일을 위해 두 가지만 부탁드립니다.
>
> **① 내 공장 번호를 적어 두세요.** (화면에 떴던 `S07` 같은 번호)
> 잊어도 `uv run 내번호.py` 를 다시 치면 같은 번호가 나옵니다.
>
> **② `2일차` 폴더를 지우지 마세요.**
> 내일 실습이 오늘 짠 `detect.py` 를 그대로 불러 씁니다.
>
> 내일은 여러분이 만든 코드를 AI가 도구로 쓰고, **화면 속 로봇이 실제로 움직입니다.**

**수업 뒤 정리**

```bash
uv run tools/제어개방.py --end       # 제어 잠금 · 주입 중단 · 배속 60 원복
```
서버는 켜 둬도 꺼도 됩니다 — 내일 다시 켜면 공장은 새로 시작하고 학생 코드에는 영향 없습니다.

---

## 막히면

| 증상 | 여는 것 / 시킬 것 |
|---|---|
| 서버·접속·배정이 이상하다 | `특강/시뮬레이터/docs/운영.md` (증상 → 처치 표) |
| 학생이 코드에서 막혔다 | `uv run 점검.py` → `--힌트 1` → 정 안 되면 `--열기 1` |
| 압축 안 풀고 ZIP 안에서 열었다 | 압축부터 풀게 하세요 (첫 실습 단골) |

## (참고) 앞뒤 4시간 세션과 잇는 한 문장

대표님 세션(「AI를 활용한 프로그램개발 및 프로젝트 관리」)이 앞이나 뒤에 붙습니다. 산출물은 서로 안 쓰고 **숫자 하나가 건너갑니다** —
저쪽이 먼저였다면 39장에서: *"어제 명세에 「오경보 몇 건 이하」라고 적었죠. 그 숫자가 어디서 나오는지 오늘 봤습니다."*
우리가 먼저라면: *"오늘 잰 이 표(81건/911건), 버리지 마세요. 다음 시간에 명세에 그대로 들어갑니다."*
학생이 *"어제는 명세만 쓰면 AI가 만든다더니 왜 오늘은 손으로?"* 라고 물으면 —
**"명세에 숫자를 못 박으려면 그 숫자가 뭘 뜻하는지 알아야 합니다. 오늘이 그걸 아는 시간입니다."**
