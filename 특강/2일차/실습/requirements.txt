# 2일차 실습 — run.py 가 CSV 를 읽고 채점하는 데 씁니다.
# pandas 3.x 는 이 환경에서 DLL 로드가 시스템 정책에 막혀 2.2.x 로 고정합니다
# (7일치 CSV 를 만든 환경과 같은 버전대).
pandas>=2.2,<3

# 내번호.py 가 강사 서버에서 내 공장 번호를 받아 오는 데 씁니다.
# 이게 없으면 첫 명령에서 ModuleNotFoundError 로 전원이 막힙니다.
httpx>=0.27
