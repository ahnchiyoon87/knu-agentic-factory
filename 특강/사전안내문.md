# [사전 안내] 경남대 RISE 피지컬AI 사관학교 — 8월 AI 특강 준비 안내

안녕하세요. 특강에서 여러분은 **AI에게 말로 시켜서, 가상 공장을 감시하고 움직이는
시스템을 이틀 만에 직접 만들게 됩니다.** 첫날 수업이 바로 실습으로 시작되므로,
아래 준비를 **특강 전날까지** 꼭 마쳐 주세요. 15~20분이면 됩니다.

> 준비가 안 된 채 오시면 첫날 첫 시간을 설치로 보내게 되어 실습 진도를 놓칩니다.
> 막히는 부분은 그대로 두고 오셔도 됩니다 — 첫날 셋업 시간에 도와드립니다.
> 단, **아래 ①~⑤를 시도는 해 보고 오는 것**이 중요합니다.

> **AI 코딩 도구는 따로 설치하지 않으셔도 됩니다.** AI로 코드를 만드는 과정은
> **강사가 화면에서 직접 보여 드립니다.** 여러분은 각자 해 보시고, 막히면
> 실습 폴더에 같이 들어 있는 **정답을 불러와** 다음으로 넘어가면 됩니다.

---

## ① uv 설치 (2분)

파이썬을 직접 설치하지 않습니다. **`uv` 하나만 깔면** 실습에 필요한 파이썬과 라이브러리를
`uv` 가 알아서 갖춥니다. 컴퓨터에 이미 파이썬이 깔려 있어도 상관없습니다 — 건드리지 않습니다.

명령 프롬프트(cmd)를 열고 **아래 한 줄을 붙여넣고 Enter.** (윈도우 키 → `cmd` → Enter)

```
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

확인 — **cmd 창을 새로 열고** `uv --version` → 버전 숫자가 나오면 성공입니다.

## ② VS Code 설치 (3분)

1. https://code.visualstudio.com 에서 다운로드 후 설치 (기본값으로)
2. 확인: 실행해서 창이 열리면 성공

## ③ 실습 파일 받기 (3분)

첫날 첫 시간에 바로 여는 폴더입니다. **미리 받아 두면 당일 5분을 아낍니다.**
**아래 둘 중 편한 쪽 하나만** 하시면 됩니다. 내용은 같습니다.

**방법 A — `git clone`** (git 을 쓰실 줄 알면 이쪽이 빠릅니다)

```
git clone https://github.com/ahnchiyoon87/k-precision-lab.git
```

**방법 B — 구글드라이브에서 ZIP 받기**

1. 아래 폴더에서 **`k-precision-lab.zip`** 을 내려받습니다 (약 1MB)

   https://drive.google.com/drive/folders/1WsQmwnfv21qAbAb79X1BS95djSz0Vvti?usp=sharing

2. **압축을 풉니다.** 위치는 어디든 상관없습니다 (바탕화면·다운로드 폴더 등)

**둘 중 무엇으로 받았든 마지막에 이것만 확인하세요**

3. `k-precision-lab` 폴더 안에 **`2일차` `3일차` `데이터`** 세 폴더가 보이면 성공입니다

4. **VS Code 에서 그 `k-precision-lab` 폴더를 엽니다.** (파일 → 폴더 열기)

> **ZIP 으로 받으셨으면 압축을 꼭 풀어서 쓰세요.** ZIP 안에서 바로 열면 코드가 안 돌아갑니다.
>
> 안 되면 **그대로 오세요.** 첫날 USB 로 드립니다. 여기서 붙잡고 있지 마세요.

## ④ 실습 준비 한 번 돌려 보기 (2분)

③ 에서 받은 폴더를 **VS Code 로 열고**, 터미널에 그대로 입력하세요.

```
cd 2일차/실습
uv run 점검.py
```

**처음 한 번은 1~2분 걸립니다** — 실습에 필요한 파이썬과 라이브러리를 이때 갖춥니다.
화면에 점검 결과가 뜨면 성공입니다. **미리 해 두면 당일 이 시간을 아낍니다.**

> 강의장에서 39명이 동시에 받으면 느립니다. 집에서 미리 한 번 돌려 두세요.

## ⑤ 노션(Notion) 계정 (3분)

**첫날 1교시가 노션입니다.** 앞으로 배울 것을 쌓을 학습 노트를 그 시간에 직접 만듭니다.
계정만 미리 만들어 두시면 됩니다 — **무료 계정으로 충분합니다.**

1. https://www.notion.so 에서 **가입** (구글 계정으로 하면 빠릅니다)
2. 확인: 로그인해서 왼쪽에 사이드바가 보이면 성공입니다

> 한국어로 쓰려면 오른쪽 아래 설정에서 언어를 한국어로 바꾸세요.
> **미리 뭘 만들어 둘 필요는 없습니다.** 첫날 1교시에 처음부터 같이 만듭니다.

---

## 준비 완료 체크리스트

| # | 확인 | 방법 |
|---|---|---|
| ① | `uv --version` → 버전 숫자 표시 | 명령 프롬프트 |
| ② | VS Code 실행됨 | — |
| ③ | `k-precision-lab` 폴더 안에 **`2일차` `3일차` `데이터`** 가 있다 | VS Code 로 열어서 확인 |
| ④ | `2일차/실습` 에서 `uv run 점검.py` 가 결과를 찍는다 | VS Code 터미널 |
| ⑤ | 노션 로그인 됨 (무료 계정) | 브라우저 |

## ⚠️ 회사·학교 노트북을 가져오시는 분

보안 프로그램이나 방화벽이 걸린 노트북은 **설치나 접속이 차단될 수 있습니다.**
- 위 ①~⑤ 중 하나라도 "권한 없음 / 차단됨"이 뜨면, **가능하면 개인 노트북**을 가져오세요
- 개인 노트북이 없다면 그 상태 그대로 오세요 — 첫날 대응 방법을 안내합니다
- 강의장에서는 전용 무선망을 제공합니다. 유심·핫스팟은 예비 수단으로만 준비하시면 충분합니다

## 문의

- 준비 중 막히면 **카톡 단체방**에 올려 주세요 (담당자가 초대해 드립니다)
- 에러 화면을 **캡처해서** 올려 주시면 빠르게 도와드릴 수 있습니다

*— 이틀 뒤, 여러분이 만든 AI가 공장을 움직입니다. 강의장에서 뵙겠습니다.*

---

# 단톡방 복붙 블록

## D-7 — 카톡용 짧은 본문 (위 문서 전체를 첨부하며 함께)

> 안녕하세요. 경남대 RISE 피지컬AI 사관학교 **8월 AI 특강** 준비 안내드립니다.
>
> 이번 특강에서 여러분은 **AI에게 말로 시켜서 가상 공장을 감시하고 움직이는 시스템을 직접 만들게** 됩니다. 첫날 수업이 바로 실습으로 시작하므로 아래 준비를 **특강 전날까지** 마쳐 주세요. **15~20분**이면 됩니다.
>
> **① uv 설치** — 파이썬은 따로 안 깔아도 됩니다. cmd 를 열고 아래 한 줄을 붙여넣으세요.
> ```
> powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
> ```
> cmd 를 새로 열고 `uv --version` 이 나오면 성공입니다.
>
> **② VS Code 설치** — https://code.visualstudio.com (기본값으로)
>
> **③ 실습 파일 받기** — 아래 폴더에서 `k-precision-lab.zip`(약 1MB)을 받아 **압축을 풀어 주세요.**
> https://drive.google.com/drive/folders/1WsQmwnfv21qAbAb79X1BS95djSz0Vvti?usp=sharing
> git 을 쓰실 줄 알면 이것도 됩니다 — `git clone https://github.com/ahnchiyoon87/k-precision-lab.git`
> 압축 푼 `k-precision-lab` 폴더 안에 **`2일차` `3일차` `데이터`** 세 폴더가 보이면 성공입니다.
> **ZIP 안에서 바로 열면 안 됩니다. 꼭 압축을 푸세요.**
>
> **④ 한 번 돌려 보기** — VS Code로 `k-precision-lab` 폴더를 열고, 터미널에 그대로 입력
> ```
> cd 2일차/실습
> uv run 점검.py
> ```
> 처음 한 번은 1~2분 걸립니다. 필요한 것을 이때 갖춥니다.
>
> **⑤ 노션 가입** — https://www.notion.so (무료 계정이면 충분)
> 첫날 1교시에 학습 노트를 함께 만듭니다. **미리 뭘 만들어 두실 필요는 없습니다.**
>
> ---
>
> **AI 코딩 도구는 설치하지 않으셔도 됩니다.** AI로 코드를 만드는 과정은 강사가 화면에서 직접 보여 드립니다. GitHub 계정도 필요 없습니다.
>
> 자세한 안내문을 첨부합니다. 막히는 부분은 **그대로 두고 오셔도 됩니다** — 첫날 도와드립니다.
> 다만 **①~⑤를 시도는 해 보고** 오시는 것이 중요합니다.
>
> 준비 중 막히시면 이 단체방에 캡처와 함께 올려 주세요.
>
> 이틀 뒤, 여러분이 만든 AI가 공장을 움직입니다. 강의장에서 뵙겠습니다.

## D-1 — 리마인드

> 내일 특강입니다. 준비 확인만 부탁드려요.
>
> ☐ uv 설치 (`uv --version` 으로 확인)
> ☐ VS Code 설치
> ☐ `k-precision-lab` 폴더 (압축 푼 상태)
> ☐ `2일차/실습` 에서 `uv run 점검.py` 한 번 돌려 봄
> ☐ 노션 로그인
>
> 안 된 게 있어도 **그대로 오세요.** 첫날 도와드립니다.
> **노트북 전원 어댑터 꼭 챙기세요.** 4시간 연속 씁니다.

## 자주 오는 질문 — 답변 복붙용

**"`uv` 가 없다고 나옵니다"**
> 설치 후 **cmd 창을 새로 열어야** 인식됩니다. 새 창에서 `uv --version` 을 다시 쳐 보세요.

**"파이썬이 이미 깔려 있는데 괜찮나요?"**
> 괜찮습니다. `uv` 는 실습 폴더 안에만 필요한 것을 따로 갖추고, 이미 깔린 파이썬은 건드리지 않습니다.

**"처음 실행이 오래 걸립니다"**
> 정상입니다. 처음 한 번만 1~2분 걸리고, 그다음부터는 바로 뜹니다.

**"ZIP을 어디에 풀어야 하나요?"**
> 어디든 상관없습니다. 다운로드 폴더나 바탕화면 아무 데나요.
> **다만 압축은 꼭 푸셔야 합니다.** ZIP 안에서 바로 열면 코드가 안 돌아갑니다.

**"AI 코딩 도구는 안 깔아도 되나요?"**
> 네, 안 깔으셔도 됩니다. AI로 코드를 만드는 과정은 강사가 화면에서 직접 보여 드립니다.

**"git 이 없는데요"**
> 안 쓰셔도 됩니다. 드라이브 링크에서 ZIP을 받아 압축만 푸시면 똑같습니다.

**"노트북이 회사/학교 것인데 설치가 막힙니다"**
> 가능하면 개인 노트북을 가져와 주세요.
> 없으시면 **그 상태 그대로 오세요.** 첫날 대응 방법을 안내드립니다.

**"준비를 못 했는데 가도 되나요?"**
> 네, 오세요. 첫날 셋업 시간에 도와드립니다.
> 다만 시도는 해 보고 오시면 훨씬 빨리 끝납니다.
