# 특강 — 특강을 하는 데 쓰는 전부

| 폴더 | 누가 보나 | 무엇 |
|---|---|---|
| **`당일/`** | 강사 (강의장) | 강의하는 날 여는 것 — `진행표.md` 하나면 됩니다 |
| **`학생배포/`** | 학생 | 학생에게 통째로 나가는 실습 파일의 **원본** (배포본은 `제작/검증도구/배포본만들기.py` 가 뽑는다) |
| **`시뮬레이터/`** | 강사 (서버) | 가상 공장 서버 — 당일에도 이걸 돌립니다 (`시작.bat` · `tools/제어개방.py` · `tools/자리배정.py`) |
| **`강의안/`** | 발주처 | 제출용 docx 2개 (★ 여기만 2·3일차 표기 — 대외 기준) |

자료를 만들거나 고치는 것은 전부 `제작/` 에 있습니다.
