# 1일차 이상감지 힌트 카드 — 강의자료 쪽 인계본

**용도** — W8 「1일차 이상감지 단계별 힌트 카드 3장」(교안 강사 노트: *"15분이 지나면 첫 번째 힌트를 전체에 공개하세요"*)의 **기술 내용**입니다.
문구·인쇄 양식·공개 시점은 강의자료 쪽에서 정하십시오. 여기에는 사실만 적었습니다.

> 교안 강사 노트 — *"1일차 이상감지가 컴공 전공생 사이에서 만족도가 가장 크게 갈리는 지점입니다."*

---

## 힌트 1 — 15분 경과 시 전체 공개 (TODO 1 을 못 넘는 학생용)

막히는 곳은 대부분 **"지금 값을 통계에 넣느냐"** 와 **슬라이싱 범위**입니다.

- 통계는 **지금 값 바로 앞** W개로만 냅니다. 지금 값을 넣으면 튀는 값이 자기 평균을 끌어올려 자기를 숨깁니다
- `values[i - window:i]` — `i` 는 포함되지 않습니다. 파이썬 슬라이싱은 뒤끝을 뺍니다
- 앞이 모자라면(`i < window`) 판단할 수 없으므로 `None` 을 돌려줍니다
- 표본 표준편차는 `(개수 - 1)` 로 나눕니다

```python
seg = values[i - window:i]
mean = sum(seg) / len(seg)
var  = sum((x - mean) ** 2 for x in seg) / (len(seg) - 1)
```

---

## 힌트 2 — 25분 경과 시 (TODO 2 · 0으로 나누기에서 멈춘 학생용)

`ZeroDivisionError` 가 나면 **표준편차가 0** 인 구간입니다. 최근 W개가 완전히 똑같았다는 뜻이고, 실제 데이터에서 생깁니다.

정답이 하나가 아닙니다. 다음 중 하나를 고르고 **왜 그런지 말할 수 있으면** 됩니다.

| 선택 | 뜻 |
|---|---|
| `False` 를 돌려준다 | 판단 불가 구간은 알람을 안 울린다 |
| 값이 평균과 다르면 `True` | 계속 같던 값이 갑자기 달라졌으니 이상이다 |
| 아주 작은 수를 더해 나눈다 | 편법. 왜 위험한지 설명할 수 있어야 함 |

z-score 자체는 한 줄입니다.

```python
z = (value - mean) / std
return abs(z) > k
```

---

## 힌트 3 — 35분 경과 시 (TODO 3 · 결측에서 멈춘 학생용)

`TypeError: unsupported operand type(s)` 가 나면 **`None` 을 계산에 넣은 것**입니다.
EQ-01 에 2시간(120개) 짜리 결측이 들어 있습니다.

선택지와 **각각의 대가**입니다. 이게 Step 3 토론의 재료입니다.

| 방침 | 대가 |
|---|---|
| 그냥 둔다 (그 구간은 판정 안 함) | 결측 자체는 못 잡는다 |
| 직전 값으로 메운다 (ffill) | 2시간 내내 같은 값 → **표준편차가 0** 이 되고, 값이 돌아오는 순간 **없던 이상이 하나 생긴다** |
| 앞뒤 평균으로 보간 | 없는 값을 지어낸다. 그 구간 z-score 는 거짓말이 된다 |
| 결측 자체를 이상으로 본다 | 센서 고장을 잡는다. 다만 온도·진동 이상과는 다른 종류다 |

값이 없는지 확인은 `if v is None:` 입니다. `== None` 이 아닙니다.

---

## 참고 — Step 3 비교 시 짚을 것

참고 구현(`정답/detect_answer.py`)은 **"메우지 않고 그대로 둔다"** 를 골랐습니다. 그래서 센서 결측 121개 중 **0개**를 잡습니다.

이것이 유일한 정답이 아닙니다. **학생이 "결측 자체를 이상으로 본다"를 골라 3종을 다 잡았다면 그쪽이 더 나은 선택**입니다. Claude 가 만든 구현은 대개 `ffill` 이나 `dropna` 를 쓰는데, 위 표의 두 번째 줄(없던 이상이 생김)을 짚어 주면 "AI가 내놓은 결과물을 판정할 수 있는 사람"이라는 Step 3 의 메시지로 이어집니다.

---

## 실측 수치 (강사가 알고 있어야 할 것)

참고 구현 · 윈도 60분 기준. 데이터는 W5 7일치 60,480행.

| 임계 | 아닌데 울린 횟수 | 하루 평균 | 드리프트 | 스파이크 | 결측 |
|---|---|---|---|---|---|
| k=4.0 | 40 | 6번 | 0/241 | 3/3 | 0/121 |
| k=3.0 | 566 | 81번 | 5/241 (노이즈 수준) | 3/3 | 0/121 |
| k=2.0 | 6,374 | 911번 | 33/241 (노이즈 수준) | 3/3 | 0/121 |

- 60,480행 처리에 약 **3초** 걸립니다. 학생 노트북에서도 기다리는 시간이 되지 않습니다
- **k 를 아무리 낮춰도 드리프트는 안 잡힙니다.** 이동평균이 함께 올라가기 때문입니다
- 스파이크는 어느 k 에서도 3/3 잡힙니다
