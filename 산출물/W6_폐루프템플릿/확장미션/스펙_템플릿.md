# 확장 미션 스펙 — ○○팀

> 평가 배점 중 **스펙 문서화 30점**이 이 문서입니다.
> Day 2 에서 쓴 4단계(의도 → 요구 → 스펙 → 인수조건)를 그대로 회수합니다.
> 코드보다 이걸 먼저 쓰면, 데모 발표의 「설계 설명 2분」이 그대로 나옵니다.

---

## 1. 의도 — 왜 이 에이전트인가

| | |
|---|---|
| 고른 미션 | A 예방정비 스케줄러 / B AMR 작업 할당 / C 안전 인터록 / D 교대 리포트 / 자유 |
| 에이전트 이름 | |
| **누가** 곤란한가 | |
| 지금은 **누가 그 일을 하는가** | |
| 이 에이전트가 없으면 **무슨 일이 벌어지는가** | |

## 2. 요구 — 무엇을 하는가

**기능**

1.
2.

**하지 않는 것** (경계를 적어야 스펙입니다)

1.
2.

**붙는 자리**

| | |
|---|---|
| `EXTRA` (회차 끝) / `ACT` 교체 (조치 앞) / 그 외 | |
| 그렇게 고른 이유 | |

## 3. 스펙 — EARS 로 씁니다

> **When** `<트리거>`, the `<시스템>` shall `<응답>` — 사건 구동
> **While** `<상태>`, the `<시스템>` shall `<응답>` — 상태 구동
> **If** `<원치 않는 일>`, then the `<시스템>` shall `<응답>` — 예외
> **Where** `<선택 기능>`, the `<시스템>` shall `<응답>` — 선택

예시 —
*When 진단 등급이 HIGH 이면, 안전 인터록 에이전트는 인접 설비를 목록으로 만들어 한 번의 승인 요청으로 묶어야 한다.*

1.
2.
3.

**승인 규칙** — 되돌릴 수 없는 행동을 내는가?

| 내는 명령 | 자동 / 승인 | 그렇게 정한 이유 |
|---|---|---|
| | | |

> 교안 기준: 감속·알람확인은 자동, **정지·로봇 파견은 사람 승인**입니다.
> 우리 에이전트가 이 기준을 바꾼다면 **왜 바꾸는지**를 여기 적어야 합니다.

## 4. 인수조건 — GWT 로 씁니다

> **Given** `<전제>`, **When** `<행동>`, **Then** `<결과>`

| # | Given | When | Then | 확인함 |
|---|---|---|---|---|
| 1 | | | | ☐ |
| 2 | | | | ☐ |
| 3 | | | | ☐ |

**데모에서 보여 줄 순서** (시연 3분)

1.
2.
3.

---

## 5. 쓴 데이터 · 도구

| | 무엇을 | 어디서 |
|---|---|---|
| 읽기 | | `ctx.api.` |
| 제어 | | `ctx.control.` |
| 기록 | | `record` / `실행기록.jsonl` |

## 6. 안 되면 어떻게 되는가

| 실패 상황 | 에이전트의 행동 |
|---|---|
| 데이터가 없다 | |
| 제어가 거부됐다 | |
| 사람이 승인을 안 했다 | |
