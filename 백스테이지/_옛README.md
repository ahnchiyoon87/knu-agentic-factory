# 강사용

| | 무엇 | 언제 |
|---|---|---|
| `진행표.md` | ★ **당일 여는 문서.** 슬라이드 번호로 「언제 뭘 누르고 말하나」 | 수업 중 계속 |
| `서버운영.md` | 서버 띄우기 · 학생 접속 · 문제 해결 | 당일 아침 |
| `리허설.py` | 이틀을 사람 없이 재생해 본다 (60항목 · 약 6분) | **D-7** |
| `배포본만들기.py` | 학생에게 줄 실습 저장소를 여기서 뽑는다 | **D-7** |
| `시뮬레이터/` | 가상 공장 서버 (`시작.bat` 더블클릭) | 당일 |
| `센서데이터/` | 7일치 CSV — 실습이 읽어 간다 | 건드릴 일 없음 |
| `인쇄물/` | 사전안내문(D-7 발송) · 힌트카드(D-1 인쇄) | 수업 전 |
| `힌트카드.md` | 힌트 카드에 적힌 내용 | 1일차 15분 뒤 공개 |
| `폐루프_강사우회.md` | 2일차 오후에 학생이 못 하는 것을 강사가 뚫는 법 | 막혔을 때 |

## D-7 에 두 번 돌립니다

```bash
cd 강사용
python 리허설.py --token <강사토큰>      # 이틀 전체가 도는지 (60/60 이 나와야 함)
python 배포본만들기.py --검증             # 학생 저장소를 뽑고 학생 조건으로 확인
```

리허설은 **학생 환경 그대로**(cp949, UTF8 강제 없음) 돌립니다.
학생 PC 에서만 나는 인코딩 결함이 여기서 걸립니다.

## 당일 아침 (30분 전)

```bash
시뮬레이터\시작.bat                                    # ① 서버 (창을 닫으면 꺼집니다)
cd 시뮬레이터 && python tools/제어개방.py --status      # ② 공장 39개 · 경고 없음
python tools/자리배정.py --전부풀기 --예                  # ③ 배정 비우기 (1일차 아침에만)
```

## 수업 중 — 슬라이드에서 누르는 것 둘

```bash
python tools/제어개방.py                                # 2일차 24장 — 제어 열기
python tools/제어개방.py --inject-all --stagger 2       # 1일차 19장 · 2일차 32장 — 고장 주입
python tools/제어개방.py --end                          # 수업 뒤 되돌리기
```

**나머지는 슬라이드를 넘기기만 합니다.** 어느 장에서 무엇을 하는지는 `진행표.md`.
