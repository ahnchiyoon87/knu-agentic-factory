# 배포본 — 여기는 **산출물**입니다

`백스테이지/배포본만들기.py` 가 만들어 넣는 곳입니다. **손으로 고치지 마세요.**
고칠 것은 `학생배포/` 쪽이고, 그다음 다시 뽑으면 됩니다.

```bash
cd 백스테이지
python 배포본만들기.py --검증
```

`k-precision-lab/` 이 학생이 `git clone` 하는 저장소가 됩니다.
한 번 push 하면 그 안에 `.git` 이 생기고, 다시 뽑아도 **`.git` 은 지우지 않습니다** —
수정본을 뽑고 commit 만 하면 학생 쪽에 반영됩니다.
