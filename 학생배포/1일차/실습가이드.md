# 1일차 핸즈온 가이드 — 이상을 잡는 자를 만든다

경남대 RISE 피지컬AI 사관학교 · 8월 Agentic AI 특강

> **이 문서 하나로 오늘 실습을 끝까지 갈 수 있습니다.**
> 막히는 자리마다 조치가 바로 아래 적혀 있습니다. 그래도 안 되면 **손만 드세요.**
> 앞에 나와서 발표하는 일은 오늘도, 내일도 없습니다.

---

## 오늘 무엇을 하나

| 시간 | 무엇 | 결과물 |
|---|---|---|
| 20분 | **§1** 내 공장 열어보기 | 브라우저에 도는 내 공장 |
| 50분 | **§2** 이상감지 알고리즘 직접 구현 | 완성된 `detect.py` |
| 20분 | **§3** 오늘 걸 노션에 정리 | 카드 2~3장 |

오늘 손으로 만드는 것은 **`detect.py` 안의 세 함수**입니다. 나머지는 전부 준비돼 있습니다.

### 준비물 확인 (지금 2개만 봅니다)

1. **VS Code** 가 열리는가.
2. **실습 저장소**를 내려받았는가 — VS Code 왼쪽 파일 목록에
   **`1일차` `2일차` `데이터`** 세 폴더가 보이면 됩니다.

**아직 안 받았으면 지금 받습니다** (사전안내문 ③ 과 같습니다).
**둘 중 편한 쪽 하나만** 하면 됩니다. 내용은 같습니다.

**방법 A — `git clone`**

```bash
git clone https://github.com/ahnchiyoon87/k-precision-lab.git
```

**방법 B — 링크에서 ZIP 받기**

1. 강사가 알려 준 링크에서 **`k-precision-lab.zip`** 을 내려받습니다 (약 1MB)
2. **압축을 풉니다.** 위치는 어디든 상관없습니다

**둘 중 무엇으로 받았든** VS Code 에서 **`k-precision-lab` 폴더를 엽니다** (파일 → 폴더 열기).

> **ZIP 으로 받았으면 압축을 꼭 풀어서 쓰세요.** ZIP 안에서 바로 열면 코드가 안 돌아갑니다.
> **안 되면 손 드세요. USB 로 드립니다.** 여기서 5분 넘게 붙잡고 있지 마세요.

**이 문서(`1일차/실습가이드.md`)를 VS Code 에 열어 두고 따라오세요.** 오늘 내내 봅니다.

> **오늘 여는 폴더는 `1일차/실습` 하나뿐입니다.** `2일차` 는 내일 엽니다.

둘 중 하나라도 안 되면 지금 손 드세요. 여기서 막히면 뒤가 전부 막힙니다.

---

# §1. 내 공장 열어보기 (20분)

### 왜 하는가

오늘 잡을 이상이 어떻게 생겼는지 **눈으로 먼저 봅니다.**
사람 눈으로 알아챌 수 있는지 없는지를 확인하는 것이 §2 를 하는 이유가 됩니다.

### 1-0. 내 공장 받기 (2분) — 이 한 줄이 시작입니다

```bash
cd 1일차/실습
pip install -r requirements.txt
python 내번호.py
```

강사가 화면에 띄운 **서버 주소**를 물어보면 그대로 넣으세요. 그러면 이렇게 뜹니다.

```
==========================================================
  당신의 공장은  ★ S07 ★  입니다
==========================================================

  ■ 1일차 — 크롬 주소창에 그대로 붙여넣으세요
     http://192.168.0.10:8000/view?tenant=S07
```

**★ 표시된 번호를 적어 두세요.** 오늘 기억할 것은 이것 하나뿐입니다.

> **자리를 옮기거나 컴퓨터를 바꿨다면** — `python 내번호.py S07` 처럼 그 번호를 뒤에
> 붙여 다시 실행하면 **같은 공장을 그대로 가져옵니다.**
>
> **지금 번호가 뭐였는지 잊었으면** — `python 내번호.py` 를 그냥 다시 치세요.
> 몇 번을 쳐도 같은 번호가 나옵니다. 번호가 바뀌거나 소모되지 않습니다.
>
> 내일 쓸 설정(`config.json`)은 **이때 자동으로 채워집니다.** 손으로 적을 것이 없습니다.

### 1-1. 접속 (3분)

방금 화면에 뜬 **공장 보기 주소**를 브라우저 주소창에 그대로 붙여 넣습니다.

```
http://<강사가 알려 준 서버 주소>/view?tenant=<내 번호>
```

- **주소 전체가 위 화면에 이미 찍혀 있습니다.** 직접 조합하지 마세요.
- 각자 번호가 다릅니다. 옆 사람 번호로 들어가면 남의 공장이 보입니다.

**성공 판정** — 2D 공장 화면이 뜨고, 그 안에서 세 가지가 보입니다.

| 무엇 | 몇 개 | 확인 |
|---|---|---|
| 네모 (CNC 설비) | 6개 — `EQ-01` ~ `EQ-06` | ☐ |
| 움직이는 점 (AMR 로봇) | 2개 — `AMR-01` `AMR-02` | ☐ |
| 숫자 (온도·진동·rpm) | 1~2초마다 바뀜 | ☐ |

**막히면**

| 증상 | 조치 |
|---|---|
| 화면이 아예 안 뜬다 | 주소를 다시 확인. `http://` 를 빼먹지 않았는지 |
| 화면은 떴는데 숫자가 안 바뀐다 | 새로고침 한 번(F5). 그래도 멈춰 있으면 손 들기 |
| 네모가 6개가 아니다 | 남의 번호로 들어갔을 수 있습니다. `python 내번호.py` 로 내 번호 다시 확인 |

### 1-2. 평소 값을 눈에 익힙니다 (5분)

**설비마다 평소 온도가 다릅니다.** 이건 고장이 아닙니다.

- `EQ-04` 는 56도 근처, `EQ-05` 는 74도 근처에서 놉니다.
- 그래서 **모든 설비에 같은 경고선 하나를 그을 수가 없습니다.** 이게 오늘 강의의 출발점입니다.

지금 **여섯 대의 온도를 한 번 적어 두세요.** 잠시 뒤에 다시 봅니다.

```
EQ-01 ______   EQ-02 ______   EQ-03 ______
EQ-04 ______   EQ-05 ______   EQ-06 ______
```

### 1-3. 강사가 고장을 하나 넣습니다 (10분)

강사 콘솔에서 여러분 공장에 **이상 세 가지 중 하나**를 주입합니다.

| 이름 | 어떻게 생겼나 |
|---|---|
| 드리프트 | 온도가 **서서히** 오릅니다 |
| 스파이크 | 진동이 **한순간** 튑니다 |
| 결측 | 센서 값이 **안 들어옵니다** (화면이 멈춘 것처럼 보임) |

주입 뒤 **2분 동안 화면만 보세요.** 그리고 아래 두 칸을 채웁니다.

```
무엇이 달라졌나?  ____________________________________
어느 설비인가?    ____________________________________
```

> **못 맞혀도 됩니다.** 맞히는 시간이 아니라, **사람 눈으로 되는지 확인하는 시간**입니다.
> 이 칸에 "모르겠다"라고 적혀 있는 것이 오늘 §2 를 하는 이유입니다.

### 1-4. 정리 (2분)

브라우저 탭은 **닫지 말고 그대로 두세요.** 오늘 끝까지 씁니다.

---

# §2. 이상감지 알고리즘 직접 구현 (50분)

### 왜 하는가 (교안 원문)

> **이 랩만큼은 AI에게 통째로 맡기지 않는다.**
> 전공에서 배운 알고리즘 감각이 AI 시대에 왜 여전히 무기인지 확인하는 시간이다.

이틀 내내 AI에게 시키는데 왜 이것만 직접 짤까요 — 마지막 단계에서 **AI가 짠 것과 나란히
놓고 판정하는 쪽에 서 보기 위해서**입니다. 판정할 수 있는 사람만이 AI를 도구로 부립니다.

### 2-0. 폴더 열기 (2분)

VS Code에서 이 폴더를 열고, **터미널에서 그 폴더로 들어갑니다.**

```bash
cd 1일차/실습
pip install -r requirements.txt
```

> **이 한 줄을 건너뛰지 마세요.** `run.py` 가 CSV 를 읽는 데 쓰는 **pandas** 를 깝니다.
> 안 깔면 잠시 뒤 `ModuleNotFoundError: No module named 'pandas'` 로 바로 멈춥니다.
> 1~2분 걸립니다. **`pip` 가 없다고 나오면** `python -m pip install -r requirements.txt` 로 하세요.

안에 이런 파일들이 있습니다.

| 파일 | 무엇 | 건드리나 |
|---|---|---|
| `detect.py` | **여기만 고칩니다** | ✅ |
| `run.py` | 내 구현을 데이터에 돌리고 채점 | ❌ |
| `점검.py` | **막혔을 때 여는 것** — 내 코드를 돌려보고 어디가 틀렸는지 짚어 줍니다 | ❌ |
| `내번호.py` | 내 공장 받기 — 처음에 한 번 | ❌ |
| `README.md` | 이 랩 설명 | 읽기만 |
| `정답/` | **시간이 다 됐을 때** `점검.py --열기` 가 읽는 완성본 | 직접 열지 마세요 |

데이터는 **K-정밀 7일치 CSV, 60,480행**(CNC 6대 × 7일 × 1분)입니다. 이미 들어 있습니다.
그 안에 **이상 세 가지가 심어져 있습니다.**

### 2-1. Step 1 — TODO 세 군데 채우기 (25분)

`detect.py` 를 열면 `# TODO 1` `# TODO 2` `# TODO 3` 이라고 적힌 자리가 세 군데 있습니다.

만드는 것은 **이동 윈도 z-score 이상감지**입니다.
— "최근 W개 값의 평균에서, 지금 값이 표준편차의 몇 배나 벗어났나"로 판정합니다.

```
z = ( x − μ ) / σ        x: 지금 값   μ: 최근 W개 평균   σ: 최근 W개 표준편차
```

| TODO | 함수 | 채울 내용 |
|---|---|---|
| 1 | `window_stats()` | 직전 W개 값으로 **평균과 표준편차** 계산 |
| 2 | `is_anomaly()` | z-score 가 임계값 **k 를 넘는지** 판정 |
| 3 | `handle_missing()` | 값이 **안 들어온 자리**를 어떻게 다룰지 |

**세 번째는 정답이 하나가 아닙니다.** 버릴지, 앞 값으로 채울지, 따로 표시할지 —
여러분이 고르는 것이고, 그 선택을 나중에 설명할 수 있으면 됩니다.

**힌트**

- C만 하다 온 사람을 위한 **인덱싱·슬라이싱 힌트 박스**가 `detect.py` 안에 붙어 있습니다.
  (예: 직전 W개는 `values[i-w:i]`)
- 표준편차가 **0이 되는 경우**를 한 번 생각해 보세요. 나누기에서 터집니다.
- **15분이 지나면 강사가 힌트 카드 1을 전체 공개**합니다.
  막혀 있는 게 정상 범위라는 뜻이니 부담 갖지 마세요.

**막히면 — `점검.py` 를 여세요. 혼자 헤매지 않게 만들어 둔 것입니다.**

`run.py` 는 다 채워야 돌아가지만, `점검.py` 는 **채우는 도중에도** 돌아갑니다.
세 함수를 하나씩 돌려 보고 **어디가 왜 틀렸는지만** 짚어 줍니다. 답은 주지 않습니다.

```bash
python 점검.py            내 코드가 지금 어디까지 됐는지
python 점검.py --힌트 1    개념 힌트
python 점검.py --힌트 2    의사코드 힌트
```

```
내 코드 점검   —   3개 중 1개 통과

  [O] TODO 1 · window_stats
  [ ] TODO 2 · is_anomaly
       표준편차가 0 인 구간에서 터집니다.

  다음에 볼 곳 — TODO 2
```

**시간이 다 됐는데도 막혀 있으면** — 마지막 수단이 있습니다.
막힌 함수 **하나만** 완성본으로 채웁니다. 나머지는 그대로 두고 계속 갑니다.

```bash
python 점검.py --열기 2     TODO 2 만 완성본으로
```

> 지금까지 쓴 것은 `detect_내가짠것.py` 로 남겨 둡니다. 없어지지 않습니다.
> **여기까지 왔다고 진 게 아닙니다.** 오늘의 목표는 세 함수를 혼자 짜는 게 아니라
> 다음 장면까지 같이 도착하는 것입니다. 채운 함수는 나중에 한 번 읽어 보세요.

| 증상 | 조치 |
|---|---|
| `ZeroDivisionError` | σ 가 0인 구간입니다. 0일 때 어떻게 할지 정해 주세요 |
| `IndexError` | 앞쪽 W개가 아직 안 쌓인 구간입니다. i < W 일 때를 먼저 처리하세요 |
| 뭘 채워야 할지 모르겠다 | `python 점검.py --힌트 1` → 그래도 모르면 `--힌트 2` |
| 시간이 없다 | `python 점검.py --열기 <번호>` 로 그 함수만 채우고 다음으로 |

### 2-2. Step 2 — 돌려보기 (15분)

**① 먼저 기본값으로 한 번 돌립니다.**

```bash
python run.py
```

> **여기서 다 같이 멈춥니다.** 먼저 끝난 사람도 다음 명령을 미리 치지 마세요.
> 강사가 "다 같이 엔터" 라고 하면 그때 돌립니다.

**② 화면에 나온 숫자를 아래 표에 그대로 옮겨 적습니다.**

```
정상 구간에서 잘못 울린 비율(기준선)  ________ %

심어진 이상 ①  ______ 개 중 ______ 개
심어진 이상 ②  ______ 개 중 ______ 개
심어진 이상 ③  ______ 개 중 ______ 개
```

**③ 이제 임계값을 흔들어 봅니다.** 한 번에 하나씩만 바꾸세요.

```bash
python run.py --k 2.0        # 임계값을 낮춘다 = 더 민감하게
python run.py --k 4.0        # 임계값을 높인다 = 더 둔하게
python run.py --window 30    # 윈도를 짧게
```

바꿀 때마다 **두 숫자를 같이** 보세요.

```
          잡은 것          아닌데 울린 것
k = 3.0   ________         ________
k = 2.0   ________         ________
k = 4.0   ________         ________
```

**읽는 법 — 이게 오늘 제일 중요합니다**

- 결과 줄의 `[O ]` 는 잡았다, `[  ]` 는 못 잡았다는 뜻입니다.
- `[~ ]` 는 **울리긴 했는데 정상 구간에서 울리는 빈도와 다를 게 없다**는 뜻입니다.
  이상 구간에서 몇 번 울렸다고 "잡았다"고 하면 안 됩니다. **아무 데서나 그만큼 울리고 있으니까요.**

<details>
<summary><b>▶ 세 번 다 돌려 본 뒤에 여기를 펴세요</b> — 먼저 펴면 오늘 제일 중요한 순간이 사라집니다</summary>

숫자가 예상과 다르게 나왔다면, **여러분 코드가 틀린 게 아닙니다.**

- 천천히 오르는 변화는 **최근 평균도 같이 올라갑니다.** 기준선 자체가 따라 움직이니
  지금 값과 평균의 차이가 벌어지지 않습니다. **z-score의 구조적 한계**입니다.
- 값이 아예 안 들어온 구간은 **계산이 통째로 건너뜁니다.** 없는 값은 평균에도, 판정에도
  들어가지 않으니 "이상하다"고 말할 기회 자체가 없습니다. 이걸 잡으려면 TODO 3에
  **별도 규칙**을 넣어야 합니다 — 그게 TODO 3의 정답이 여러 개인 이유입니다.
- 임계값을 낮추면 **아닌데 울리는 것이 훨씬 빠르게 늘어납니다.** 참고 구현 실측으로는
  기본값에서 하루 약 81번이던 것이 k=2.0에서 **하루 약 911번, 11배**가 됩니다.

**이 한계를 적어 두세요. 내일 실습이 정확히 이 문제에서 시작합니다.**

</details>

### 2-3. Step 3 — AI가 짠 것과 나란히 놓기 (10분)

**이 자리는 강사가 화면에서 직접 합니다.** 여러분은 화면을 보면서
**내가 짠 코드와 비교**하면 됩니다. 따로 설치할 것은 없습니다.

강사가 AI에게 시키는 것은 여러분이 방금 짠 것과 **같은 함수**입니다.

```
이동 윈도 z-score 방식의 이상감지 함수를 파이썬으로 짜 줘.
윈도 통계, 임계 판정, 결측 처리를 포함해서.
```

화면에 뜬 코드를 내 `detect.py` 와 나란히 놓고 봅니다. **특히 결측 처리(TODO 3)를 보세요.**

```
AI는 결측을 어떻게 처리했나?   ____________________________
내 방식과 다른 점은?           ____________________________
어느 쪽이 낫나, 왜?            ____________________________
```

세 번째 칸에 답할 수 있다는 것 — **그게 오늘 이 랩이 준 무기입니다.**

---

# §3. 오늘 걸 노션에 정리 (20분)

### 왜 하는가

오늘 겪은 것은 3일이면 못 알아봅니다. **지금 남겨야 남습니다.**
이틀치가 쌓이면 **링크 하나로 제출**합니다. 그게 수료 조건입니다.

### 3-1. 카드 만들기 (3분)

1교시에 만들어 둔 노션 페이지를 엽니다. 표 오른쪽 위 **새로 만들기 ▾ → 개념 카드**.

**오늘 안 풀린 것 2~3개만** 고릅니다. 많이 만들면 하나도 안 채워집니다.

고를 거리가 없으면 §2 에서 적은 칸을 보세요. 빈칸으로 남은 것이 곧 카드 제목입니다.

### 3-2. 채우기 (12분)

카드를 열면 네 구획이 자동으로 뜹니다.

| 구획 | 무엇을 쓰나 |
|---|---|
| 🗣 안 보고 설명하면 | **화면을 덮고** 내 문장으로. 안 써지면 그게 모르는 겁니다 — 그대로 두세요 |
| ❓ 이게 없으면 뭐가 안 되나 | 이 개념이 빠지면 무엇이 깨지는지 한 줄 |
| 💻 코드로는 | `/코드` 로 코드 블록을 만들고 오늘 쓴 줄을 붙입니다 |
| ▸ 수업 화면 / 내 필기 | **토글 안에** 넣고 **다시 접습니다** |

### 3-3. 마무리 (5분)

- 오늘 화면을 캡처해 붙입니다 (`Win`+`Shift`+`S` → 카드에서 `Ctrl`+`V`).
- **이해도** 태그를 솔직하게 찍습니다. 전부 「설명가능」이면 이 표는 아무 쓸모가 없습니다.
- **갤러리 뷰**로 바꿔서 오늘 카드가 제대로 보이는지 확인합니다.

### 숙제

**오늘 제일 헷갈렸던 것 카드 2장.** 30분이면 됩니다. 검사하지 않습니다. 쌓이는 게 목적입니다.

---

# 부록 A. 오늘 막힐 수 있는 곳 전부

| 언제 | 증상 | 조치 |
|---|---|---|
| §1 | 공장 화면이 안 뜬다 | 주소에 `http://` 가 있는지 · `python 내번호.py` 가 찍어 준 주소 그대로인지 |
| §1 | 숫자가 안 바뀐다 | F5 한 번 → 그래도면 손 들기 |
| §1 | 네모가 6개가 아니다 | 남의 번호로 들어갔을 가능성. `python 내번호.py` 재확인 |
| §1 | 내 번호를 잊었다 | `python 내번호.py` 를 그냥 다시 치세요. 같은 번호가 나옵니다 |
| §1 | 자리를 옮겼다 / 컴퓨터를 바꿨다 | `python 내번호.py S07` — 그 번호를 그대로 가져옵니다 |
| §2 | `python` 이 없다고 나온다 | `py run.py` 로 해 보세요 (윈도우 런처). 그래도면 손 들기 |
| §2 | `ModuleNotFoundError: No module named 'pandas'` | **2-0 의 `pip install` 을 안 했습니다.** 그것부터 하세요 |
| §2 | 그 밖의 `ModuleNotFoundError` | 폴더가 다릅니다. `1일차/실습` 안에서 실행하세요 |
| §2 | `센서 데이터를 못 찾았습니다` | 받은 폴더에 **`데이터`** 폴더가 같이 있어야 합니다. 없으면 저장소를 통째로 다시 받으세요 |
| §2 | `ZeroDivisionError` | σ 가 0인 구간. TODO 1·2에서 0일 때를 처리하세요 |
| §2 | `IndexError` | i < W 인 앞쪽 구간을 먼저 처리하세요 |
| §2 | `아직 채우지 않은 TODO 가 있습니다` | 친절 안내입니다. 이어서 적힌 그 함수를 채우면 됩니다 |
| §2 | 결과가 전부 0이다 | TODO 2의 부등호 방향을 확인하세요 |
| §2 | 결과가 전부 이상으로 나온다 | k 를 절댓값으로 비교하고 있는지 확인하세요 |
| §3 | 템플릿 버튼이 안 보인다 | **새로 만들기** 옆 작은 **▾** 를 눌러야 나옵니다 |
| §3 | 캡처가 안 붙는다 | 카드 안 빈 줄을 클릭한 뒤 `Ctrl`+`V` |

**여기 없는 증상이면 손 드세요.** 혼자 20분 붙잡고 있는 것이 제일 손해입니다.

---

# 부록 B. 오늘 쓴 명령 한눈에

```bash
cd 1일차/실습
pip install -r requirements.txt   # 처음 한 번만
python 내번호.py                   # 내 공장 받기 — 처음 한 번
python 내번호.py S07               # 자리를 옮겼을 때만

python 점검.py               # 내 코드가 어디까지 됐나
python 점검.py --힌트 1       # 개념 힌트 (--힌트 2 는 의사코드)
python 점검.py --열기 2       # ★ 시간이 다 됐을 때만, 그 함수 하나만

python run.py                # 기본값 (W=60, k=3.0)
python run.py --k 2.0        # 더 민감하게
python run.py --k 4.0        # 더 둔하게
python run.py --window 30    # 윈도를 짧게
```

---

# 부록 C. 먼저 끝낸 사람용 (선택)

1. `--window` 를 10 · 60 · 240 으로 바꿔 가며 돌려 보고, **윈도 크기와 잡히는 정도**의
   관계를 한 줄로 적어 보세요.
2. TODO 3을 **다른 전략**으로 한 번 더 짜 보고, 두 결과를 비교해 보세요.
3. 결측 구간을 **잡아내는 별도 규칙**을 만들어 보세요. (힌트 — 값이 아니라 *값이 안 온 것*을
   신호로 삼습니다.)

세 개 다 노션 카드로 남기면 내일이 훨씬 쉬워집니다.
