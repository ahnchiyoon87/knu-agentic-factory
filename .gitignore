# ── 자격증명 — 절대 커밋 금지 ─────────────────────────────
.env
.env.*
!.env.example

# ── 파이썬 ────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/

# ── 실행 중 생기는 것들 ───────────────────────────────────
*.log
실행기록.jsonl
_control_mcp.log
_verify_server.log
loadtest_result_*.json

# ── 저장소에 넣기엔 큰 원본 (…/_보관/ 에 있음) ────────────
*.pptx
*.zip
배포표.html
