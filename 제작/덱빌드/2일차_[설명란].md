# 2일차 — 설명란 (37장)

> **업로드할 파일 2개**
> ① `00_디자인기준판\디자인기준판.pdf`
> ② `2일차_[첨부]원고.md` ← **이 덱 범위만 담긴 원고. 다른 덱 원고를 같이 올리지 마라.**
>
> ※ **NotebookLM이 받는 형식** — PDF · txt · **Markdown(.md)**. HTML·PNG는 안 된다.
> **맞춤설정** — 언어 한국어 / 길이 기본값
> **설명 칸** — 아래 ▼부터 ▲까지 전부 복사해 붙여넣기
> **생성 결과** — 37장 → PPT로 내려받아 확정. 머리글·페이지 번호는 슬라이드 마스터에서 일괄 처리한다.

---

▼ 복붙 시작 ▼

**정확히 37장**의 슬라이드를 만들어라. 아래 1~37페이지 지정을 **한 지정 = 한 장**으로 따르고 합치거나 나누지 마라.

## 이 자료에는 발표 대본이 없다

강사가 읽을 대본이 따로 없다. **슬라이드만 보고도 뜻이 통해야 한다.**

- **글로 다 쓰지 마라.** 화면을 글자로 채우면 읽느라 강의를 못 듣는다.
- **그렇다고 비우지도 마라.** 낱말만 덩그러니 있으면 대본 없이는 아무 뜻도 안 통한다.
- **답은 삽화다.** 그림이 뜻을 지고, 글은 그림이 못 하는 말만 한다.
- **그림만으로 뜻이 안 통하면 그때 라벨을 박아라.** 보면 아는 물건에는 붙이지 않는다.

## 범위 — 이걸 어기면 실패다

- 이 덱은 **「그런데 이건 전부 소프트웨어입니다」에서 끝난다.**
- **PLC·래더 로직·현장 사고 사례는 마지막 날 몫이다. 한 글자도 꺼내지 마라.**

- **37장을 넘겨 만들지 마라.** 37페이지가 마지막이다.
- 업로드한 원고에 없는 것을 **만들어 넣지 마라.**

## 전 장 공통 금지

- **어떤 이상이 잡히고 어떤 것이 안 잡히는지 한 글자도 쓰지 마라.** 스파이크·드리프트·결측 각각의 결과는 **학생이 코드를 돌려서 발견할 반전이다.**
- 오탐 수치(하루 81건 → 911건, 11배)는 이론으로 다루는 것이라 나와도 된다.
- 화면을 좌우로 갈라 칠하지 마라. 배경은 **순백 하나**, 표지형만 짙은 남색이다.
- **숫자를 화면 절반만 하게 키우지 마라.** 큰 낱말도 제목보다 조금만 크다.
- 격자·청사진 무늬·뜻 없는 숫자 더미를 그림 안에 깔지 마라.
- 사진·제품 로고·회사 이름 금지.

# 1페이지

**레이아웃** 1번 (표지형)

**화면 글자**
- 윗줄: `2일차`
- 제목: `판단하고, 움직이게 한다`
- 부제: `경남대 RISE 피지컬AI 사관학교 · 8월 Agentic AI 특강`
- 하단 두 줄: `에이전트란 무엇인가 · 도구를 쥐여주는 표준 · 사람이 지키는 관문` / `오늘, 여러분의 코드가 공장을 움직입니다.`（두 번째 줄 굵게）

**삽화** 오른쪽에 흰 동심원 다섯 겹, 투명도 30%. 다른 그림 금지.

**금지**
- 배경은 짙은 남색 #12305A 하나. 사진·로고 금지.

---

# 2페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `1일차에 여러분은 자를 하나 만들었습니다`
- 설명문: `이동 윈도 z-score. 뼈대를 받아 세 군데를 직접 채웠습니다.`
- 본문 2행:
  - `최근 W개의 평균과 표준편차를 구하고, 지금 값이 k배를 넘는지 판정했다.`
  - `그리고 k를 바꿔 가며 무엇이 늘고 무엇이 주는지 숫자로 봤다.`
- 하단 강조(아주 연한 주황 밑칠): `오탐과 미탐은 맞바꿈 — 1일차의 한 문장이다.`

**삽화** 코드 편집기 창. 회색 코드 줄 사이에 **주황 줄 하나**가 비어 있고 `TODO 3` 라벨. 오른쪽에 손 모양.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 3페이지 ★ 1일차 회수

**레이아웃** 5번 (나란히 비교형)

**화면 글자**
- 제목: `규칙은 미리 정한 것만 봅니다`
- 설명문: `자를 정교하게 만들어도 성질은 같습니다.`
- 왼쪽: 분류 `정해 둔 것` / 이름 `여기까지는 잡는다` / 설명 `우리가 미리 적어 둔 조건이라면 몇 번이든 정확히 잡아낸다.`
- 오른쪽: 분류 `정하지 못한 것` / 이름 `여기서는 말이 없다` / 설명 `미리 적어 두지 못한 상황 앞에서는 **아무 말도 하지 못한다.**`
- 하단 강조(아주 연한 주황 밑칠): `어제 남은 질문 — 그럼 누가 판단하나.`

**삽화** 왼쪽 — 눈금이 있는 자.

오른쪽 — 되돌리기 화살표인데 **회색 점선**이고 위에 큰 주황 X.

**두 쪽 배경을 다르게 칠하지 마라.** 가운데 가는 세로선 하나로만 나눈다.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 4페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `오늘은 세 도막입니다`
- 설명문: `쥐여주고, 판단하게 하고, 움직이게 합니다.`
- 표 머리행: `(빈칸)` / `무엇을 하나` / `쓰는 것`
  - `쥐여준다` / `내가 짠 코드를 AI가 부를 수 있게 내놓는다` / `mcp_server.py`
  - `판단하게 한다` / `AI가 스스로 조회하고 원인을 추정한다` / `지시 한 문장`
  - `움직이게 한다` / `판단대로 설비가 실제로 바뀐다` / `폐루프 템플릿`
- 하단 강조(아주 연한 주황 밑칠): `마지막 20분은 오늘 겪은 것을 노션에 정리한다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 5페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `그냥 물어보면 이렇게 됩니다`
- 설명문: `“EQ-03 지금 온도 어때?” — 답은 매끄럽고, 틀렸습니다.`
- 본문 3행:
  - `LLM은 훈련이 끝난 시점까지의 글을 읽었을 뿐이다.`
  - `창원 공장에 오늘 새벽 무슨 일이 있었는지는 그 글에 없다.`
  - `더 나쁜 것은 모른다고 말하지 않는다는 점이다. 그럴듯한 숫자를 지어내 문장을 완성한다.`
- 하단 강조(아주 연한 주황 밑칠): `지어낸 답을 사실처럼 말하는 것을 환각이라 한다.`

**삽화** 큰 말풍선. 안에 회색 글줄 세 개(그럴듯한 문장), 그 위에 **주황 X**. 오른쪽 아래 `지어낸 값`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 6페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `눈을 달아 줘도, 손이 없습니다`
- 설명문: `그럼 온도 값을 복사해 붙여 넣으면 되지 않나 — 한 번은 됩니다.`
- 본문 3행:
  - `“속도를 낮춰” — 문장만 만든다. 설비는 그대로 돈다.`
  - `“지난 일주일 이력도 봐” — 매번 사람이 찾아서 붙여 넣어야 한다.`
  - `“이상하면 알려 줘” — 물어볼 때만 대답한다. 스스로 계속 보지 않는다.`
- 하단 강조(아주 연한 주황 밑칠): `판단은 하는데 볼 눈도, 쓸 손도, 계속 돌 심장도 없다.`

**삽화** 뇌 윤곽. 오른쪽에 **점선 원 두 개**가 각각 주황 사선으로 지워져 있고 `눈` `손` 라벨.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 7페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `에이전트는 넷으로 이루어집니다`
- 설명문: `에이전트(Agent) — 대리인. 사람을 대신해 일을 맡아서 끝내는 존재.`
- 행 4개 (번호 / 굵은 제목 / 설명 한 줄):
  - `01` · `뇌 — LLM` · `판단을 맡는다.`
  - `02` · `도구 — 눈과 손` · `조회하고 제어하는 실제 기능들.`
  - `03` · `루프 — 반복` · `결과를 보고 다시 판단한다.`
  - `04` · `자율성 — 스스로` · `다음에 무엇을 할지 자기가 정한다.`
- 하단 강조(아주 연한 주황 밑칠): `1일차에 그린 폐루프와 모양이 같다. 에이전트는 폐루프를 소프트웨어로 만든 것이다.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 (삽화 설명 없음 / (삽화 설명 없음 / (삽화 설명 없음 / (삽화 설명 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 8페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `챗봇도 코파일럿도 아닙니다`
- 설명문: `차이는 하나뿐입니다 — 다음에 무엇을 할지 누가 정하는가.`
- 표 머리행: `(빈칸)` / `챗봇` / `코파일럿` / `에이전트`
  - `하는 일` / `묻는 말에 답한다` / `사람 옆에서 거든다` / `일을 맡아서 끝낸다`
  - `도구 사용` / `안 쓴다` / `사람이 시킬 때만` / `스스로 골라 쓴다`
  - `작동 횟수` / `한 번` / `한 번씩 여러 번` / `끝날 때까지 반복`
  - `다음을 정하는 건` / `사람` / `사람` / `자기가 정한다`  ← 이 행만 아주 연한 남색 #EEF3F9
  - `공장에 비유하면` / `안내원` / `보조원` / `야간 당직자`
- 하단 강조(아주 연한 주황 밑칠): `야간 당직자는 물어보지 않아도 순찰을 돈다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 9페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `추론과 행동을 번갈아 합니다`
- 설명문: `ReAct — Reasoning(추론)과 Acting(행동)을 번갈아 한다는 뜻입니다.`
- 큰 낱말: `ReAct`
- 낱말 아래 한 줄: `생각 → 행동 → 관찰 → 다시 생각`
- 본문 2행:
  - `한 번에 답을 내지 않는다. 한 걸음 가고, 결과를 보고, 다음을 정한다.`
  - `그래서 도중에 예상과 다른 것이 나와도 방향을 바꿀 수 있다.`
- 하단 강조(아주 연한 주황 밑칠): `새벽 3시의 EQ-03에 돌리면 아홉 걸음이 된다.`

**삽화** 원형 화살표 고리. 세 구간 색이 다르고 안쪽에 `생각`, 바깥에 `행동`(주황) `관찰`(회색).

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 10페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `새벽 3시 EQ-03에 돌리면`
- 설명문: `아홉 걸음입니다. 사람은 아무것도 하지 않습니다.`
- 표 머리행: `(빈칸)` / `무엇을` / `결과`
  - `생각 → 행동` / `온도가 이상한지 보려면 최근 값을 알아야겠다 → 온도 조회` / `계속 오른다`
  - `생각 → 행동` / `네 시간째 안 내려갔다. 정상 변동이 아니다 → 정비 이력 조회` / `냉각 점검이 보류 중`
  - `생각 → 행동` / `냉각 쪽 문제로 보인다. 속도를 낮추자 → 속도 조절` / `온도가 내려가기 시작한다`
- 하단 강조(아주 연한 주황 밑칠): `고리가 닫혔다. 오늘 여러분이 만들 것이 정확히 이 아홉 걸음이다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 11페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `하나에게 다 시키면 이렇게 됩니다`
- 설명문: `앞 장의 아홉 걸음을 지시 하나에 몰아넣으면 벌어지는 일입니다.`
- 본문 3행:
  - `지시가 길어질수록 뒤쪽 요구를 흘린다. 다섯을 시키면 셋만 하고 끝난다.`
  - `어디서 틀렸는지 짚을 수가 없다. 통째로 한 번에 하니 중간이 안 보인다.`
  - `하나만 고치고 싶어도 전체를 다시 시켜야 한다.`
- 하단 강조(아주 연한 주황 밑칠): `일을 나누고 이어 붙일 방법을 정해 두는 것 — 이것이 에이전틱 디자인 패턴이다.`

**삽화** 왼쪽에 작은 원(에이전트 하나), 오른쪽에 회색 글줄이 **여섯 줄 길게** 늘어짐. 아래 주황 점선과 `뒤쪽은 흘린다`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 12페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `패턴은 다섯입니다`
- 설명문: `외울 필요는 없습니다. 오늘 이 가운데 하나를 손으로 짭니다.`
- 표 머리행: `패턴` / `한 줄 뜻` / `공장에서`
  - `프롬프트 체이닝` / `쪼개서 줄줄이 잇는다` / `탐색 → 이력 조회 → 원인 추정 → 조치안`
  - `라우팅` / `성격을 보고 알맞은 곳으로 보낸다` / `알람 등급별 대응 분기`
  - `병렬 처리` / `상관없는 일은 동시에` / `CNC 6대 동시 점검`
  - `총괄과 담당` / `총괄이 나누고 담당이 처리한다` / `감지·진단·조치 담당 + 총괄`  ← 이 행만 아주 연한 남색 #EEF3F9
  - `검증과 되짚기` / `검사하는 쪽을 따로 둔다` / `근거 없는 리포트를 돌려보낸다`
- 하단 강조(아주 연한 주황 밑칠): `굵게 칠한 총괄과 담당을 오늘 오후에 짠다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 13페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `도구를 쥐여주는 표준이 있습니다`
- 설명문: `함수는 이미 있습니다. 에이전트가 부를 방법이 없을 뿐입니다.`
- 큰 낱말: `MCP`
- 낱말 아래 한 줄: `Model Context Protocol`
- 본문 2행:
  - `표준이 없으면 에이전트마다 붙이는 방법이 달라, 도구 하나에 매번 다시 짜야 한다.`
  - `한 번 내놓으면 어느 에이전트든 갖다 쓴다.`
- 하단 강조(아주 연한 주황 밑칠): `오늘은 읽는 손이다. 움직이는 손은 오후에 연다.`

**삽화** 왼쪽 상자(에이전트)와 오른쪽 상자(내 도구)가 **두 개의 다리로 꽂혀** 있음. 라벨 `에이전트` `내 도구`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 14페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `오늘 만드는 도구는 두 개입니다`
- 설명문: `서버 뼈대와 데이터 가져오기는 이미 되어 있습니다.`
- 본문 2행:
  - `**detect_anomaly** — 어제 내가 짠 detect() 를 에이전트가 부를 수 있게 내놓는다.`
  - `**query_equipment** — 설비 하나의 최근 센서 요약과 정비 이력을 모아 돌려준다.`
- 하단 강조(아주 연한 주황 밑칠): `★ 표시된 두 자리만 채우면 된다.`

**삽화** 상자 두 개. 왼쪽엔 주황 꺾은선(이상감지), 오른쪽엔 회색 글줄(조회 결과). 아래 `detect_anomaly` `query_equipment`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 15페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `도구가 돌려주는 것`
- 설명문: `이 모양으로 에이전트에게 건네집니다.`
- 표 머리행: `도구` / `돌려주는 것`
  - `detect_anomaly` / `equipment_id · sample_count · anomaly_count · k · window · anomalies`
  - `query_equipment` / `recent(온도 평균·최대·결측) · maintenance(작업지시 목록)`
- 하단 강조(아주 연한 주황 밑칠): `정비 이력의 note 가 진단의 실마리다. 빠뜨리면 원인을 추정할 재료가 없다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 16페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `오늘은 전원 fallback 으로 돕니다`
- 설명문: `개인 데이터베이스는 오늘 쓰지 않습니다.`
- 본문 3행:
  - `config.json 의 data_source 를 **fallback** 으로 둔다.`
  - `센서값은 나눠받은 7일치 CSV를 직접 읽고, 정비 이력은 강사 서버에서 온다.`
  - `도구 이름과 응답 형태는 어느 쪽으로 두든 같다. 여러분 코드는 고칠 게 없다.`
- 하단 강조(아주 연한 주황 밑칠): `설정은 어제 내번호.py 가 이미 채워 뒀다. 확인만 하면 된다.`

**삽화** 왼쪽 상자(에이전트)와 오른쪽 상자(내 도구)가 **두 개의 다리로 꽂혀** 있음. 라벨 `에이전트` `내 도구`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 17페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `도구 두 개를 채웁니다`
- 설명문: `2일차 / 실습 / 도구만들기 폴더를 엽니다.`
- 큰 낱말: `실습 1`
- 낱말 아래 한 줄: `55분 · 각자`
- 본문 3행:
  - `고칠 파일은 mcp_server.py 하나다. ★ 두 자리를 채운다.`
  - `막히면 python 점검.py --힌트 1 · --힌트 2 · --열기 1 순서로.`
  - `순서는 2일차 / 실습가이드.md 에 다 있다. 막히면 python 점검.py.`
- 하단 강조(아주 연한 주황 밑칠): `먼저 python mcp_server.py --check 로 서버 없이 도구만 호출해 본다.`

**삽화** 상자 두 개. 왼쪽엔 주황 꺾은선(이상감지), 오른쪽엔 회색 글줄(조회 결과). 아래 `detect_anomaly` `query_equipment`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 18페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `지시는 한 문장입니다`
- 설명문: `도구를 언제 어떤 순서로 부를지는 말하지 않습니다.`
- 본문 2행:
  - `“지난 주 설비 이상을 점검하고, 이상이 있으면 해당 설비의 정비 이력을 조회해 원인 추정과 권고 조치를 담은 진단 리포트를 작성하라.”`
  - `에이전트가 도구를 줄줄이 이어서 호출하며 리포트를 뽑아낸다.`
- 하단 강조(아주 연한 주황 밑칠): `순서를 에이전트가 정한다. 이것이 아까 본 ReAct다.`

**삽화** (삽화 설명 없음 — artdesc.py 에 등록할 것)

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 19페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `무슨 일이 일어나는지 봅니다`
- 설명문: `지시 하나에 세 걸음이 이어집니다.`
- 행 3개 (번호 / 굵은 제목 / 설명 한 줄):
  - `01` · `이상을 찾는다` · `내가 어제 짠 detect() 를 부른다.`
  - `02` · `이력을 뒤진다` · `이상이 난 설비의 정비 기록을 조회한다.`
  - `03` · `리포트를 쓴다` · `근거가 되는 작업지시를 인용해 원인을 추정한다.`
- 하단 강조(아주 연한 주황 밑칠): `내가 짠 코드를 AI가 도구로 쓴다. 이것이 오늘의 첫 장면이다.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 (삽화 설명 없음 / (삽화 설명 없음 / 문서 아이콘.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 20페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `막혔을 때`
- 설명문: `이 셋이 대부분입니다. 그 외에는 손 드세요.`
- 표 머리행: `증상` / `원인` / `조치`
  - `detect.py 를 불러오지 못했습니다` / `1일차 TODO 세 곳이 비어 있다` / `1일차 / 실습 에서 python 점검.py`
  - `NotImplementedError` / `★ 두 자리가 비어 있다` / `mcp_server.py 를 채운다`
  - `서버가 안 붙는다` / `방화벽 · 포트` / `python mcp_server.py --check 로 도구만 확인 → 손 들기`
- 하단 강조(아주 연한 주황 밑칠): `다 채웠으면 손 들지 말고 기다리세요. 다 같이 한 번에 실행합니다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 21페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `여기까지는 제안만 합니다`
- 설명문: `리포트가 “속도를 낮추라”고 권고해도 공장은 그대로 돕니다.`
- 본문 2행:
  - `읽는 손까지는 달았다. 움직이는 손이 아직 없다.`
  - `그리고 지금 이 에이전트에는 한 가지 문제가 더 있다.`
- 하단 강조(아주 연한 주황 밑칠): `지시 한 문장에 역할이 몇 개나 들어 있었을까.`

**삽화** 같은 크기 화면 여덟 개가 격자로 놓임. 각 화면 아래 주황 막대(결과). 아래 `39개 화면`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 22페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `한 문장에 네 가지 일이 들어 있습니다`
- 설명문: `점검하고, 뒤지고, 추정하고, 씁니다.`
- 본문 3행:
  - `아까는 잘 돌았다. 데이터가 얌전했고 할 일이 적었으니까.`
  - `그런데 여기에 「움직이기」까지 얹으면 어떻게 될까.`
  - `오늘 이 판단은 **진짜 설비를 움직인다.** 흔들리면 안 되는 이유가 하나 더 생겼다.`
- 하단 강조(아주 연한 주황 밑칠): `지시가 길어질수록 뒤쪽 요구를 흘린다.`

**삽화** 왼쪽에 작은 원(에이전트 하나), 오른쪽에 회색 글줄이 **여섯 줄 길게** 늘어짐. 아래 주황 점선과 `뒤쪽은 흘린다`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 23페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `그래서 셋으로 쪼갭니다`
- 설명문: `첫날 배운 「총괄이 나누고 담당이 처리한다」를 오늘 손으로 만듭니다.`
- 큰 낱말: `총괄과 담당`
- 낱말 아래 한 줄: `오케스트레이터 + 담당 셋`
- 본문 3행:
  - `감지는 이상 구간을 찾고, 진단은 원인을 추정하고, 조치는 무엇을 할지 정한다.`
  - `오케스트레이터는 직접 일하지 않는다. 결과를 받아 다음 담당에게 넘긴다.`
  - `조치한 뒤 다시 감지로 돌아가는 것이 폐루프다.`
- 하단 강조(아주 연한 주황 밑칠): `각자 하나씩만 맡으면 틀린 자리가 보이고, 그 자리만 고칠 수 있다.`

**삽화** 위에 원 하나(총괄), 아래로 갈라져 상자 셋. 라벨 `감지` `진단` `조치`(주황). 아래 `총괄이 나누고 담당이 처리한다`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 24페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `지금 제어 권한이 열렸습니다`
- 설명문: `지금까지는 읽기만 됐습니다. 이제 코드가 공장에 명령을 내립니다.`
- 본문 2행:
  - `첫날 무대에서 “명령을 넣는 통로가 네 개 준비돼 있다”고 했다.`
  - `그 네 개를 지금 연다.`
- 하단 강조(아주 연한 주황 밑칠): `읽는 손에서 움직이는 손으로. 무게가 달라진다.`

**삽화** 왼쪽 상자(에이전트)와 오른쪽 상자(내 도구)가 **두 개의 다리로 꽂혀** 있음. 라벨 `에이전트` `내 도구`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 25페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `열린 도구는 넷입니다`
- 설명문: `무엇이 자동이고 무엇이 사람 승인인지, 여기서 갈립니다.`
- 표 머리행: `도구` / `무엇을 하나` / `승인`
  - `set_equipment_speed` / `설비 회전수를 바꾼다 (감속)` / `자동`
  - `ack_alarm` / `알람을 확인 처리한다` / `자동`
  - `stop_equipment` / `설비를 정지시킨다` / `사람 승인 필수`  ← 이 행만 아주 연한 남색 #EEF3F9
  - `dispatch_robot` / `정비 로봇(AMR)을 파견한다` / `사람 승인 필수`
- 하단 강조(아주 연한 주황 밑칠): `왜 아래 둘만 승인이 필요할까.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 26페이지 ★ 아하

**레이아웃** 5번 (나란히 비교형)

**화면 글자**
- 제목: `되돌릴 수 있는가 — 이것이 갈림선입니다`
- 설명문: `물리 세계에는 한 번 하면 되돌릴 수 없는 행동이 있습니다.`
- 왼쪽: 분류 `자동` / 이름 `되돌릴 수 있다` / 설명 `감속은 잘못돼도 다시 올리면 된다. 알람 확인도 다시 열면 된다.`
- 오른쪽: 분류 `승인 필수` / 이름 `되돌릴 수 없다` / 설명 `정지는 라인 전체를 멈춘다. **그 시간은 안 돌아온다.** 로봇은 이미 움직였다.`
- 하단 강조(아주 연한 주황 밑칠): `되돌릴 수 없는 행동 앞에 사람을 세운다 — 이것이 HITL이다.`

**삽화** 왼쪽 — 되돌리기 화살표(원형, 남색 실선).

오른쪽 — 되돌리기 화살표인데 **회색 점선**이고 위에 큰 주황 X.

**두 쪽 배경을 다르게 칠하지 마라.** 가운데 가는 세로선 하나로만 나눈다.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 27페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `고리 안에 사람이 한 자리 있습니다`
- 설명문: `HITL — Human In The Loop.`
- 큰 낱말: `승인 관문`
- 낱말 아래 한 줄: `실행 대신 물어본다`
- 본문 3행:
  - `에이전트가 정지나 파견을 하려 하면, 실행 대신 승인 요청이 뜬다.`
  - `이 구분은 템플릿이 강제한다. 여러분 코드가 실수로 정지를 자동 실행하는 일은 구조적으로 불가능하다.`
  - `여러분이 못 건드리는 계층으로 만들어 둔 이유다.`
- 하단 강조(아주 연한 주황 밑칠): `안전은 조심해서 지키는 게 아니라 구조로 지킨다.`

**삽화** 차단기 그림. 두 기둥 사이에 주황 가로 막대가 내려와 있고 그 아래 시계 아이콘. 라벨 `사람` `에이전트` `설비`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 28페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `AI에게 다 맡기는 것도, 사람이 다 하는 것도 아닙니다`
- 설명문: `되돌릴 수 없는 곳에만 사람을 둡니다.`
- 본문 3행:
  - `전부 사람이 승인하면 야간 무인 운전이 성립하지 않는다.`
  - `전부 자동이면 오판 한 번에 라인이 선다.`
  - `그래서 되돌릴 수 있는 것은 자동, 없는 것만 승인으로 갈랐다.`
- 하단 강조(아주 연한 주황 밑칠): `이 선을 어디에 긋느냐가 현장 설계의 핵심이다.`

**삽화** 차단기 그림. 두 기둥 사이에 주황 가로 막대가 내려와 있고 그 아래 시계 아이콘. 라벨 `사람` `에이전트` `설비`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 29페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `폐루프를 닫습니다`
- 설명문: `2일차 / 실습 / 폐루프 폴더를 엽니다.`
- 큰 낱말: `실습 2`
- 낱말 아래 한 줄: `70분 · 각자`
- 본문 3행:
  - `오케스트레이터는 이미 완성돼 있다. 담당 셋의 속만 채운다.`
  - `config 는 어제 내번호.py 가 채워 뒀다. python loop.py --check 로 먼저 확인한다.`
  - `순서는 2일차 / 실습가이드.md §2 에 다 있다. 막히면 손 드세요.`
- 하단 강조(아주 연한 주황 밑칠): `채울 곳은 감지 · 진단 · 조치 세 군데다.`

**삽화** 완전히 닫힌 원형 고리. 위 끝에 주황 화살촉. 라벨 `인지` `판단` `행동` `확인`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 30페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `담당 셋이 하는 일`
- 설명문: `각자 하나씩만 맡습니다. 지시가 짧아집니다.`
- 행 3개 (번호 / 굵은 제목 / 설명 한 줄):
  - `01` · `감지 — 이상 구간을 찾는다` · `온도 이력을 보고 이상이 시작된 지점을 집는다.`
  - `02` · `진단 — 원인을 추정한다` · `정비 이력을 뒤져 근거가 될 작업지시를 찾는다.`
  - `03` · `조치 — 무엇을 할지 정한다` · `감속으로 될지, 로봇을 보내야 할지 정하고 도구를 부른다.`
- 하단 강조(아주 연한 주황 밑칠): `총괄은 이미 완성돼 있다. 셋의 속만 채우면 돈다.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 (삽화 설명 없음 / (삽화 설명 없음 / (삽화 설명 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 31페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `폐루프는 일곱 걸음입니다`
- 설명문: `승인 한 번을 빼고는 사람이 아무것도 하지 않습니다.`
- 표 머리행: `(빈칸)` / `무엇이 일어나나` / `누가`
  - `01 주입` / `강사 콘솔에서 EQ-03에 온도 드리프트를 넣는다` / `강사`
  - `02 감지` / `감지 담당이 포착한다` / `에이전트`
  - `03 진단` / `정비 이력을 뒤져 원인을 추정한다` / `에이전트`
  - `04 감속` / `회전수를 낮춘다 — 되돌릴 수 있으므로 자동` / `에이전트`
  - `05 승인 요청` / `로봇 파견 — 되돌릴 수 없으므로 멈춰 선다` / `에이전트`  ← 이 행만 아주 연한 남색 #EEF3F9
  - `06 승인` / `y 를 누른다` / `사람`
  - `07 파견` / `AMR 이 움직이고 값이 바뀐다` / `에이전트`
- 하단 강조(아주 연한 주황 밑칠): `05 에서 루프가 멈춰 기다린다. 누르기 전까지 아무 일도 안 일어난다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 32페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `감지까지 1분에서 3분 걸립니다`
- 설명문: `조용해도 고장 난 게 아닙니다.`
- 본문 2행:
  - `주입한 뒤 바로 잡히지 않는다. 값이 충분히 쌓여야 판정이 선다.`
  - `설계된 대기다. 기다리세요.`
- 하단 강조(아주 연한 주황 밑칠): `터미널에 아무것도 안 뜨는 동안이 정상이다.`

**삽화** (삽화 설명 없음 — artdesc.py 에 등록할 것)

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 33페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `화면에서 세 가지를 보세요`
- 설명문: `숫자는 각자 다릅니다. 자기 화면의 숫자를 보세요.`
- 행 3개 (번호 / 굵은 제목 / 설명 한 줄):
  - `01` · `리포트` · `진단이 정비 이력의 어느 줄을 근거로 댔는지.`
  - `02` · `승인 요청` · `언제 멈춰 섰는지, 무엇을 하려고 물었는지.`
  - `03` · `공장 화면` · `회전수 · AMR 위치 · 온도가 실제로 어떻게 바뀌는지.`
- 하단 강조(아주 연한 주황 밑칠): `끝나면 진단 리포트를 캡처해 두세요. 오늘 제일 남길 만한 화면입니다.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 문서 아이콘 / (삽화 설명 없음 / (삽화 설명 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 34페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `막혔을 때`
- 설명문: `이 셋이 대부분입니다.`
- 표 머리행: `증상` / `조치`
  - `아무 일도 안 일어난다` / `1~3분 기다렸는지 확인. 그래도 안 되면 손 들기`
  - `감지는 되는데 진단이 비어 있다` / `정비 이력을 안 부르고 있다. 진단 담당을 본다`
  - `승인 요청이 안 뜬다` / `조치 담당이 파견까지 안 가고 있다`
- 하단 강조(아주 연한 주황 밑칠): `다 채웠으면 기다리세요. 서른아홉 개 공장에 동시에 주입합니다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 35페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `오늘 세 가지를 했습니다`
- 설명문: `정리하고 마무리하겠습니다.`
- 행 3개 (번호 / 굵은 제목 / 설명 한 줄):
  - `01` · `쥐여줬다` · `내가 짠 코드를 도구로 내놓고, AI가 갖다 썼다.`
  - `02` · `쪼갰다` · `감지·진단·조치와 총괄. 첫날 배운 패턴을 손으로 짰다.`
  - `03` · `움직였다` · `판단대로 설비가 바뀌었다. 승인 한 번을 빼고는 자동으로.`
- 하단 강조(아주 연한 주황 밑칠): `고리가 닫혔다. 인지 → 판단 → 행동 → 다시 인지.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 (삽화 설명 없음 / (삽화 설명 없음 / (삽화 설명 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 36페이지 ★ 넘기는 질문

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `그런데 이건 전부 소프트웨어입니다`
- 설명문: `여러분이 세운 것은 화면 안의 공장입니다.`
- 큰 낱말: `마지막 1미터`
- 낱말 아래 한 줄: `내일의 질문`
- 본문 2행:
  - `진짜 공장에서는 그 명령이 PLC를 거쳐야 한다.`
  - `그리고 안전 인터록은 결국 하드웨어에 남는다.`
- 하단 강조(아주 연한 주황 밑칠): `내일 그 마지막 1미터를 봅니다.`

**삽화** 모니터 화면 안에 공장 설비 여섯 개가 그려져 있음. 위에 `화면 안` 라벨.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 37페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `남은 20분은 노션입니다`
- 설명문: `오늘 겪은 것을 카드로 남깁니다.`
- 행 3개 (번호 / 굵은 제목 / 설명 한 줄):
  - `01` · `고른다` · `오늘 안 풀린 것 두세 개. 그게 카드 제목이 된다.`
  - `02` · `쓴다` · `화면을 덮고 내 문장으로 쓴다. 안 써지면 그게 모르는 것이다.`
  - `03` · `붙인다` · `진단 리포트를 캡처해 붙이고 이해도 태그를 찍는다.`
- 하단 강조(아주 연한 주황 밑칠): `오늘까지가 우리 이틀입니다. 노션 링크 제출이 수료입니다.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 (삽화 설명 없음 / (삽화 설명 없음 / (삽화 설명 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

▲ 복붙 끝 ▲
