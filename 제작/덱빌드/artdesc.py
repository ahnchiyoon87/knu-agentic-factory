# -*- coding: utf-8 -*-
"""삽화 설명 — 설명란(.md)에 그대로 들어가는 문장.
   덱의 SVG 와 1:1로 묶여 있어 둘이 어긋날 수 없다."""

DESC = {
    # ── 1일차
    "NIGHT": "밤 공장. 톱니 지붕 건물 + 옆 동. 창 여러 개 중 **한 칸만 주황으로 켜져** 있고 그 아래 `EQ-03` 라벨. 왼쪽 위에 작은 시계와 `새벽 3시`.",
    "SCRAP": "버려진 부품 더미. 원통형 부품 세 개가 통 안에 담겨 있고 각각 **주황 X 표시**. 오른쪽에 `하루치` 라벨.",
    "QMARK": "큰 원 안에 주황 물음표 하나. 다른 요소 없음.",
    "SENSOR": "왼쪽에 계기(사각 틀 안 눈금), 점선으로 이어진 오른쪽에 축과 **주황 기록선**(톱니 모양). 왼쪽 아래 `1초마다 기록`.",
    "CLOCK16": "24시간 원. **16시간에 해당하는 부채꼴이 연한 남색으로 칠해짐.** 바늘 둘. 라벨 `근무 8시간`(회색), `무인으로 도는 16시간`(남색).",
    "FLOOD": "설비 네모 여섯 개. 각 네모 아래로 **주황 세로선이 여러 가닥 쏟아짐**. 아래에 `1초마다 24개`.",
    "LINE80": "위쪽 남색 점선 하나(임계선), 아래쪽에 완만히 오르는 주황 곡선. 둘 사이가 크게 벌어져 있고 `넘지 않는다` 라벨.",
    "GRAPH80": "가는 선 그래프. 위 남색 점선 `임계값 80℃`, 아래 62→64로 오르는 주황 곡선(`62℃` `64℃`). **두 선 사이 빈 공간에 세로 화살표와 `16℃ 남음`.**",
    "STAGE": "공장 평면. 큰 사각 테두리 안에 설비 네모 여섯 개, 아래쪽에 주황 원 두 개가 점선으로 이어짐. 라벨 `CNC 6` `AMR 2`.",
    "ROWS8": "데이터베이스 표 그림. 남색 머리행 + **여덟 줄**, 각 줄에 회색 막대 네 개. 아래 `행 여덟 개`.",
    "FOLLOW": "지그재그 주황 선(지금 값)과 그것을 따라 올라가는 남색 점선(최근 평균). 라벨 `지금 값` `최근 평균`.",
    "SLIDE": "긴 남색 시계열 선. 그 위에 **주황 사각 테두리(창)가 오른쪽 일부를 덮고** 있고 `최근 W개` 라벨. 창 오른쪽에 진행 화살표.",
    "SCALE": "저울. 양쪽 접시가 수평. 왼쪽 접시 주황 아래 `오탐`, 오른쪽 접시 남색 아래 `미탐`.",
    "BYHAND": "코드 편집기 창. 회색 코드 줄 사이에 **주황 줄 하나**가 비어 있고 `TODO 3` 라벨. 오른쪽에 손 모양.",
    "ALLRUN": "같은 크기 화면 여덟 개가 격자로 놓임. 각 화면 아래 주황 막대(결과). 아래 `39개 화면`.",
    "OPENLOOP": "고리 그림인데 **왼쪽 위 한 구간이 점선으로 끊겨** 있음. 끊긴 자리에 주황 점과 `?`. 라벨 `인지` `행동` `판단`.",
    "TRACE_A": "거의 평평한 남색 톱니 선 하나. 기준선 회색.",
    "TRACE_B": "TRACE_A 와 **거의 똑같은 모양**의 남색 톱니 선. 기준선만 주황 점선으로 조금 아래.",
    "AN_DRIFT": "완만히 오르는 주황 선.",
    "AN_SPIKE": "평평하다가 한 지점만 뾰족하게 솟는 주황 선.",
    "AN_MISS": "주황 선이 가운데에서 끊기고 회색 점선으로 이어짐.",
    "SCR_BOXES": "네모 여섯 개 격자.",
    "SCR_DOTS": "주황 원 두 개와 점선 경로.",
    "SCR_NUM": "남색 가로 막대 세 개와 주황 세로 막대 세 개.",
    "RULER": "눈금이 있는 자.",
    "WAVE": "물결선과 기준선.",
    "WINDOW": "시계열 선 위에 주황 사각 창.",
    "FP": "거의 평평한 남색 선 위로 **주황 핀 세 개**가 솟음 — 아무 일 없는데 울린 자리들.",
    "FN": "완만히 오르는 주황 선. 중간에 **회색 점선 원**과 `놓침` 라벨 — 지나쳤다는 표시.",
    "T1": "작은 격자 표 아이콘 — 창 안의 값들.",
    "T2": "기준선 위 점들과 임계 눈금 아이콘 — 넘었는지 판정.",
    "T3": "선이 끊기고 빈 자리에 작은 원 — 값이 안 온 자리.",
    "ICO_CLOCK": "시계 아이콘.",
    "ICO_EYE": "눈 아이콘.",
    "ICO_GRID": "네모 격자 아이콘.",
    "ICO_PEN": "연필 아이콘.",
    "ICO_SLIDE": "조절 슬라이더(손잡이 둘) 아이콘.",
    "ICO_CMP": "남색·주황 상자 두 개를 나란히 둔 비교 아이콘.",
    "ICO_SCREEN": "모니터 아이콘.",
    "ICO_CODE": "코드 괄호 `</>` 아이콘.",
    "ICO_BAL": "저울 아이콘.",
    "ICO_PICK": "모서리가 접힌 카드 아이콘.",
    "ICO_WRITE": "쓰는 연필 아이콘.",
    "ICO_PIN": "게시판에 꺾은선 그래프가 붙은 아이콘.",
    # ── 2일차
    "HALLU": "큰 말풍선. 안에 회색 글줄 세 개(그럴듯한 문장), 그 위에 **주황 X**. 오른쪽 아래 `지어낸 값`.",
    "NOEYE": "뇌 윤곽. 오른쪽에 **점선 원 두 개**가 각각 주황 사선으로 지워져 있고 `눈` `손` 라벨.",
    "REACT": "원형 화살표 고리. 세 구간 색이 다르고 안쪽에 `생각`, 바깥에 `행동`(주황) `관찰`(회색).",
    "OVERLOAD": "왼쪽에 작은 원(에이전트 하나), 오른쪽에 회색 글줄이 **여섯 줄 길게** 늘어짐. 아래 주황 점선과 `뒤쪽은 흘린다`.",
    "SPLIT3": "위에 원 하나(총괄), 아래로 갈라져 상자 셋. 라벨 `감지` `진단` `조치`(주황). 아래 `총괄이 나누고 담당이 처리한다`.",
    "PLUG": "왼쪽 상자(에이전트)와 오른쪽 상자(내 도구)가 **두 개의 다리로 꽂혀** 있음. 라벨 `에이전트` `내 도구`.",
    "TOOL2": "상자 두 개. 왼쪽엔 주황 꺾은선(이상감지), 오른쪽엔 회색 글줄(조회 결과). 아래 `detect_anomaly` `query_equipment`.",
    "UNDO": "되돌리기 화살표(원형, 남색 실선).",
    "NOUNDO": "되돌리기 화살표인데 **회색 점선**이고 위에 큰 주황 X.",
    "GATE": "차단기 그림. 두 기둥 사이에 주황 가로 막대가 내려와 있고 그 아래 시계 아이콘. 라벨 `사람` `에이전트` `설비`.",
    "CLOSED": "완전히 닫힌 원형 고리. 위 끝에 주황 화살촉. 라벨 `인지` `판단` `행동` `확인`.",
    "SOFTONLY": "모니터 화면 안에 공장 설비 여섯 개가 그려져 있음. 위에 `화면 안` 라벨.",
    "ICO_REPORT": "문서 아이콘 — 모서리가 접힌 종이와 글줄.",
    "ICO_WAIT": "모래시계.",
    "ICO_BRAIN": "뇌 아이콘.",
    "ICO_TOOLS": "도구(돋보기와 손잡이) 아이콘.",
    "ICO_LOOP2": "원형 화살표(반복) 아이콘.",
    "ICO_AUTO": "시계에 조준선이 겹친 아이콘 — 스스로 도는 것.",
    "ICO_FIND": "돋보기 아이콘.",
    "ICO_DIAG": "문서에 주황 동그라미가 찍힌 아이콘 — 근거를 짚는다.",
    "ICO_ACT": "재생(실행) 삼각형 아이콘.",
    "ICO_ORCH": "위 원 하나에서 아래로 갈라지는 분기 아이콘 — 총괄.",
    "ICO_STOP": "원 안에 네모(정지 버튼) 아이콘.",
    "ICO_ROBOT": "로봇 머리 아이콘.",
}
