# -*- coding: utf-8 -*-
"""3일차 덱 — 「AI 에이전트 직접 만들기」  기준판 v2 · 1600×900
   ★ 실습 결과 수치(온도 하강값 등)는 쓰지 않는다 — 학생이 자기 화면에서 본다
   ★ 도구만들기 은 전원 fallback (Supabase 없음) · Step 3(대시보드 표시)는 없앰
"""
import pathlib, deckkit as D, svgs as V, svgs3 as W

S = []

# 01 ─ 표지
S.append(D.cover("3일차", "AI 에이전트<br>직접 만들기",
                 "경남대 RISE 피지컬AI 사관학교 · 8월 Agentic AI 특강",
                 "어제 짠 그 코드를 AI 가 부를 수 있게 내놓습니다. "
                 "AI 가 스스로 조회하고 원인을 대는 것을 보고,",
                 # detect.py 는 <b>눈</b>이지 손이 아니다. 표지가 그 구분을 뭉개면
                 # 32~34장에서 공들여 세우는 「읽기 / 움직이기」가 시작부터 흐려진다.
                 "그 판단이 실제로 설비를 움직이는 데까지 갑니다.", V.RINGS))

# ── A. 2일차 회수 (2~5)
S.append(D.pic("어제 여러분은 detect.py 를 완성했습니다",
               "최근 값과 견줘 지금 값이 얼마나 벗어났는지 재는 코드입니다.", V.BYHAND,
               # 어제 마지막 장면(AI 가 짠 코드와 숫자로 견주기)을 회고에 되살린다.
               # 그게 있어야 오늘의 「그 AI 에게 도구를 쥐여준다」가 이어진다 —
               # 전에는 z-score 와 미탐만 회수해서 이 다리가 끊겨 있었다.
               ["최근 W개의 평균과 흔들리는 폭을 구하고, 지금 값이 그 폭의 k배를 넘는지 판정했다.",
                "그리고 k를 바꿔 가며 무엇이 늘고 무엇이 주는지 숫자로 봤다.",
                "마지막에는 <b>같은 함수를 AI 가 몇 초 만에 짜는 것</b>을 보고, "
                "그 코드가 믿을 만한지 숫자로 판정했다 — 직접 짜 봤기에 가능한 판정이었다."],
               '더 잡으려 하면 오탐이 는다 — <b>어제의 한 문장</b>이다.'))

S.append(D.pic("어제 그 코드가 못 잡은 것이 있었습니다",
               "여러분 화면에 <b>[~ ]</b> 로 찍혔던 그 줄입니다.", V.DRIFTCHASE,
               ["순간 튀는 것은 잘 잡혔다. 흔들리는 폭의 몇 배라는 계산이 정확히 일했다.",
                "그런데 새벽 3시처럼 서서히 오르는 것 앞에서는 아무 말이 없었다.",
                "창이 함께 미끄러지며 평균이 따라 올라가서, 기준 자체가 끌려갔기 때문이다."],
               '코드를 탓할 일이 아니다. <b>규칙이라는 것의 성질</b>이다.'))

S.append(D.cmp("규칙은 미리 정한 것만 봅니다",
               "더 정교하게 만들 수는 있습니다. 그래도 남는 것이 있습니다.",
               ("정해 둔 것", "t-on", V.RULER.replace('"76"', '"200"'), "여기까지는 잡는다",
                "우리가 미리 적어 둔 조건이라면<br>몇 번이든 정확히 잡아낸다."),
               ("정하지 못한 것", "t-off", W.HANDNOTE.replace('400', '360'), "여기서는 말이 없다",
                "사람이 쓴 메모는 임계값으로<br><b>걸러지지 않는다.</b>"),
               '어제 남은 질문 — <b>그럼 누가 판단하나.</b>'))

S.append(D.tbl("오늘은 세 도막입니다",
               "쥐여주고, 판단하게 하고, 움직이게 합니다.",
               ["", "무엇을 하나", "쓰는 것"],
               [["쥐여준다", "내가 짠 코드를 AI가 부를 수 있게 내놓는다", "<code>mcp_server.py</code>"],
                ["판단하게 한다", "AI가 스스로 조회하고 원인을 추정한다", "지시 한 문장"],
                ["움직이게 한다", "판단대로 설비가 실제로 바뀐다", "<code>loop.py</code>"]],
               foot='세 도막을 다 지나면 <b>감지에서 조치까지 한 바퀴</b>가 돈다.'))

# ── B. 에이전트 (6~14)
S.append(D.pic("그냥 물어보면 이렇게 됩니다",
               "“EQ-03 지금 온도 어때?” — 답은 매끄럽고, 틀렸습니다.", W.HALLU,
               ["LLM은 훈련이 끝난 시점까지의 글을 읽었을 뿐이다.",
                "창원 공장에 오늘 새벽 무슨 일이 있었는지는 그 글에 없다.",
                "더 나쁜 것은 모른다고 말하지 않는다는 점이다. 그럴듯한 숫자를 지어내 문장을 완성한다."],
               '지어낸 답을 사실처럼 말하는 것을 <b>환각</b>이라 한다.'))

S.append(D.pic("환각은 버그가 아닙니다",
               "고장이 아니라, 만들어진 방식이 그렇습니다.", W.NEXTWORD,
               ["LLM은 다음에 올 낱말을 확률로 고르는 기계다. 가장 그럴듯한 낱말을 하나씩 이어 붙인다.",
                "「EQ-03 온도는」 다음에는 어떤 숫자든 그럴듯하게 이어진다 — 사실인지는 따지지 않는다.",
                "고쳐서 없앨 수 있는 오류가 아니다. 지어내지 않도록 <b>사실을 쥐여주는</b> 수밖에 없다."],
               '거짓말이 아니라 <b>확률로 문장을 완성한 것</b>이다. 그래서 도구가 필요하다.'))

S.append(D.pic("눈도 손도, 붙여 줘야 생깁니다",
               "그럼 온도 값을 복사해 붙여 넣으면 되지 않나 — 한 번은 됩니다.", W.NOEYE,
               # 그림의 라벨이 위에서부터 눈 · 손 · 심장이다. 본문이 손부터 시작하면
               # 학생 눈이 그림과 글 사이를 왔다 갔다 한다. 순서를 그림에 맞춘다.
               ["“지난 일주일 이력도 봐” 하면 매번 사람이 찾아서 붙여 넣어야 한다 — "
                "직접 가져올 <b>눈</b>이 없다.",
                "“속도를 낮춰” 하면 그럴듯한 문장을 만들어 준다. 그런데 설비는 그대로 돈다 — "
                "명령을 내보낼 <b>손</b>이 없다.",
                "“이상하면 알려 줘” 해도 물어볼 때만 대답한다 — 계속 지켜볼 <b>심장</b>이 없다."],
               '판단은 하는데 <b>볼 눈도, 쓸 손도, 계속 돌 심장도 없다.</b>'))

S.append(D.rail("그 없는 것들을 붙이면 에이전트가 됩니다",
                "에이전트(Agent) — 대리인. 사람을 대신해 일을 맡아서 끝내는 존재.",
                [(W.ICO_BRAIN, "01", "뇌 — LLM", "판단을 맡는다. 이건 원래 있었다."),
                 (W.ICO_TOOLS, "02", "도구 — 눈과 손", "조회하고 제어하는 실제 기능들. 앞 장의 눈과 손이다."),
                 (W.ICO_LOOP2, "03", "루프 — 계속 돈다", "결과를 보고 다시 판단한다. 앞 장의 심장이다."),
                 (W.ICO_AUTO, "04", "자율성 — 스스로", "다음에 무엇을 할지 자기가 정한다.")],
                '인지 → 판단 → 행동 → 다시 인지. 이 <b>고리를 소프트웨어로 만든 것</b>이 에이전트다.'))

S.append(D.tbl("챗봇도 코파일럿도 아닙니다",
               "차이의 뿌리는 하나입니다 — 다음에 무엇을 할지 누가 정하는가.",
               ["", "챗봇", "코파일럿", "에이전트"],
               [["하는 일", "묻는 말에 답한다", "사람 옆에서 거든다", "일을 맡아서 끝낸다"],
                ["도구 사용", "안 쓴다", "사람이 시킬 때만", "스스로 골라 쓴다"],
                ["작동 횟수", "한 번", "한 번씩 여러 번", "끝날 때까지 반복"],
                ["다음을 정하는 건", "사람", "사람", "자기가 정한다"],
                ["공장에 비유하면", "안내원", "보조원", "야간 당직자"]],
               on=3, dense=True,
               foot='<b>야간 당직자</b>는 물어보지 않아도 순찰을 돈다.'))

S.append(D.sect("추론과 행동을 번갈아 합니다",
                "ReAct — Reasoning(추론)과 Acting(행동)을 번갈아 한다는 뜻입니다.", W.REACT,
                "ReAct", "생각 → 행동 → 관찰 → 다시 생각",
                ["한 번에 답을 내지 않는다. 한 걸음 가고, 결과를 보고, 다음을 정한다.",
                 "그래서 도중에 예상과 다른 것이 나와도 방향을 바꿀 수 있다."],
                '한 걸음마다 무엇을 봤는지가 남는다.'
                '<b> 그래서 나중에 어디서 어긋났는지 짚을 수 있다.</b>'))

S.append(D.tbl("새벽 3시 EQ-03에 돌리면",
               "세 바퀴입니다. 사람은 아무것도 하지 않습니다.",
               ["", "생각하고 → 행동하고", "관찰한다"],
               [["1바퀴", "온도가 이상한지 보려면 최근 값을 알아야겠다 → 온도 조회", "계속 오른다"],
                ["2바퀴", "네 시간째 안 내려갔다. 정상 변동이 아니다 → 정비 이력 조회",
                "점검이 보류 중"],
                ["3바퀴", "보류된 점검이 원인으로 보인다. 속도를 낮추자 → 속도 조절",
                 "온도가 내려가기 시작한다"]],
               foot='<b>고리가 닫혔다.</b> 오전에는 첫 두 바퀴까지 갑니다. '
                    '실제로 속도를 낮추는 셋째 바퀴는 <b>오후에 엽니다.</b>'))

# ★ 이 장의 그림은 **화면에 찍히는 줄** 자체다.
#   전에는 앞 장(ReAct 고리)과 같은 그림을 그대로 다시 썼는데, 그 그림은 「번갈아 한다」를
#   그리지 「글로 남는다」를 그리지 않는다. 연속 두 장이 같은 그림이기도 했다.
S.append(D.pic("생각을 소리 내어 합니다",
               "ReAct 에이전트의 화면에는 생각이 글로 찍힙니다.", W.THOUGHTLOG,
               ["한 바퀴를 돌 때마다 무엇을 생각했고 어느 도구를 불렀는지가 글로 찍힌다. "
                "그래서 나중에 <b>어디서 어긋났는지 짚을 수 있다.</b>",
                "찍히는 모양은 만든 사람마다 다르다. 오늘 오전 화면에서는 "
                "<code>도구 호출  query_equipment(equipment_id=EQ-03)</code> 한 줄이 뜨고, "
                "그 아래에 정비 이력이 딸려 온다."],
               '판단이 <b>머릿속이 아니라 화면에 남는다.</b>'))

S.append(D.pic("하나에게 다 시키면 이렇게 됩니다",
               "앞 장의 세 바퀴를 지시 하나에 몰아넣으면 벌어지는 일입니다.", W.OVERLOAD,
               ["지시가 길어질수록 뒤쪽 요구를 흘린다. 다섯을 시키면 셋만 하고 끝난다.",
                "어디서 틀렸는지 짚을 수가 없다. 통째로 한 번에 하니 중간이 안 보인다.",
                "하나만 고치고 싶어도 전체를 다시 시켜야 한다."],
               '일을 나누고 이어 붙일 방법을 정해 두는 것 — 이것이 <b>에이전틱 디자인 패턴</b>이다.'))

S.append(D.tbl("패턴은 다섯입니다",
               "외울 필요는 없습니다. 오늘 이 가운데 하나를 손으로 짭니다.",
               ["패턴", "한 줄 뜻", "공장에서"],
               [["프롬프트 체이닝", "쪼개서 줄줄이 잇는다", "탐색 → 이력 조회 → 원인 추정 → 조치안"],
                ["라우팅", "성격을 보고 알맞은 곳으로 보낸다", "알람 등급별 대응 분기"],
                ["병렬 처리", "상관없는 일은 동시에", "CNC 6대 동시 점검"],
                ["총괄과 담당", "총괄이 나누고 담당이 처리한다", "감지·진단·조치 담당 + 총괄"],
                ["검증과 되짚기", "검사하는 쪽을 따로 둔다", "근거 없는 리포트를 돌려보낸다"]],
               on=3, dense=True,
               foot='오후에 짜는 것은 이 둘을 겹친 모양이다 — '
                    '<b>순서는 고정해 놓고, 그 순서를 굴리는 총괄을 따로 세운다.</b>'))

# ── C. MCP (16~21)
S.append(D.cmp("도구 M개 × 에이전트 N개",
               "표준이 없던 시절의 셈법입니다.",
               ("표준이 없으면", "t-off",
                W.MXN.replace('width="540" height="400"', 'width="310" height="230"'),
                "M × N 번 잇는다",
                "도구 5개 × 에이전트 4개 = 이음매 20개.<br>도구가 늘 때마다 <b>전부 다시 고친다.</b>"),
               ("표준이 있으면", "t-on",
                W.PLUG.replace('width="520" height="400"', 'width="310" height="238"'),
                "M + N 번이면 된다",
                "도구는 한 번만 내놓고, 에이전트는 한 번만 배운다.<br>서로 몰라도 규격이 맞는다."),
               'USB 가 주변기기에서 한 일을, 이 표준이 <b>도구</b>에서 한다.'))

S.append(D.sect("도구를 쥐여주는 표준이 있습니다",
                "함수는 이미 있습니다. 에이전트가 부를 방법이 없을 뿐입니다.", W.PLUG,
                "MCP", "Model Context Protocol",
                ["표준이 없으면 에이전트마다 붙이는 방법이 달라, 도구 하나에 매번 다시 짜야 한다.",
                 "한 번 내놓으면 어느 에이전트든 갖다 쓴다."],
                '오늘은 <b>읽는 손</b>이다. 움직이는 손은 오후에 연다.'))

S.append(D.pic("오늘 만드는 도구는 두 개입니다",
               "서버 뼈대와 데이터 가져오기는 이미 되어 있습니다.", W.TOOL2,
               ["<code>detect_anomaly</code> — 어제 짠 <code>detect()</code> 를 에이전트가 부를 수 있게 내놓는다. "
                "어제 <code>돌려보기.py</code> 가 그랬듯 <b>온도와 진동에 각각</b> 한 번씩 태운다.",
                "<code>query_equipment</code> — 설비 하나의 최근 센서 요약과 정비 이력을 모아 돌려준다."],
               '내가 채울 곳은 <b>네 자리</b>다.'))

S.append(D.tbl("도구가 돌려주는 것",
               "이 모양으로 에이전트에게 건네집니다.",
               ["도구", "돌려주는 것"],
               # 실제 응답과 글자 단위로 맞춘다. 전에는 query_equipment 쪽에 **진동이 빠져 있었다** —
               # 화면에는 vibration 평균·최대가 나오는데 슬라이드에만 없으면 학생이 자기 코드를 의심한다.
               [["<code>detect_anomaly</code>",
                 "<code>sample_count</code> · <code>anomaly_count</code> · "
                 "<code>anomalies</code>[시각 · 지표 · 값] — 지표는 <b>온도와 진동</b>"],
                ["<code>query_equipment</code>",
                 "<code>recent</code>(온도·진동 평균·최대 · 결측 수) · "
                 "<code>maintenance</code>(정비 이력) · <code>open_work_orders</code>(미완)"]],
               foot='정비 이력의 <code>note</code> 가 진단의 실마리다. 빠뜨리면 원인을 추정할 재료가 없다.'))

S.append(D.pic("사람이 쓴 문장은 규칙이 못 읽습니다",
               "정비 이력에는 표에 안 담기는 것이 하나 있습니다 — 사람이 쓴 메모.",
               W.ICO_REPORT.replace('"76"', '"320"'),
               ["「소음 있어 다음 점검 때 확인」·「윤활유 교체 예정」 — 숫자가 아니라 임계값에 안 걸린다.",
                "그런데 원인의 실마리는 대개 이런 문장 안에 있다.",
                "규칙은 못 읽고, 사람은 다 못 뒤진다. <b>읽고 종합하는 것</b> — LLM 이 잘하는 바로 그 일이다."],
               '오늘 오전, AI 가 이 <b>메모를 근거로 인용</b>하는 것을 보게 됩니다.'))

S.append(D.pic("도구가 값을 어디서 가져오는가",
               "센서값과 정비 이력이 각각 다른 데서 옵니다.", W.TWOSRC,
               # 「fallback」은 **내부 용어**다. 학생 화면에서 개념 이름으로 쓰면
               # 「그럼 원래 경로는 뭔데」가 따라붙는다. 다만 config.json 에 그 글자가
               # 실제로 적혀 있으므로, 이름을 세우는 대신 **그 줄의 뜻**으로만 풀어 준다.
               ["센서값은 어제 받은 7일치 CSV를 그대로 읽는다. 정비 이력은 강사 서버에서 온다.",
                "설정 파일에 <code>\"data_source\": \"fallback\"</code> 이라 적혀 있다. "
                "이름은 「예비 경로」라는 뜻이지만 <b>우리 반에서는 이것이 본 경로다.</b>",
                "어느 쪽에서 오든 도구 이름과 돌려주는 모양은 같다. 여러분 코드는 고칠 게 없다."],
               '설정은 미리 채워져 있다. 단톡방의 열쇠로 <code>3일차준비.py</code> 한 줄만 치면 된다.'))

# ── D. 실습 1 (22~26)
S.append(D.sect("도구 두 개를 채웁니다",
                "도구는 둘, 채울 자리는 넷입니다. 어제 짠 코드에 AI 가 부를 손잡이를 답니다.", W.TOOL2,
                "실습 1", "55분 · 각자",
                ["3일차 / 실습 / 도구만들기 폴더를 연다. 고칠 파일은 <code>mcp_server.py</code> 하나다.",
                 "자세한 순서는 3일차 실습 가이드 오전에 다 있다.",
                 "막히면 <code>uv run 확인.py</code> 를 돌린다. 그래도 막히면 <code>uv run 확인.py --정답 1</code>."],
                '먼저 <b><code>uv run mcp_server.py --확인</code></b> 로 서버 없이 도구만 호출해 본다.'))

S.append(D.pic("지시는 한 문장입니다",
               "도구를 언제 어떤 순서로 부를지는 말하지 않습니다.", W.ONELINE,
               ["“지난 주 설비 이상을 점검하고, 이상이 있으면 해당 설비의 정비 이력을 조회해 "
                "원인 추정과 권고 조치를 담은 진단 리포트를 작성하라.”",
                "<code>uv run agent.py</code> — 이 한 줄이 위 문장을 AI 에게 준다.",
                "AI 가 내가 만든 도구를 줄줄이 이어서 호출하며 리포트를 뽑아낸다."],
               '순서를 <b>AI 가 정한다.</b> 이것이 아까 본 ReAct다.'))

S.append(D.rail("무슨 일이 일어나는지 봅니다",
                "지시 하나에 세 걸음이 이어집니다.",
                [(W.ICO_FIND, "01", "이상을 찾는다", "내가 어제 짠 <code>detect()</code> 를 부른다."),
                 (W.ICO_DIAG, "02", "이력을 뒤진다", "이상이 난 설비의 정비 기록을 조회한다."),
                 (W.ICO_REPORT, "03", "리포트를 쓴다", "근거가 되는 작업지시를 인용해 원인을 추정한다.")],
                '내가 짠 코드를 <b>AI가 도구로 쓴다.</b> 이것이 오늘의 첫 장면이다.'))

S.append(D.rail("55분을 이렇게 씁니다",
                "세 토막입니다. 시계를 보고 넘어가세요.",
                [(V.ICO_PEN, "35분", "채운다",
                  "네 자리. 하나 채울 때마다 <code>uv run mcp_server.py --확인</code> 로 확인한다."),
                 (W.ICO_EYE, "15분", "본다",
                  "다 같이 <code>uv run agent.py</code>. AI 가 도구를 부르는 줄과 리포트를 본다."),
                 (W.ICO_POINT, "5분", "짚는다",
                  "리포트가 작업지시 번호를 근거로 댔는지, 그리고 "
                  "<b>「이상 N건」의 N 이 믿을 만한 숫자인지</b> 확인한다.")],
                '<b>10분 넘게 막히면</b> <code>uv run 확인.py --정답 1</code> 로 그 자리만 열고 갑니다.'))

# ★ 학생이 반드시 하는 질문에 먼저 답하는 자리.
#   리포트 첫 줄이 「이상 97건」인데, 어제 배운 눈으로 보면 그중 대부분이 헛울림이다.
S.append(D.pic("리포트의 「이상 N건」, 어제 배운 눈으로 보면",
               "리포트가 근거로 댄 첫 줄입니다.", W.MANYFALSE,
               ["준 것은 한 문장뿐이었다. 어느 도구를 먼저 부를지는 말하지 않았는데 "
                "<b>AI 가 스스로 이상을 찾고 이력을 뒤졌다.</b> "
                "그 첫 도구 속에 어제 여러분이 채운 다섯 줄이 들어 있다.",
                "그런데 도구 속은 어제 그 코드다. 어제 헛울림이 <b>여섯 대에서 하루 81번</b> — "
                "한 대 7일이면 백 건 언저리다. 리포트의 N 에는 그것이 다 들어 있다.",
                "AI 는 <b>도구가 준 숫자를 의심하지 않는다.</b> "
                "옆 사람과 숫자가 다르다면 어제 빈칸 3 과 5 에서 다른 쪽을 골랐기 때문이다."],
               'AI 가 내놓은 것을 <b>판정할 수 있는 사람</b>이 아직 필요하다.'))

S.append(D.tbl("막혔을 때",
               "이 셋이 대부분입니다. 그 외에는 손 드세요.",
               ["증상", "원인", "조치"],
               # 전에는 「서버가 안 붙는다 → 방화벽·포트」가 있었다. **오전에는 서버를 안 띄운다** —
               # `--check` 는 함수를 직접 부르고(transport=stdio), 가이드도 「방화벽과 상관없습니다」라고
               # 못 박는다. 일어나지 않는 사고를 적어 두면 학생이 엉뚱한 데를 뒤진다.
               # 「NotImplementedError」도 뺐다 — `raise` 를 없애서 이제 그 글자를 볼 일이 없다.
               # 첫 열은 230px 고정이다. 증상을 길게 쓰면 세 줄로 접혀 표가 무너진다 —
               # 학생이 화면에서 알아볼 만큼만 남긴다.
               [["<code>detect.py</code> 를 못 불러옴", "2일차 빈칸이 비어 있다",
                 "2일차 / 실습 에서 <code>uv run 확인.py</code>"],
                ["<code>아직 안 채움</code>", "오늘 도구 둘 중 하나가 비어 있다",
                 "적힌 이름의 자리를 채운다"],
                ["정비 이력이 비어서 온다", "내 컴퓨터를 보고 있다",
                 "공장 폴더에서 <code>docker compose up -d</code>"]],
               foot='세 가지 다 아니면 손을 드세요. <b>혼자 오래 붙잡는 것이 제일 손해입니다.</b>'))

S.append(D.pic("여기까지는 제안만 합니다",
               "리포트가 “속도를 낮추라”고 권고해도 공장은 그대로 돕니다.", W.NOHAND,
               ["리포트는 나왔다. 원인도 짚었고 무엇을 하라고까지 적혀 있다. "
                "그런데 그걸 읽고 실제로 회전수를 낮추는 건 여전히 사람이다.",
                "그리고 <b>이 리포트에는 새벽 3시가 없다.</b> 아까 도구를 채우며 "
                "「너무 길면 AI 가 다 못 읽으니 최근 것만 추린다」고 한 줄을 넣었다. "
                "그 한 줄이 앞을 잘라 냈다.",
                "눈은 달았는데 손이 없다. 게다가 "
                "<b>도구를 만든 사람의 판단 하나가 AI 가 보는 세계의 크기를 정했다.</b>"],
               '지시 한 문장에 <b>역할이 몇 개나 들어 있었을까.</b>'))

# ── E. 분해 + HITL (27~34)
# ★ 그림을 FOURPLUS 로 바꿨다. 전에는 14장(「뒤쪽은 흘린다」)과 **같은 파일**이었는데,
#   이 장의 본문은 「그래도 잘 돌았다」다. 그림과 글이 정반대를 말하고 있었다.
#   그리고 「진짜 설비」는 2일차의 「이 기계들은 전부 가짜입니다」와 부딪힌다 —
#   무게를 주는 말은 남기되 **거짓이 아닌 쪽**으로 쓴다.
S.append(D.pic("한 문장에 네 가지 일이 들어 있습니다",
               "점검하고, 뒤지고, 추정하고, 씁니다.", W.FOURPLUS,
               ["아까 준 그 한 문장을 다시 보면 — 점검하고, 이력을 뒤지고, 원인을 추정하고, "
                "리포트를 쓴다. 넷이다.",
                "그래도 잘 돌았다. 데이터가 얌전했고 할 일이 적었으니까. "
                "그런데 여기에 「움직이기」까지 얹으면 어떻게 될까.",
                "오늘부터 이 판단은 <b>곧바로 명령이 된다.</b> 흔들리면 안 되는 이유가 하나 더 생겼다."],
               '아까는 읽기만 했다. 이제 <b>틀리면 설비가 잘못 움직인다.</b>'))

S.append(D.sect("그래서 셋으로 쪼갭니다",
                "아까 표에서 굵게 칠했던 그 모양을 이제 손으로 만듭니다.", W.SPLIT3,
                "총괄과 담당", "담당 셋 + 총괄 하나",
                ["감지는 이상 구간을 찾고, 진단은 원인을 추정하고, 조치는 무엇을 할지 정한다. "
                 "각자 하나씩만 맡으니 지시가 짧아진다.",
                 "총괄은 직접 일하지 않는다. 앞 담당의 결과를 받아 다음 담당에게 넘길 뿐이다. "
                 "이 총괄을 <b>오케스트레이터</b>라 부른다.",
                 "조치가 끝나면 다시 감지로 돌아간다. 이렇게 <b>끝이 처음으로 이어지는 것</b>을 "
                 "폐루프라 한다."],
                '각자 하나씩만 맡으면 <b>틀린 자리가 보이고, 그 자리만 고칠 수 있다.</b>'))

S.append(D.pic("지금 제어 권한이 열렸습니다",
               "오전에는 지난 주 기록을 읽었습니다. <b>오후부터는 지금 이 순간 도는 공장입니다.</b>",
               W.UNLOCK4,
               ["이 공장에는 명령을 넣는 통로가 네 개 있다. 지금까지는 잠겨 있었다.",
                "그 네 개를 지금 연다 — 다 같이 <code>uv run 제어열기.py</code>. "
                "화면 위 「제어」가 <b>개방</b>으로 바뀐다."],
               '읽는 손에서 <b>움직이는 손</b>으로. 무게가 달라진다.'))

S.append(D.tbl("열린 도구는 넷입니다",
               "무엇이 자동이고 무엇이 사람 승인인지, 여기서 갈립니다.",
               ["도구", "무엇을 하나", "승인"],
               [["<code>set_equipment_speed</code>", "설비 회전수를 바꾼다 (감속)", "자동"],
                ["<code>ack_alarm</code>", "알람을 확인 처리한다", "자동"],
                ["<code>stop_equipment</code>", "설비를 정지시킨다", "사람 승인 필수"],
                ["<code>dispatch_robot</code>", "정비 로봇(AMR)을 파견한다", "사람 승인 필수"]],
               on=2, 키폭=390,
               foot='오늘 실제로 쓰이는 것은 위 둘이다. '
                    '<b>아래 둘은 쓰지 않는데도 열어 둔다 — 왜 그 둘만 승인이 필요한지가 '
                    '오늘 배울 것이다.</b>'))

S.append(D.pic("감속이 첫 수인 이유",
               "어제 봤습니다 — 이 공장의 온도는 회전수의 함수입니다.", V.RPMTEMP,
               ["기계를 늦추면 열이 줄어든다. 온도가 실제로 내려간다.",
                "생산은 느려지지만 멈추지는 않는다. 잘못 판단했더라도 속도를 되올리면 그만이다.",
                "그래서 과열 앞에서 첫 수는 정지가 아니라 감속이다 — <b>효과는 있고, 되돌릴 수 있다.</b>"],
               '조치의 순서는 <b>되돌릴 수 있는 것부터</b>다.'))

S.append(D.cmp("되돌릴 수 있는가 — 이것이 갈림선입니다",
               "물리 세계에는 한 번 하면 되돌릴 수 없는 행동이 있습니다.",
               ("자동", "t-on", W.UNDO, "되돌릴 수 있다",
                "감속은 잘못돼도 다시 올리면 된다.<br>알람 확인도 다시 열면 된다."),
               ("승인 필수", "t-off", W.NOUNDO, "되돌릴 수 없다",
                "정지는 그 기계에서 깎던 것을 버리게 만든다. <b>그 시간은 안 돌아온다.</b>"
                "<br>로봇은 부르면 다른 일을 놓고 온다."),
               '되돌릴 수 없는 행동 앞에 사람을 세운다 — 이것이 <b>HITL</b>이다.'))

S.append(D.sect("고리 안에 사람이 한 자리 있습니다",
                "HITL — Human In The Loop.", W.GATE,
                "승인 관문", "실행 대신 물어본다",
                ["에이전트가 정지나 파견을 하려 하면, 실행 대신 승인 요청이 뜬다.",
                 # 1일차 모듈 5가 HITL/OHOTL/AHOTL 을 운영 모드로 가르치고,
                 # 부록 A-4 가 「작업요청의 최종 발행은 반드시 사람이 승인한다」를
                 # 완료 기준으로 못 박는다. 여기가 그것이 실제로 도는 자리다.
                 "<b>어제 배운 그 승인 지점</b>이다. 어제는 명세에 적었고, 오늘은 그것이 실제로 돈다.",
                 "이 구분은 <b><code>hitl.py</code></b> 가 강제한다. 잠시 뒤 열 폴더에 이미 들어 있고, "
                 "여러분이 채우는 파일이 아니다.",
                 "그래서 실수로 정지를 자동 실행하는 일은 코드를 어떻게 짜든 일어나지 않는다."],
                '<b>안전은 조심해서 지키는 게 아니라 구조로 지킨다.</b>'))

S.append(D.pic("AI에게 다 맡기는 것도, 사람이 다 하는 것도 아닙니다",
               "되돌릴 수 없는 곳에만 사람을 둡니다.", W.SPECTRUM,
               ["전부 사람이 승인하면 야간 무인 운전이 성립하지 않는다.",
                "전부 자동이면 오판 한 번에 라인이 선다.",
                "그래서 되돌릴 수 있는 것은 자동, 없는 것만 승인으로 갈랐다."],
               '이 선을 어디에 긋느냐가 <b>현장 설계의 핵심</b>이다.'))

# ── F. 실습 2 (35~40)
# ★ judge() 가 어제 z-score 와 무엇이 다른지, **실습에 들어가기 전에** 말해 준다.
#   전에는 이 설명이 실습이 다 끝난 뒤(45장 표)에만 있었다. 그러면 학생은 로직을 모른 채
#   70분을 시작하고, 가이드를 보고 베끼게 된다 — 2일차에서 그토록 피하려던 그것이다.
S.append(D.cmp("기준을 과거에 고정합니다",
               "어제 코드가 못 잡은 이유가 바로 기준이 따라 올라간 것이었습니다.",
               ("어제 · z-score", "t-off",
                V.DRIFTCHASE.replace('width="560" height="400"', 'width="320" height="229"'),
                "기준이 같이 오른다",
                "지금 값을 <b>바로 앞 60개</b>와 견준다.<br>온도가 오르면 그 60개도 같이 오른다."),
               ("오늘 · <code>judge()</code>", "t-on",
                W.TWOHALF.replace('width="520" height="400"', 'width="320" height="246"'),
                "기준이 안 따라간다",
                "구간을 <b>둘로 갈라 앞쪽</b>을 기준으로 삼는다.<br>기준이 과거에 묶여 거리가 벌어진다."),
               '<b>감지 담당은 이 두 자리가 전부다</b> — 최근 것만 남기고, 앞뒤로 갈라 견준다.'))

S.append(D.sect("폐루프를 닫습니다",
                "판단이 설비를 바꾸고, 바뀐 값을 다시 읽는 데까지 갑니다.", W.CLOSED,
                "실습 2", "70분 · 각자",
                ["3일차 / 실습 / 폐루프 폴더를 연다. 오케스트레이터는 이미 완성돼 있고, 담당 셋의 속만 채운다.",
                 "config 는 미리 채워져 있다. <code>uv run loop.py --확인</code> 부터 친다.",
                 "순서는 3일차 실습 가이드 오후에 다 있다. 막히면 손 드세요."],
                '채울 곳은 <b>감지 · 진단 · 조치</b> 세 군데다.'))

S.append(D.rail("담당 셋이 하는 일",
                "각 담당이 어느 파일의 어느 함수인지까지 적어 뒀습니다.",
                [(W.ICO_FIND, "01", "감지 — 이상 구간을 찾는다",
                  "온도 이력을 보고 이상이 시작된 지점을 집는다. &nbsp;<code>detector.py 의 judge()</code>"),
                 (W.ICO_DIAG, "02", "진단 — 원인을 추정한다",
                  "정비 이력을 뒤져 근거가 될 작업지시를 찾는다. &nbsp;<code>diagnoser.py 의 build_prompt()</code>"),
                 (W.ICO_ACT, "03", "조치 — 무엇을 할지 정한다",
                  "감속으로 될지, 로봇을 보내야 할지 정한다. &nbsp;<code>actuator.py 의 to_commands()</code>")],
                '총괄은 <b>이미 완성돼 있다.</b> 셋의 속만 채우면 돈다.'))

S.append(D.rail("70분을 이렇게 씁니다",
                "세 토막입니다. 시계를 보고 넘어가세요.",
                [(V.ICO_PEN, "40분", "채운다",
                  "감지 → 진단 → 조치 순서로. 하나 채울 때마다 <code>uv run loop.py --once</code> 로 확인한다."),
                 (W.ICO_LOOP2, "20분", "돌린다",
                  "<code>uv run loop.py</code> 로 계속 돌리며 기다린다. 강사가 한 대의 온도를 올리기 시작한다."),
                 (W.ICO_EYE, "10분", "본다",
                  "승인 요청에 y 를 치고, 공장 화면에서 로봇이 움직이는 것을 본다.")],
                '한 자리에서 <b>10분 넘게 막히면</b> <code>uv run loop.py --정답 1</code> 로 그 자리만 열고 갑니다.'))

S.append(D.tbl("폐루프는 일곱 걸음입니다",
               "승인 한 번을 빼고는 사람이 아무것도 하지 않습니다.",
               ["", "무엇이 일어나나", "누가"],
               [["01 시작", "한 대의 온도가 슬금슬금 오르기 시작한다", "강사"],
                ["02 감지", "감지 담당이 포착한다", "에이전트"],
                ["03 진단", "정비 이력을 뒤져 원인을 추정한다", "에이전트"],
                ["04 감속", "회전수를 낮춘다 — 되돌릴 수 있으므로 자동", "에이전트"],
                ["05 승인 요청", "로봇 파견 — 되돌릴 수 없으므로 멈춰 선다", "에이전트"],
                ["06 승인", "y 를 누른다", "사람"],
                ["07 파견", "AMR 이 움직이고 값이 바뀐다", "에이전트"]],
               on=4, dense=True,
               foot='<b>05 에서 루프가 멈춰 기다린다.</b> 누르기 전까지 아무 일도 안 일어난다.'))

S.append(D.pic("감지까지 1분에서 3분 걸립니다",
               "루프를 돌려 둔 채, 어제 그 버튼 — <b>「이상 시작」</b>을 다 같이 누릅니다.",
               W.ICO_WAIT.replace('"76"', '"300"'),
               ["온도가 오르기 시작해도 바로 잡히지는 않는다. 값이 충분히 쌓여야 판정이 선다.",
                "설계된 대기다. 기다리세요."],
               '터미널에 아무것도 안 뜨는 동안이 <b>정상</b>이다.'))

S.append(D.rail("화면에서 세 가지를 보세요",
                "옆 사람과 숫자가 달라도 정상입니다. 각자 공장이 다르니까요.",
                [(W.ICO_REPORT, "01", "리포트", "진단이 정비 이력의 어느 줄을 근거로 댔는지."),
                 (W.ICO_STOP, "02", "승인 요청", "언제 멈춰 섰는지, 무엇을 하려고 물었는지."),
                 (W.ICO_ROBOT, "03", "공장 화면", "회전수 · AMR 위치 · 온도가 실제로 어떻게 바뀌는지.")],
                # 정지를 열어 놓고 한 번도 안 쓰면 「열린 도구 넷」이 둘로만 실감된다.
                # 안 쓴 것이 아니라 **안 써도 됐다**는 것이 조치 담당의 판단 결과다.
                '<b>정지는 오늘 안 씁니다</b> — 감속으로 충분했기 때문입니다. '
                '끝나면 진단 리포트를 캡처해 두세요.'))

S.append(D.tbl("막혔을 때",
               "이 넷이 대부분입니다.",
               ["증상", "조치"],
               # 「n 을 쳤다」를 넣는다 — 누군가는 반드시 오타를 내거나 일부러 거부해 본다.
               # 그때 화면에 「거부됨」이 찍히는데, 안 적어 두면 자기가 망친 줄 안다.
               [["아무 일도 안 일어난다", "1~3분 기다렸는지 확인. 그래도 안 되면 손 들기"],
                ["감지는 되는데 진단이 비어 있다", "정비 이력을 안 부르고 있다. 진단 담당을 본다"],
                ["승인 요청이 안 뜬다", "조치 담당이 파견까지 안 가고 있다"],
                ["<code>y</code> 말고 다른 걸 눌렀다", "<code>거부됨</code> 으로 넘어간다. 다음 회차에 다시 묻는다"]],
               dense=True,
               foot='네 가지 다 아니면 손을 드세요. <b>혼자 오래 붙잡는 것이 제일 손해입니다.</b>'))

# ── G. 닫기 (41~44)
S.append(D.rail("오늘 세 가지를 했습니다",
                "아침에 봤던 세 도막이 이렇게 끝났습니다.",
                [(W.ICO_TOOLS, "01", "쥐여줬다", "내가 짠 코드를 도구로 내놓고, AI가 갖다 썼다."),
                 (W.ICO_ORCH, "02", "판단하게 했다", "그 판단을 감지·진단·조치 셋으로 쪼개 손으로 짰다."),
                 (W.ICO_ROBOT, "03", "움직였다", "판단대로 설비가 바뀌었다. 승인 한 번을 빼고는 자동으로.")],
                '<b>고리가 닫혔다.</b> 인지 → 판단 → 행동 → 다시 인지.'))

S.append(D.tbl("이틀의 지도",
               "2일차의 질문마다 오늘 답이 하나씩 놓였습니다.",
               ["2일차의 질문", "오늘의 답"],
               [["서서히 오르는 것을 못 잡았다", "기준을 과거에 고정한 <b><code>judge()</code></b> 로 잡았다"],
                ["새벽 3시를 아무도 못 봤다", "잠들지 않는 야간 당직자 — 에이전트가 계속 돈다"],
                ["규칙은 정하지 못한 것 앞에서 말이 없다", "사람이 쓴 메모까지 읽고 원인을 추정하는 판단이 들어왔다"],
                ["그럼 누가 판단하나", "AI 가 판단하되, 되돌릴 수 없는 것 앞에는 사람이 선다"]],
               dense=True,
               foot='이 지도 위에 내일 <b>마지막 1미터</b>가 남아 있다.'))

S.append(D.tbl("여러분이 오늘 한 계산, 현장에 이름이 있습니다",
               "오늘 <code>judge()</code> 에서 한 그 계산은, 현장에서 수십 년째 쓰는 방법과 같은 생각입니다.",
               ["", "기준을 어디서 잡나", "서서히 오르는 것"],
               # 첫 열이 좁아 이름이 두세 줄로 접힌다 — 열 이름을 짧게 둔다
               [["어제 · z-score", "최근 값들 — 기준이 같이 따라 오른다", "못 잡는다"],
                ["오늘 · <code>judge()</code>", "구간을 둘로 갈라 <b>앞쪽</b>을 기준으로 — 기준이 안 따라간다", "잡는다"],
                ["현장 · CUSUM · EWMA",
                 "벗어난 양을 쌓아 두거나(CUSUM), 기준이 따라가되 아주 천천히 가게 한다(EWMA)",
                 "잡는다"]],
               on=1, 키폭=420,
               foot='오늘 짠 것에도 이름이 있다 — <b>구간을 갈라 앞뒤를 견주는 변화점 탐지</b>다. '
                    '셋 다 기준이 도망가지 못하게 붙잡는다.'))

S.append(D.sect("그런데 이건 전부 소프트웨어입니다",
                "여러분이 세운 것은 화면 안의 공장입니다.", W.SOFTONLY,
                "마지막 1미터", "내일의 질문",
                ["여기서 낸 명령은 곧바로 설비에 닿았다. 진짜 공장에서는 그 사이에 "
                 "<b>PLC</b> 라는 제어기가 하나 더 있다. 기계를 직접 움직이는 것은 그쪽이다.",
                 "그리고 사람이 다치는 것을 막는 안전 장치는 소프트웨어가 아니라 하드웨어에 남는다. "
                 "코드가 아무리 잘 짜여도 그 부분은 쇳덩이가 맡는다."],
                '화면과 쇳덩이 사이에 남은 <b>그 마지막 1미터</b>는 내일 이어집니다.'))

S.append(D.rail("남은 20분은 노션입니다",
                "오늘 겪은 것을 카드로 남깁니다.",
                [(V.ICO_PICK, "01", "고른다", "오늘 안 풀린 것 두세 개. 그게 카드 제목이 된다."),
                 (V.ICO_WRITE, "02", "쓴다", "화면을 덮고 내 문장으로 쓴다. 안 써지면 그게 모르는 것이다."),
                 (V.ICO_PIN, "03", "붙인다", "진단 리포트를 캡처해 붙이고 이해도 태그를 찍는다.")],
                '<b>우리 이틀은 여기까지</b>입니다. 오늘 할 일은 <b>노션 링크 제출</b> 하나입니다.'))

(pathlib.Path(__file__).resolve().parents[2] / '제작' / '산출물' / '덱').mkdir(parents=True, exist_ok=True)
n = D.write(S, pathlib.Path(__file__).resolve().parents[2] / "제작" / "산출물" / "덱" / "3일차덱.html", "3일차 · AI 에이전트 직접 만들기")
print("슬라이드:", n)
