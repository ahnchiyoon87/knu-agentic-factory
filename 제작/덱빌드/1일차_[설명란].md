# 1일차 — 설명란 (36장)

> **업로드할 파일 2개**
> ① `00_디자인기준판\디자인기준판.pdf`
> ② `1일차_[첨부]원고.md` ← **이 덱 범위만 담긴 원고. 다른 덱 원고를 같이 올리지 마라.**
>
> ※ **NotebookLM이 받는 형식** — PDF · txt · **Markdown(.md)**. HTML·PNG는 안 된다.
> **맞춤설정** — 언어 한국어 / 길이 기본값
> **설명 칸** — 아래 ▼부터 ▲까지 전부 복사해 붙여넣기
> **생성 결과** — 36장 → PPT로 내려받아 확정. 머리글·페이지 번호는 슬라이드 마스터에서 일괄 처리한다.

---

▼ 복붙 시작 ▼

**정확히 36장**의 슬라이드를 만들어라. 아래 1~36페이지 지정을 **한 지정 = 한 장**으로 따르고 합치거나 나누지 마라.

## 이 자료에는 발표 대본이 없다

강사가 읽을 대본이 따로 없다. **슬라이드만 보고도 뜻이 통해야 한다.**

- **글로 다 쓰지 마라.** 화면을 글자로 채우면 읽느라 강의를 못 듣는다.
- **그렇다고 비우지도 마라.** 낱말만 덩그러니 있으면 대본 없이는 아무 뜻도 안 통한다.
- **답은 삽화다.** 그림이 뜻을 지고, 글은 그림이 못 하는 말만 한다.
- **그림만으로 뜻이 안 통하면 그때 라벨을 박아라.** 보면 아는 물건에는 붙이지 않는다.

## 범위 — 이걸 어기면 실패다

- 이 덱은 **「그럼 누가 판단하나」라는 질문에서 끝난다.**
- **그 질문에 답하지 마라.** 에이전트·MCP·제어·승인 관문은 전부 2일차 몫이다. 한 글자도 꺼내지 마라.

- **36장을 넘겨 만들지 마라.** 36페이지가 마지막이다.
- 업로드한 원고에 없는 것을 **만들어 넣지 마라.**

## 전 장 공통 금지

- **어떤 이상이 잡히고 어떤 것이 안 잡히는지 한 글자도 쓰지 마라.** 스파이크·드리프트·결측 각각의 결과는 **학생이 코드를 돌려서 발견할 반전이다.**
- 오탐 수치(하루 81건 → 911건, 11배)는 이론으로 다루는 것이라 나와도 된다.
- 화면을 좌우로 갈라 칠하지 마라. 배경은 **순백 하나**, 표지형만 짙은 남색이다.
- **숫자를 화면 절반만 하게 키우지 마라.** 큰 낱말도 제목보다 조금만 크다.
- 격자·청사진 무늬·뜻 없는 숫자 더미를 그림 안에 깔지 마라.
- 사진·제품 로고·회사 이름 금지.

# 1페이지

**레이아웃** 1번 (표지형)

**화면 글자**
- 윗줄: `1일차`
- 제목: `이상을 잡는 자를 만든다`
- 부제: `경남대 RISE 피지컬AI 사관학교 · 8월 Agentic AI 특강`
- 하단 두 줄: `새벽 3시의 사건 · 이상을 재는 세 가지 자 · 오탐과 미탐` / `오늘, 알고리즘 하나를 직접 짭니다.`（두 번째 줄 굵게）

**삽화** 오른쪽에 흰 동심원 다섯 겹, 투명도 30%. 다른 그림 금지.

**금지**
- 배경은 짙은 남색 #12305A 하나. 사진·로고 금지.

---

# 2페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `새벽 3시, 아무도 없는 공장에서`
- 설명문: `창원 K-정밀. 밤에도 공장은 돌고, 사람은 없습니다.`
- 본문 3행:
  - `EQ-03이라는 CNC 한 대의 온도가 조금씩 오르기 시작했다.`
  - `CNC는 컴퓨터 수치제어 공작기계. 창원 공장에 가장 흔하게 깔린 장비다.`
  - `아침 7시, EQ-03은 여전히 돌고 있었다. 멈추지도, 불이 나지도 않았다.`
- 하단 강조(아주 연한 주황 밑칠): `한 시간에 오른 폭은 0.5도. 사람이 옆에 서 있었더라도 몰랐을 속도다.`

**삽화** 밤 공장. 톱니 지붕 건물 + 옆 동. 창 여러 개 중 **한 칸만 주황으로 켜져** 있고 그 아래 `EQ-03` 라벨. 왼쪽 위에 작은 시계와 `새벽 3시`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 3페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `문제는 그날 오후에 드러났습니다`
- 설명문: `밤새 아무도 몰랐고, 아무 일도 없어 보였습니다.`
- 본문 3행:
  - `밤새 깎아 낸 부품의 치수가 미세하게 틀어져 있었다.`
  - `하루치 생산분을 통째로 버렸다.`
  - `설비가 고장 난 것도, 알람이 울린 것도 아니다. 그냥 조용히 나빠졌다.`
- 하단 강조(아주 연한 주황 밑칠): `경고음은 한 번도 울리지 않았다.`

**삽화** 버려진 부품 더미. 원통형 부품 세 개가 통 안에 담겨 있고 각각 **주황 X 표시**. 오른쪽에 `하루치` 라벨.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 4페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `오늘 물을 것은 온도가 아닙니다`
- 설명문: `왜 올랐는가가 아니라, 왜 네 시간 동안 아무도 몰랐는가.`
- 큰 낱말: `왜 몰랐나`
- 낱말 아래 한 줄: `오늘의 출발점`
- 본문 2행:
  - `원인을 찾는 것은 정비 담당의 일이다.`
  - `우리가 풀 문제는 **알아채지 못했다**는 쪽이다.`
- 하단 강조(아주 연한 주황 밑칠): `오늘 만드는 것은 고치는 도구가 아니라 알아채는 도구다.`

**삽화** 큰 원 안에 주황 물음표 하나. 다른 요소 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 5페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `센서가 없어서 생긴 사고가 아닙니다`
- 설명문: `EQ-03에는 온도계가 달려 있었고, 값은 계속 기록되고 있었습니다.`
- 본문 2행:
  - `데이터는 1초마다 꼬박꼬박 쌓이고 있었다.`
  - `쌓인 값을 다시 열어 보니 온도가 오른 것이 그대로 남아 있었다.`
- 하단 강조(아주 연한 주황 밑칠): `볼 사람이 없었을 뿐이다.`

**삽화** 왼쪽에 계기(사각 틀 안 눈금), 점선으로 이어진 오른쪽에 축과 **주황 기록선**(톱니 모양). 왼쪽 아래 `1초마다 기록`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 6페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `사람이 못 본 이유는 셋입니다`
- 설명문: `센서 탓도, 게으름 탓도 아닙니다.`
- 행 3개 (번호 / 굵은 제목 / 설명 한 줄):
  - `01` · `볼 시간이 없다` · `여덟 시간 일하면 열여섯 시간은 아무도 안 본다. 새벽 3시가 정확히 그 안에 있다.`
  - `02` · `봐도 알아채지 못한다` · `62.1이 62.6으로 바뀌는 것을 보고 이상하다고 말할 사람은 없다.`
  - `03` · `볼 것이 너무 많다` · `설비 6대 × 값 4가지 — 숫자 24개가 1초마다 바뀐다. 로봇 2대까지 더해진다.`
- 하단 강조(아주 연한 주황 밑칠): `셋 다 사람을 더 뽑는 것으로는 안 풀린다.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 (삽화 설명 없음 / (삽화 설명 없음 / (삽화 설명 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 7페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `사람은 볼 시간이 없습니다`
- 설명문: `하루 스물네 시간 가운데, 사람이 지키고 있는 시간은 얼마나 될까요.`
- 본문 2행:
  - `K-정밀은 주간 근무만 하고, 야간에는 무인으로 돌아간다.`
  - `3교대로 돌려도 야간에는 인원이 줄고 집중도가 떨어진다.`
- 하단 강조(아주 연한 주황 밑칠): `사고는 정확히 아무도 보지 않는 그 시간에 난다.`

**삽화** 24시간 원. **16시간에 해당하는 부채꼴이 연한 남색으로 칠해짐.** 바늘 둘. 라벨 `근무 8시간`(회색), `무인으로 도는 16시간`(남색).

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 8페이지

**레이아웃** 5번 (나란히 비교형)

**화면 글자**
- 제목: `봐도 알아채지 못합니다`
- 설명문: `한 시간 사이의 변화입니다. 둘 중 하나라도 이상해 보입니까?`
- 왼쪽: 분류 `새벽 3시` / 이름 `62.1℃` / 설명 `평소와 같은 값이다. 기록에도 아무 표시가 없다.`
- 오른쪽: 분류 `새벽 4시` / 이름 `62.6℃` / 설명 `0.5도 올랐다. **이것을 이상이라 부를 사람은 없다.**`
- 하단 강조(아주 연한 주황 밑칠): `급격한 변화라면 누구나 안다. 문제는 이 사건이 급격하지 않았다는 것이다.`

**삽화** 왼쪽 — 거의 평평한 남색 톱니 선 하나. 기준선 회색.

오른쪽 — TRACE_A 와 **거의 똑같은 모양**의 남색 톱니 선. 기준선만 주황 점선으로 조금 아래.

**두 쪽 배경을 다르게 칠하지 마라.** 가운데 가는 세로선 하나로만 나눈다.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 9페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `볼 것이 너무 많습니다`
- 설명문: `한 대만 보면 되는 게 아닙니다.`
- 본문 3행:
  - `설비 6대에서 온도·진동·rpm·가동상태가 쏟아진다.`
  - `여기에 자율이동로봇 2대의 값까지 더해진다.`
  - `여섯 대를 동시에, 쉬지 않고 봐야 한다.`
- 하단 강조(아주 연한 주황 밑칠): `사람 눈이 따라갈 양이 아니다.`

**삽화** 설비 네모 여섯 개. 각 네모 아래로 **주황 세로선이 여러 가닥 쏟아짐**. 아래에 `1초마다 24개`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 10페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `여러분이라면 어떻게 하시겠습니까`
- 설명문: `아마 가장 먼저 떠오르는 답은 이것일 겁니다.`
- 큰 낱말: `80℃`
- 낱말 아래 한 줄: `고정 임계값 · 미리 정해 둔 경고선`
- 본문 2행:
  - `공장에서 수십 년째 쓰는 방법이고, 실제로 쓸모도 있다.`
  - `잠도 안 자고, 0.5도도 정확히 재고, 여섯 대를 동시에 본다.`
- 하단 강조(아주 연한 주황 밑칠): `앞의 세 문제를 한 번에 푸는 것처럼 보인다.`

**삽화** 위쪽 남색 점선 하나(임계선), 아래쪽에 완만히 오르는 주황 곡선. 둘 사이가 크게 벌어져 있고 `넘지 않는다` 라벨.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 11페이지 ★ 반전

**레이아웃** 8번 (가는 선 그래프형)

**화면 글자**
- 제목: `그런데 80도를 넘은 적이 없습니다`
- 설명문: `선을 넘지 않고도 부품은 망가졌습니다.`
- 본문 3행:
  - `그럼 선을 64도로 낮추면 되지 않나. 그러면 정상적으로 돌던 다른 설비들이 하루 종일 울린다.`
  - `경고가 자주 울리면 사람은 그것을 꺼 버린다. 그러면 처음 상태로 돌아간다.`
  - `높이면 못 잡고, 낮추면 다 울린다.`
- 하단 강조(아주 연한 주황 밑칠): `“값이 얼마인가”가 아니라 “값이 어떻게 변해 왔는가”를 봐야 한다.`

**삽화** 가는 선 그래프. 위 남색 점선 `임계값 80℃`, 아래 62→64로 오르는 주황 곡선(`62℃` `64℃`). **두 선 사이 빈 공간에 세로 화살표와 `16℃ 남음`.**

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 12페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `오늘의 무대 — K-정밀`
- 설명문: `경남 창원의 정밀기계 부품 제조사. 다만 이 공장은 전부 소프트웨어입니다.`
- 큰 낱말: `K-정밀`
- 낱말 아래 한 줄: `CNC 6대 · AMR 2대`
- 본문 2행:
  - `AMR은 자율이동로봇이다. 레일 없이 스스로 경로를 찾아 부품과 공구를 실어 나른다.`
  - `값은 1초마다 바뀌며 쌓인다.`
- 하단 강조(아주 연한 주황 밑칠): `온도와 진동은 기계가 지금 무리하고 있는가를 알려 주는 신호다.`

**삽화** 공장 평면. 큰 사각 테두리 안에 설비 네모 여섯 개, 아래쪽에 주황 원 두 개가 점선으로 이어짐. 라벨 `CNC 6` `AMR 2`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 13페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `설비가 내놓는 값`
- 설명문: `오늘 다룰 것은 이 가운데 온도입니다.`
- 표 머리행: `(빈칸)` / `무엇` / `읽는 값`
  - `CNC 6대` / `EQ-01 ~ EQ-06 · 컴퓨터 수치제어 공작기계` / `온도 · 진동 · rpm · 가동상태`  ← 이 행만 아주 연한 남색 #EEF3F9
  - `AMR 2대` / `자율이동로봇 · 부품과 공구를 실어 나른다` / `위치 · 배터리 · 적재상태`
- 하단 강조(아주 연한 주황 밑칠): `두 줄 모두 1초마다 새로 쓰인다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 14페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `이 기계들은 전부 가짜입니다`
- 설명문: `진짜 공장을 빌린 것이 아닙니다.`
- 본문 3행:
  - `CNC 6대와 AMR 2대는 데이터베이스 테이블의 행 여덟 개다.`
  - `“설비가 돈다”는 것은 1초마다 숫자를 바꿔 쓰는 프로그램이다.`
  - `공장은 한 사람에 하나씩 주어진다. 옆 사람 명령이 내 공장에 닿지 않는다.`
- 하단 강조(아주 연한 주황 밑칠): `진짜 물리 세계는 9월 본 과정에서 다룬다.`

**삽화** 데이터베이스 표 그림. 남색 머리행 + **여덟 줄**, 각 줄에 회색 막대 네 개. 아래 `행 여덟 개`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 15페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `오늘은 세 도막입니다`
- 설명문: `보고, 잡아보고, 그 자가 어디까지 되는지 확인합니다.`
- 표 머리행: `(빈칸)` / `무엇을 하나` / `쓰는 것`
  - `본다` / `내 공장을 열고 여섯 대가 도는 것을 눈으로 본다` / `브라우저`
  - `잡아본다` / `이상을 찾아내는 알고리즘을 직접 짠다` / `detect.py`
  - `확인한다` / `임계값을 흔들어 무엇이 늘고 무엇이 주는지 잰다` / `run.py`
- 하단 강조(아주 연한 주황 밑칠): `마지막 20분은 오늘 겪은 것을 노션에 정리한다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 16페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `먼저 내 공장을 엽니다`
- 설명문: `1일차 / 실습 에서 python 내번호.py 를 한 번 칩니다.`
- 큰 낱말: `실습 1`
- 낱말 아래 한 줄: `20분 · 각자`
- 본문 3행:
  - `받아 둔 폴더에서 1일차 / 실습가이드.md 를 열어 두고 따라온다.`
  - `내번호.py 가 내 공장 번호와 주소를 화면에 찍어 준다. 그 번호를 적어 둔다.`
  - `각자 번호가 다르다. 옆 사람 주소로 들어가면 남의 공장이 보인다.`
- 하단 강조(아주 연한 주황 밑칠): `안 뜨면 손만 드세요. 앞에 나올 일은 없습니다.`

**삽화** 공장 평면. 큰 사각 테두리 안에 설비 네모 여섯 개, 아래쪽에 주황 원 두 개가 점선으로 이어짐. 라벨 `CNC 6` `AMR 2`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 17페이지

**레이아웃** 7번 (낱말 나열형)

**화면 글자**
- 제목: `화면에 세 가지가 보여야 합니다`
- 설명문: `하나라도 안 보이면 손을 드세요.`
- 세 칸 (낱말 / 설명):
  - `네모 여섯` · `CNC 설비 EQ-01 ~ EQ-06`
  - `점 둘` · `AMR 자율이동로봇`  ← 낱말을 주황으로
  - `바뀌는 숫자` · `1~2초마다 값이 갱신된다`
- 하단 강조(아주 연한 주황 밑칠): `숫자가 멈춰 있으면 새로고침을 한 번 하세요.`

**삽화** 낱말 **위에** 각각 큰 그림 하나씩. 낱말 위 굵은 가로줄을 그리지 마라.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 18페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `기계마다 평소 온도가 다릅니다`
- 설명문: `내 화면의 숫자가 옆 사람과 달라도 정상입니다.`
- 본문 2행:
  - `EQ-04는 56도 근처, EQ-05는 74도 근처에서 논다.`
  - `그래서 모든 설비에 같은 경고선 하나를 그을 수가 없다.`
- 하단 강조(아주 연한 주황 밑칠): `이것이 고정 임계값의 두 번째 약점이다.`

**삽화** 지그재그 주황 선(지금 값)과 그것을 따라 올라가는 남색 점선(최근 평균). 라벨 `지금 값` `최근 평균`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 19페이지

**레이아웃** 7번 (낱말 나열형)

**화면 글자**
- 제목: `이 공장은 일부러 고장 낼 수 있습니다`
- 설명문: `강사 콘솔에서 세 가지를 만들어 낼 수 있습니다.`
- 세 칸 (낱말 / 설명):
  - `드리프트` · `온도가 서서히 오른다`  ← 낱말을 주황으로
  - `스파이크` · `진동이 순간 튄다`
  - `결측` · `센서 값이 안 들어온다`
- 하단 강조(아주 연한 주황 밑칠): `지금 여러분 공장에 하나를 넣겠습니다. 화면을 보세요.`

**삽화** 낱말 **위에** 각각 큰 그림 하나씩. 낱말 위 굵은 가로줄을 그리지 마라.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 20페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `지금 화면에서 무엇이 달라졌습니까`
- 설명문: `정답을 맞히는 시간이 아닙니다.`
- 큰 낱말: `관찰`
- 낱말 아래 한 줄: `눈으로 알아챌 수 있는가`
- 본문 2행:
  - `방금 세 가지 중 하나를 넣었다.`
  - `무엇인지 맞히지 못해도 괜찮다. 그게 오늘의 출발점이다.`
- 하단 강조(아주 연한 주황 밑칠): `사람 눈으로 안 되니까 코드가 잡게 만든다.`

**삽화** 큰 원 안에 주황 물음표 하나. 다른 요소 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 21페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `이상을 재는 자는 셋입니다`
- 설명문: `무엇이 「평소와 다름」인가 — 재는 방법이 셋 있습니다.`
- 행 3개 (번호 / 굵은 제목 / 설명 한 줄):
  - `01` · `고정 임계값` · `선 하나를 긋고 넘으면 울린다. 급격한 과열은 잡지만 선 아래는 못 본다.`
  - `02` · `이동평균` · `최근 값들의 평균과 비교한다. 평소가 시간대에 따라 변해도 기준이 따라간다.`
  - `03` · `이동 윈도 z-score` · `최근 W개의 평균에서 지금 값이 표준편차의 몇 배 벗어났는지 잰다.`
- 하단 강조(아주 연한 주황 밑칠): `세 번째가 오늘 여러분이 짜는 방법이다.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 눈금이 있는 자 / 물결선과 기준선 / 시계열 선 위에 주황 사각 창.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 22페이지

**레이아웃** 8번 (가는 선 그래프형)

**화면 글자**
- 제목: `최근 W개만 보고 잽니다`
- 설명문: `전체 평균이 아니라, 창 안에 들어온 값만 씁니다.`
- 본문 3행:
  - `z는 지금 값이 최근 평균에서 표준편차 몇 배만큼 떨어져 있는지를 나타낸다.`
  - `창이 오른쪽으로 미끄러지면 기준도 같이 새로 계산된다.`
  - `z = ( x − μ ) / σ`
- 하단 강조(아주 연한 주황 밑칠): `μ와 σ는 창 안에서만 구한다. 이것이 「윈도」다.`

**삽화** 긴 남색 시계열 선. 그 위에 **주황 사각 테두리(창)가 오른쪽 일부를 덮고** 있고 `최근 W개` 라벨. 창 오른쪽에 진행 화살표.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 23페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `정해야 하는 숫자는 둘입니다`
- 설명문: `둘 다 오늘 실습에서 직접 바꿔 봅니다.`
- 표 머리행: `(빈칸)` / `무엇을 정하나` / `크게 하면` / `작게 하면`
  - `W · 윈도` / `몇 개를 보고 평균을 낼 것인가` / `둔해진다` / `민감해진다`
  - `k · 임계` / `표준편차 몇 배부터 이상으로 볼 것인가` / `덜 울린다` / `자주 울린다`
- 하단 강조(아주 연한 주황 밑칠): `오늘의 기본값은 W = 60 · k = 3.0이다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 24페이지

**레이아웃** 5번 (나란히 비교형)

**화면 글자**
- 제목: `틀리는 방법도 두 가지입니다`
- 설명문: `둘은 서로 반대 방향입니다.`
- 왼쪽: 분류 `아닌데 울린다` / 이름 `오탐 · false positive` / 설명 `정상인데 이상이라고 울린다. 많아지면 **사람이 알람을 꺼 버린다.**`
- 오른쪽: 분류 `맞는데 못 잡는다` / 이름 `미탐 · false negative` / 설명 `이상인데 못 잡는다. **설비 사고는 여기서 난다.**`
- 하단 강조(아주 연한 주황 밑칠): `새벽 3시의 EQ-03은 미탐이었다.`

**삽화** 왼쪽 — (삽화 설명 없음 — artdesc.py 에 등록할 것)

오른쪽 — (삽화 설명 없음 — artdesc.py 에 등록할 것)

**두 쪽 배경을 다르게 칠하지 마라.** 가운데 가는 세로선 하나로만 나눈다.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 25페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `민감하게 하면 다 잡히지 않을까`
- 설명문: `실측해 봤습니다. 임계값 k를 낮추면 벌어지는 일입니다.`
- 표 머리행: `k 값` / `아닌데 울린 횟수` / `하루로 치면`
  - `k = 3.0` / `566 회 (기본값)` / `하루 약 81 건`
  - `k = 2.0` / `6,374 회` / `하루 약 911 건 — 11배`  ← 이 행만 아주 연한 남색 #EEF3F9
- 하단 강조(아주 연한 주황 밑칠): `기본값에서도 하루 81번이다. 이 정도면 현장에서는 아무도 알람을 안 본다.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 26페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `민감함은 공짜가 아닙니다`
- 설명문: `잘 잡으려 할수록 헛울림이 늘고, 헛울림이 늘면 사람이 알람을 끕니다.`
- 큰 낱말: `맞바꿈`
- 낱말 아래 한 줄: `오탐과 미탐 · trade-off`
- 본문 2행:
  - `둘 다 줄이는 방법은 없다.`
  - `어느 쪽이 더 손해인지 보고 정하는 것뿐이다.`
- 하단 강조(아주 연한 주황 밑칠): `오늘 실습에서 이 저울을 직접 흔들어 본다.`

**삽화** 저울. 양쪽 접시가 수평. 왼쪽 접시 주황 아래 `오탐`, 오른쪽 접시 남색 아래 `미탐`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 27페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `오늘 이것만은 AI에게 맡기지 않습니다`
- 설명문: `이틀 내내 AI에게 시키는데, 왜 이것만 직접 짤까요.`
- 본문 3행:
  - `마지막에 AI가 짠 것과 나란히 놓고 **판정하는 쪽에 서 보기 위해서**다.`
  - `판정할 수 있는 사람만이 AI를 도구로 부린다.`
  - `뼈대는 준다. 채울 곳은 세 군데뿐이다.`
- 하단 강조(아주 연한 주황 밑칠): `여러분은 이미 더 어려운 걸 배웠습니다. 오늘 그걸 증명합니다.`

**삽화** 코드 편집기 창. 회색 코드 줄 사이에 **주황 줄 하나**가 비어 있고 `TODO 3` 라벨. 오른쪽에 손 모양.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 28페이지

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `이상감지 알고리즘을 직접 짭니다`
- 설명문: `1일차 / 실습 폴더를 엽니다.`
- 큰 낱말: `실습 2`
- 낱말 아래 한 줄: `50분 · 각자`
- 본문 3행:
  - `고칠 파일은 detect.py 하나다. 다른 파일은 건드리지 않는다.`
  - `막히면 python 점검.py — 어디가 왜 틀렸는지 짚어 준다. 답은 안 준다.`
  - `자세한 순서는 1일차 / 실습가이드.md 에 다 적혀 있다.`
- 하단 강조(아주 연한 주황 밑칠): `데이터는 7일치 60,480행. 이상 세 가지가 심어져 있다.`

**삽화** 코드 편집기 창. 회색 코드 줄 사이에 **주황 줄 하나**가 비어 있고 `TODO 3` 라벨. 오른쪽에 손 모양.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 29페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `채울 곳은 세 군데입니다`
- 설명문: `detect.py 안에서 TODO 라고 적힌 자리를 찾으세요.`
- 행 3개 (번호 / 굵은 제목 / 설명 한 줄):
  - `01` · `window_stats()` · `앞 W개로 평균과 표준편차를 구한다.`
  - `02` · `is_anomaly()` · `z-score 가 k 를 넘는지 판정한다.`
  - `03` · `handle_missing()` · `값이 안 들어온 자리를 어떻게 다룰지 정한다.`
- 하단 강조(아주 연한 주황 밑칠): `세 번째가 가장 답이 갈리는 자리다.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 (삽화 설명 없음 / (삽화 설명 없음 / (삽화 설명 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 30페이지

**레이아웃** 6번 (표형)

**화면 글자**
- 제목: `돌려 보는 법`
- 설명문: `고칠 때마다 다시 돌립니다. 숫자가 어떻게 움직이는지 보는 것이 목적입니다.`
- 표 머리행: `명령` / `무엇을 하나`
  - `python run.py` / `내 구현을 7일치 데이터에 돌린다`
  - `python run.py --k 2.0` / `임계값을 낮춰서 다시 돌린다`
  - `python run.py --window 30` / `윈도를 짧게 해서 다시 돌린다`
- 하단 강조(아주 연한 주황 밑칠): `한 번 돌리는 데 몇 초면 된다. 아끼지 말고 여러 번 돌리세요.`

**삽화** 없음. 표만. 세로줄을 긋지 마라. 머리행만 남색.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 31페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `50분을 이렇게 씁니다`
- 설명문: `세 토막입니다. 시계를 보고 넘어가세요.`
- 행 3개 (번호 / 굵은 제목 / 설명 한 줄):
  - `25분` · `채운다` · `TODO 세 군데. 15분이 지나면 힌트 카드 1을 엽니다.`
  - `15분` · `흔든다` · `k 와 W 를 바꿔 가며 다시 돌린다. 숫자를 적어 두세요.`
  - `10분` · `비교한다` · `같은 것을 AI 에게 시켜 만든 구현과 나란히 놓는다.`
- 하단 강조(아주 연한 주황 밑칠): `특히 결측 처리를 비교해 보세요. 여기서 갈립니다.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 (삽화 설명 없음 / (삽화 설명 없음 / (삽화 설명 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 32페이지

**레이아웃** 5번 (나란히 비교형)

**화면 글자**
- 제목: `숫자 두 개를 같이 봅니다`
- 설명문: `한쪽만 보면 잘못된 결론에 닿습니다.`
- 왼쪽: 분류 `잡은 쪽` / 이름 `이상 구간에서 몇 번 울렸나` / 설명 `심어 둔 구간에서 몇 번이나 울렸는지 센다.`
- 오른쪽: 분류 `틀린 쪽` / 이름 `정상 구간에서 몇 번 울렸나` / 설명 `아무 일 없는 구간에서 몇 번이나 울렸는지 센다.`
- 하단 강조(아주 연한 주황 밑칠): `이상 구간의 횟수가 정상 구간의 횟수와 다를 게 없다면 잡은 것이 아니다.`

**삽화** 왼쪽 — (삽화 설명 없음 — artdesc.py 에 등록할 것)

오른쪽 — (삽화 설명 없음 — artdesc.py 에 등록할 것)

**두 쪽 배경을 다르게 칠하지 마라.** 가운데 가는 세로선 하나로만 나눈다.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 33페이지

**레이아웃** 3번 (큰 그림 + 짧은 글)

**화면 글자**
- 제목: `다 채웠으면 기다리세요`
- 설명문: `손을 들지 않아도 됩니다.`
- 본문 2행:
  - `다 같이 한 번에 돌립니다.`
  - `먼저 끝난 사람은 k 를 바꿔 가며 한 번 더 돌려 두세요.`
- 하단 강조(아주 연한 주황 밑칠): `서른아홉 개 화면에서 동시에 결과가 뜹니다.`

**삽화** 같은 크기 화면 여덟 개가 격자로 놓임. 각 화면 아래 주황 막대(결과). 아래 `39개 화면`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 34페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `오늘 세 가지를 했습니다`
- 설명문: `정리하고 마무리하겠습니다.`
- 행 3개 (번호 / 굵은 제목 / 설명 한 줄):
  - `01` · `봤다` · `내 공장을 열고 여섯 대가 도는 것을 눈으로 봤다.`
  - `02` · `짰다` · `이동 윈도 z-score 이상감지를 직접 구현했다.`
  - `03` · `흔들었다` · `임계값을 바꿔 가며 무엇이 늘고 무엇이 주는지 숫자로 봤다.`
- 하단 강조(아주 연한 주황 밑칠): `오탐과 미탐은 맞바꿈 — 오늘의 한 문장이다.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 (삽화 설명 없음 / (삽화 설명 없음 / (삽화 설명 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 35페이지 ★ 넘기는 질문

**레이아웃** 2번 (구간 표지형)

**화면 글자**
- 제목: `규칙으로는 여기까지입니다`
- 설명문: `선을 하나 긋든, 자를 정교하게 만들든 — 규칙은 규칙입니다.`
- 큰 낱말: `누가 판단하나`
- 낱말 아래 한 줄: `내일의 질문`
- 본문 2행:
  - `규칙은 우리가 미리 정해 둔 것만 본다.`
  - `미리 정하지 못한 것 앞에서는 아무 말도 하지 못한다.`
- 하단 강조(아주 연한 주황 밑칠): `내일 그 답을 만듭니다.`

**삽화** 고리 그림인데 **왼쪽 위 한 구간이 점선으로 끊겨** 있음. 끊긴 자리에 주황 점과 `?`. 라벨 `인지` `행동` `판단`.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

# 36페이지

**레이아웃** 4번 (번호 레일형)

**화면 글자**
- 제목: `남은 20분은 노션입니다`
- 설명문: `오늘 겪은 것을 카드로 남깁니다.`
- 행 3개 (번호 / 굵은 제목 / 설명 한 줄):
  - `01` · `고른다` · `오늘 안 풀린 것 두세 개. 그게 카드 제목이 된다.`
  - `02` · `쓴다` · `화면을 덮고 내 문장으로 쓴다. 안 써지면 그게 모르는 것이다.`
  - `03` · `붙인다` · `오늘 화면을 캡처해 붙이고 이해도 태그를 찍는다.`
- 하단 강조(아주 연한 주황 밑칠): `숙제는 카드 두 장. 내일 링크 제출이 수료입니다.`

**삽화** 각 행 왼쪽에 작은 아이콘 하나씩(76×76, 선만, 남색). 아이콘은 (삽화 설명 없음 / (삽화 설명 없음 / (삽화 설명 없음.

**금지**
- 같은 문장을 두 번 쓰지 마라.
- 배경을 갈라 칠하지 마라. 순백 하나다.

---

▲ 복붙 끝 ▲
