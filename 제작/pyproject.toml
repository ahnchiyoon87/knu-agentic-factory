[project]
name = "knu-tooling"
version = "1.0.0"
description = "특강 제작·검증 도구 (덱빌드 · 배포본 · 리허설 · 산출물)"
requires-python = ">=3.12,<3.13"
dependencies = [
    # 리허설이 학생 코드를 그대로 불러 쓴다 — 학생과 같은 패키지가 있어야 한다
    "mcp>=2.0,<3",
    "httpx>=0.27",
    "playwright>=1.45",
    "python-pptx>=1.0",
    "Pillow>=10.0",
    "python-docx>=1.1",
    "pandas>=2.2,<3",
    "numpy>=1.26",
]
