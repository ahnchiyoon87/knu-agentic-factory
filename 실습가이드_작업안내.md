# 실습 가이드를 고치려면 — 이 문서 하나면 된다

경남대 RISE 피지컬AI 사관학교 8월 특강 · **2일차·3일차** 실습 가이드 작업 안내.
저장소는 `D:\work\study\경남대특강`.

> **이 문서의 쓰임** — **다른 세션·다른 도구에 이 파일 하나만 넘겨도 되게** 만든 발췌본이다.
> 가이드 작업에 필요한 것만 뽑아 한 장에 담았다.
>
> **규칙의 원본은 `핸드오프.md` 다.** 둘이 어긋나면 `핸드오프.md` 가 맞다.
> **규칙을 바꾸면 두 곳을 같이 고친다** — 안 그러면 다음 사람이 옛 규칙으로 작업한다.

---

## 1. 누가 읽는 문서인가

**코딩을 한 번도 안 해 본 성인 39명**이 고개를 숙이고 각자 속도로 따라가는 문서다.
강사가 앞에서 넘기는 **슬라이드와는 역할이 다르다.**

| | 무엇을 맡나 |
|---|---|
| **슬라이드** | 전원이 같은 순간 봐야 하는 것 — 왜 하는지 · 무엇을 만드는지 · 시작/정지 신호 |
| **가이드** | 각자 속도로 보는 것 — 무엇을 치는지 · 나오는 화면 · 막히면 어디로 |

**가이드에는 실제 실습만 넣는다.** 이론·시간표·당부·운영 안내는 슬라이드 몫이다.

---

## 2. 이틀이 한 이야기다

제목만 죽 읽어도 이야기가 되게 썼다. **이 흐름을 깨는 수정은 하지 않는다.**

**2일차 — 감시자를 만든다**

```
1 내 공장을 받습니다        2 내 공장을 눈으로 봅니다      3 계속 지켜볼 수 있습니까
4 그래서, 쉬지 않는 감시자를 만듭니다                       5 감시자는 아직 비어 있습니다
6 「평소」를 가르칩니다      7 이상의 선을 긋습니다         8 값이 빈 자리를 정합니다
9 7일치에 풀어놓습니다      10 숫자를 흔들어 봅니다        11 같은 것을 AI 가 짜는 걸 봅니다
```

**3일차 — 그 감시자를 AI 에게 쥐여주고, 공장을 움직인다**

```
1 어제 것이 그대로 있는지 봅니다   2 도구는 아직 비어 있습니다
3 감시자를 AI 의 도구로 내놓습니다  4 진단의 재료를 만듭니다   5 말 한마디로 시킵니다
6 공장에 닿는지 확인합니다        7 서서히 오르는 것을 잡습니다
8 원인을 대게 만듭니다           9 제안을 실제 명령으로 바꿉니다
10 고리를 닫습니다              11 닫혔는지 확인합니다
```

각 단계의 **끝 문장은 다음 단계로 손을 내민다.** 같은 말로 세 번 끝나면 실패다.

---

## 3. ★ 학생이 실제로 하는 일 — 빈칸 채우기

파일 안에 **뼈대가 이미 다 들어 있고**, 학생은 `...` 이라고 적힌 줄만 고친다.
**새로 만들 줄도, 지울 주석도 없다.**

```python
    # ── 빈칸 1 ──────────────────────────────────────────
    #   seg 의 평균. 다 더해서 개수로 나눈다.
    #   쓸 것 :  sum(seg)   len(seg)      ← 방법은 여기 다 적혀 있다
    mean = ...                            ← 이 줄만 고친다
```

| | 고치는 곳 |
|---|---|
| 2일차 `detect.py` | `빈칸 1`~`빈칸 5` — **다섯 줄** |
| 3일차 `mcp_server.py` | `빈칸 1`~`빈칸 4` |
| 3일차 `agents/*.py` | `빈칸 1`~`빈칸 5` (파일 셋에 걸쳐 이어진다) |

**표식은 「빈칸」이다.** `Ctrl`+`F` 로 「빈칸」을 치면 채울 자리만 걸린다.
`TODO` 는 쓰지 않는다 — 머리말·구역 제목에도 걸려 검색이 오염된다.

### 학생이 쓰는 명령 — 이름이 곧 하는 일

```
uv run 내번호.py       내 공장 번호를 받는다
uv run 확인.py         내가 채운 게 맞는지 확인한다
uv run 확인.py --정답 1  막히면 그 함수 자리에 정답을 넣는다
uv run 돌려보기.py      7일치 진짜 데이터에 돌린다
```

옛 이름(`점검.py`·`run.py`·`--열기`)도 계속 받지만 **문서에는 새 이름만 쓴다.**

### 길은 둘, 둘 다 정당하다

**직접 채운다 / 정답을 넣고 결과부터 본다.** 채우는 단계마다 확인 명령과 정답 명령을 둔다.
정답을 넣어도 학생이 쓰던 것은 `detect_내가짠것.py` 로 남고,
정답 파일은 **학생 뼈대에서 생성**되므로 주석·두 갈래 코드가 그대로 보인다.

---

## 4. 문체 — 소설처럼

1. **위에서 아래로 한 번만.** 「맨 뒤 답안을 보세요」 같은 역참조 금지 (그래서 참고 답안 부록을 뗐다)
2. **쉽게 말할 수 있는 것을 돌려 말하지 않는다.** 괄호에 부연을 눌러 넣지 않는다
3. **반대로 읽힐 수 있으면 실패다**
4. **번역투·훈계 금지**
5. **애매한 낱말은 처음 나오는 자리에서 한 번 정의**하고 그 말만 쓴다 (예: 「평소」 = 지금 값 바로 앞 한 시간, 값 60개)
6. **명령에는 이유를 붙인다** — 「다 같이 돌립니다 **— 결과 화면을 같이 읽기 위해서입니다**」
7. **선택지가 있는 자리는 메우지 않는다** — 고르게 하고, 왜 골랐는지 말할 수 있으면 된다고 쓴다

### ★ 실제로 틀렸던 다섯 — 쓰고 나서 이 표로 자기검열한다

| 함정 | 나쁨 | 좋음 |
|---|---|---|
| 괄호에 부연 | (재는 자리가 겉면이 아니라 베어링 안쪽이라 숫자가 이만큼 높습니다) | 온도는 겉면이 아니라 **베어링 안쪽**에서 잽니다. 그래서 숫자가 이렇게 높습니다. |
| 반대로 읽힘 | 한 기계에게 정상인 온도가 **다른 기계에게는 이상**일 수 있습니다 | **「정상」의 기준이 기계마다 다릅니다.** |
| 부정형 | 정답이 하나가 아닙니다 | **정답이 두 개입니다. 둘 중 하나를 고르세요** |
| 번역투 | **손은 온도로 연습했지만** 채점은 진동에도 태웁니다 | 감시자를 **온도와 진동, 두 데이터에** 돌립니다 |
| 훈계 | 10분을 넘기지 마세요 · 오래 붙잡지 마세요 | (통째로 삭제) |

**세 물음** — ①괄호를 열었는가 ②반대로 읽힐 수 있는가 ③지시하고 있는가(지시 대신 이유를).
**구조만 훑는 검토로는 안 잡힌다. 한 줄씩 소리 내어 읽어야 한다.**

---

## 5. 넣지 않는 것

| 무엇 | 왜 |
|---|---|
| **코드 블록** | PDF 에서 긁으면 들여쓰기가 깨진다. **뼈대는 파일 안에** 두고 가이드는 자리만 가리킨다 |
| **편집 조작 나열** | 「①찾고 ②고치고 ③또…」는 파일 주석이 이미 한다. 가이드가 반복하면 지저분해진다 |
| **「이런 오류가 나면」 목록** | 학생은 종이를 안 뒤진다. **화면이 제대로 말하게** 하고 나머지는 강사가 말로 받는다 |
| **시간표·당부·운영 안내** | 슬라이드 몫 |
| **임계값 같은 내부 규칙** | 학생이 알 필요가 없다 |
| **`돌려보기.py` 결과 화면 전체** | 무엇이 안 잡혔는지가 그대로 적혀 있다 — 학생이 직접 발견해야 한다 |
| **아직 안 찍은 그림의 `![]()`** | 빌드가 멈춘다. 자리는 `<!-- 사진 … -->` 주석으로 |

---

## 6. 화면 사진은 전부 자동 생성이다

**사람이 찍을 것은 없다.** 파일이 바뀌면 그림도 다시 뽑는다.

```bash
cd 제작/빌드도구
uv run --with playwright 터미널그림.py 원고.txt 나갈이름.png   # 터미널 화면
uv run --with playwright 편집기그림.py 전부                    # 편집기 화면 (실제 파일을 읽어 그린다)
```

**원고에 넣는 출력은 반드시 실제로 돌려서 얻은 것만 쓴다.** 지어낸 화면은 넣지 않는다.

---

## 7. 디자인 — `md2pdf.py` 가 알아서 한다

마크다운을 어떻게 쓰느냐로 결정된다.

| 쓰는 법 | 나오는 모양 |
|---|---|
| ` ``` ` 안에 `uv run …` | **검은 터미널 상자** + 「터미널에 칩니다」 (프롬프트 `>` 없음 — 긁으면 그대로 붙어야 하니까) |
| ` ``` ` 안에 그 밖의 것 | 「화면에 이렇게 나옵니다」 상자 |
| ` ```수식 ` · ` ```주소 ` · ` ```json ` · ` ```모양 ` · ` ```지시문 ` | 각자 다른 라벨 |
| `## 1 · 제목` | 큰 단계 — 굵은 윗줄로 확실히 끊긴다 |
| `**① 소제목**` | 작은 단계 — 주황 막대 |
| `> 인용` | 노란 강조 상자 |

**상자를 연달아 쌓지 않는다.** 한 화면에 강조 상자는 하나면 충분하다.

---

## 8. 고쳤으면 여기까지 돌린다

```bash
# ① PDF
uv run --with playwright python 제작/빌드도구/md2pdf.py \
    강의/실습가이드_2일차.md 실습가이드_2일차.pdf "2일차 실습 가이드"

# ② 학생 배포본 — 학생 조건(cp949)으로 실제 실행까지 확인한다
cd 제작/검증도구 && uv run 배포본만들기.py --검증

# ③ 드라이브에 올릴 zip
cd ../빌드도구 && uv run --with playwright 드라이브폴더만들기.py
```

**★ 그다음 PDF 를 눈으로 본다.** 바꾼 절과 그 앞뒤는 반드시.

**같이 고쳐야 하는 것** — 가이드 문구를 바꾸면 **그 문구를 기다리는 검증 기대값**도 바뀐다
(`제작/검증도구/배포본만들기.py` · `리허설.py`). 절 번호를 바꾸면 `진행.md` 의 참조도 같이.

---

## 9. 더 알고 싶으면

| 무엇 | 어디 |
|---|---|
| 수업 전체 규칙·리소스·운영 | `핸드오프.md` (뿌리) |
| 슬라이드를 고치려면 | `슬라이드_작업안내.md` |
| 이 장에서 강사가 무엇을 하나 | `강의/2일차/진행.md` · `강의/3일차/진행.md` |
| 수치의 1차 출처 | `검증리서치/04_딥리서치_결과.md` |
