# 수업 진행 — 강의하는 사람이 읽는 문서

> **이 문서 하나로 4일 강의를 진행할 수 있습니다.** 각 날에 학생이 무엇을 겪고,
> 내가 언제 무엇을 누르고, 그 날의 '와' 순간이 어디인지. 수치는 전부 실측입니다. (20분)
>
> · 전체 그림·산출물 위치가 궁금하면 → [README.md](README.md)
> · 서버 띄우기·키 배포·문제 해결 → [서버운영.md](서버운영.md)
> · 학생에게 나가는 실습 안내서 → `산출물/N일차_*/가이드.md` (그날 것만 보면 됩니다)
> · 교안 정본 → `작업장/참고자료/경남대_문서모음/…쉬운풀이본.html`

---

## 수업 전날 밤 · 당일 아침 (5분 체크)

| 시점 | 할 일 |
|---|---|
| 전날 | 오늘 쓸 카드 인쇄 확인 (`산출물/W8_카드/카드_인쇄용.html`) |
| 당일 아침 | ① 서버 기동 → ② **키 배포표를 그날 IP로 재인쇄**해 자리 배치 → ③ 학생 1명 화면으로 접속 확인 |
| 4일차만 | 실습 시작 전 강사 콘솔의 **「Day 4 시작」** 버튼 (제어 개방 + 팀 키) |

서버 기동·키 배포 명령은 전부 [서버운영.md](서버운영.md) 에 있습니다.

---

## 한 문장으로

가상 CNC 공장(K-정밀)을 무대로, 학생이 4일 동안 **공장을 보고(1) → 설계하고(2) → 이상을 알아내고(3) → AI가 직접 움직이게(4)** 만든다. 마지막 날, 학생 팀의 에이전트가 이상을 감지해 설비를 감속시키고 사람 승인을 받아 로봇을 파견하면 — 4일이 하나의 폐루프로 닫힌다.

**관통하는 서사** — "새벽 3시, EQ-03 온도가 시간당 0.5℃씩 오른다. 80℃ 경보선은 끝까지 안 넘는다. 사람은 이걸 못 본다." Day 1에 이야기로 심고, Day 3에 데이터로 겪게 하고, Day 4에 AI가 잡아낸다. **그래서 드리프트 기울기(0.5℃/h·62→64℃)는 절대 건드리면 안 되는 값**이다 — 시연을 빠르게 만들고 싶으면 기울기가 아니라 배속(이미 기본 ×60)을 쓴다.

| | Day 1 보기 | Day 2 시키기 | Day 3 알기 | Day 4 움직이기 |
|---|---|---|---|---|
| 학생이 만드는 것 | 관제 대시보드 | 스펙 → 백엔드 | 이상감지 + 진단 에이전트 | 폐루프 (팀) |
| 그 날의 '와' | 말로 만든 대시보드가 실제로 돎 | 어제 무너진 게 스펙으론 안 무너짐 | 내 알고리즘을 AI가 도구로 씀 | AI가 공장을 실제로 움직임 |
| 쓰는 산출물 | W1 | W1 + (W4 스펙) | W5 · Lab3-2 · Lab3-3 | W6 + 강사콘솔 |

---

## Day 1 — 보기 (대시보드)

**흐름** — OT 15분 → 강의1 55분(피지컬AI 폐루프 · 에이전틱 패턴 5종 · 무대 소개) → 셋업랩 30분(VS Code+Claude, 저장소, 파이썬 브릿지 1장) → **Lab 1-1 110분** → 랩업 20분(데모 2~3명 + 투표).

**Lab 1-1 의 4단계가 이 날의 설계다:**
1. **작은 성공 (40분)** — 준비된 프롬프트 시드로 시뮬레이터 API를 읽는 대시보드가 만들어진다. 브라우저에 자기 공장이 움직인다.
2. **일부러 겪는 불편 (40분)** — 요구사항 카드 5장을 순서대로. **3~4번째 카드쯤에서 잘 되던 게 깨진다 — 이게 노린 것**(컨텍스트 붕괴). Day 2를 듣고 싶게 만드는 장치.
3. **내일 예고 (20분)** — 강사가 같은 요구를 스펙 문서로 한 번에 지시하는 걸 시연. 안 무너진다. "차이가 뭘까요?"만 던지고 답은 내일로.
4. **자산화 (10분)** — 커밋 + 프롬프트 기록 저장.

**내가 하는 것**
- 수업 전: 서버 기동 + 키 쪽지 배치 → [서버운영.md](서버운영.md) (명령 2줄이면 끝)
- 학생은 쪽지의 주소를 크롬에 치기만 하면 된다. 설치할 것 없음. 폴링은 **2초 고정**(시드에 박혀 있음)
- 원하면 콘솔에서 센서 결측을 잠깐 주입해 "null 이 와도 화면이 안 죽는다"를 보여줄 수 있다
- 강사 노트: Step 2에서 안 무너진 학생이 있으면 "어떻게 지시했는지" 발표시킬 것 → 스펙 필요성으로 자연 연결

## Day 2 — 시키기 (스펙 주도 개발)

**흐름** — 어제 회수 10분(무너진 이유: 컨텍스트·암묵 요구·회귀) → 강의2 40분(Intent→PRD→Spec(EARS)→GWT 사다리) → **Lab 2-1** 빈칸 채우기 30분 → **Lab 2-2** Supabase 온보딩 30분 → **Lab 2-3** 스펙→구현→검증 90분 → 랩업 30분(어제 vs 오늘 대조 시연).

- Lab 2-1: 일부러 미완성인 스펙(W4 — 빈칸 6 + **오류 3**, 예: "온도 80℃ 초과 시 알람 *해제*"). 정답은 안 알려준다 — **틀리게 채우면 Lab 2-3에서 구현이 실제로 틀리게 나와** 스스로 드러난다.
- Lab 2-3 Step 1에서 시뮬레이터 값을 자기 Supabase `sensor_readings` 에 쌓기 시작한다 — **내일 쓸 재료를 오늘 모은다.** Step 3에서 어제 무너뜨렸던 "알람 담당자 배정" 카드를 스펙 수정으로 처리 → 안 무너짐 = 이 날의 '와'.

**내가 하는 것** — 시뮬레이터는 켜져 있기만 하면 됨. Step 2 검증 때 콘솔에서 온도 이벤트(스파이크 프리셋)를 쏴 주면 학생 시스템에 알람이 뜬다. 주의: Supabase 같은 IP 대량 가입 제한 — 사전 테스트 필수(교안 강사 노트).

## Day 3 — 알기 (이상감지 → MCP 도구)

**흐름** — 강의3 55분(레거시 데이터 현실 · RAG/Text2SQL/GraphRAG 비교 · MCP="에이전트의 손" · 오탐↔미탐 트레이드오프) → **Lab 3-1** 적재 30분 → **Lab 3-2** 이상감지 구현 50분 → **Lab 3-3** MCP 도구화 70분 → 랩업 25분.

- **Lab 3-1** — W5 CSV(7일치 60,480행, 이상 3종 내장)를 자기 Supabase에 적재. 어제부터 쌓인 실시간 스트림과 컬럼이 같아 "과거+현재"가 한 테이블에 합쳐진다.
- **Lab 3-2** — 유일하게 **AI에게 통째로 맡기지 않는** 랩. TODO 3곳(윈도 통계·임계 판정·결측 처리)을 직접 짠다. 실측 결과: 스파이크 3/3 잡힘 · **드리프트는 끝내 안 잡힘**(k를 2.0으로 낮춰도 여전히 노이즈 수준인데 **오탐만 11배** — 하루 81건→911건) · 결측은 참고 구현도 0/121(그래서 TODO 3은 정답이 여럿). 만족도가 가장 갈리는 랩 — **15분 지나면 힌트 카드 1번을 전체 공개**(교안 강사 노트).
- **Lab 3-3** — 그 함수를 MCP 도구 2개(`detect_anomaly`, `query_equipment`)로 포장. 에이전트에게 말로 지시하면 도구를 줄줄이 불러 **진단 리포트**를 뽑는다 = 이 날의 '와'. 단 아직 '제안'만 — "움직이는 건 내일입니다."
- 막히는 팀: `config.json` 한 줄로 강사 우회 경로(`fallback`/`http`) 전환 — 학생 코드 수정 없음.

**내가 하는 것** — Lab 3-2 시작 전 힌트 카드 3장 준비 확인. MCP는 로컬 stdio 기준이라 방화벽 이슈 시 공용 서버 우회. 정비 이력이 없는 팀(Day 2 미완)은 `data_source: fallback`.

## Day 4 — 움직이기 (폐루프 · 팀 챌린지)

**흐름** — 강의4+라이브 시연 50분 → 챌린지 브리핑·팀 편성 10분 → **Lab 4-1 팀 챌린지 120분**(기본 미션 60분 목표 + 확장 미션) → 데모 발표 50분(팀당 5분 × 8팀) → 시상·로드맵 10분.

- 강의4: 어제 에이전트 부검(역할 과부하) → 감지/진단/조치 + 오케스트레이터로 분해(Day 1 Orchestrator-Worker 회수) → **제어 MCP 도구 4개 공개** → HITL(감속은 자동, **정지·파견은 사람 승인**) → 라이브 시연.
- 기본 미션: W6 템플릿에서 ★ 3곳(judge/build_prompt/to_commands)만 채우면 폐루프가 돈다. 못 끝낸 팀엔 **「동작 보장 최소 경로」 카드**(W6 폴더에 있음) — 전원 데모 성공이 최우선, 확장은 가점.
- 확장 미션(택1): A 예방정비 스케줄러 / B AMR 배차(Routing) / C 안전 인터록(Parallelization) / D 교대 리포트 / 자유. 등록 한 줄로 붙는다 — `W6/확장미션/README.md`.
- 평가: 폐루프 동작 40 / 스펙 문서화 30(→ `확장미션/스펙_템플릿.md`) / 창의성 30 + 즉석 투표.

> ⚠️ **클라우드 서버로 수업한다면** 콘솔 버튼 대신 명령을 쓸 때 `--base http://34.64.94.16:8000` 을
> 반드시 붙이세요. 안 붙이면 로컬에 대고 실행돼 제어가 안 열립니다 → [서버운영.md](서버운영.md) 4번

**내가 하는 것 — 버튼 세 개가 전부다 (강사 콘솔 맨 위)**
1. 실습 시작 전: **「Day 4 시작」** — 제어 개방 + 배속 ×120 + 팀별 키 표 출력. 키를 팀에 나눠 준다.
2. 발표 운영: **직전 팀이 발표하는 동안** 다음 팀 공장에 **「시연 주입」**. 주입→감지가 **79~190초**라, 발표석에서 켜면 시연 3분이 대기로 날아간다. **팀은 `loop.py` 를 켜 둔 채 발표석에 온다** — 이 순서가 발표 안내에 들어가야 한다.
3. 끝나면: **「Day 4 종료」** — 원상복구.
- 강사 라이브 시연이 필요하면: `W6` 폴더에서 `python loop.py --use-answers` (참고 답안으로 완성된 폐루프가 즉시 돈다).
- 리허설 때 여러 팀 동시 제어 테스트 필수(교안 강사 노트) — `python 산출물/0_공통/리허설_4일치.py` 가 그 검사를 포함한다.

---

## 자주 틀리는 것 (실측으로 확인된 함정)

| 함정 | 사실 |
|---|---|
| "드리프트가 빨리 오르게 기울기를 키우자" | **금지.** Day 1~3 서사("미묘해서 못 본다")가 무너진다. 빠르게는 배속으로 — 이미 기본 ×60 |
| 진단 AI가 감속만 하고 로봇 파견을 안 함 | 실측으로 잡은 함정. 지시문에 **"원인이 정비 보류·지연이면 감속과 함께 파견 요청까지 낼 것"** 을 넣어야 승인 장면이 나온다. 학생 가이드에 반영돼 있음 |
| "배속을 올리면 데이터가 이상해지지 않나" | 안 이상해진다. 샘플당 상승폭이 W5 CSV와 동일(실측 0.008353 vs 0.008333℃) — CCTV 빨리감기와 같다 |
| 주입했는데 금방 끝남 | 주입 지속시간은 **공장 시간** 기준 — 배속 ×60이면 60배 빨리 끝난다. 주입 응답이 실제 시간을 알려준다 |
| Day 4 시연이 조용함 | 감지까지 79~190초가 정상. 미리 주입해 두는 운영 순서로 해결 |
| 학생 진단 AI가 "별것 아니니 지켜보자"며 조치를 안 냄 | 알려진 함정 — 드리프트는 "지금 작아도 계속 오른다"를 지시문에 넣어야 한다. 학생 힌트에 적어 둠 |
