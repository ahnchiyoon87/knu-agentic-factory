# 경남대 8월 특강 — 이 문서 하나로 이어받는다

경남대 RISE 피지컬AI 사관학교 · **이틀 × 4시간 · 39명** · 전체 4일 과정 중 **2일차·3일차**.
저장소 전체가 이 번호로 통일돼 있다. 저장소는 `D:\work\study\경남대특강`.

> ## ★ 처음 이어받았다면 — 이 순서로 읽는다
>
> 1. **5-1절 「어디까지 왔나」** — 지금 상태. 자료·검증·서버가 어디까지 닫혔는지.
> 2. **1부 규칙** — 어기면 되돌릴 수 없는 것들. 특히 **2-1절(슬라이드와 가이드의 분업)**
>    과 **4절 「실제로 틀린 다섯」**.
> 3. **2부 「다시 밟지 말 것」** — 전부 실제로 당한 것이다. 같은 자리에 다시 빠지지 않는다.
> 4. 나머지는 필요할 때 찾아 읽는다.
>
> **이 문서에 없는 것을 추측으로 채우지 않는다.** 근거 없이 지어낸 수치로 실제 두 번 틀렸다
> (「베어링 0.5℃/h」·「실제 공장은 설비 수백 대」). 모르면 **재 보거나 문서를 열어 본다.**

이 문서는 세 가지를 담는다 — **지켜야 할 규칙** · **저장소의 모든 리소스가 무엇인가** · **어떻게 돌리는가**.
날짜별 작업 일지는 남기지 않는다. 규칙에는 **왜 그것이 생겼는지**를 같이 적었다.
이유를 모르는 규칙은 지켜지지 않고, 대부분 실제로 한 번 당하고 나서 생긴 것들이다.

---

# 1부 · 규칙 — 어기면 되돌릴 수 없다

## 1. 수업의 절대 규칙

| # | 규칙 | 왜 |
|---|---|---|
| 1 | **개인 단위. 팀·PBL·발표·시상이 없다** | 39명 중 2명만 앞에 세우면 나머지는 구경꾼이 된다. 대신 **「다 같이 엔터」** 로 같은 순간을 만든다 |
| 2 | **39명 전원이 같은 장면을 본다** | 창의성으로 갈리는 구간을 만들지 않는다. 결정적 문장은 가이드에 원문으로 준다 |
| 3 | **이탈을 절대 막는다** | 2일차를 못 끝내도 3일차를 시작할 수 있어야 한다 (`--정답`) |
| 4 | **반전은 학생이 발견한다** | 무엇이 잡히고 무엇이 안 잡히는지 **강의자료에 한 글자도 쓰지 않는다** |
| 5 | **조용히 실패하지 않는다** | 진단이 안 되면 학생이 **알 수 있게** 한다. 화면의 안내가 거짓말이 되면 안 된다 |
| 6 | **AI 키는 하루짜리다** | 전용 프로젝트에 **예산 상한**을 걸고, 키는 3일차 아침에 만들어 단톡방으로 주고, **수업이 끝나면 그 자리에서 삭제**한다. 유출돼도 피해가 상한을 못 넘고, 삭제하면 그 순간 죽는다 |
| 7 | **대본이 없다** | 슬라이드에 적힌 문장을 그대로 읽어도 뜻이 통해야 한다 |
| 8 | **무대는 K-정밀 하나** | 창원 가상 정밀기계 제조사. 다른 예를 끌어오지 않는다 |
| 9 | **장난감이 아니다** | z-score 를 라이브러리 없이 직접 짠다. 표본표준편차 · σ=0 · 결측 처리까지 |
| 10 | **`.env` 를 커밋하지 않는다** | 커밋 전 `git status` 를 **눈으로** 본다. 키가 실제로 스테이징된 적이 있다 |

**비밀정보는 이제 하나다** — 시뮬레이터 `.env` 의 **OpenAI 키.**
공장이 학생 PC 에서 돌면서 DB 접속줄·배정장부·접속 키가 전부 없어졌다 —
compose 파일의 DB 줄은 각자 PC 안의 로컬 값이라 비밀이 아니다.
커밋 메시지는 **파일로 만들어 `-F` 로 넘긴다.** 명령줄에 직접 쓰면 줄바꿈과 따옴표가 깨진다.

## 2. 학생 문서에 넣지 않는 것

**실습 가이드에는 실제 실습만 있어야 한다.** 이건 가장 자주 어긴 규칙이고, 어길 때마다 학생이 막혔다.

| 넣지 않는 것 | 왜 |
|---|---|
| **시간표 · 당부 · 운영 안내 · 수료 조건** | 학생이 자기 얘기가 아닌 글을 읽게 된다. 그 20분은 슬라이드 한 장으로 끝난다 |
| **시스템 내부 규칙** (임계값 80℃·8.0mm/s·30초 같은 것) | 학생이 알 필요가 없다. 필요한 건 「색은 안 바뀝니다」 한 줄이다 |
| **슬라이드가 이미 하는 논증** | 가이드를 읽는 시점은 **아직 이론을 안 들은 때**다. 압축된 논증은 이해가 아니라 「엥?」을 만든다 |
| **짝활동 · 옆 사람과 말하기** | 개인전이다. 규칙 1 |
| **강사만 아는 말** (주입 · 리모컨 · 콘솔 · 배정장부) | 학생이 겪는 일로 쓴다. 「주입」이 아니라 「시작」 |
| **`돌려보기.py` 결과 화면** | 2일차 결과에는 **무엇이 안 잡혔는지가 그대로 적혀 있다.** 엔터를 치기 전에 답을 보게 된다 (규칙 4). 머리줄·행수·`[O ] [~ ] [  ]` 읽는 법까지만 |
| **`.md` 파일 자체** | 학생 실습 폴더에는 **코드와 데이터만.** 문서는 PDF 로 따로 간다 |
| **`<details>` 같은 HTML 태그** | `md2pdf` 가 `<` 를 이스케이프해 **학생 PDF 에 태그 글자가 그대로 찍혔다** |
| **아직 안 찍은 화면의 `![]()`** | `md2pdf.py` 는 가리키는 그림이 없으면 **빌드를 멈춘다**(의도된 안전장치). 그날부터 PDF 도 드라이브 ZIP 도 안 나온다. 자리는 **`<!-- 사진 … -->` 주석**으로 심는다 |

## 2-1. 슬라이드와 가이드의 분업 — 개념은 슬라이드, 절차는 가이드

「이론 자료」와 「실습 자료」가 따로 있는 것이 아니다. **덱은 이론 장과 실습 진행 장이
섞인 한 벌**이고, 그것이 원래 구조다. 노션 1교시는 아예 28장이 실제 화면 캡처다.
비전공 성인 39명에게 슬라이드가 실습을 모른 척하면 **전환 순간마다 제각각 흩어진다.**

나누는 기준은 하나면 된다.

| | 무엇 | 왜 |
|---|---|---|
| **슬라이드** | **전원이 같은 순간 봐야 하는 것** — 왜 하는지 · 무엇을 만드는지 · 시작·정지 신호 · 시간 배분 · 막혔을 때 규칙 · 다 같이 볼 결과 | 강사가 말로 진행하는 리듬. 여기 없으면 39명이 흩어진다 |
| **가이드** | **각자 속도로 보는 것** — 무엇을 치는지 · 나오는 화면 · 에러 나면 어디를 보는지 | 고개 숙이고 따라가는 문서 |

**두 방향 모두 지킨다.**
- 절차를 슬라이드로 끌어오지 않는다 — 「자세한 순서는 실습 가이드에 다 있다」로 위임한다
- **개념을 가이드로 위임하지 않는다** — 가이드에만 있으면 39명이 각자 읽다 이해가 갈리고
  절반은 안 읽고 베낀다. `judge()` 가 어제 z-score 와 무엇이 다른지가 그 예다

**같은 사실은 두 자료가 같게 말해야 한다.** 별개 문서인 것과 어긋나는 것은 다른 문제다.

### 왜 「먼저 다 보여주고 똑같이 해 본다」인가

이 구조에는 이름이 있다 — **시연 후 재현(worked example → practice)**. 코딩 무경험
성인에게 백지 탐색을 시키면 인지 과부하로 무너지고, 먼저 본 것을 따라 하게 한 뒤
빈칸을 점점 늘리는(faded) 쪽이 시간은 덜 들고 학습은 같다는 것이 딥리서치로 확인됐다.

덱이 실제로 그렇게 짜여 있다 — 3일차 12장에서 **세 바퀴**를 미리 보여주고 오전 실습에서
그것이 자기 터미널에 찍히고, 39장에서 **일곱 걸음**을 미리 보여주고 오후에 그것이 실제로 돈다.
「아까 슬라이드에서 본 그 장면이 내 화면에 나온다」가 이 방식의 보상이다.

**조건 셋을 지킨다.**

1. **이론 블록이 길어지면 앞을 잊는다.** 2일차 오전 개념 구간이 제일 긴 앉아있기다.
   처지면 잘라 넘어갈 장을 미리 정해 둔다.
2. **「똑같이」가 100% 면 안 된다.** `빈칸 5` 의 방침 고르기, `judge()` 의 σ=0 처리 같은
   작은 선택지가 베끼기와 배우기를 가른다 — **이 선택지들은 절대 메우지 않는다.**
3. **의도된 예외 하나** — 2일차 「못 찾기」 장면만은 답을 미리 안 보여준다(productive failure).
   즉 규칙은 「다 보여주고 따라 하게 한다, 단 **왜 이게 필요한지 몸으로 느껴야 하는 한 장면만**
   일부러 안 보여준다」이다.

## 3. 자료를 만들 때

| # | 규칙 | 왜 |
|---|---|---|
| 11 | **화면은 실제 캡처만 쓴다. 그려서 흉내 내지 않는다** | 흉내 낸 화면은 실제와 조금씩 달라 「내 화면엔 없는데」로 끝난다. 실제로 두 번 갈아엎었다 |
| 12 | **화면과 설명이 100% 일치해야 한다** | 화면에 없는 것을 설명하지 않는다. 완성본에 있는 것은 만드는 단계에도 반드시 있어야 한다 |
| 13 | **강조 상자는 대상만 정확히 담는다** | 크거나 애매하면 무엇을 가리키는지 사라진다. 좌표는 **재서** 넣는다 — 눈대중 금지 |
| 14 | **번호는 원 정가운데. 왼쪽 설명과 화면 뱃지가 1:1** | 없는 번호가 있으면 학생이 찾다 흐름이 끊긴다 |
| 15 | **오타만 보는 게 아니다** | 여백 · 간격 · 정렬 · 비율 · 잘림 · 가림 · 겹침 · 글자 크기까지 전부 본다 |
| 16 | **강의장 뒷자리에서 읽히는가** | 화면 속 글자가 슬라이드에서 14px 아래로 그려지면 안 읽힌다. 볼 곳만 잘라 키운다 |
| 17 | **장수를 아끼지 않는다** | 한 장에 조작 서넛을 욱여넣지 않는다. 한 장에 하나씩이면 학생이 따라온다 |
| 18 | **문서에는 그 제목의 내용만** | 준비 안내에 운영 원고·FAQ·체크리스트를 섞지 않는다 |
| 19 | **흐름은 한 방향** | 앞에서 한 이야기를 뒤에서 되풀이하지 않는다. 세팅과 내용 채우기를 왔다 갔다 하지 않는다 |
| 20 | **행위를 명시한다** | 「제목 입력」이 아니라 「**칩니다**」·「**누릅니다**」·「**고릅니다**」·「**끌어다 놓습니다**」 |
| 21 | **막히면 우회하지 말고 멈춘다** | 자동화가 안 되면 다른 화면을 찍어 넣지 않는다. 그대로 보고하고 지시를 받는다 |
| 22 | **소스만 읽고 「전수조사했다」고 말하지 않는다** | `build_*.py` 를 다 읽어도 도해는 `V.FOLLOW` 처럼 **이름만** 보인다. 그림이 제목과 딴소리를 해도 안 보인다. **화면을 안 봤으면 전수조사가 아니다** |
| 22-1 | **도해를 붙일 때 「이 그림이 이 제목을 그리는가」를 묻는다** | 이름이 그럴듯해서 갖다 쓰면 **다음 장의 내용을 미리 그리거나**(반전 유출) 앞 장과 똑같은 그림이 연속으로 나온다. 실제로 여덟 장에서 그랬다 |
| 22-2 | **한국어는 어절 단위로 끊는다** | `word-break: keep-all` 이 없으면 브라우저가 글자 아무 데서나 줄을 바꿔 「하루치 생 / 산분을」처럼 낱말이 쪼개진다. 슬라이드(`_기준판.css`)와 가이드(`md2pdf.py`) 양쪽에 걸려 있다 |
| 22-3 | **자간을 라틴 기준으로 벌리지 않는다** | `letter-spacing:.34em` 은 라틴 대문자엔 멋있지만 한글은 「2 일 차」로 낱글자가 흩어진다 |
| 22-4 | **반 칸(cmp)에 들어가는 삽화는 라벨을 키운다** | 실제로 절반 크기로 그려져 공용 라벨 크기로는 슬라이드에서 12px 언저리가 된다 — 뒷자리에서 안 읽힌다 |
| 23 | **절대 온도를 「정상값」처럼 쓰지 않는다** | 정상 온도는 회전수의 함수라 **56℃(EQ-04)~74℃(EQ-05)** 로 제각각이다. 「정상은 62℃」라고 쓰면 EQ-04 를 받은 학생 화면과 어긋난다. 견주는 대상은 언제나 **그 기계 자신** |
| 24 | **「설비가 많아서 못 본다」로 설명하지 않는다** | 미국 잡숍 조사에서 **업체당 CNC 중앙값이 16대**다 — 우리 6대와 같은 급이고, 숫자 24개는 사람이 볼 수 있는 양이다. 학생이 바로 반박한다. 근거는 **개수가 아니라 「기억해야 보인다」**와 완만·미약 신호의 탐지율 저하(Mackworth 1948)다. **「실제 공장은 수백 대라서」는 출처가 없다 — 쓰지 않는다** |

## 4. 문체 — 강사가 직접 고친 결과에서 뽑았다

원본과 강사 수정본을 한 장씩 대조해 얻은 것이다. **덱을 새로 쓰거나 고칠 때 이 절을 먼저 읽는다.**

### 지우는 것

| 무엇 | 왜 |
|---|---|
| **대구로 맞춘 세 줄** | 같은 길이·같은 어미로 떨어지면 광고 문구로 읽힌다. 한두 덩어리로 붙이고 길이를 제각각 둔다 |
| **아직 안 배운 개념·용어** | 뒤에서 만들 것을 앞 장에서 아는 것처럼 쓰면 못 따라온다 |
| **앞 장을 참조하는 말** | 「앞에서 ~했으면 이제 ~」 금지. 그 장 일은 **그 장 안에서** 다 말한다 |
| **따로 세운 비유** | 비유를 독립 문장으로 세우면 설명조가 된다. **문장 안에 녹인다** |
| **추상적 대비** | 「A와 B는 다르다」로 끝내지 않는다. **판정 기준**을 준다 |
| **본문 속 예시 나열** | 그림이 할 일을 글로 또 하지 않는다 |
| **장식용 큰 낱말·작은 글씨** | 제목과 같은 말을 크게 한 번 더 쓰는 칸 |
| **각주의 부품 목록** | 「A 하나 · B 하나 · C 세 개」 → 학생 것으로 말한다 |
| **각주의 주황 밑칠** | 마무리 한 줄에는 강조를 넣지 않는다. 굵게까지만 |
| **결과 화면 장의 본문 글** | 「이렇게 됩니다」류는 **그림과 각주만** |

### 바꾸는 것

| 자리 | 규칙 |
|---|---|
| **제목** | 기능 이름이 아니라 **지금 하는 일**. 한 장에 두 가지를 넣지 않는다. 하지 말 것은 직설로 |
| **부제** | **왜 하는지** 한 줄. 조작(누른다·친다·고른다)은 **절대 넣지 않는다** — 전부 왼쪽 번호로. 화면을 읊지 말고 안 하면 뭐가 곤란한지를 쓴다 |
| **본문** | 문단을 잘게 쪼개지 않고 덩어리로 붙인 뒤 마지막에 **「즉, ~」** 로 결론 |
| **어미** | 「~입니다」로만 떨어지면 기계 같다. 반문·단정을 섞는다 |
| **주어** | 밝힌다 — 「보통은」이 아니라 「대부분의 사람들은」 |
| **관점** | 무슨 일이 일어나는지가 아니라 **학생이 얻는 것**으로 |
| **문턱** | 「틀려도 된다」 「~보다 백배 낫다」 「걱정하실 필요 없습니다」 — 지시보다 **안심**이 먼저 |
| **넣을 값** | 설명에 섞지 않는다. 한 줄이면 회색 부연 「예 : ○○○」, 여러 줄이면 회색 블록 |
| **왼쪽 번호** | 굵은 줄에 조작 하나 + 회색 부연 한 줄. 화면에 안 찍힌 조작도 순서에서 빼지 않는다 — 번호만 두고 **주황 상자는 안 붙인다** |

### ★ 이번에 실제로 틀린 다섯 — 문장을 쓰고 나서 이 표로 자기검열한다

규칙만 있고 실례가 없어서 **같은 자리에 세 번 빠졌다.** 고친 문장을 그대로 남긴다.

| 함정 | 나쁨 (실제로 썼던 것) | 좋음 (고친 것) |
|---|---|---|
| **괄호에 부연을 눌러 넣기** | (재는 자리가 기계 겉면이 아니라 회전축을 받치는 **베어링 안쪽**이라 숫자가 이만큼 높습니다) | 온도는 기계 겉면이 아니라 **회전축을 받치는 베어링 안쪽**에서 잽니다. 그래서 숫자가 이렇게 높습니다. |
| **반대로 읽히는 문장** | 한 기계에게 정상인 온도가, **다른 기계에게는 이상**일 수 있습니다 | **「정상」의 기준이 기계마다 다릅니다.** 그래서 여섯 대에 같은 선 하나를 그을 수 없습니다. |
| **부정형으로 꼬기** | **정답이 하나가 아닙니다.** 고르고, 왜 그랬는지 말할 수 있으면 됩니다 | **정답이 두 개입니다. 둘 중 하나를 고르세요** — 왜 골랐는지만 말할 수 있으면 됩니다 |
| **번역투** | **손은 온도로 연습했지만**, 채점은 같은 함수를 진동에도 태웁니다 | `돌려보기.py` 는 여러분이 만든 감시자를 **온도와 진동, 두 데이터에** 돌립니다 |
| **훈계** | **10분을 넘기지 마세요** · 부끄러운 일이 아닙니다 · 오래 붙잡지 마세요 | (통째로 삭제. 갈래는 「직접 채운다 / 정답을 넣고 간다」 둘뿐이고 **둘 다 정당한 길**로 쓴다) |

**자기검열 세 물음** — 문장을 쓴 뒤 반드시 소리 내어 읽고 묻는다.

1. **괄호를 열었는가?** 열었으면 대개 두 문장으로 나눠야 할 것을 눌러 넣은 것이다.
2. **반대로 읽힐 수 있는가?** 「A가 B일 수 있다」류는 대개 「A와 B의 기준이 다르다」가 본뜻이다.
3. **읽는 사람에게 지시하고 있는가?** 지시 대신 **왜 그렇게 하는지**를 쓴다.

> **구조만 훑는 검토로는 이게 안 잡힌다.** 두 가이드를 「전수 검토했다」고 말한 뒤에도
> 위 다섯이 살아 있었다. **문장을 한 줄씩 소리 내어 읽는 것**만이 이 함정을 잡는다.

### 「AI 스러움」이란 무엇인가

강사가 표지를 보고 **「엥?」** 했던 일에서 나온 진단이다. 슬라이드 문장이 강의 표지가 아니라
**넷플릭스 다큐 예고편**처럼 쓰여 있었다.

```
2일차 표지 (고치기 전)
  제목  이상을 잡는 자를 만든다          ← 추격물 제목. 「자(尺)」를 사람으로 읽는다
  부제  새벽 3시의 사건 · 이상을 재는 …   ← 아직 아무 얘기도 안 했는데 「사건」이 튀어나온다
  꼬리  오늘, 알고리즘 하나를 직접 짭니다  ← 쉼표로 뜸 들이는 광고 카피 리듬
```

원인은 규칙 7(대본이 없다)을 지키려다 **읽히기 좋은 쪽이 아니라 멋있게 들리는 쪽**으로
흐른 것이다. 성우가 읽으면 그럴듯하지만 **강사가 강의실에서 읽으면 어색하다.**
정보를 주는 대신 분위기를 잡는 것 — 그게 AI 스러움의 정체다.

## 5. 손대지 말 것

| 무엇 | 왜 |
|---|---|
| **노션 1교시 전부** | 끝났다 (강사 지시). `제작/덱빌드/build_노션.py`·`svgs_노션.py`·`shots노션.py`·`노션좌표.py` · `강의/그림/노션/*` · `강의/2일차/강의자료/노션_자산화.pptx` |
| **`강의/강의안/*_대외.md` 와 docx** | 제출한 문서다. 장수 같은 숫자가 지금과 달라도 고치지 않는다 |
| **`제작/덱빌드/노션좌표.py`** | 계산해서 넣은 값이다. 손으로 고치면 강조 상자가 어긋난다 |
| **본문 주황 밑칠 두 곳** | `build_2일차.py` 의 「높이면 못 잡고, 낮추면 다 울린다」와 **z-score 식**. 금지된 것은 *각주* 밑칠이고, 이 둘은 눈이 갈 곳을 정해 주려고 **일부러 남긴 것이다** |
| **생성된 pptx** | 손으로 고치면 다음에 뽑을 때 사라진다. 글자를 고치려면 `build_*.py` 를 고치고 다시 뽑는다 |

## 6. 절대 바꾸면 안 되는 수치

| | 값 |
|---|---|
| 드리프트 | **0.5℃/h × 4시간 = +2℃** · EQ-03 기준 62 → 64℃ · **80℃ 를 끝까지 안 넘는다** |
| 정상 온도 | 회전수의 함수 `24 + 16.7 × (rpm/1000)^1.4` — EQ-04 **56℃** ~ EQ-05 **74℃** |
| 잡음 | 온도 σ **0.35℃**(라이브는 **앞 값에 0.8 만큼 끌린다** — CSV 자기상관 0.80 과 같은 질감) · 진동 σ 0.12 · 열 시간상수 τ **60초** |
| 데이터 | **60,480행** (CNC 6대 × 7일 × 1분) · CSV 새벽 실측 61.0 → 61.6℃ |
| 오탐 실측 | 기준선 **0.94%** · k=3.0 **567행 = 하루 81건** · k=2.0 **6,375행 = 하루 911건(11배)** |
| 실습 결과 | 스파이크 3/3 · 드리프트 240 중 4 · 결측 0/120 ※ **강의자료에 쓰지 말 것** |
| 정비 이력 | **WO-2026-0801** 「부품 입고 지연으로 보류」 |
| 제어 도구 | `set_equipment_speed`·`ack_alarm` (자동) / `stop_equipment`·`dispatch_robot` (**사람 승인**) |
| 감지 소요 | 배속 ×120 기준 **약 100초** |
| 진단 응답 | 중앙 **4.3초** · p95 9.0초 |

## 7. 손코딩 원칙

**없애는 것은 막히는 스트레스**(왜 안 되는지 모름 · 나만 안 됨).
**남기는 것은 생각하는 스트레스**(*"왜 이건 안 잡히지?"*).

| | 손코딩 | 나머지 |
|---|---|---|
| 2일차 | **25분** — 빈칸 다섯 줄 (함수 셋) | 관찰 · 숫자 흔들기 · AI 비교 |
| 3일차 | **거의 0** — 막히면 `--정답` | 진단 · 판단 · 관찰 |

세 자리는 **갈수록 설명이 줄어든다.** 일부러 그렇게 두었다.
첫 자리는 쓸 것(`sum`·`len`)까지 주석으로 주고, 둘째는 식만, 셋째는 방침을 학생이 고른다.

**힌트를 단계로 주는 것은 없앴다.** 필요한 것은 처음부터 그 자리 주석에 다 있다.
그래도 시간이 다 되면 `확인.py --정답 N` 으로 그 자리만 정답으로 채운다.
**이름은 `--정답` 이다.** 「열기」는 무엇을 연다는 건지 학생이 알 수 없어 바꿨다 —
옛 이름 `--열기` 도 계속 받는다(배포본 검증에 항목이 있다).

## 8. 시간

| | 무엇 | 합계 |
|---|---|---|
| **2일차** | 노션 1교시 60 + 셋업 20 + 슬라이드 46장(실습 20·55분 포함) 135 + 마무리 20 | 235분 |
| **3일차** | 슬라이드 49장(실습 55·70분 포함) 200 + 마무리 20 | 220분 |

**실습 시간은 상한이지 소진할 값이 아니다.**
노션 1교시를 빼고 세면 2일차에 시간이 비는 것처럼 보인다 — 실제로 한 번 그렇게 오판했다.
노션은 만들고 끝이 아니라 **이틀 마지막 20분에 실제로 채우고**, 3일차 끝에 링크를 내는 것이 수료 조건이다.

---

# 2부 · 다시 밟지 말 것

전부 실제로 당한 것이다. 같은 자리에 다시 빠지지 않으려고 남긴다.

## 코드·환경

| 함정 | 내용 |
|---|---|
| **cp949 인코딩** | `—` `→` 출력에서 `UnicodeEncodeError` 로 실습이 중간에 죽었다. 리허설은 UTF-8 을 주입해 돌아 안 보였다 — **학생은 그 환경변수가 없다.** 새 진입점에는 인라인 UTF-8 가드를 반드시 넣는다 |
| **BOM** | PowerShell `Out-File -Encoding utf8` 은 BOM 을 붙인다. 그냥 `utf-8` 로 읽으면 첫 글자에 BOM 이 붙어 판정이 깨진다 — 읽을 때 **`utf-8-sig`** 를 쓴다 |
| **검증 도구가 학생 환경을 잘못 흉내** | 리허설이 학생 코드를 자기 프로세스에서 불러 쓰는데 제작 환경에 `mcp`·`asyncpg` 가 없어 자식이 즉사했다. 30초 타임아웃으로만 보여 **학생 코드 결함으로 오인**했다 |
| **VM 이 옛 상태로 남아 있음** | VM 의 `.env` 가 옛것이라 **OpenAI 키와 진단 설정이 통째로 없었다.** 그대로 수업했으면 3일차가 전부 실패했다. push 해도 VM 은 저절로 안 바뀐다 — 수업 전날 `git pull` + 재기동 필수 |
| **접속 키가 커밋에 딸려감** | 수동 테스트로 채워진 `config.json` 이 스테이징에 들어갔다. 커밋 전 `git status` 육안 확인 |
| **배포본에 강사 값이 남음** | `config.json` 에 강사 주소와 **실재하는 남의 번호(S01)** 가 박힌 채 ZIP 이 한 번 나갔다. 안 돌린 학생이 조용히 그 값으로 진행한다. 지금은 `드라이브폴더만들기.py` 가 **검사해서 막는다** |
| **한글 경로에 Bash** | 깨진다. **PowerShell + 절대경로**를 쓴다 |
| **같은 값을 두 파일이 따로 읽음** | `mcp_server.py` 는 `SHARED_API` 환경변수를 먼저 보는데 `확인.py` 는 `config.json` 만 봤다. **도구는 붙는데 점검만 「서버에 못 닿습니다」**라고 했다. 판정하는 쪽과 실제로 쓰는 쪽이 **같은 규칙**을 써야 한다 |
| **출력의 마지막 줄만 보고 판정** | 안내가 세 줄인데 검사가 한 줄만 봐서, 문구를 조금 바꾸자 실패로 떴다. **학생이 보는 화면 전체**로 판정한다 |
| **검증 도구를 백그라운드로 띄우고 방치** | 셸이 끝나면 같이 죽는다. 11분짜리 리허설이 3분 만에 잘렸다 |
| **산출물에 옛 구조가 남긴 폴더** | `제작/산출물/경남대 AI 특강 (8월)/` 에 **`raise` 가 든 옛 학생 파일**이 그대로 있었다. 지금은 그 폴더를 매번 새로 만든다 — 손으로 넣은 것은 다음 실행에서 사라진다 |
| **최종본을 보려고 zip 을 풀게 만듦** | 전에는 zip 만 남기고 폴더를 지웠고, 그 zip 안에 실습 zip 이 또 있어 **한 줄 고칠 때마다 두 번 풀어야** 확인이 됐다. 게다가 가이드 PDF 를 저장소 뿌리에도 따로 뽑아 같은 문서가 두 곳에서 각각 만들어졌다. 지금은 **풀린 것이 정본이고 zip 이 그림자**다 (4-3절 표) |
| **풀린 실습 폴더를 최종본 안에 둠** | 최종본은 통째로 드라이브에 올라간다. 풀린 폴더가 그 안에 있으면 **학생이 zip 과 폴더 둘을 보고 어느 것을 받을지 헷갈린다.** 풀린 것은 **올라가지 않는 옆자리**(`산출물/k-precision-lab/`)에 둔다 |
| **`.gitignore`·`.md` 를 PowerShell 치환으로 수정** | 인코딩이 깨진다. `Edit` 도구나 python 으로 고친다 |

## 문서

| 함정 | 내용 |
|---|---|
| **문서 치환이 본문까지 번짐** | 슬라이드 문구를 일괄 치환했더니 단축키 표·본문 용어까지 바뀌었다. **장을 지정해서만** 바꾼다 |
| **가이드 출력 블록을 코드에서 역산** | 안 돌리고 소스만 보고 「이렇게 나옵니다」를 적었더니 틀렸다 — 정비 이력 `status` 는 `HOLD` 가 아니라 **`IN_PROGRESS`**, `control_mcp.py --확인` 는 한 줄 요약이 아니라 **JSON** 이다. **출력 블록은 반드시 돌려서 붙인다** |
| **근거 없는 수치를 지어냄** | 「베어링 윤활이 마르면 시간당 0.5℃」를 출처 없이 썼다가 반박당했다. **1차 출처가 없으면 쓰지 않는다** |
| **측정 없이 「이러면 보인다」를 씀** | 「한 대만 1℃ 넘게 올라 있습니다」라고 가이드에 썼는데, 재 보니 이상 **+0.47℃** vs 정상 **−0.63℃** 였다. **정상이 더 흔들렸다** |
| **학생이 못 하는 것을 시킴** | 「평소 온도를 적어 두고 대조」 — 39명이 종이에 숫자를 적지 않는다. 게다가 위 실측 때문에 **대조해도 안 나온다** |
| **구조만 훑고 「전수 검토했다」고 말함** | 절 구성·번호·링크만 보고 통과시켰더니 **괄호 부연·반대로 읽히는 문장·번역투가 그대로 살아 있었다.** 문장은 **한 줄씩 소리 내어 읽어야** 잡힌다 (1부 4절의 「실제로 틀린 다섯」) |
| **지적받은 유형을 그 문장만 고침** | 「다른 기계에게는 이상」을 고치면서 **같은 문단의 괄호 부연은 그대로 뒀다.** 지적은 **유형**으로 받아 그 유형 전체를 훑는다 |
| **표식을 바꾸면서 그 표식을 세는 곳을 안 고침** | 「여기부터 구현합니다」→「빈칸」으로 바꿨더니 **검사기 넷과 슬라이드 한 장**이 옛 표식을 세고 있었다. 표식·문구를 바꾸면 `grep` 으로 전수 확인한다 |
| **가이드가 파일 주석과 같은 말을 반복** | 「①찾고 ②고치고 ③또 고치고」를 가이드가 다시 적었다. **위치는 사진이, 방법은 파일 안 주석이** 말한다. 가이드는 이야기와 확인 명령만 |
| **가이드에 코드 블록을 넣음** | PDF 에서 긁으면 들여쓰기가 깨지고, 파이썬은 그 순간 학생이 못 고치는 에러를 낸다. **뼈대는 파일 안에** 두고 가이드는 자리만 가리킨다 |
| **터미널 상자에 `>` 프롬프트** | 긁으면 프롬프트까지 딸려 와 그대로 못 붙인다. 검은 상자와 라벨만으로 충분하다 |
| **정답이 학생 파일의 모양을 지움** | `--정답` 이 주석·두 갈래 코드를 통째로 갈아끼워 **가이드가 설명한 파일과 달라졌다.** 정답 파일은 **학생 뼈대에서 생성**해 빈칸만 채운 모습이어야 한다 |
| **같은 사실을 1초라고도 1분이라고도** | `virtual_sample_seconds=60`, 기본 배속 ×60 → **실제 1초 tick = 가상 1분 샘플.** 데이터 간격은 **1분**이다 (60,480행이 증거) |

## 그림·슬라이드

| 함정 | 내용 |
|---|---|
| **실제 화면을 그림으로 바꿔치기** | 캡처해 둔 노션 화면을 손그림 목업으로 갈아 끼웠다. 화면과 설명이 겉돌았다 |
| **강조를 원본에 구움** | `실습_2_공장뷰.png` 에 주황 상자가 **그림 자체에 구워져** `EQ-01`·`EQ-04` 글자를 가렸다. 원본이 안 남아 지울 수도 없었다. 대시보드는 **`공장뷰찍기.py`** 로 뽑아 원본을 깨끗이 두고, 강조는 `그림표시.py` 로 따로 얹는다 |
| **강조 상자를 눈대중으로** | 좌표를 어림해 찍었더니 글자를 물고 창 밖으로 나갔다 |
| **겹치는 상자를 밀어서 해결** | 밀면서 높이를 깎으니 상자가 납작한 선이 되어 **아무것도 안 가리키는 자국**만 남았다. 이제 겹치면 빌드가 멈춘다 |
| **번호가 화면에 없는데 설명에만 있음** | 학생이 「②는 어디?」 하고 찾다 흐름이 끊긴다. `deckkit.step` 이 다르면 빌드를 멈춘다 |
| **학생 화면이 아닌 캡처** | VS Code 자동화가 막히자 따로 띄운 콘솔을 찍어 넣었다. 학생은 VS Code 안 터미널만 본다 |
| **도해를 이름만 보고 재사용** | 같은 z-score 그림이 *온도는 회전수의 함수* · *기계마다 평소 온도가 다르다* · *감속이 첫 수인 이유* 세 장에 붙어 있었다. 어느 것도 그 장의 주장을 그리지 않았다 |
| **그림 개수와 라벨 숫자가 어긋남** | 화면 **8개**를 그려 놓고 라벨은 「**39개** 화면」이었다. 숫자는 각주가 말하고 라벨은 그림이 그린 것만 말한다 |
| **아이콘 뜻이 제목과 어긋남** | 「부숴도 된다」(안전)에 **저울**이 붙어 있었다 — 저울은 오탐·미탐 맞바꿈 전용이다. 방패로 바꿨다 |
| **어제 회고 그림이 옛 판** | 3일차 2장이 「채울 곳 셋」을 그리고 있었는데 2일차 최종은 「함수 셋 · 빈칸 다섯」이었다. **다음 날 회고 그림은 전날 최종판과 같아야 한다** |
| **그림 설명에 대괄호** | `![… [O] 로 바뀌었습니다](…)` 처럼 대괄호를 쓰면 마크다운이 깨져 **그림이 글자로 찍힌다** |
| **덱 문구만 고치고 render 를 안 돌림** | `레이아웃검사`만 돌렸더니 PNG·pptx·드라이브 ZIP 에 **옛 문구가 그대로** 남았다 |

## pptx

| 함정 | 내용 |
|---|---|
| **문단마다 상자를 쪼갬** | 옮기려면 문단 수만큼 따로 잡아야 해서 못 쓴다. 세로로 이어지는 글은 **한 텍스트 상자**에 담고 줄간격으로 벌린다 |
| **테두리를 선 네 개로** | 강조 상자를 옮기려고 네 번 잡게 된다. 네 면이 같으면 **사각형 하나**로 묶는다 |
| **흰 배경을 도형으로 깖** | 요소를 누를 때마다 흰 판이 먼저 잡혀 아무것도 못 만진다. 바탕은 **슬라이드 배경색**으로 |
| **문단 갈아끼우기** | 문단을 통째로 새 런에 넣으면 첫 런의 서식(파란 번호)이 항목 전체로 번진다. **런 구조를 지켜** 해당 런만 바꾼다 |
| **머리말 글상자는 폭 고정** | 길어지면 옆 글과 겹친다. 대상을 드러내려면 머리말이 아니라 **부제**에 쓴다 |
| **파워포인트가 열린 채 COM 도구 실행** | `Quit()` 이 **강사가 편집하던 창까지 닫는다.** 실제로 그렇게 껐다 |
| **삽화가 통째로 사라짐** | `편집pptx.py` 의 워커가 `.art` 칸의 `el.textContent` 로 글 유무를 보는데 **svg 안 `<text>` 까지 딸려 와서**, 라벨이 든 삽화는 칸 전체가 텍스트 상자가 되고 그림이 안 들어갔다. 지금은 `querySelector('svg')` 로 먼저 거른다 |
| **두 도구가 같은 파일에 씀** | 통이미지를 만드는 도구와 `편집pptx.py`(요소편집)가 같은 `슬라이드.pptx` 에 써서 **나중에 돌린 쪽이 이겼다.** 지금은 **통이미지 도구 자체를 없앴다** — 편집이 안 되는 pptx 는 쓸 데가 없었다 |

---

## 오늘 실측으로 확정한 것 — 추측으로 되돌리지 말 것

| 무엇 | 실측 |
|---|---|
| **오전 도구가 심어 둔 이상을 AI 에게 못 보낸다** | 여섯 대 모두 온도 40~70건·진동 40~50건이 잡히고(전부 헛울림), `detect_anomaly` 가 **최근 30건**에서 자른다. 그 30건은 마지막 이틀치뿐이라 **3일차 스파이크·5일차 드리프트가 통째로 잘린다.** 그래서 3일차 26장이 「AI 는 도구가 준 숫자를 의심하지 않는다」로 이 사실을 다룬다 |
| **기울기를 8배로 올려도 z-score 는 못 잡는다** | 0.5 / 1.0 / 2.0 / 4.0 ℃/h 로 재 봄 — 구간 241개 중 **4~5개**로 전부 같다. 미탐은 상승폭이 아니라 **기준이 따라 올라가는 성질**에서 온다. 상한을 정하는 것은 z-score 가 아니라 **사람 눈과 80℃ 선**이다 |
| **CNC 대수는 「수백 대」가 아니다** | 미국 잡숍 400곳 조사에서 **업체당 중앙값 16대**. 우리 6대와 같은 급이다. 「설비가 많아서 못 본다」로 설명하면 학생이 바로 반박한다 |
| **7일치 CSV 는 이미 부드럽다** | 1차 자기상관 **0.80** · 표준편차 0.79(일주기 1℃ + 잡음 0.35). 라이브만 백색 잡음이라 톡톡 튀던 것을 AR(1) `noise_memory=0.8` 로 맞췄다 — 폭은 그대로라 오탐·미탐 성질이 안 바뀐다 |
| **라이브 30초 관찰이 여전히 안 보인다** | 주입 뒤 30초 실측 — 이상 EQ-03 **−0.39℃**, 정상 EQ-02 **+0.61℃**. **이상한 쪽이 오히려 내려갔다** |

## 실습 파일 구조를 두 번 바꿨다 — `raise` 를 없애고, 다시 **빈칸**으로

**1차 — `raise NotImplementedError` 를 없앴다.**
전에는 `# TODO 1: 여기를 채우세요` 아래에 `raise NotImplementedError(...)` 가 있었다.
학생이 **그 줄을 지울지 · 아래에 쓸지 · 고칠지**를 판단해야 했고, 지우다 들여쓰기를 무너뜨리는
것이 「자주 나는 에러」에 실릴 만큼 잦았다. 그래서 **주석 블록 하나**(「여기부터 구현합니다」)로
바꾸고 그 아래에 통으로 쓰게 했다.

**2차 — 그것도 안 됐다. 이제 빈칸이다.**
직접 실습해 보니 6~8번에서 멈춘다. 원인은 난이도가 아니라 **흩어짐**이었다.

| 필요한 것 | 있던 곳 |
|---|---|
| 뭘 만들지 | 가이드 |
| 어떻게 쓰는지 | 파일 주석 (가이드와 같은 말을 다르게 반복) |
| 답 | 가이드 맨 뒤 |
| 답 여는 명령 | 가이드 다른 절 |

게다가 가이드에 코드 블록을 넣어 두면 **PDF 에서 복붙할 때 들여쓰기가 깨진다.**
파이썬은 들여쓰기가 문법이라, 그 순간 학생이 못 고치는 에러를 만난다.

**지금은 뼈대가 파일 안에 다 들어 있고, 학생은 `...` 만 고친다.**

```
    # ── 빈칸 1 ──────────────────────────────────────────────
    #   seg 의 평균. 다 더해서 개수로 나눈다.
    #   쓸 것 :  sum(seg)   len(seg)
    mean = ...
```

- **새로 만들 줄 0 · 지울 주석 0.** 들여쓰기·괄호·`return` 은 이미 맞춰져 있다
- 고칠 곳은 **2일차 5줄 · 3일차 10줄**
- 판정 근거는 **`...`(Ellipsis)** — 파이썬이 아는 값이라 파일이 안 깨지고,
  그대로 두면 `... ** 0.5` 에서 **소리 내며 터진다**(조용한 실패가 아니다)
- 같은 규칙을 네 곳이 공유한다 — `확인.py` · `돌려보기.py` · `mcp_server.py` · `agents/__init__.py`
- 가이드에는 **코드 블록을 넣지 않는다.** 「`빈칸 1` 을 채웁니다」라고만 한다
- 표식은 **`빈칸 1`~`빈칸 5`** — `Ctrl+F` 로 치면 채울 자리만 걸린다 (`TODO` 는 여기저기 걸려서 버렸다)
- **힌트를 단계로 주던 것(`--힌트 1/2`)은 없앴다.** 필요한 것은 그 자리 주석에 다 있다
- `확인.py` 는 남긴다 — 그건 힌트가 아니라 **「어디가 왜 틀렸는지」를 짚는 것**이다

> ### ★ 표식을 바꿀 때 **검사기 네 개와 슬라이드 한 장**이 같이 걸린다
> 「여기부터 구현합니다」가 몇 곳이어야 한다고 **세고 있던 검사기**가 넷 있었고
> (2일차·도구만들기·폐루프 `verify_lab.py` · `리허설.py`),
> **2일차 35장 슬라이드가 그 주석을 찾으라고 학생에게 말하고 있었다.**
> 표식을 바꾸면 **`grep` 으로 전수 확인하고 이 다섯을 같이 고친다.**

### ★ `raise` 를 빼면 판정 근거도 같이 사라진다 — 여기가 함정이었다

파이썬에서 **속이 설명글뿐인 함수는 문법 오류가 아니라 조용히 `None` 을 돌려준다.**
그래서 `raise` 만 빼고 두면 절대 규칙 5(조용히 실패하지 않는다)를 어기게 된다.
실제로 세 자리가 그렇게 어긋나 있었다 —

- `mcp_server.py --확인` 가 빈 도구를 부르고 **「정상」이라 찍었다** (가이드 캡처와 반대)
- `agent.py` 가 빈 도구의 `None` 을 **AI 에게 `null` 로 보냈다.** AI 는 「데이터가 없습니다」라는
  그럴듯한 리포트를 써 내고, 학생은 **자기 도구가 빈 줄도 모른 채** 오전을 넘긴다
- 정답 넣기가 옛 `raise` 패턴을 못 찾아 **안 채운 학생에게 「이미 채워져 있습니다」**라고 답했다

**그래서 판정을 소스 읽기로 옮겼다.** 설명글과 주석 말고 실행되는 줄이 하나도 없으면 안 채운 것으로 본다.

| 파일 | 판정 함수 |
|---|---|
| `2일차/실습/확인.py` · `돌려보기.py` | `_아직안채움()` · `안채운자리()` |
| `3일차/도구만들기/mcp_server.py` | `안채운도구()` — `--확인` 과 `agent.py` 가 같이 쓴다 |
| `3일차/도구만들기/확인.py` | `_빈함수()` — 2일차 `detect.py` 까지 같이 본다 |
| `3일차/폐루프/agents/__init__.py` | `비었나()` · `확인()` · `안채운자리()` — 등록표가 판정한다 |

폐루프는 **자기 차례가 왔을 때** 멈춘다(`확인("judge")`). 가이드가 감지만 채우고 한 바퀴
돌려 보게 하므로, 시작할 때 통째로 막으면 그 흐름이 깨진다.
`--정답` 으로 완성본을 꽂으면 함수가 다른 파일에서 오므로 `co_filename` 으로 알아본다.

## 점검이 어떻게 「어디가 왜 틀렸는지」를 아는가

**학생 코드를 읽지 않는다. 나온 값만 본다.** 그래서 어떻게 짜든 상관없다.

`window_stats([1,2,3,4,5,99], i=5, window=5)` 의 정답은 평균 3.0 · 표준편차 √2.5 다.

| 나온 값 | 판정 |
|---|---|
| 평균 19.0 | 지금 값 99 를 넣었다 |
| 표준편차 √2.0 | 개수로 나눴다(모표준편차) |
| `i=1` 에서 값이 나옴 | 앞이 모자란 자리를 안 걸렀다 |
| 그 밖 | 기대값과 실제값을 보여 주고 **볼 곳만** 알려 준다 |
| 예외 | `except Exception` 으로 받아 그대로 보여 준다 |

**세 층이다** — ① 흔한 실수는 값으로 특정해 콕 짚고 ② 모르면 기대값을 보여 주고
③ 터지면 예외를 그대로 보여 준다.

---

# 3부 · 리소스 — 이 저장소에 무엇이 있는가

최상위는 둘이다. **「하는 것」(`강의/`)과 「만드는 것」(`제작/`).**
`제작/` 에는 문서를 두지 않는다 — 스크립트의 첫머리 설명이 곧 문서다.

## 3-1. 뿌리

| 파일 | 무엇 |
|---|---|
| `핸드오프.md` | **이 문서.** 규칙 · 리소스 · 명령 |
| `가이드보기.py` | **최종본 폴더를 열어 준다.** `--가이드` 면 가이드 두 권을 바로 띄운다. 뽑는 것은 없다 — 만드는 곳은 `드라이브폴더만들기.py` 한 곳이다 |
| `.gitignore` | `산출물/` · `.env` · `배정장부.json` · `.내번호` |

## 3-2. `강의/` — 가르칠 때 쓰는 것

### 학생이 읽는 것

| 파일 | 무엇 |
|---|---|
| `준비안내.md` | 전날까지 하는 설치 — 노션 → VS Code → 파일 받기 → uv |
| `실습가이드_2일차.md` | 그날 쓰는 것만. **이야기 → 치는 명령 → 나오는 화면 → 막히면 `--정답`** |
| `실습가이드_3일차.md` | 같은 짜임 |

정답은 **`--정답` 명령과 `정답/` 폴더**가 맡는다. 전에 붙였던 「참고 답안」 부록은 뗐다 —
읽는 사람을 앞뒤로 오가게 만들었기 때문이다. **가이드는 위에서 아래로 한 번만 읽는다.**

`정답/detect_answer.py` 는 **학생 뼈대에서 생성한다** — 주석과 두 갈래 코드가 그대로 있고
빈칸만 채워져 있다. 그래서 `--정답` 을 눌러도 **가이드가 설명한 파일 모양이 유지된다.**
(전에는 모양이 다른 코드로 통째로 갈아끼워져 학생 파일과 가이드가 어긋났다.
`math.sqrt` 의존도 그때 없앴다 — 뼈대가 `** 0.5` 를 쓴다.)

### 강사가 읽는 것

| 파일 | 무엇 |
|---|---|
| `2일차/진행.md` | **그날의 전부.** 리모컨 화면 기준으로 쓰였다. 슬라이드 번호별 할 일 · 주입 순서 · 단톡방 공지 원문 · 학생 질문 답변 |
| `3일차/진행.md` | 같은 짜임 |
| ~~`촬영목록.md`~~ | **지웠다.** 화면이 전부 자동 생성이라 「무엇을 어떻게 찍는가」 목록이 필요 없어졌다 — 각 그림의 사양은 `터미널그림.py` 의 원고와 `편집기그림.py` 의 장면 함수가 들고 있다 |

**강사가 치는 터미널 명령은 VM 켜고 끄기 두 줄뿐이다.** 나머지는 리모컨 버튼이다.
**슬라이드를 넣거나 빼면 `진행.md` 의 번호를 반드시 같이 고친다** — 강사가 그 번호로 진행한다.

### 문서가 어디에 있고 무엇을 맡나

**루트 넷이 전부다.** 나머지는 그 폴더에서만 쓰는 것이다.

| 문서 | 맡는 것 |
|---|---|
| `핸드오프.md` (이 문서) | **규칙의 원본.** 프로젝트 전체를 이어받는 자리 |
| `작업보고.md` | 무엇을 왜 고쳤는지 — **비기술자에게 화면공유용** |
| `강의/준비안내.md` · `실습가이드_*.md` | **학생이 읽는 것** (PDF 로 나간다) |
| `강의/<일차>/진행.md` | **강사가 그날 보는 것** |
| `강의/시뮬레이터/docs/운영.md` | **서버를 띄우는 사람**이 읽는 상세 절차 (이 문서 4부는 요약) |
| `강의/시뮬레이터/docs/배포_기동_절차서.md` · `API_명세_인계본.md` · `부하테스트_결과.md` | 서버 자체의 사양·측정 기록 |
| `검증리서치/04_딥리서치_결과.md` | **수치의 1차 출처.** 의심될 때 여는 곳 |
| `제작/검증도구/센서데이터/README.md` | 7일치 데이터셋을 어떻게 만들고 검증하나 |

**새 문서를 함부로 만들지 않는다.** 규칙은 **이 문서 하나**에, 서버 얘기는 시뮬레이터 `docs/` 에 넣는다.
늘리면 다음 사람이 어느 것이 최신인지 모른다 — 실제로 발췌본 두 장을 만들었다가 그 이유로 지웠다.

### 학생 실습 원본 — 여기 있는 것이 그대로 학생에게 간다

**2일차 (`2일차/실습/`)**

| 파일 | 무엇 |
|---|---|
| `내번호.py` | 서버에서 내 공장 번호를 받아 `.내번호` 와 `config.json` 에 채운다. 서버 주소는 **묻지 않는다** — 한 곳뿐이라 물으면 학생이 첫 줄에서 멈춘다 |
| `detect.py` | **★ 오늘 채우는 파일.** `빈칸 1` … `빈칸 5` — 고칠 것은 `...` **다섯 줄** |
| `확인.py` | 값 몇 개짜리 시험 데이터로 함수가 맞는지만 본다. 막히면 `--정답 N` 이 그 함수만 채운다 |
| `돌려보기.py` | 진짜 7일치 60,480줄에 태운다. `--k` · `--window` · `--impl` |
| `정답/detect_answer.py` | `--정답` 이 읽는 완성본. **학생 뼈대에서 생성해 빈칸만 채운 모습이다** — 갈아끼워도 주석·두 갈래 코드가 그대로 남는다. 학생에게 같이 나간다 |
| `verify_lab.py` | 학생 조건에서 이 랩이 도는지 확인 (검증용) |

**3일차 오전 (`3일차/실습/도구만들기/`)** — 어제 코드를 AI 가 부를 수 있게 내놓는다

| 파일 | 무엇 |
|---|---|
| `mcp_server.py` | **★ 채우는 파일.** 도구 둘(`detect_anomaly` · `query_equipment`) — `빈칸 1`~`빈칸 4` |
| `agent.py` | 지시 한 문장으로 도구를 부르는 ReAct 루프 |
| `config.json` | 서버 주소·번호·키 (어제 `내번호.py` 가 채운다) |
| `확인.py` · `정답/` | 2일차와 같은 짜임 (`--정답 1`·`2`) |

**3일차 오후 (`3일차/실습/폐루프/`)** — 판단이 설비를 실제로 움직인다

| 파일 | 무엇 |
|---|---|
| `agents/detector.py` | **★ `judge()`** — 구간을 둘로 갈라 **앞쪽(과거)을 기준으로** 삼는다. 기준이 안 따라 올라가서, 어제 z-score 가 못 잡던 드리프트를 잡는다 |
| `agents/diagnoser.py` | **★ `build_prompt()`** — 서버 중계로 AI 에게 원인을 묻는다 |
| `agents/actuator.py` | **★ `to_commands()`** — 진단을 실제 제어 명령으로 바꾼다 |
| `loop.py` | 총괄. 감지 → 진단 → 조치를 돌린다. `--once` · `--확인` · `--정답 N` |
| `hitl.py` | 되돌릴 수 없는 명령 앞에서 멈춰 사람에게 묻는다 |
| `factory_api.py` · `control_client.py` · `control_mcp.py` | 서버와 말하는 부분 (다 되어 있다) |

### 슬라이드 (`강의/<일차>/강의자료/`)

| 파일 | 무엇 |
|---|---|
| `슬라이드.pptx` | **강의장에서 넘기는 것.** 글자가 진짜 텍스트 상자라 직전에 문구를 고칠 수 있다 |
| `노션_자산화.pptx` | 2일차 1교시 (**손대지 않는다**) |

### 그림 (`강의/그림/`)

| 폴더 | 무엇 |
|---|---|
| `vscode_*.png` | 설치 안내용 실제 캡처 7장 |
| `노션/` | 노션 1교시 화면 30장 (**손대지 않는다**) |
| `실습/` | 가이드에 들어가는 화면 — **전부 자동 생성**(`터미널그림.py`·`편집기그림.py`). 「이런 오류가 나면」 목록을 걷어내면서 딸린 에러 그림도 지웠다 |

### 시뮬레이터 (`강의/시뮬레이터/`) — 클라우드 서버

학생 39명이 각자 자기 공장(CNC 6대 + AMR 2대)을 받는 멀티테넌트 서버다.
FastAPI + asyncpg + Supabase.

| 자리 | 무엇 |
|---|---|
| `server/app/sim/engine.py` | **물리.** 온도 = 회전수의 함수 · 1차 지연 · 잡음 · 이상 주입 · 알람 판정 |
| `server/app/sim/runner.py` | 시계. `real_tick = 60 ÷ 배속` — 배속을 올리면 쓰기 부하가 그만큼 는다 |
| `server/app/api/read.py` | 학생이 읽는 것 (센서·정비이력) |
| `server/app/api/control.py` | 학생이 쓰는 것 (감속·정지·로봇·알람) + **HITL 승인** |
| `server/app/api/diagnose.py` | AI 중계 — 키는 서버만 쥔다 |
| `server/app/api/claim.py` | 자리 배정. `tenant_type` 으로 학생/강사를 가른다 |
| `server/app/api/instructor.py` | 리모컨이 부르는 것 — 주입·제어개방·배속·리셋 |
| `web/view/index.html` | **학생이 보는 공장 화면** (Konva 2D) |
| `web/console/index.html` | **강사 리모컨.** 일차별 카드 몇 개가 전부 |
| `config/sim_profile.json` | 물리·이상주입 파라미터. **여기 숫자가 슬라이드 서사의 근거다** |
| `config/layout.json` | 설비 배치와 정격 회전수 (1600·1800·1800·1900·2000·2200) |
| `db/migrations/` | 스키마. **DDL 은 여기에만 쓴다** — 콘솔에서 직접 고치지 않는다 |
| `tools/` | 운영 보조 — 자리배정 · 키배포표 · 부하테스트 · 마이그레이션 |
| `docs/` | API 명세 · 배포 절차 · 부하테스트 결과 |
| `.env` | **커밋 금지.** DB 접속과 OpenAI 키 |
| `배정장부.json` | **커밋 금지.** 누가 어느 공장을 받았는가 |

**강사 공장은 `S00` 이다.** 학생 39자리(`S01`~`S39`)는 그대로다.
`tenant_type='instructor'` 로 갈라 뒀으므로 배정에서 저절로 빠진다 — 번호로 예외를 두지 않았다.
짝이 되는 것 하나 — `db.list_tenants()` 가 개인 모드에 instructor 도 **같이 싣는다.**
안 실으면 공장이 만들어지기만 하고 **돌지 않는다.** (마이그레이션 `008`)

## 3-3. `제작/` — 만들 때만 쓰는 것

### 덱빌드 (`제작/덱빌드/`) — 슬라이드는 코드다

| 파일 | 무엇 |
|---|---|
| `build_2일차.py` | **2일차 46장의 원본.** 여기를 고친다 |
| `build_3일차.py` | **3일차 49장의 원본** |
| `build_노션.py` | 노션 41장 (**손대지 않는다**) |
| `deckkit.py` | 레이아웃 렌더러 — `cover`·`sect`·`pic`·`shot`·`step`·`rail`·`cmp`·`tbl`·`words`·`graph` |
| `svgs.py` · `svgs3.py` · `svgs_노션.py` | 삽화(SVG) |
| `shotkit.py` | 실제 화면을 슬라이드에 박고 누를 자리에 표시를 얹는다 |
| `_기준판.css` | 색·글꼴·간격의 기준판 |
| `render.py` | 덱 HTML → 장별 PNG (playwright) |
| `레이아웃검사.py` | **전수 검사.** 넘침·잘림·글자 크기·상자 겹침·번호 일치 |

### 빌드도구 (`제작/빌드도구/`)

| 파일 | 무엇 |
|---|---|
| `편집pptx.py` | **PNG 가 아니라 요소로** pptx 를 만든다. 브라우저가 실제로 그린 위치를 읽어 온다 |
| `md2pdf.py` | 가이드 마크다운 → 학생용 PDF. 아래 표대로 칸 모양이 정해진다 |
| `드라이브폴더만들기.py` | 드라이브에 올릴 것 한 벌. **강사 값이 남아 있으면 멈춘다** |
| `공장뷰찍기.py` | 학생 공장 화면을 **라이브로** 캡처 |
| `그림표시.py` | 캡처에 붉은 강조 테두리를 얹는다 |
| `터미널그림.py` | 터미널 로그 → VS Code 터미널 그림 (실측한 글자 크기·줄 간격) |
| `노션좌표뽑기.py` · `노션화면추출.py` · `뒷장갈기.py` | 노션 전용 (**손대지 않는다**) |

**가이드 디자인은 마크다운을 어떻게 쓰느냐로 정해진다** — CSS 를 손대지 않는다.

| 쓰는 법 | 나오는 모양 |
|---|---|
| ` ``` ` 안에 `uv run …` 만 | **검은 터미널 칸** + 「터미널에 칩니다」. **프롬프트 `>` 를 붙이지 않는다** — 긁으면 딸려 와서 그대로 못 붙인다 |
| ` ``` ` 안에 그 밖의 것 | 「화면에 이렇게 나옵니다」 칸 |
| ` ```수식 ` | 민무늬 (z 식 같은 것) |
| ` ```주소 ` | 「크롬 주소창에 붙여넣습니다」 |
| ` ```json ` | 「파일에 이렇게 있습니다」 |
| ` ```모양 ` | 「이런 모양으로 돌려줍니다」 |
| ` ```지시문 ` | 「AI 에게 주는 말 — 그대로 씁니다」 |
| ` ```python ` | 「파일에 씁니다」 (가이드에서는 쓰지 않는다 — 1부 2절) |
| `## 1 · 제목` | 큰 단계 — 굵은 윗줄로 끊긴다 |
| `**① 소제목**` | 작은 단계 — 주황 막대 |
| `> 인용` | 노란 강조 칸 |

**강조 칸을 연달아 쌓지 않는다.** 한 화면에 하나면 충분하다.
그림 설명(`![…]`)에 **대괄호를 쓰지 않는다** — 마크다운이 깨져 그림이 글자로 찍힌다.

### 검증도구 (`제작/검증도구/`)

| 파일 | 무엇 |
|---|---|
| `배포본만들기.py` | 학생 ZIP 을 이 저장소에서 뽑아낸다. `--검증` 이면 학생 조건으로 실제 실행 |
| `리허설.py` | **이틀치 무인 재생.** `--부하` 는 39명 실모델 동시 진단까지 (**비용 발생 — 강사 확인 없이 돌리지 않는다**) |
| `pptx눈으로.py` | 만든 pptx 를 낱장 PNG 로 — 눈으로 보려고. **파워포인트가 열려 있으면 멈춘다** |
| `센서데이터/` | 7일치 60,480행을 만드는 곳. `generate.py` · `verify.py` · 라벨 CSV |

## 3-4. `검증리서치/` — 시나리오가 현실과 맞는가

「고장을 낸다 → 못 찾는다 → 로직을 짠다」가 진짜 현장 이야기인지 외부 검증을 받은 기록이다.
**근거 없는 주장을 다시 쓰지 않기 위한 자료이므로 남긴다.**

| 파일 | 무엇 |
|---|---|
| `04_딥리서치_결과.md` | **1차 출처가 붙은 검증 보고서.** 수치를 의심할 때 여기를 연다 |

처리 상태는 여기 두지 않는다 — **5-3절 한 곳**에 있다. 전에는 `05_고칠것_전수목록.md` 에
따로 두었는데, 5-3 과 역할이 겹치는 데다 **번호까지 충돌해서**(양쪽 다 `B4`·`C1` 을 쓰면서
뜻이 달랐다) 이어받는 사람이 엉뚱한 항목을 봤다. 이력까지 5-3 으로 옮기고 지웠다.

**핵심 결론** — 「초기 고장은 티가 안 난다」는 사실이다. 베어링 4단계 열화 모델에서
1단계는 온도 상승이 없고 **2단계가 기준선 대비 2~5℃**, 3단계가 5~10℃ 다.
**온도는 늦은 지표**라서 「온도 계기판만 보다 놓쳤다」는 설정이 오히려 정확하다.
2℃ 가 대수인 이유는 공작기계 오차의 **40~70%가 열변형**이기 때문이다 (Bryan, *CIRP Annals* 39(2), 1990).

**말하면 안 되는 것** — 「실제로는 하루 몇 도씩 오릅니다」 같은 구체적 속도.
우리 0.5℃/h 는 수업용으로 정한 값이다. **「4시간에 2℃」로만 말한다.**

---

# 4부 · 어떻게 돌리는가

## 4-1. 공장 — 학생 PC 에서 하나씩 돈다. 서버가 없다

수업의 모든 것이 **각자 컴퓨터의 docker compose 한 벌** 안에서 돈다 —
FastAPI 공장 + Postgres DB. 클라우드 서버·VM·리모컨·자리 배정이 전부 없어졌다.

```bash
cd 공장                      # (강사 저장소는 강의/시뮬레이터)
docker compose up -d         # 켠다 — 첫 실행은 빌드+스키마 자동 적용까지 몇 분
docker compose down          # 끈다 (데이터는 남는다)
docker compose down -v       # 끄고 데이터까지 — 공장 초기화
```

- 전원이 같은 `S01` 이다. **번호 관리가 없다** — DB 가 각자 것이라 부딪힐 상대가 없다
- 스키마는 켜질 때 스스로 맞춘다 (`migrate up` 이 CMD 에 들어 있다)
- 다 같이 쓰는 것은 **3일차의 OpenAI 키 하나**뿐이다 — 그래서 진단·에이전트
  창구에 지수 백오프 재시도가 들어 있다 (39명 동시 혼잡을 각자 공장이 넘긴다)
- 왜 이렇게 바꿨나 — 서버는 자리 배정·부하·와이파이·수업 뒤 정리까지 강사가
  짊어질 것이 많았다. 로컬이면 **학생이 망가뜨려도 자기 것만** 망가지고,
  초기화가 한 줄이고, 강의장 와이파이가 느려도 안 무너진다

## 4-2. 수업 중 강사가 하는 것 — 신호 셋

버튼·스위치는 전부 학생 것이다. 강사는 **때를 정해 줄 뿐**이다.

| 일차 | 신호 | 학생이 하는 것 |
|---|---|---|
| 2일차 23장 | "다 같이 누르세요" | 화면의 **[이상 시작]** — 배속 ×120·소진 후 ×60 복귀까지 자동 |
| 3일차 아침 | 단톡방에 열쇠 | `uv run 3일차준비.py sk-열쇠` — 열쇠 넣고 재기동 |
| 3일차 31장 | "다 같이 —" | `uv run 제어열기.py` — 화면 「제어」가 잠김→개방 |
| 3일차 42장 | "다 같이 누르세요" | **[이상 시작]** (루프를 먼저 돌려 둔 채) |

**2일차 주입은 2분 30초면 소진된다.** 누르게 한 뒤 딴 얘기 말고 바로 24장으로 간다.
그리고 **관찰은 30초에서 끊는다** — 2분을 보면 +2℃ 라 눈에 띄어 **「찾아내기 게임」이 된다.**
이 장면이 묻는 것은 「찾았느냐」가 아니라 **「이걸 계속 볼 수 있느냐」**다.

버튼은 **드리프트만** 건다. 스파이크·결측을 학생 손에 두지 않은 것도 같은 이유다 —
결측은 `NULL` 과 알람이 떠서 학생이 그 자리에서 맞혀 버린다.

## 4-3. 자료를 다시 뽑는 순서

문구를 고쳤으면 **끝까지** 돌린다. 중간에 멈추면 섞인다.
**순서가 있다** — 최종본은 학생 실습파일이 먼저 풀려 있어야 만들어진다.

**가이드 PDF 를 따로 뽑지 않는다.** 마지막 ⑤ 가 `.md` 에서 세 권(준비안내·가이드 둘)을
직접 뽑아 최종본에 넣는다. 전에는 여기서 한 번, 저장소 뿌리에서 또 한 번 뽑아서
같은 문서가 두 곳에 생겼다 — 한쪽만 다시 뽑으면 어느 것이 최신인지가 그때 생긴다.

```bash
# ① 덱
cd 제작/덱빌드   && uv run build_2일차.py && uv run build_3일차.py
cd 제작/덱빌드   && uv run 레이아웃검사.py          # 걸리면 문구·좌표를 고친다. 검사를 끄지 않는다
cd 제작/덱빌드   && uv run render.py 2일차덱 && uv run render.py 3일차덱

# ②  ★ 렌더된 PNG 를 눈으로 본다  →  제작/산출물/슬라이드/{2일차,3일차}/*.png
#    검사는 넘침·잘림까지만 본다. 그림이 제목과 딴소리를 하거나 앞 장과 같아도 통과한다.
#    실제로 그렇게 통과한 것이 여섯 장 있었다 (5-3절).

# ③ pptx  — PowerPoint 를 먼저 닫는다. COM 의 Quit() 이 강사 편집 창을 닫은 적이 있다
cd 제작/빌드도구 && uv run 편집pptx.py 2일차 && uv run 편집pptx.py 3일차

# ④ 학생 실습파일  →  ⑤ 최종본   (뒤집으면 ⑤ 가 「실습파일이 없습니다」로 멈춘다)
cd 제작/검증도구 && uv run 배포본만들기.py --검증        # 학생 조건(cp949)으로 실제 실행까지
cd 제작/빌드도구 && uv run --with playwright 드라이브폴더만들기.py
#   ⑤ 가 가이드 PDF 세 권까지 여기서 뽑아 넣는다 (md2pdf 를 playwright 로 부른다)

# ⑥  ★ 최종본을 눈으로 본다  →  uv run 가이드보기.py   (폴더가 열린다)
```

**끝나면 확인할 것** — 배포본 검증이 「학생 조건에서 그대로 돕니다」로 끝나고,
최종본이 「강사 것·정답은 섞이지 않았습니다」로 끝나야 한다.

**산출물은 이렇게 놓인다 — 압축을 풀 일이 없다.**

| 무엇 | 어디 | 언제 쓰나 |
|---|---|---|
| **최종본** | `제작/산출물/경남대 AI 특강 (8월)/` | **이 폴더를 통째로** 드라이브에 끌어다 놓는다. 확인도 여기서 더블클릭 |
| 최종본을 압축한 것 | `제작/산출물/경남대 AI 특강 (8월).zip` | 통째로 옮길 일이 있을 때만 |
| **학생 실습파일 (풀린 것)** | `제작/산출물/k-precision-lab/` | **여기서 고치고 바로 본다.** 최종본 안의 실습 zip 이 이 폴더에서 만들어진다 |

풀린 것은 **하나뿐이다.** 전에는 `산출물/배포본/k-precision-lab` 에 한 겹 더 들어가 있어서,
학생이 받을 것을 보려면 zip 을 풀어야 했다.

**풀린 폴더를 최종본 안에 두지 않는다.** 최종본은 통째로 올라간다 — 그 안에 풀린 폴더가
있으면 학생이 zip 과 폴더 둘을 보고 어느 것을 받을지 헷갈리고, 폴더째 받으면 파일을
하나씩 눌러 받게 된다. 그래서 **올라가지 않는 바로 옆자리**에 둔다.

그 밖에 —

```bash
cd 제작/검증도구 && uv run 리허설.py --token <토큰>          # 이틀치 무인 재생
cd 제작/검증도구 && uv run 리허설.py --token <토큰> --부하    # 39명 동시 진단 (비용 발생 — 확인 뒤에)
cd 강의/시뮬레이터 && uv run --with asyncpg --with pydantic-settings --with pydantic                      tools/migrate.py up                  # 스키마·시드 (매번 안전)
```

## 4-4. 학생 환경 — 파이썬을 설치하지 않는다

`uv` 하나만 깔면 실습 폴더에 파이썬 3.12 와 패키지를 uv 가 갖춘다.
학생이 치는 명령은 앞에 `uv run` 이 붙는 것뿐이다.

**「uv 를 찾을 수 없다」를 구조로 막았다.** uv 는 `사용자폴더\.local\bin\uv.exe` 에 놓이는데,
① 이미 켜져 있던 창은 **켜질 때 복사해 온 옛 PATH** 를 계속 쓰고 ② 정책·권한으로 등록이
조용히 실패하는 PC 가 있다. 껐다 켜도 안 되는 사례가 실제로 있었다. 그래서 배포본에 둘을 넣었다 —

| 파일 | 하는 일 |
|---|---|
| `.vscode/settings.json` | 터미널 환경에 `.local\bin` 을 미리 얹는다 — **폴더를 여는 순간** uv 가 잡힌다 |
| `.vscode/tasks.json` | 「환경 점검」 — 폴더·uv 자리·버전 세 줄을 찍어 학생이 원인을 스스로 가른다 |

- **폴더로 열어야 적용된다.** 파일 하나만 끌어다 열면 둘 다 안 먹는다
- 태스크 명령에 **따옴표를 쓰지 않는다** — PowerShell 로 넘기면 겹따옴표가 벗겨져 진단이 오히려 에러를 낸다

## 4-5. 39명이 몰려도 안 터지는 장치

| 겹 | 장치 |
|---|---|
| 1 | 서버→AI **동시 호출 12건 관문** (`DIAGNOSE_CONCURRENCY=12`) — 나머지는 서버 안에서 줄을 선다 |
| 2 | 학생 1인당 **분당 12회** 제한 (연타 방지) |
| 3 | 리포트당 **AI 호출 6번 상한** (비용 폭주 방지) |
| 4 | 일시 오류 자동 재시도 · 죽은 키 10분 격리 |
| 5 | **설비별 진단 1회 캐시** — 매 회차 다시 부르던 것이 호출 폭증의 주범이었다 |
| 6 | 주입 시차 `--stagger 2` — 빼면 중앙 3.8초가 15.8초까지 늘어난다 |

> **40명이 같은 순간에 몰려도 되는가** — 실서버에서 재서 닫았다. **5-1-1절**을 보라.
> 요약: 39명 동시 진단 **11초에 39/39** · 폴링 40명 개인별 실패 0 · 폴링은 수업의 **6배(240명)까지 여유**.
> 키를 여러 개 넣어도 처리량은 안 는다(OpenAI 한도는 조직 단위) — 순환의 실효는 페일오버뿐이다.

## 4-6. 필수 프롬프트 — 이 세 문장이 39/39 를 만든다

`diagnoser.py` 프롬프트에 반드시 들어간다. **가이드에 원문으로 주고 학생이 복사해 넣는다.**

```
① "이 변화는 지금 작아도 계속 오른다."
   → 없으면 "조금 올랐네요, 지켜보죠" 하고 아무 조치도 안 낸다

② "원인이 정비 보류·지연이면 감속과 함께 로봇 파견 요청까지 낼 것.
   파견은 사람이 승인하므로 요청을 망설이지 말 것."
   → 없으면 감속만 하고 끝나서 승인 화면이 안 뜬다

③ "근거(evidence)에는 작업지시 번호를 그대로 인용할 것."
   → 없으면 WO 인용이 약 95% 로 떨어진다. 39명 중 2명이 하이라이트를 놓친다
```

---

# 5부 · 지금 상태와 남은 일

## 5-1. 어디까지 왔나

| 무엇 | 상태 |
|---|---|
| 슬라이드 | 2일차 **46장** · 3일차 **49장** · 노션 41장 — 레이아웃 검사 **0건** · **95장 전부 렌더를 한 장씩 눈으로 봤다**(결함 4건 발견·수정) |
| 실습 가이드 | 두 권 **전면 재작성** — 위에서 아래로 한 방향, 「감시자」 서사, 참고 답안 부록 삭제, 훈계 0 |
| 실습 코드 | **빈칸 방식** — `빈칸 1`~`빈칸 5` 의 `...` 만 고친다. 2일차 5줄 · 3일차 10줄. 새로 만들 줄도 지울 주석도 없다 |
| 이름 | `확인.py` · `돌려보기.py` · `--정답` (옛 이름 `점검.py`·`run.py`·`--열기` 도 그대로 받는다) |
| 화면 사진 | **전부 자동 생성** — 터미널은 `터미널그림.py`, 편집기 2장은 `편집기그림.py`. **사람이 찍을 것은 없다** |
| 검증 | 세 랩 `verify_lab` 전 항목 · 배포본 학생 조건(cp949) 전 항목 |
| 산출물 | **최종본 한자리** — `제작/산출물/경남대 AI 특강 (8월)/` 에 준비안내·가이드 두 권·실습 zip·슬라이드 2벌이 다 있다. 학생 실습파일은 그 옆 `산출물/k-precision-lab/` 에 **풀린 채로** 있고 zip 은 거기서 만들어진다 |
| 서버 | GCP **Compute Engine VM 한 대**(`knu-factory` · asia-northeast3-a · e2-small · `34.64.94.16`). GKE 아님 |
| 클라우드 리허설 | `--부하` **73건 전 항목** (39명 실모델 동시 진단 포함) |
| **40명 동시성** | **전 항목 통과** — 배터리 8/9 + 진단 단계는 따로 재실측해 39/39. 아래 5-1-1 |

### 5-1-1. 40명 동시성 — 실서버 실측 (원본: `강의/시뮬레이터/docs/부하테스트_결과.md` 3절)

옛 부하테스트는 **무료 티어·단일 PC** 조건이었다. 그 단서를 이번에 해소했다 —
**운영 VM 에 강사+39명이 같은 순간에** 수업 전 과정을 걸었고, 판정은 평균이 아니라 **개인별 장부**다.

| 단계 (전부 동시) | 결과 |
|---|---|
| 강사(S00 되찾기)+39명 배정 | 40명 전원 · 중복 0 |
| 배속 ×120 + 전체 드리프트 주입 | 폴링 40명 위에서 정상 |
| 39명 센서+정비 조회 | 39/39 · 전원 `WO-2026-0801` |
| **39명 AI 진단 (실제 OpenAI 중계)** | **39/39 · 전체 11초** (가장 빠른 사람 3초 · 가장 느린 사람 11초) |
| 39명 감속+로봇 파견 | 전원 접수 · 39곳 전수 대조 **교차 오염 0** · 남의 키 39곳 거부 |
| 폴링 40명 (위 단계들이 도는 내내 유지) | 화면 200장 · **개인별 실패 0 · 뒤처짐 0** · p95 53ms · 최대 72ms |
| 여유 탐사 (각 45초) | 80명 p95 19ms · 160명 p95 26ms(최대 116ms) · **240명 p95 21ms · 셋 다 실패 0** |

**결론 — 화면 폴링은 수업(40명)의 6배인 240명까지 실패 0. VM 증설 불필요.**

**한 곳만 위험하다 — OpenAI 호출이 몰릴 때다.**

나머지는 우리 VM 안에서 끝나는 일이라 몰려도 줄만 서면 된다. 그런데 진단은 **바깥 한도**에 걸린다.
그리고 그 한도는 **키 단위가 아니라 조직·프로젝트 단위**다 — 키를 여러 개 발급해도 처리량은 그대로다.
그래서 서버가 할 수 있는 일은 **키를 바꾸는 것이 아니라 줄을 세우는 것**뿐이고, 그렇게 짜여 있다.
39명이 동시에 눌러도 12명씩 세 묶음으로 나가서 **11초에 전원 성공**한 것이 그 결과다.

**한 번, 39명 전원이 실패한 적이 있다 (2026-08-19 낮).** 같은 배터리의 진단 단계가 `0/39 · HTTP 503` 으로 찍혔다.
같은 서버에 같은 스키마·같은 프롬프트로 다시 걸었을 때는 **39/39 · 11초** 로 정상이었고, **재현되지 않았다.**
원인은 확정하지 못했다. 서버 설정(키·`DIAGNOSE_ENABLED`)은 그 전부터 계속 정상이었으므로 그쪽은 아니다.
**모르는 것을 안다고 적지 않는다** — 다만 이 한 줄은 남긴다: *진단이 통째로 안 되는 순간이 있을 수 있다.*
그때도 수업은 멈추지 않는다. 학생 쪽 `diagnoser.py` 가 **`rules` 로 떨어져 계속 돌기 때문**이다.
당일 진단이 전부 실패하면 그 대체 경로로 진행하고, 강사는 리모컨에서 상태만 확인하면 된다.

**몰림 대비 로직** (코드 감사 완료)

- 진단 중계 — 테넌트당 **분당 12회**(429) · 전역 **동시 12 세마포어**(나머지는 서버 안에서 줄을 선다) ·
  429/503 **지수 백오프 4회**(1·2·4초+흔들기) · 실패하면 학생 쪽은 `원인(rules)` 로 이어간다
- 배정 장부 — 원자적 쓰기(tmp→rename) · 깨지면 원본 보관 후 재시작
- 프로세스 — `Restart=always`
- **키 순환의 한계** — OpenAI RPM/TPM 은 **키가 아니라 조직·프로젝트 단위**다. 같은 조직 키를
  여러 개 돌려도 처리량은 안 는다. 순환의 실효는 **페일오버뿐**. 처리량을 정말 늘려야 하면
  키 추가가 아니라 **usage tier 승급**이 맞는 길이다. 지금 키 1개로 실측 39/39 이므로 충분하다
- **유령 배정 1건이면 마지막 학생이 「자리 없음」을 본다**(실측 재현) → 아침 점검 4번으로 방어

## 5-2. 마지막에 만진 것 — 이어받는 자리

**자료를 고쳤으면 반드시 4-3절 사슬을 끝까지 돌린다.** 중간에 멈추면 네 곳(원본·PDF·zip·바탕화면)이 어긋난다.

- **VM 동기화** — push 해도 VM 은 저절로 안 바뀐다.
  ```
  gcloud compute ssh knu-factory --zone=asia-northeast3-a     --command="cd ~/knu-agentic-factory && git pull && sudo systemctl restart factory"
  ```
- **강사 실습 폴더** — 바탕화면 `실습해보기` 는 최종본 안의 실습 zip 을 푼 것이다.
  다시 뽑을 때 **작업 중이던 `detect.py` 와 `config.json`(S00)은 보존**해야 한다.
- **강사 공장은 `S00`** — `uv run 내번호.py S00` 으로 받는다. 그냥 치면 **학생 자리를 하나 먹는다.**

## 5-3. 슬라이드를 고치려면

**이 문서 하나면 된다.** 슬라이드는 1부(규칙·문체) · 3부(덱빌드 리소스) · 4-3절(빌드 순서),
가이드는 1부 2절(넣지 않는 것) · 3-3절(md2pdf 디자인) · 4-3절을 보면 된다.

전에 `슬라이드_작업안내.md`·`실습가이드_작업안내.md` 라는 발췌본을 만들었다가 **지웠다** —
규칙 원본이 둘로 갈리면 한쪽만 고쳐져 어긋난다. 실제로 그 사고(검사기 넷이 옛 표식을
세고 있던 것)를 당했다. **규칙은 이 문서 하나에만 둔다.**

## 5-4. 외부 검토 — 전량 처리했다


슬라이드 pptx 와 가이드 PDF 를 외부에 넘겨 세 번 검토받았다(덱 22건 · pptx 파일검사 11건 ·
가이드 12건). **원문은 채팅으로만 왔고 저장소에 없다** — 그래서 처리 결과를 여기 남긴다.

**고친 것** — 그림 5(11≡13장 · 14≡29장 · 20≡23장 중복 · `SPLIT3` 되돌아가는 화살표 ·
`CLOSED` 「확인」→「다시 인지」) · 아이콘 3 · 표기 5(식별자 줄바꿈 · 고정폭 · 정답 명령 인자 ·
`&nbsp;` · 부제 모순) · 사실 6(19장 진동 · 27장 방화벽 · 표지 부제 · `fallback` · 「진짜 설비」) ·
누락 4(2장 AI 비교 회수 · 43장 정지 · 44장 `n` · 47장 제목).

**본문 크기(26/27/29/30px)는 그대로 둔다** — 표·비교형·촘촘한표에 각각 의도가 있고,
통일하면 레이아웃검사가 넘침을 잡는다.

22장 실물 화면도 채워졌다 — `편집기그림.py` 가 실제 파일 내용으로 그린다.
48·49장의 「내일」 혼란은 **「우리 이틀은 여기까지 / 마지막 1미터는 내일 이어집니다」**로 닫았다.
**4일차는 우리 영역이 아니다** — 담당 강사 이름을 받아 넣지 않는다.

### 그때 자동 검사가 못 잡아 눈으로만 나온 것 — 반드시 눈으로 볼 것

`레이아웃검사.py` 는 아래 여섯을 **전부 통과시켰다.** 넘침·잘림은 잡지만 **뜻은 못 본다.**

| 어디 | 무엇 |
|---|---|
| 3일차 26장 | **다음 장(28장) 그림을 미리 쓰고 있었다** — 반전 유출 |
| 3일차 4장 | 34장에서 뜻이 확정되는 그림(`NOUNDO`)을 미리 다른 뜻으로 씀 |
| 3일차 30장 | 화살촉이 「감지」 **글자를 뚫고** 있었다 |
| 3일차 36장 | 라벨이 겹쳐 「우리가 그은 선전부 승인」으로 읽혔다 |
| 3일차 27·44장 | 첫 열 3줄 접힘 · 「셋」이라 써 놓고 표는 네 행 |
| 덱 전체 | 자동 치환이 만든 `<code>` **중첩** 5곳 |

### 실측으로 뒤집힌 것 — 추측으로 되돌리지 말 것

**「창을 안 자르면 12분 뒤 +0.50℃ 로 주저앉아 미탐이 된다」는 틀렸다.**
수치 자체는 맞지만(실측 +0.51℃) 임계가 **0.4℃** 라 그 값은 **여전히 잡힌다.**

| 이상이 끝나고 | 창을 자르면 | 안 자르면 |
|---|---|---|
| 0분 | **+1.20℃ 잡음** | +0.51℃ 잡음 |
| 180분 | **조용해짐** | +0.52℃ 계속 울림 |
| 300분 | 조용해짐 | +0.52℃ 계속 울림 |

자르는 진짜 이유는 둘이다 — **신호가 +1.2℃ 로 또렷해지고**, **이상이 끝나면 스스로 조용해진다.**
안 자르면 감속이 먹혀 온도가 내려간 뒤에도 계속 울려 **가이드 11번의 「고리가 닫혔다」를 못 본다.**
그리고 `watch.minutes = 12` 라 **창은 처음 12분 뒤로는 안 길어진다** — 「오래 돌릴수록 길어진다」도 틀렸다.

### 딥리서치 권고를 어디에 반영했는가 (이력)

`검증리서치/04_딥리서치_결과.md` 가 1차 출처다. 그 권고가 실제로 어디에 들어갔는지는 이렇다.

| 권고 | 어디에 |
|---|---|
| `EQ-05` 74℃ 방어 — **측정점을 명시**(하우징 아님, 베어링 내부) | 2일차 가이드 2절 · 공장 화면 표 머리글 「베어링 온도」 · 진행.md 답변 |
| 「하루 12℃ 상당」 금지 — **「4시간에 2℃」로만** | 1부 6절 · 2일차 진행.md 에 금지로 명기 |
| 「이 공장은 온도만 본다」를 밝힐 것 (진동이 더 이른 지표) | 2일차 15장 부제 · 12장 표 「진동이 먼저 달라지기 시작한다」 |
| 눈으로 찾기에 **상한** + 「왜 못 찾았나」 언어화 | 2일차 가이드 3절(30초) · 24장 |
| 세 함수를 **난이도 점증**으로 두고 그 의도를 밝힐 것 | 1부 7절 · `detect.py` 주석 세 자리 |
| z-score 미탐을 「발견」으로 프레이밍 + **EWMA/CUSUM 예고** | 2일차 42장 · 3일차 47장 |
| 손코딩↔AI 비교를 **정탐/오탐/미탐 숫자로** | 2일차 가이드 11절 표 세 줄 · 38장 |
| 「종이에 적고 대조」 제거 (실측상 성립 안 함) | 2일차 가이드에서 삭제 |
| 「못 찾는 것이 정답」에 걸지 말 것 | 2일차 가이드 3-② · 24장 「짚었다면 근거를 숫자로」 |

## 5-5. 강사 몫

1. **실습 완주** — 학생과 같은 조건(드라이브 ZIP → **폴더로** 열기 · 강사 공장 `S00`)으로
   **가이드 두 권만 보고** 이틀치를 끝까지. 한 번이라도 「다음에 뭘 하지?」 하고 멈추면
   그 자리가 문서 결함이다
2. **화면은 전부 자동 생성** — 터미널은 `터미널그림.py`, 편집기 두 장은 `편집기그림.py`
   (실제 파일 내용을 읽어 그린다 — 파일이 바뀌면 그림도 다시 뽑는다). 사람이 찍을 것은 없다
3. **강의 리허설** — 슬라이드를 넘기며 구간별 시간을 실측 (2일차 230분 · 3일차 220분)
3-1. **강의장 네트워크 확인** — 학생 와이파이에서 서버가 열리는지 (진행.md 아침 점검 5번).
   서버·자료·동시성은 전부 실측으로 닫았다 — **남은 미검증은 이 둘뿐이고 둘 다 사람 몫이다**
4. **드라이브 업로드** — ZIP 을 풀어 **옛 파일을 지우고** 교체. 가이드가 두 권으로 갈리고
   강의 자료가 `4.` → `5.` 로 밀렸다
5. **제출처(노션) 확정** — 안내문·진행 문서의 `[ 제출처 ]` 채우기
6. **D-7 강의장 점검** — 서버가 열리는지 · 실습실 PC 에서 `uv` 설치가 권한에 막히지 않는지
