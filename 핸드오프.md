# 핸드오프 — 경남대 8월 특강 (1일차 · 2일차)

작성 2026-08-10 · **이 문서 하나만 읽고 이어받을 수 있게 씀. 숨긴 것 없음.**
읽는 순서 — 1 → 2 → 3 을 먼저 읽고, 작업할 때 4~7 을 참조한다.

---

# 1. 무슨 일을 하는 중인가

경남대 컴퓨터공학과 **1~4학년 39명**에게 **이틀(하루 4시간)** 강의한다.
이 저장소는 **그 이틀만** 다룬다. 앞뒤로 대표님 세션과 PLC 강사 세션이 붙지만 우리 범위가 아니다.

| | 제목 | 학생이 하는 일 |
|---|---|---|
| **1일차** | **잡기** | 이상감지 알고리즘을 **직접 짜고**, 그것이 어디까지 되는지 확인한다 |
| **2일차** | **움직이기** | 내 코드를 AI 의 도구로 내놓고, AI 가 판단해 **설비를 실제로 움직이게** 한다 |

목적은 지식 전달이 아니라 **9월 본과정 이탈 방지**다.
"얼마나 가르쳤나"가 아니라 **"끝까지 남았나"** 로 성패가 갈린다.

## 이야기 흐름

**1일차** — 새벽 3시 EQ-03 사건 → 사람이 못 본 이유 셋 → *"80도 넘으면 알람 걸면 되잖아"*
→ **★ 반전: 80도를 넘은 적이 없다** → 내 공장 열어보기 → 이상감지 직접 구현
→ **학생이 직접 두 번째 반전을 발견** → *"규칙으론 못 잡습니다. 그럼 누가 판단하죠?"*

**2일차** — 1일차 회수 → 환각 → 에이전트·ReAct → MCP 도구 2개
→ **AI 가 WO-2026-0801 을 원인으로 지목** → 역할 분해 → HITL
→ 폐루프 일곱 걸음 → **승인 누르면 AMR 이 실제로 움직임**
→ *"그런데 이건 전부 소프트웨어입니다"*

교안의 **「4비트 학습 루프」**(작은 성공 → 의도된 불편 → 대조 해결 → 자산화)를 이틀 다 지킨다.

---

# 2. 절대 규칙 — 어기면 수업이 무너진다

| # | 규칙 | 왜 |
|---|---|---|
| 1 | **개인 단위. 팀·PBL 없음** | 교안의 팀 챌린지는 마지막 날 것이었고 그 날이 PLC 세션으로 바뀌었다. PBL 은 9월 본과정 몫 |
| 2 | **발표·시연·투표·시상 없음** | 39명 중 2명만 앞에 세우면 나머지는 구경꾼이 되고 지목당할까 봐 긴장한다. 대신 **"다 같이 엔터"** |
| 3 | **39명 전원이 같은 장면을 본다** | 창의성으로 갈리는 구간을 만들지 않는다. 결정적인 문장은 전부 가이드에 원문으로 준다 |
| 4 | **이탈 절대 방지** | 막힌 학생도 다음 단계로 갈 수 있어야 한다. 1일차를 못 끝내도 2일차를 시작할 수 있어야 한다 |
| 5 | **반전은 학생이 발견한다** | 어떤 이상이 잡히고 안 잡히는지 **강의자료에 한 글자도 쓰지 않는다.** 오탐 수치만 이론으로 다룬다 |
| 6 | **AI 없이 조용히 넘어가지 않는다** | 진단이 안 되면 학생이 **알 수 있게** 한다. 규칙 폴백은 강사가 직접 켤 때만 |
| 7 | **키는 학생에게 주지 않는다** | 회수가 안 된다. 서버가 OpenAI 키를 쥐고 중계한다 |
| 8 | **대본이 없다** | 슬라이드만 보고 뜻이 통해야 한다. 삽화 안 라벨이 대본 노릇을 한다 |
| 9 | **무대는 K-정밀 하나** | 창원 가상 정밀기계 제조사 · CNC 6대(EQ-01~06) · AMR 2대. 여기 없는 숫자를 지어내지 않는다 |
| 10 | **장난감이 아니다** | z-score 를 라이브러리 없이 직접 짠다. 표본표준편차·σ=0·결측 처리 — 실무에서 진짜 터지는 것들 |

## 손코딩 원칙 — 두 스트레스를 가른다

**없애는 것: 막히는 스트레스.** 문법 오류로 30분 날림 · 왜 안 되는지 모름 · 남들 다 됐는데 나만 안 됨.
**남기는 것: 생각하는 스트레스.** *"왜 이건 안 잡히지?"* · *"민감하게 했는데 왜 더 나빠지지?"*

| | 손코딩 | 나머지 |
|---|---|---|
| 1일차 | **25분** (함수 3개, 각 3~8줄) | 관찰 · 숫자 흔들기 · 비교 |
| 2일차 | **거의 0** — 전부 Claude 와 | 진단 · 판단 · 관찰 |

**타이핑 난이도는 내리고 판단 난이도는 올린다.**
학년 편차 대응 — 못 따라오면 `점검.py --열기` 로 함수 하나씩, 먼저 끝내면 가이드 부록의 선택 과제.

## 절대 바꾸면 안 되는 수치

실습 코드·데이터와 묶여 있다. 바꾸려면 양쪽을 같이 바꿔야 한다.

| | 값 |
|---|---|
| 드리프트 | **0.5℃/h** · 62 → 64℃ · 고정 임계값 **80℃ 를 끝까지 안 넘음** |
| 데이터 | **60,480행** (CNC 6대 × 7일 × 1분). 이상 3종이 심어져 있음 |
| 오탐 실측 | 기준선 **0.94%** · k=3.0 **566행 = 하루 81건** · k=2.0 **6,374행 = 하루 911건(11배)** · k=4.0 40행 |
| 실습 결과 | 스파이크 3/3 · 드리프트 241 중 5(2.1%) · 결측 0/121 ※ **강의자료에 쓰지 말 것** |
| 정비 이력 | **WO-2026-0801** 「부품 입고 지연으로 보류」 |
| 제어 도구 | `set_equipment_speed` · `ack_alarm` (자동) / `stop_equipment` · `dispatch_robot` (**사람 승인**) |
| 감지 소요 | 배속 x120 기준 **약 100초** (x60 이면 190초) |
| 진단 응답 | 중앙 **3.8초** · 평균 출력 약 970자 |

---

# 3. 지금 어디까지 됐나

## 완료 — 실행으로 증명됨

| 무엇 | 결과 |
|---|---|
| **39명 동시 진단** (본번 조건: 2초 시차·동시성 12) | **39/39 성공 · 429 0건 · 중앙 3.8초 · 전체 81초** |
| **전원이 같은 장면을 보는가** | **WO 인용 39/39 · 감속 39/39 · 파견 요청 39/39** |
| 최악 조건 (전원 동시) | 39/39 성공. 단 중앙 15.8초·최대 30초 → **`--stagger 2` 필수** |
| 학생 환경(cp949)에서 완주 | ✅ `exit=0` |
| 실측 수치 = 강의자료 수치 | ✅ 0.94% · 566행 · 6,374행 |
| 못 채운 학생도 완주 | ✅ `점검.py --열기` 3회 → `run.py` 정상 |
| 전 파일 문법·JSON | ✅ 오류 0 |
| 옛 이름 잔재 | ✅ 0건 |

## 만들어진 것

| | |
|---|---|
| **강의자료** | 1일차 덱 36장 · 2일차 덱 37장 (기준판 v2 · 1600×900 · SVG 삽화) + 원고·설명란 각 2벌 |
| **실습 가이드** | 1일차 · 2일차 · 강사진행표 |
| **이탈 방지** | `점검.py` (1일차·2일차) — 진단 → 힌트 2단계 → 함수 하나씩 공개 |
| **진단 중계** | `server/app/api/diagnose.py` — 서버가 키를 쥔다 |
| **부하 테스트** | `tools/부하테스트_진단.py` — 39명 재현 |

## 남은 일 (우선순위 순)

| # | 무엇 | 왜 막혀 있나 |
|---|---|---|
| 1 | **`리허설.py` 재작성** | 4일치를 재생하도록 짜여 있다. 삭제된 랩(스펙·바이브코딩)을 참조한다. 경로는 고쳤지만 **구조는 그대로** |
| 2 | **`2일차 점검.py` 실행 검증** | 서버가 떠 있어야 한다. 아직 한 번도 못 돌렸다 |
| 3 | **W6 폐루프 실주행** | 일곱 걸음이 실제로 도는지. 서버 + 제어 개방 필요 |
| 4 | **학생 저장소(`k-precision-lab`) 동기화** | 폴더 구조가 통째로 바뀌었다. 배포본을 새 구조로 다시 만들어야 한다 |
| 5 | **가이드에 `점검.py` 반영** | 가이드가 아직 `점검.py` 를 안내하지 않는다 |
| 6 | **필수 프롬프트 세 번째 문장 추가** | 아래 §5 참조. 이게 WO 인용을 39/39 로 만든 결정적 한 줄인데 가이드에 아직 없다 |
| 7 | 사전안내문(W7) 갱신 | pandas 설치·노션 가입 안내가 빠져 있다 |

---

# 4. 뭐가 어디 있나

```
경남대특강/
├─ README.md          전체 지도 (5분)
├─ 핸드오프.md          ← 지금 이 문서
├─ 서버운영.md          서버 띄우는 법
│
├─ 산출물/             ★ 수업에 실제로 쓰이는 것
│  ├─ 공통/
│  │  ├─ 시뮬레이터/      가상 공장. 서버 + 2D 화면 + 강사 콘솔 + 진단 중계
│  │  ├─ 센서데이터/      7일치 60,480행 CSV (이상 3종)
│  │  ├─ 사전안내문/      D-7 학생 발송용
│  │  ├─ 인쇄카드/        힌트 카드 · 최소 경로 카드
│  │  ├─ 리허설.py        ⚠ 4일치 기준. 재작성 필요
│  │  └─ 콘솔인코딩.py     cp949 에서 출력이 깨져 죽는 것을 막는다
│  ├─ 1일차_잡기/
│  │  ├─ 이상감지/        detect.py(TODO 3) · run.py · 점검.py · 정답/
│  │  └─ 힌트카드.md
│  └─ 2일차_움직이기/
│     ├─ 도구만들기/      mcp_server.py(★2) · 점검.py · 정답/
│     └─ 폐루프/          agents/(★3) · loop.py · hitl.py · 정답/
│
└─ 작업장/
   ├─ 강의자료/          덱 빌드 + 원고·설명란 (한 소스에서 같이 나옴)
   ├─ 가이드/            실습 가이드 2벌 + 강사진행표
   ├─ 기준판/            디자인 규격 원본 (PDF + HTML)
   ├─ 참고자료/          교안 · 리서치 · 견적확정서 · 전수조사 기록
   └─ 기록/              검증 기록
```

## 폴더 사이 의존 — 옮기면 깨진다

```
2일차/도구만들기/mcp_server.py  →  1일차_잡기/이상감지/detect.py   (import)
2일차/도구만들기/config.json    →  공통/센서데이터/데이터/*.csv     (csv_path)
1일차/이상감지/run.py           →  공통/센서데이터/데이터/*.csv
```

**2일차가 1일차 코드를 그대로 불러 쓴다.** 이건 버그가 아니라 설계다 —
*"1일차에 짠 코드를 오늘 AI 의 도구로 내놓는다"* 가 2일차의 핵심 장면이다.

---

# 5. 핵심 설계 — 왜 이렇게 만들었는지

## 진단 중계 — 학생 PC 에 LLM 키를 두지 않는다

```
학생 PC ──(내 접속 키)──> 강사 서버 ──(OPENAI_API_KEY)──> OpenAI
```

39명에게 키를 나눠 주면 **회수가 안 된다.** 종이로 나간 키는 사진으로 남는다.
서버가 중계하면 키는 서버 `.env` 에만 있고, 특강이 끝나면 서버만 내리면 된다.

**프롬프트는 서버가 만들지 않는다.** 학생이 `diagnoser.py` 에서 고친 프롬프트가 그대로 올라가야
실습이 성립한다. 서버는 **받은 프롬프트를 그대로 넘기는 중계**다. 대신 응답을 스키마로 강제하고,
분당 한도와 토큰 상한을 건다.

**한도 관련 사실** — OpenAI 한도는 **조직 단위**다. 같은 조직의 키를 여러 개 만들어도 한도가 늘지 않는다.
늘리려면 티어를 올려야 한다. 다만 **우리 부하는 분당 13~25 요청이라 한도 근처도 안 간다. 키 1개로 충분.**
(코드는 쉼표로 여러 개도 받는다 — 한도가 아니라 키 사고 대비용)

## 39명이 몰려도 안 터지는 장치 네 겹

1. **동시 12개로 줄 세우기** (`DIAGNOSE_CONCURRENCY=12`). 20으로 올려도 개선 없음 — 병목이 우리 쪽이 아니다
2. **429 면 1·2·4초 백오프 재시도**
3. **설비별 진단 1회 캐시** (`loop.py`). 매 회차 다시 부르던 것이 호출 폭증의 주범이었다
4. **주입 시차** — `제어개방.py --inject-all --stagger 2`

## 필수 프롬프트 — 이 세 문장이 39/39 를 만든다

`diagnoser.py` 프롬프트에 반드시 들어가야 한다. **가이드에 원문으로 주고, 학생은 복사해 넣는다.**
창의적으로 쓰게 두면 안 된다 — 결과가 갈린다.

```
① "이 변화는 지금 작아도 계속 오른다."
   → 없으면 LLM 이 "조금 올랐네요, 지켜보죠"라며 아무 조치도 안 낸다

② "원인이 정비 보류·지연이면 감속과 함께 로봇 파견 요청까지 낼 것.
   파견은 사람이 승인하므로 요청을 망설이지 말 것."
   → 없으면 감속만 하고 끝나서 승인 화면이 안 뜬다

③ "근거(evidence)에는 작업지시 번호를 그대로 인용할 것."
   → 없으면 WO 인용이 약 95% 로 떨어진다. 39명 중 2명이 하이라이트를 놓친다
```

**③ 은 아직 가이드에 없다. 넣어야 한다.**

## 이탈 방지 4단계

```
점검.py            내 코드를 돌려보고 어디가 왜 틀렸는지만 짚는다 (답은 안 준다)
점검.py --힌트 1    개념
점검.py --힌트 2    의사코드
점검.py --열기 N    ★ 시간이 다 됐을 때만. 막힌 함수 하나만 완성본으로
```

일부러 흔한 실수 5종을 심어 검증했고 전부 정확히 잡았다 —
지금 값 포함 · n 으로 나눔 · abs 누락 · σ=0 미처리 · 결측 제거로 길이 변함.

---

# 6. 함정 — 겪고 고친 것들. 다시 밟지 말 것

| 함정 | 내용 |
|---|---|
| **cp949 인코딩** | `—` `→` 출력에서 `UnicodeEncodeError` 로 실습이 **중간에 죽었다.** `리허설.py` 는 자식 프로세스에 `PYTHONUTF8=1` 을 주입해 돌아 이 문제가 안 보였다. **학생은 그 환경변수가 없다.** 진입점 17개에 UTF-8 가드를 넣었다. **새 진입점을 만들면 반드시 같은 가드를 넣을 것** |
| **pandas 미설치** | 1일차 랩에 `requirements.txt` 가 없었고 사전안내문은 Python 만 설치했다. 첫 명령에서 전원이 `ModuleNotFoundError`. 리허설은 강사 PC 에 pandas 가 있어 못 잡았다 |
| **`--열기` 후 NameError** | 완성본이 `math.sqrt` 를 쓰는데 `detect.py` 에 `import math` 가 없었다. **마지막 수단을 썼는데 또 막히는** 최악의 경우. 이제 필요한 import 를 같이 넣는다 |
| **문서만 읽고 산출물을 안 봄** | 교안에서 "팀 챌린지"를 읽고 "개인으로 축소"라고 문서에 썼지만, **W6 config 의 `tenant: "T1"`, `제어개방.py` 의 팀 분기는 그대로였다.** 결정을 내렸으면 **파일까지 따라가서 확인할 것** |
| **읽기만 하고 실행 안 함** | 위 세 개는 전부 **처음 실제로 돌려본 순간** 나왔다. 읽어서는 안 나온다 |
| **조용한 폴백** | 진단 실패 시 규칙으로 조용히 대체하면 학생은 **AI 진단을 못 봤다는 것 자체를 모른다.** 오늘의 핵심 장면인데. 알리도록 바꿨다 |

---

# 7. 자주 쓰는 명령

## 서버

```bash
cd 산출물/공통/시뮬레이터
python -m server.run                 # 기동 (시작.bat 도 가능)
python tools/제어개방.py               # 제어 개방 + 배속 120
python tools/제어개방.py --inject-all --stagger 2   # 39명에게 주입
python tools/제어개방.py --end         # 제어 잠금 · 주입 중단 · 배속 원복
python tools/키배포표.py               # 학생 쪽지 (개인만)
python tools/부하테스트_진단.py --n 39 --stagger 2
```

`.env` 필요 항목 — `SUPABASE_DB_URL` · `INSTRUCTOR_TOKEN` · `OPENAI_API_KEY` ·
`DIAGNOSE_CONCURRENCY=12` · `DIAGNOSE_PER_MIN=12`
**루트 `.env` 와 시뮬레이터 `.env` 에 실제 값이 들어 있다. 커밋 금지.**

## 학생 실습

```bash
# 1일차
cd 산출물/1일차_잡기/이상감지
pip install -r requirements.txt
python 점검.py            # 내 코드 점검
python run.py             # 7일치에 돌리기
python run.py --k 2.0     # 임계값 흔들기

# 2일차
cd 산출물/2일차_움직이기/도구만들기
python 점검.py
python mcp_server.py --check
cd ../폐루프
python loop.py
```

## 강의자료

```bash
cd 작업장/강의자료
python build_1일차.py     # 덱 HTML
python emit_docs.py       # 원고·설명란 (같은 소스에서 — 어긋날 수 없다)
python render.py 1일차덱   # PNG
```

**PNG·HTML 을 손으로 고치지 말 것.** 빌드 스크립트가 원본이다.

---

# 8. 놓치면 안 되는 것

- **문서를 읽었다고 확인한 것이 아니다.** 돌려봐야 안다. 이번에 나온 치명적 결함 셋이 전부 실행에서 나왔다
- **결정을 내렸으면 파일까지 따라가라.** "개인으로 바꾼다"고 쓰고 config 는 그대로 두면 당일 터진다
- **반전 유출 금지.** 어떤 이상이 잡히고 안 잡히는지 강의자료에 쓰지 않는다. 학생이 `run.py` 결과로 발견한다
- **39명 전원이 같은 장면을 본다.** 여기서 갈리면 실습 실패다
- **`k-precision-lab`(학생 배포본)이 아직 옛 구조다.** 새 구조로 다시 만들어야 한다
