# 핸드오프 — 경남대 8월 특강 (1일차 · 2일차)

작성 2026-08-10 · 갱신 2026-08-12 · **이 문서 하나만 읽고 이어받을 수 있게 씀. 숨긴 것 없음.**

---

# ★ 이어받는 사람이 맨 먼저 읽을 것 (2026-08-12 인계)

## ◆ 최신 인계 — 여기부터. 아래 절들보다 이것이 최신이다

### 2026-08-12 에 끝난 것 — 전부 실행으로 확인했다

**① 구조 개편 완료** — 최상위가 「하는 것 / 만드는 것」 둘로 갈라졌다. `백스테이지` 라는 이름은 없다.

```
특강/     당일/ · 학생배포/ · 시뮬레이터/(서버 — 당일에도 돌림) · 강의안/(docx 2)
제작/     덱빌드/ 기준판/ 강의안원본/ 참고자료/ 기록/
          + 검증도구/(리허설·배포본만들기·센서데이터) + 빌드도구/(pptx·드라이브·공유본·md2pdf)
          + 산출물/(통째 gitignore)
README.md · 핸드오프.md 는 루트.
```

전부 `git mv`(이력 보존). 경로 참조 전수 수정 — 스크립트 ROOT/REPO 사슬, **학생 코드
데이터 탐색 4곳**(`제작/검증도구/센서데이터/데이터`, 탐색 깊이 +1), 시작.bat,
cloud_deploy.sh(옛 `W1_*` 탐색 사슬 제거), .gitignore(`제작/산출물/`·`특강/당일/*.pptx`,
`git check-ignore` 로 배정장부·.env·산출물 차단 확인함), 진행표·서버운영·일차별안내·README·절차서.
개편 후 실측: **배포본 26/26 · 리허설 75/75(10:52·건너뜀 0)** 통과. 커밋 `834c6d6`.

**★ 절차서 §13-3-b 신설** — VM 에서 개편 후 첫 `git pull` 을 하면 추적 파일만 옮겨가고
**untracked 인 `.env`·`.venv` 는 옛 폴더에 남아 서버가 못 뜬다.** systemd 유닛 경로 수정까지
포함한 이관 명령을 절차서에 적어 뒀다. (절차서의 clone 경로에 있던 잘못된 `경남대특강/` 층도
제거 — 리모트 루트를 `git ls-tree origin/main` 으로 실제 확인한 결과다.)

**② 슬라이드 증량 완료** — 1일차 **42장** · 2일차 **44장**. 렌더링·emit_docs 끝.
2일차 신규 7장: 어제 자가 못 잡은 것(3) · 환각은 버그가 아닙니다(7) · 생각을 소리 내어(13) ·
도구 M×N(16) · 사람이 쓴 문장(20 — WO 번호 없음) · 감속이 첫 수(31) · 이틀의 지도(42).
**깨져 있던 「폐루프는 일곱 걸음」(현 37장)은 기준판에 「촘촘한 표형(.dense)」을 신설해 수리**
— 7행 표가 기본 규격으로는 약 280px 넘친다는 실측에 근거. 「패턴은 다섯」(15장)도 같은 수리.
「돌려 보는 법」(1일차 34장)의 명령 중간 줄바꿈(`-- / window`)은 nowrap 으로 제거.
cmp/pic 삽화 축척 3건 교정. **수리·신규·의심 장 전부 PNG 를 열어 눈으로 확인했다.**
밀도 실측: 설명 구간 장당 **1.7~2.5분** — 전 구간 목표(≤2.5) 달성.
반전 유출 없음(2일차 3장의 「어제 못 잡았다」 회수는 유출 아님 · 20장에 WO 번호 없음).

**③ 장 번호 문서 전부 갱신 + 기계 대조** — 진행표 표 2개(42장·44장), 일차별안내 2개(구간·분·장당),
강의안 대외 md(42·44·29장 제어·38장 주입), 설명대본. **진행표↔원고 제목 기계 대조 전 행 일치**
(스크립트는 scratchpad 방식 — 원고 `## N장 · 제목` 과 진행표 표 행을 나란히 비교).
실라버스 공유본은 슬라이드 번호 독립이라 손대지 않았다(그대로 최종본).

**④ 학생 워크스루 완주 — ZIP·clone 두 경로 다** (cp949 · `PYTHONIOENCODING` 제거 · 가이드 명령만):
`pip install` → `내번호.py`(실주소 자동 채움) → 공장 화면 200 → `점검.py`/`--힌트`/`--열기 1·2·3`
→ `run.py` **0.94% · 567행 · 하루 81번 · 드리프트 4/240 · 스파이크 3/3 · 결측 0/120**
→ `--k 2.0` **6,375행 · 911번** → 2일차 `점검.py --열기 1·2` → `mcp_server.py --check`
→ **`agent.py` 에서 AI 가 detect_anomaly×6 → query_equipment×6 를 스스로 부르고
WO-2026-0801 을 원인으로 인용** → 폐루프 `--check`(제어 잠김을 사람 말로) → 제어개방 →
`loop.py --열기 전부` + 주입 → 감지 → `원인(server)`·WO 인용 → 감속(자동, 1796→1437rpm) →
승인 요청 [y/N] → 승인 → **AMR-01 이 EQ-03 로 이동.** 두 경로 모두 마지막 장면까지.
(주의 — 파이프로 주소를 넣으면 BOM 이 붙어 InvalidURL 이 난다. 학생은 손으로 치므로 무관하고,
스크립트는 역추적 없이 사람 말로 세우는 것까지 확인했다.)

**⑤ 학생 저장소 push 완료** — `k-precision-lab` 커밋 `e1325a1`. 바뀐 것은 데이터 탐색 경로
3파일뿐(학생 동작 무변화 — 배포본은 `데이터/` 를 쓴다). clone 워크스루가 이 커밋으로 완주했다.

**⑥ 그 밖에 이번에 잡은 것** — `cloud_deploy.sh` 의 옛 경로(`산출물/강사용/W1_…`)와
「쪽지 재인쇄」 문구 · 진행표의 학생 시점 경로 4곳(학생 저장소에 없는 `학생배포/` 접두사) ·
서버운영.md 의 낡은 「24항목」 2곳 → 26 · 센서데이터 README 의 **없는 옵션 `--w1-config`** →
`--sim-config` · 같은 README 의 새벽 3시 온도 서술을 실측(61.0→61.6)과 라이브(62→64) 구분으로 ·
`덱B 33장` 잔재 주석 9곳(view/index.html·read.py·API 명세)을 현재 장 제목 기준으로 ·
루트 `D:\work\study\.env` 주석의 두 세대 전 경로 ·
**★ 최소경로 카드가 폐지된 제도 둘을 안내**(「막힘 티켓」= 4일 과정 팀 제도 · 「확장 미션」)
→ 손 들기·최소 조합 문구로, `access_key` 행도 실제 출력·`내번호.py` 조치로 교정 ·
도구만들기 README 파일 목록에 `agent.py` 누락 → 추가 ·
**대외 강의안 docx 2개의 장수·장번호를 서식 보존 치환으로 갱신**(36→42 · 37→44 ·
24장 제어→29장 · 32장 주입→38장. 런 경계에 걸친 문자열은 런 병합 치환 — 잔존 0 확인.
docx 의 Claude·실측치는 앞 세션에서 이미 교정돼 있었음을 스캔으로 확인).

**⑦ 마무리 체인 실측 (2026-08-12 · 전부 이 순서로 실제 실행)**
— 배포본만들기 `--검증` **26/26** → 리허설 `--부하` **75/75 · 10:46 · 건너뜀 0**
(39명 동시 진단 429 0건 · WO 인용 39/39) → pptx 42·44장 → 드라이브폴더(ZIP 1개·7파일·9.3MB)
→ 공유본 PDF+docx → **재렌더링 후 git diff 0**(원고↔PNG 픽셀 일치) → 두 저장소 push.
리허설 통과 뒤 학생 md 문구 3곳(위 ⑥의 카드·README)만 더 고쳤고 — 실행 경로와 코드는
불변이라 리허설 재실행은 하지 않았다. 그 수정을 포함한 배포본은 `--검증` 26/26 을 다시 통과했고,
학생 저장소에 push 했다(`b7cf1c2`).

**⑧ 인쇄 전제 폐기 (강사 지시 · 2026-08-12)** — 학생에게 종이로 나가는 것은 없다.
**전달은 단톡방(SNS) + 구글드라이브 링크가 전부다.** `인쇄물/` → `안내문/` 으로 git mv,
`힌트카드_인쇄용.html` 삭제 → **`복붙용_힌트카드.md`** 신설(15/25/35분에 단톡방에 붙여넣는
블록 3개 + 2일차 최소경로 안내 블록). 최소경로는 배부물이 아니라 **학생 저장소 안 파일**
(`폐루프/동작보장_최소경로.md`)임을 진행표·가이드가 그렇게 안내하도록 고쳤다.
같은 김에 최소경로 카드에서 폐지된 제도 잔재(「막힘 티켓」·「확장 미션」)를 걷어냈고,
**왜 이런 잔재가 실행 게이트를 통과하고도 살아남는가**에 대한 대비로
**배포본 검증에 「폐지 개념 금칙어」 상시 검사를 신설했다(26→27항목)** —
쪽지·막힘 티켓·확장 미션·인쇄·덱B·넷째 날이 학생 자료에 문장으로 남으면 검증이 실패한다.
(실행 게이트는 「돌아가는 것」만 보장하고 문장 속 죽은 개념은 못 잡는다 — 이게 그 네 번째 겹이다.)

**⑨ 전 파일 라인 전수 정독 (2026-08-12 · 강사 지시 「표집 금지」)** — 슬라이드 PNG 86장 전부
육안 + 나머지 전 파일을 라인 단위로 정독(읽기 에이전트 3벌 병렬, 발견은 전부 본선에서 라인
재검증 후 수리). 나온 것 중 큰 것:

- **★ 폐루프 진단 실패 시 루프가 죽었다** — diagnoser 가 「다음 회차에 다시 겁니다」라고
  말해 놓고 예외를 다시 던지는데 loop.py 가 안 잡았다. 39명 동시 호출에서 일시 실패는
  반드시 나오므로 본번 사고 후보 1순위였다. `DiagnoseFailed` 예외를 신설해 루프가 잡고
  다음 회차로 넘어가게 수리.
- **★ 시뮬레이터 requirements.txt 에 openai 누락** — 절차서대로 새로 깐 VM 에서 2일차
  진단·agent 창구가 500 으로 죽는 폭탄. requirements·절차서 §1 둘 다 수리.
- **★ 강사 콘솔 안내문이 주입 순서를 정반대로** — 「학생이 코드를 돌리기 **전에** 쏘라」.
  검증된 순서(전원이 돌린 **뒤** 주입)와 반대라 콘솔만 보고 따르면 39명이 마지막 장면을
  못 본다. 수리(이건 에이전트도 놓쳐 본선 육안에서 잡음).
- `.env.example` 이 자기 주석과 반대로 `TENANT_MODE=both` · 자리배정 `--전부풀기` 의
  안전 게이트가 상태 조회 실패 시 조용히 뚫림(멈추게 수리) · 배포본만들기의 시뮬레이터
  `.env` 경로 개편 미반영(원복이 조용히 실패하던 원인 — 아까 배정 1이 남았던 이유) ·
  `DAY4_*` 경고 코드 → `DAY2_*`(서버·리허설·verify 동시 개명) · 콘솔 `day4*` id → `day2*` ·
  1일차 점검.py/run.py/agent.py/loop.py 가 학생 문법 오류에서 역추적을 뱉던 것 →
  「N행에 문법 오류」 사람 말로 · 설명란 두 벌의 「삽화 설명 없음」 다량(artdesc 27종 등록 +
  emit_docs 엔티티/blockquote/옛 경로 수리) · 슬라이드 14장 라벨 겹침(ROWS8) ·
  22장 겹말 문안 · 3일차 강의안 md+docx 의 「hitl.py 를 학생이 직접 쓴다」 낡은 설계 서술
  6곳(현행: 템플릿이 구조로 강제) · 실라버스 76.6도 사건 귀속 오기·M4/M8 절 누락 ·
  진단 39명 부하도구의 「1일차」 오기·없는 `--model` 안내 · check_grants 의 없는 004 안내 ·
  절차서 §6 배속·교안/덱 장번호 혼동·기계치환 조사 깨짐 3곳 · 설명대본 73→75 ·
  서버운영 71→73항목 · 부하테스트_결과 48→53 주석 · 안내문자 ③ 서버주소 예시를 빈칸으로.
- 의도 확인 후 **안 고친 것**: run.py 끝줄 「드리프트가 안 잡힌다면 — 그게 정상」(실행해야
  보이는 출력 = 발견 직후 회수라 유출 아님) · W6_* 환경변수(내부 계약) · verify_lab 폐루프
  :467 확장미션 주석(강사 파일의 이력 설명) · 「3일 과정」(대표님 1일+우리 이틀 문맥) ·
  claim 번호 되찾기 무인증(의도된 트레이드오프 — 서버운영 §8에 위험을 명문화).

**⑩ 전수 정독 수리 후 재검증 (3차 · 2026-08-12)** — 수리된 코드로 전 게이트 재실행:
배포본 `--검증` **27/27**(금칙어 검사 포함) → 리허설 `--부하` **75/75 · 10:47 · 건너뜀 0**
→ pptx 42·44장 → 드라이브폴더(7파일·9.3MB) → 공유본(갱신된 실라버스+재캡처 콘솔 그림)
→ `_연습` 새 ZIP → 학생 저장소 push(`6df8169`) → 강사 저장소 push. 노션 pptx 24장
금칙어 0건 · 실라버스 그림 3종 육안(콘솔 캡처는 순서 문구 수리 후 재촬영).

### 남은 것

1. **★ 클라우드 VM 켜기 + 절차서 §13-3-a·b 수행** — 지금 VM 꺼져 있음(당일 서버는 클라우드 확정).
   켠 뒤 `git pull` 만으로는 안 된다 — **§13-3-b(유닛 경로·.env·.venv 이관)** 를 반드시 거친 뒤
   `curl …/openapi.json | grep agent` 로 agent 창구를 확인할 것. 안 하면 2일차 오전이 404 로 빈다.
2. (강사) 드라이브에 `제작/산출물/드라이브업로드/경남대 AI 특강 (8월)` 업로드(링크는
   `특강/당일/안내문/README.md`) · 대표님 공유본은 `제작/산출물/공유/` 그대로 발송 가능(최종본).
3. 제출처(노션 링크 받을 곳) 확정 → 안내문 2곳의 `[ ]` 채우기.
4. 강의장에서 D-7 재실측(지금 수치는 제작 PC + 로컬 서버 기준. 클라우드면 `--base-url` 붙여서).

### 이번 세션(2026-08-11)에서 끝난 것 — 과거 기록. 경로 표기는 개편 전 기준이다

| 무엇 | 상태 |
|---|---|
| 2일차 오전 **agent.py** 신설 (서버 `api/agent.py` tool call 중계 · 최대 6왕복) | AI 가 스스로 detect_anomaly→query_equipment 부르고 WO-2026-0801 지목. 검증·리허설 상시 항목 |
| 검증 항목 수 | **배포본 26 · 리허설 75** (73/24 로 적힌 문서는 낡은 것) |
| Claude(학생 AI 코딩 도구) 완전 제거 | 강사 시연 + `--열기`. 준비물 파이썬·VS Code·노션 셋, 사전안내문 5단계 |
| 배포 경로 | 학생 저장소 `ahnchiyoon87/k-precision-lab`(배포본 push 됨) + 드라이브 폴더(링크는 `당일/인쇄물/README.md`) |
| 라벨 오프바이원 교정 | 240/3/120 · 실측 567·6,375·4/240·0/120 · CSV SHA `5ed976be…` 불변 |
| 새벽 3시 온도 | CSV 실측 **61.0→61.6** 로 교정. 라이브 시뮬레이터 62→64 는 딴 값이고 맞다 |
| 산출물 체계 | `산출물/`(gitignore) 하나로. ZIP 은 드라이브업로드 폴더 안 **하나뿐** |
| 새 도구 | `md2pdf.py`(그림 지원)·`pptx만들기.py`·`드라이브폴더만들기.py`·`공유본만들기.py` |
| 대표님 공유본 | `산출물/공유/` 실라버스 PDF+docx **한 문서만**(1일차 강의안 형식 · 실측 캡처/출력 포함). 원본 `제작/강의안원본/2·3일차_실라버스_공유본.md`, 그림 `그림/` |
| 중복 전수 대조(파이썬 해시) | **소스끼리 중복 0건.** 나머지는 산출물 사본(정상) |
| 강의안/ | 저희 docx 2개만. 1일차 강의안 원본은 `제작/참고자료/외부자료/` |

### 남은 것 (2026-08-11 시점 — ✅ 0·1번은 2026-08-12 에 완료됐다. 위 최신 인계를 보라)

0. ✅ **★ 구조 개편 — 「만드는 것」과 「하는 것」을 최상위에서 가른다 (슬라이드 증량보다 먼저)**

   강사 지시: 특강을 하는 데 쓰는 것과 만드는 것이 한 층에 섞여 있다. 이렇게 가른다 —

   ```
   특강/     ← 하는 것:   당일/ · 학생배포/ · 시뮬레이터/(서버, 당일에도 돌림) · 강의안/
   제작/     ← 만드는 것: 덱빌드·기준판·강의안원본·참고자료·기록 (기존 그대로)
              + 검증도구/(리허설·배포본만들기·센서데이터) + 빌드도구/(md2pdf·pptx만들기·
              드라이브폴더만들기·공유본만들기) + 산출물/(gitignore)
   README.md · 핸드오프.md 는 루트. 「백스테이지」라는 이름은 사라진다.
   ```

   **반드시**: git mv 로(이력 보존) · 옮기기 전 경로 문자열 전수 그렙(「백스테이지」·「산출물/」·
   「당일/」·「학생배포」·「제작/」). 걸리는 곳 — 코드 상대경로 사슬(리허설·배포본만들기·
   드라이브폴더만들기·공유본만들기·pptx만들기·센서데이터 generate/verify) ·
   **★ 학생 코드의 데이터 탐색 문자열 3곳**(1일차/실습/run.py · verify_lab.py ·
   도구만들기/mcp_server.py 가 「백스테이지/센서데이터/데이터」를 위로 탐색 — 새 경로로 수정.
   배포본은 데이터/ 라 학생 영향 없음) · 시작.bat · tools/*.py 의 .env 상대경로 ·
   cloud_deploy.sh · **★ 클라우드 systemd 유닛 WorkingDirectory/ExecStart**(VM 쪽도 수정 —
   절차서에 명령을 적을 것) · .gitignore(고친 뒤 git check-ignore 로 배정장부·.env·산출물
   차단 확인) · 진행표 cd 명령 전부 · 서버운영 · README 2개 · 일차별안내 · 실라버스.
   순서: 이동 → 참조 수정 → 서버 기동 → 배포본 26 → 리허설 75 → 워크스루(ZIP·clone) →
   pptx·드라이브·공유본 재생성 → 절차서·문서 갱신 → push. **끝난 뒤에 슬라이드 증량 시작**
   (순서를 바꾸면 문서 갱신을 두 번 한다).

1. ✅ **슬라이드 증량 마무리** (위 절차 그대로 — 단, 0번 구조 개편 뒤에)
2. **클라우드 VM 켜기 + `git pull` + §13-3-b 이관 + `systemctl restart factory` + agent 창구 curl 확인**
   (안 하면 2일차 오전 404). 지금 VM 꺼져 있음. 당일 서버는 클라우드 확정
3. (강사) 드라이브에 `산출물/드라이브업로드/경남대 AI 특강 (8월)` 업로드 ·
   **대표님 공유본은 발송 준비 완료** — `산출물/공유/` 의 실라버스 PDF+docx 를 그대로 보내면 된다.
   슬라이드 장 번호·장수 표기를 전부 뺐으므로 **슬라이드 증량과 무관하게 최종본**이다
   (장 번호가 필요한 자리는 「진행표에 적혀 있다」로 안내). 새 세션이 다시 손댈 필요 없음
4. **강사 연습 대기 상태 복원**: 서버 켜짐 · `자리배정.py --전부풀기 --예`(캡처용 S01 이 잡혀 있다) ·
   `_연습/k-precision-lab` 을 최신 ZIP 으로 새로 풀기 · `_연습/내일_이것부터.md` 가 안내서

### 이번 세션의 사고 — 재발 방지 수칙

- **`.gitignore`·`.md` 를 PowerShell 치환으로 고치지 말 것** — 줄바꿈이 뭉개져 배정장부.json(실제 키) 차단이 무효가 됐었다. Edit/파이썬으로만, 고친 뒤 `git check-ignore` 확인
- **커밋 전 `git status` 를 눈으로** — 비밀정보 스테이징 사고를 그렇게 잡았다
- 커밋 메시지는 **`-F` 파일로** (히어스트링 깨짐)
- 해시 중복 검사는 **파이썬으로** (PowerShell Get-FileHash 가 대괄호 파일명에서 null)
- **리허설·검증 도는 동안 학생배포/ 편집 금지** (도중 편집으로 3건 깨짐)
- 없다고 단정 금지 (1일차 강의안 docx 가 강의안/ 안에 있었다)
- 학생·강사 문서에 제작 판단 쓰지 말 것 — 근거는 이 문서에만

## 정리·산출물 체계 (2026-08-11 작업 — 명령은 2026-08-12 개편 후 경로로 갱신함)

**폴더가 잡다했던 원인은 산출물이 소스와 같은 층에 있었기 때문이다.**
`배포본/`·`드라이브업로드/`·`공유/` 를 **`산출물/` 하나로 내렸다.**
`제작/산출물/` 은 통째로 gitignore 이고 **전부 스크립트가 뽑는다. 손으로 만들지 않는다.**

| 산출물 | 뽑는 명령 (저장소 루트에서) |
|---|---|
| 학생 배포본 | `python 제작/검증도구/배포본만들기.py --검증` (27항목) |
| 실습 ZIP · 드라이브 업로드 폴더 | `python 제작/빌드도구/드라이브폴더만들기.py` |
| 슬라이드 pptx | `python 제작/빌드도구/pptx만들기.py` |
| 대표님께 보낼 것 | `python 제작/빌드도구/공유본만들기.py` |

> 08-11 시점에는 「폴더 이름은 안 바꾼다(62개 파일 참조)」로 판단했으나,
> **08-12 에 강사 지시로 구조 개편을 실행했다.** 참조 62곳은 전수 그렙으로 따라갔다.

**새로 만든 것 셋**

| 무엇 | 왜 |
|---|---|
| `백스테이지/md2pdf.py` | `.md` 를 드라이브에 올리면 미리보기가 안 되고 메모장에서 마크업이 그대로 보인다. playwright(슬라이드 렌더링에 이미 쓰던 것)로 PDF 를 뽑는다. 맑은 고딕 embed 확인 |
| `백스테이지/pptx만들기.py` | 강의장에서 넘기는 것은 pptx 여야 한다. 이미지 뷰어는 창이 닫히거나 순서가 섞이고 발표자 화면을 못 쓴다 |
| `백스테이지/공유본만들기.py` | 1일차 담당(대표님)께 보낼 것. **1일차 강의안과 같은 형식**으로 쓴 실라버스 + 저희 강의안 docx 2개 |

**대표님 공유본은 1일차 강의안 형식을 따랐다.** 1일차 강의안(`강의안/피지컬AI시대의_…docx`,
15,138자)이 `1. 과정 개요 → 2. 커리큘럼 한눈에 → 3. 모듈별 상세` 구조이고 각 모듈이
**체험 키워드 / 목표 / 다루는 내용 / 실습 / 산출물** 다섯 칸이다. 처음에 4,936자짜리
가벼운 소개문을 썼다가 **형식이 따로 논다는 지적을 받고** 10,634자로 다시 썼다.
원본은 `제작/강의안원본/2·3일차_실라버스_공유본.md`.

> **`.gitignore` 를 스크립트로 치환하다 줄바꿈을 뭉갠 적이 있다.**
> 그 순간 `배정장부.json`(39명 실제 접속 키) 패턴이 주석에 먹혀 **커밋 대상으로 떠올랐다.**
> `git checkout -- .gitignore` 로 되살리고 손으로 고쳤다.
> **`.gitignore` 는 PowerShell `-replace` + `Out-File` 로 건드리지 말 것.**

## 전수조사에서 나온 것 — 고친 것 (2026-08-11 · 전부 돌려서 나왔다)

| # | 무엇 | 왜 심각한가 |
|---|---|---|
| **1** | **★ 2일차 오전에 에이전트가 없었다** | 아래 절에 따로 적었다. 22분이 통째로 비어 있었다 |
| **2** | **★ 학생 코드 11곳이 없어진 「쪽지」를 가리켰다** | 2일차 오후 첫 명령이 「config.json 을 아직 안 채웠습니다. **쪽지 보고** 채우세요」로 끝났다. 쪽지는 폐지됐고 자기배정으로 바뀌었다. **학생이 자력으로 빠져나올 방법이 없었다.** `factory_api.py`·`loop.py`·`control_mcp.py`·`도구만들기/점검.py`·`config.json` 전부 `python 내번호.py` 를 정확히 안내하도록 고쳤다 (그 경로로 `cd` 가 실제로 되는 것까지 확인) |
| **3** | **★ 도구만들기 기본값이 실재하는 남의 번호 `S01`** | `내번호.py` 를 안 돌린 학생도 점검이 「[O] 내 번호 S01」로 **통과했다.** 조용히 남의 번호로 진행된다. 폐루프는 빈 칸인데 여기만 달랐다 → 빈 칸으로 통일 |
| **4** | **★ 라벨에 오프바이원** | 데이터는 반열린 `[start, end)`, 라벨은 닫힘 `[start, end]` 로 찍혔다. 결측 라벨 121행 중 마지막 행은 **`temp=62.14` 로 값이 멀쩡히 있었다.** `config.json` 도 힌트카드도 「**120개**」라고 적는데 화면만 121 이었다 → 라벨을 반열린으로(스파이크는 점이라 닫힘 유지). **센서 CSV 는 SHA-256 이 그대로**(`5ed976be…`)고 라벨만 바뀐다 |
| **5** | 배포본 `base_url` 이 빈 칸이 아닌 예시 주소 | 안 채운 학생이 없는 주소로 접속을 시도했다 → 빈 칸으로 |
| **6** | 정답본이 「학생에게 배포하지 않습니다」 | 실제로는 배포된다(`--열기` 가 읽는다). 학생이 열면 모순된 문장을 본다 |
| **7** | 가이드가 **나오지 않는 문자열**을 안내 | 「아직 안 채움 — ~를 완성하세요」 · 실제는 「아직 채우지 않은 TODO 가 있습니다」 |
| **8** | 검증 하네스 기준이 낮았다 | 「빈 칸을 **짚는가**」만 봤다 → 「**무엇을 하라고 말하는가**(`python 내번호.py`)」까지 검사 |

**라벨 교정으로 바뀐 실측치** — 문서 7곳을 따라가 고쳤다(슬라이드 원고 포함).

| | 오탐 | 하루 | 드리프트 | 스파이크 | 결측 |
|---|---|---|---|---|---|
| k=4.0 | 40 | 6번 | 0/240 | 3/3 | 0/120 |
| **k=3.0** | **567** | 81번 | **4/240** | 3/3 | **0/120** |
| k=2.0 | **6,375** | 911번 | 32/240 | 3/3 | 0/120 |

기준선 0.94% · 하루 81번 · 911번(11배)는 **그대로**다.

## 전수조사에서 **통과**를 확인한 것 (돌려보거나 파일을 연 것만)

`except: pass` **0건** · 진짜 땜빵(FIXME·HACK) **0건** · 하드코딩 경로 1건뿐이고 변수 ·
z-score 직접구현/표본표준편차(n−1)/σ=0/결측 처리 **전부 실재** ·
**온도 = rpm 의 함수**(`engine.py:160`, 실측 1794→1436rpm 에 62.5→53.9℃) ·
승인 관문이 **구조로 강제**(`actuator.run()` 이 항상 `hitl.ask()` 경유, 목록에 없는 명령은 물어봄) ·
데이터 60,480행·6대·7일·**80℃ 미도달(최고 76.63)** · EQ-04 56.2℃ · EQ-05 74.4℃ ·
**슬라이드 원고 ↔ PNG 픽셀 동일**(재렌더링 후 diff 0) · 장수 36/37 일치 ·
**문서가 시키는 명령 96건 전부 실재하고 옵션도 받음**(`--help` 를 실제로 돌려 대조) ·
진행표가 장 번호로 가리킨 21곳 **전부 정확** · 일차별안내 구간·분 합계 일치 ·
데이터 검증기 전 항목 · clone 과 ZIP 양쪽에서 학생 환경 완주.

## 아직 안 고친 것 — 판단이 필요하다

| 무엇 | 상태 |
|---|---|
| ~~슬라이드 「새벽 3시 62.1℃ → 4시 62.6℃」~~ | **고쳤다 → 61.0 → 61.6℃.** CSV 실측이 61.08 → 61.99(앞뒤 15분 평균 61.01 → 61.64)였다. 드리프트가 **새벽**에 일어나는데 일주기 진폭 `amplitude_c: 1.0` 이 그때 최저점이라 1.1도 낮게 나온다. EQ-03 전체 중앙값 62.1 은 맞지만 **그 시각의 값이 아니다.** 슬라이드·원고·설명대본을 실측으로 맞췄다. **「62 → 64℃」는 라이브 시뮬레이터 값이라 그대로 둔다**(리허설 실측 기준선 62.0) — 둘은 다른 것이다 |
| **대외 강의안 docx 2개** | 원본 마크다운은 고쳤는데 **docx 를 다시 뽑는 도구가 저장소에 없다**(pandoc 없음 · 변환 스크립트 없음). 서식 보존 교체 스크립트를 만들어 뒀으나 **작업 중 Word 로 열려 있어 멈췄다**(`~$…docx` 잠금). Word 를 닫고 `scratchpad/fix_docx.py` 를 돌리면 된다. 고칠 곳 12군데(준비물의 `Claude · git` · 「Claude와 같이 짜도 됩니다」 · 실측치) |
| 1일차 `점검.py --열기` 두 번 | 핸드오프가 「이미 채워져 있습니다로 갈라서 말함」이라 적었는데 그 분기는 **2일차에만** 있다. 1일차는 함수를 통째로 다시 채워서 **두 번 눌러도 안전은 하다**(실행 확인) |

## 앞 세션이 넘긴 두 가지 — **둘 다 끝났다**

**① 검증 2종 — 전부 통과.** 실제로 돌렸다.

| 무엇 | 결과 |
|---|---|
| `배포본만들기.py --검증` | **24/24 통과** (학생 조건 cp949 · 비밀정보 검사 포함) |
| `리허설.py --token <토큰> --부하` | **73/73 통과 · 10분 44초** · 건너뜀 0 · 39명 동시 진단 429 0건 · WO 인용 39/39 |

**② 학생 코드의 Anthropic 잔재 — 제거했고 실제로 도는 것까지 확인했다.**

`폐루프/agents/diagnoser.py` 에서 `_ask_claude()`, 「Anthropic 키가 우선」 주석,
`backend == "claude"` 분기를 들어냈다. `credentials()` 는 이제 `openai_key_env` 하나만 본다.
`verify_lab.py` 의 안내 문구도 같이 고쳤다. **`학생배포/` 에 `anthropic` 0건.**

> 죽은 분기가 맞았다 — `config.json` 에 `anthropic_key_env` 가 없을 뿐 아니라
> `model` 키 자체가 없어서 그 경로로 들어갔으면 `KeyError` 로 죽었을 코드였다.

라이브 확인 — 리허설 폐루프가 이 파일로 돌았고 `backend=server` 로 실제 모델까지 갔다.
WO-2026-0801 인용 · 감속 1794→1436rpm · AMR-01 이 EQ-03 로 이동 · 62.5℃→53.9℃.

## 2일차 오전에 **에이전트가 없었다** — 새로 만들었다 (2026-08-11)

전수조사에서 나온 것 중 **가장 큰 구멍**이다. 이건 이번에 생긴 것이 아니라 원래 있었다.

**증상** — 가이드는 `python mcp_server.py` 로 서버를 띄우고 「에이전트에게 이 한 문장을
주세요」라고 시키는데, **그 에이전트가 무엇이고 어떻게 내 MCP 서버에 붙는지가
어느 문서에도 없었다.** MCP 클라이언트 등록 안내(`mcpServers`)가 **0건**.
학생은 도구를 다 만들고도 **AI 가 그것을 부르는 장면을 볼 수 없었다.**

**왜 진단 창구로는 안 되나** — `diagnose.py` 는 **단발**이다. 프롬프트를 주면 JSON 하나를
돌려준다. `tools` 를 안 넘기고 `tool_calls` 도 안 받는다. 오후 폐루프가 되는 이유는
`loop.py` 가 **파이썬 코드로** 감지→진단→조치를 돌리고 진단만 한 번 부르기 때문이다.
오전은 「AI 가 스스로 도구를 고른다」라서 성격이 다르다.

**무엇을 만들었나**

| 무엇 | 어디 |
|---|---|
| **도구 호출 중계 창구** (새로 만듦 · 진단 창구는 안 건드림) | `server/app/api/agent.py` — `POST /api/v1/{ns}/agent` |
| **학생 러너** | `2일차/실습/도구만들기/agent.py` — `python agent.py` |

**어떻게 도나** — 학생이 문장 하나를 준다 → 서버가 도구 목록과 함께 OpenAI 로 넘긴다 →
AI 가 「detect_anomaly 를 불러 줘」라고 답한다 → **도구는 학생 컴퓨터에서 실행된다** →
결과를 다시 올린다 → AI 가 다음 도구를 고른다 → 리포트. 최대 6왕복에서 서버가 끊는다.

**키는 여전히 학생에게 안 간다**(절대규칙 7). 서버만 `OPENAI_API_KEY` 를 쥔다.

**라이브 확인** — 학생 조건에서 실제로 돌렸다. AI 가 `detect_anomaly` → `query_equipment`
순서로 **스스로** 부르고 **WO-2026-0801(냉각 계통 · 부품 입고 지연으로 보류)** 을 원인으로
지목했다. 배포본 검증과 리허설에 **상시 검사로 넣었다** — 이 구간이 죽으면 D-3 에 잡힌다.

> **왜 창구를 새로 팠나** — 오후 폐루프가 진단 창구로 이미 39/39 를 통과했다.
> 거기를 고치면 검증된 경로가 흔들린다. 문제가 생기면 오전만 되돌리면 된다.

## 그 뒤에 방향이 바뀐 것 — **여기가 지금 제일 중요하다**

| 무엇 | 어떻게 바뀌었나 |
|---|---|
| **학생 AI 코딩 도구** | **안 쓴다.** AI 로 코드를 만드는 과정은 **강사가 화면에서 시연**하고, 학생은 각자 해 보다 막히면 `--열기` 로 정답을 채워 넘어간다. 39명 좌석 문제가 사라졌다. 학생 준비물이 **파이썬·VS Code·노션 셋**으로 줄어 사전안내문이 **6단계 → 5단계** |
| **학생이 코드 받는 법** | **clone 과 드라이브 ZIP 둘 다** 준다. 내용은 같다 |
| **학생용 저장소** | **`github.com/ahnchiyoon87/k-precision-lab`** — 현재 배포본으로 **교체해서 push 완료** |
| **구글드라이브** | 강의자료·ZIP 을 두는 폴더. 링크 확정됨 (`특강/당일/안내문/README.md` 참조). **학생 전달은 단톡방에 이 링크를 붙이는 것으로 끝** |
| **문의 채널** | **카톡 단체방** — 담당자가 초대한다. 우리가 만들 것이 없다 |
| **학생 개인 깃 저장소** | 이틀 과정에 필요한 지점이 없어 **커리큘럼에 안 넣는다.** 금지가 아니다 |

> **문서에 작업 판단을 적지 말 것.** 「git 은 세 군데서 막힌다」 같은 제작 과정의 근거를
> 진행표·가이드에 적어 뒀다가 지적받았다. 그런 것은 **이 핸드오프에만** 남긴다.
> 학생·강사가 읽는 문서에는 **실제로 할 일만** 적는다.

## 절대 규칙 — 어기면 수업이 무너진다

**손대기 전에 반드시 읽을 것.** 자세한 이유는 §2 에 있다.

| # | 규칙 |
|---|---|
| 1 | **개인 단위. 팀·PBL 없음** |
| 2 | **발표·시연·투표·시상 없음** — 대신 "다 같이 엔터" |
| 3 | **39명 전원이 같은 장면을 본다** — 창의성으로 갈리는 구간을 만들지 않는다 |
| 4 | **이탈 절대 방지** — 1일차를 못 끝내도 2일차를 시작할 수 있어야 한다 |
| 5 | **반전은 학생이 발견한다** — 무엇이 잡히고 안 잡히는지 **강의자료에 한 글자도 쓰지 않는다** |
| 6 | **조용히 넘어가지 않는다** — 진단이 안 되면 학생이 **알 수 있게** 한다 |
| 7 | **키는 학생에게 주지 않는다** — 서버가 쥐고 중계한다. `.env` 커밋 금지 |
| 8 | **대본이 없다** — 슬라이드만 보고 뜻이 통해야 한다 |
| 9 | **무대는 K-정밀 하나** — CNC 6대(EQ-01~06) · AMR 2대 |
| 10 | **장난감이 아니다** — z-score 를 라이브러리 없이 직접 짠다 |

## 일하는 방식 — 이것을 안 지켜서 오늘 시간을 버렸다

| # | 규칙 | 무슨 뜻인가 |
|---|---|---|
| 1 | **실제로 돌려보고 판단한다** | 문서를 읽은 것은 확인이 아니다. **돌려 보거나 파일을 열어 본 것만** 「됐다」고 말한다 |
| 2 | **없다고 단정하기 전에 찾아본다** | 「저장소 미확정」이라고 썼는데 리모트가 이미 연결돼 있었다 |
| 3 | **결정을 내렸으면 파일까지 따라간다** | 한 곳을 고치면 그것이 걸린 코드·문서·슬라이드를 **전부** 맞춘다 |
| 4 | **막히면 땜빵으로 우회하지 않는다** | 멈춰서 보고한다. FIXME·임시 우회를 남기지 않는다 |
| 5 | **패턴 검색이 아니라 파일을 연다** | grep 으로 훑고 「전수조사했다」고 하지 않는다. 라인 단위로 읽는다 |
| 6 | **시킨 범위를 줄이지 않는다** | 「study 아래 전부」면 저장소 밖까지다. 골라서 하지 않는다 |
| 7 | **기계 치환 뒤에는 눈으로 확인한다** | 일괄 치환이 문맥을 깨뜨린다 (「`1일차` `1일차` `데이터`」가 실제로 나왔다) |
| 8 | **대외 문서는 원고와 대조한다** | 기억으로 쓰면 틀린다. 슬라이드 원고(`제작/덱빌드/build_*.py`)가 기준이다 |

## 오늘 무엇을 잘못했나 — 같은 실수를 반복하지 말 것

이 프로젝트에서 **가장 많이 시간을 버린 원인**이다. 반드시 읽을 것.

| 무엇 | 왜 그랬나 | 어떻게 해야 했나 |
|---|---|---|
| **확인 없이 「완료」라고 보고** | 여러 번 반복했다. 사용자가 4번 물었는데 4번 다 「했다」고 답했다 | **돌려 보거나 파일을 열어 본 것만** 완료라고 말한다 |
| **강의안을 원고와 대조 없이 기억으로 씀** | 「에이전트는 넷」을 모델·도구·관측·기억이라 썼는데 원고는 **뇌(LLM)·도구·루프·자율성**. 「패턴 다섯」 이름도 전부 틀렸다 | 대외 문서를 쓸 때는 **슬라이드 원고(`제작/덱빌드/build_*.py`)를 먼저 읽는다** |
| **「저장소 미확정」이라고 적음** | 리모트가 이미 연결돼 있었고 배포 절차서에도 주소가 있었다. 확인을 안 했다 | 「없다」고 쓰기 전에 **찾아본다** |
| **노션 1교시를 60분간 몰랐다** | 자료가 저장소 밖에 있어서 못 봤다. 그래서 「1일차에 90분이 빈다」고 오판했다 | 범위를 **폴더 밖까지** 본다 |
| **전수조사를 폴더 안만 했다** | 「study 아래 전부」라고 했는데 `경남대특강/` 안만 만졌다 | 시킨 범위를 **줄이지 않는다** |
| **기계 치환으로 문맥을 깨뜨림** | 일차 번호를 일괄 치환했더니 「`1일차` `1일차` `데이터`」 같은 게 나왔다 | 치환 뒤 **반드시 눈으로 확인**한다 |

## 헷갈리기 쉬운 것 — 일차 표기

| | 저장소 안 | 대외 문서(강의안 docx) |
|---|---|---|
| 이상감지를 직접 짜는 날 | **1일차** | **2일차** |
| 폐루프를 닫는 날 | **2일차** | **3일차** |

**저장소·코드·진행표·실습가이드는 전부 1일차·2일차**다(담당자 기준).
**`강의안/` 의 docx 두 개만 2·3일차**다 — 전체 4일 과정 기준이라 받는 쪽이 그렇게 부른다.

---
# 0. 폴더를 어떻게 쓰나 — 이것부터

**강의하는 날 여는 곳은 딱 하나입니다.** (2026-08-12 구조 개편 후 기준)

```
경남대특강/
│
├─ 특강/              ← 특강을 **하는** 데 쓰는 전부
│   ├─ 당일/            ★ 강의하는 날 여는 것. 이 폴더만
│   │   ├─ 시작.bat        아침에 더블클릭. 서버가 켜진다
│   │   ├─ 진행표.md       ★ 수업 중 계속 보는 것
│   │   ├─ 노션_자산화_강의자료.pptx   1일차 1교시 (60분 · 24장)
│   │   ├─ 슬라이드/        1일차 42장 · 2일차 44장
│   │   ├─ 안내문/          단톡방으로 보내는 것 — 사전안내문 · 복붙용 안내문자 · 복붙용 힌트카드
│   │   ├─ 일차별안내/      하루 흐름 한 장 (1일차 · 2일차)
│   │   ├─ 서버운영.md · 힌트카드.md · 폐루프_강사우회.md
│   ├─ 학생배포/         ★ 이 폴더가 통째로 학생 저장소가 된다 (1일차 · 2일차 · README)
│   ├─ 시뮬레이터/       가상 공장 서버 — 당일에도 이걸 돌린다 (시작.bat · tools/)
│   └─ 강의안/           밖으로 보내는 것. docx 2개 ★ 여기만 2·3일차 표기(대외 기준)
│
└─ 제작/              ← 특강을 **만드는** 전부. 강의할 때는 안 연다
    ├─ 덱빌드/         슬라이드 원고 → PNG (원고·설명란도 여기서 자동 생성)
    ├─ 기준판/         디자인 규격
    ├─ 강의안원본/      대외 강의안 docx 의 원본 마크다운 + 실라버스 공유본
    ├─ 참고자료/       교안 · 리서치 · 견적 · 설명대본 · 앞 세션 실라버스
    ├─ 기록/           검증 기록 (과거 기록 표시 있음)
    ├─ 검증도구/        리허설.py(75항목) · 배포본만들기.py(27항목) · 센서데이터/
    ├─ 빌드도구/        pptx만들기 · 드라이브폴더만들기 · 공유본만들기 · md2pdf
    └─ 산출물/         배포본 · 드라이브업로드 · 공유 (통째로 gitignore)
```

**나누는 기준은 「누가 보는가」다.** 무엇인지가 아니라.
학생이 받는 것은 `학생배포/` 하나뿐이라, 배포본을 뽑을 때 강사 것이 딸려 갈 수가 없다.

> **일차 표기** — 저장소 안은 전부 **1일차·2일차**(담당자 기준)다.
> **`강의안/` 의 docx 두 개만 2일차·3일차**다 — 전체 4일 과정 기준이라 받는 쪽이 그렇게 부른다.
> 헷갈리면 `강의안/README.md` 에 표로 적어 뒀다.
각 폴더에 `README.md` 가 있어 열면 바로 무엇인지 나옵니다.

## 폴더 사이 의존 — 옮기면 깨진다

```
2일차/실습/도구만들기/mcp_server.py  →  1일차/실습/detect.py        (import)
1일차/실습/run.py                   →  센서 CSV (아래 참조)
2일차/실습/도구만들기/mcp_server.py  →  센서 CSV (아래 참조)
```

**2일차가 1일차 코드를 그대로 불러 쓴다.** 버그가 아니라 설계다 —
*"1일차에 짠 코드를 오늘 AI 의 도구로 내놓는다"* 가 2일차의 핵심 장면이다.

## 센서 CSV 는 경로를 박지 않는다 — 스스로 찾는다

이 저장소와 학생 배포본이 데이터를 다른 자리에 둔다. 그래서 하드코딩하지 않고
위로 올라가며 **두 자리를 다 본다.** 강사본·배포본이 **같은 코드로** 돌아야
학생 저장소가 다시 어긋나지 않는다.

| 어디서 | 데이터 자리 |
|---|---|
| 이 저장소 (강사) | `제작/검증도구/센서데이터/데이터/` |
| 학생 배포본 | `데이터/` |

찾는 코드가 있는 곳 — `1일차/실습/run.py` · `1일차/실습/verify_lab.py` ·
`2일차/실습/도구만들기/mcp_server.py`·`점검.py` 의 `_csv_경로()`.
`도구만들기/config.json` 의 `csv_path` 기본값은 **`"auto"`** 다. 경로를 적으면 그것을 쓴다.

---

# 1. 무슨 일을 하는 중인가

경남대 컴퓨터공학과 **1~4학년 39명**에게 **이틀(하루 4시간)** 강의한다.
이 저장소는 **그 이틀만** 다룬다. 앞에 대표님 세션(4시간), 뒤에 PLC 세션이 붙는다.

> **호칭 주의** — 전체 과정 기준으로 대표님 세션이 **1일차**, 우리가 **2일차·3일차**다.
> 이 저장소 안에서는 우리 이틀을 `1일차`·`2일차` 폴더로 부른다(코드·경로가 다 그렇게 돼 있다).
> **대외 문서에서는 2일차·3일차로 쓴다** — 최상위 `강의안.md` 가 그 기준이다.

**앞 세션과 무엇이 겹치나** — 자료는 안 맞춰도 된다. 산출물이 문서(`spec`·`.feature`) 대
코드라 오갈 파일이 없다. 대신 겹치는 세 지점이 있고 강사가 한 문장씩 얹어 잇는다.
겹침 지도와 말할 문장은 **진행표 「앞뒤에 붙는 4시간 세션과 잇는 법」** 절에 있다.

**앞 세션에 물어볼 것 하나** — 모듈 6 실습이 「각자 현장 과제를 고르라」로 돼 있는데
학부생은 현장이 없다. **우리 공장(CNC 6대 + AMR 2대)으로 통일해 주면** 1일차에 쓴
`.feature` 가 3일차 완료 기준이 되어 3일이 한 흐름이 된다. 안 되면 지금대로도 성립한다.

| | 제목 | 학생이 하는 일 |
|---|---|---|
| **1일차** | **잡기** | 이상감지 알고리즘을 **직접 짜고**, 그것이 어디까지 되는지 확인한다 |
| **2일차** | **움직이기** | 내 코드를 AI 의 도구로 내놓고, AI 가 판단해 **설비를 실제로 움직이게** 한다 |

목적은 지식 전달이 아니라 **9월 본과정 이탈 방지**다.
"얼마나 가르쳤나"가 아니라 **"끝까지 남았나"** 로 성패가 갈린다.

## 이야기 흐름

**1일차** — 새벽 3시 EQ-03 사건 → 사람이 못 본 이유 셋 → *"80도 넘으면 알람 걸면 되잖아"*
→ **★ 반전: 80도를 넘은 적이 없다** → 내 공장 열어보기 → 이상감지 직접 구현
→ **학생이 직접 두 번째 반전을 발견** → *"규칙으론 못 잡습니다. 그럼 누가 판단하죠?"*

**2일차** — 1일차 회수 → 환각 → 에이전트·ReAct → MCP 도구 2개
→ **AI 가 WO-2026-0801 을 원인으로 지목** → 역할 분해 → HITL
→ 폐루프 일곱 걸음 → **승인 누르면 AMR 이 실제로 움직임**
→ *"그런데 이건 전부 소프트웨어입니다"*

교안의 **「4비트 학습 루프」**(작은 성공 → 의도된 불편 → 대조 해결 → 자산화)를 이틀 다 지킨다.

---

# 2. 절대 규칙 — 어기면 수업이 무너진다

| # | 규칙 | 왜 |
|---|---|---|
| 1 | **개인 단위. 팀·PBL 없음** | 교안의 팀 챌린지는 마지막 날 것이었고 그 날이 PLC 세션으로 바뀌었다 |
| 2 | **발표·시연·투표·시상 없음** | 39명 중 2명만 앞에 세우면 나머지는 구경꾼이 된다. 대신 **"다 같이 엔터"** |
| 3 | **39명 전원이 같은 장면을 본다** | 창의성으로 갈리는 구간을 만들지 않는다. 결정적 문장은 가이드에 원문으로 준다 |
| 4 | **이탈 절대 방지** | 1일차를 못 끝내도 2일차를 시작할 수 있어야 한다 |
| 5 | **반전은 학생이 발견한다** | 어떤 이상이 잡히고 안 잡히는지 **강의자료에 한 글자도 쓰지 않는다** |
| 6 | **AI 없이 조용히 넘어가지 않는다** | 진단이 안 되면 학생이 **알 수 있게** 한다 |
| 7 | **키는 학생에게 주지 않는다** | 회수가 안 된다. 서버가 쥐고 중계한다 |
| 8 | **대본이 없다** | 슬라이드만 보고 뜻이 통해야 한다 |
| 9 | **무대는 K-정밀 하나** | 창원 가상 정밀기계 제조사 · CNC 6대(EQ-01~06) · AMR 2대 |
| 10 | **장난감이 아니다** | z-score 를 라이브러리 없이 직접 짠다. 표본표준편차·σ=0·결측 처리 |

## 손코딩 원칙

**없애는 것: 막히는 스트레스.** 왜 안 되는지 모름 · 남들 다 됐는데 나만 안 됨.
**남기는 것: 생각하는 스트레스.** *"왜 이건 안 잡히지?"*

| | 손코딩 | 나머지 |
|---|---|---|
| 1일차 | **25분** (함수 3개, 각 3~8줄) | 관찰 · 숫자 흔들기 · 비교 |
| 2일차 | **거의 0** — 막히면 `--열기` | 진단 · 판단 · 관찰 |

학년 편차 — 못 따라오면 `점검.py --열기`, 먼저 끝내면 가이드 부록의 선택 과제.

## 하루 4시간에 들어가는가 — 견적 대조

**견적서는 4일 과정(강의 200분 + 실습 560분 = 760분) 기준이었다.**
지금은 **2일 × 240분 = 480분** 이라 스코프가 280분 줄었다 —
팀 챌린지(120) · 대시보드 랩(110) · Supabase 온보딩(30) · 센서 적재 랩(30) 이 빠진 결과다.

| | 슬라이드+실습 | 노션 | 합계 | 240분 대비 |
|---|---|---|---|---|
| **1일차** (대외 2일차) | 130분 | **80분** (자산화 강의 60 + 마무리 20) | **210분** | +30분 |
| **2일차** (대외 3일차) | 200분 | 20분 (마무리만) | **220분** | +20분 |

> **노션 자산화 강의(60분)는 1일차 1교시다.** 별도 자료가 있다
> (`노션_자산화_강의자료.pptx` · 24장). 이걸 빼고 세면 1일차에 90분이 비는 것처럼 보인다 —
> 실제로 한 번 그렇게 오판했다.

**실습 시간은 상한이지 소진해야 하는 값이 아니다.**
55분·70분은 「여기까지 쓸 수 있다」는 뜻이고, 학생이 일찍 채우면 그만큼 일찍 넘어간다.
실제로 변수는 **학생이 코드를 채우는 시간 하나뿐**이고, 그 뒤는 전원이 같이 간다 —
주입 대기 3분 + 진단·감속·승인·AMR 5분 = **8분 고정**.
(리허설이 이틀치를 11분에 도는 것이 그 증거다. 코드를 안 짜고 `--열기` 로 채웠기 때문이다.)

**그래서 지금 시간표 그대로 간다.** 내용을 줄이지 않는다.
막히는 학생이 많아 상한을 다 쓰는 최악의 경우만 20분이 빠듯한데,
그때 쓰라고 `--열기` 가 있다 (막힌 자리만 채우고 5분 안에 마지막 장면까지).

## 하루 4시간에 무엇이 들어가나 — 노션 1교시를 빠뜨리지 말 것

**1일차 1교시가 노션 자산화 60분이다.** 자료는 `특강/당일/노션_자산화_강의자료.pptx` (24장).

| | 무엇 | 시간 |
|---|---|---|
| **1일차** | **1교시 노션 자산화** + 셋업 + 슬라이드 36장(실습 20·50분 포함) + 마무리 20분 | 60 + 20 + 130 + 20 = **230분** |
| **2일차** | 슬라이드 37장(실습 55·70분 포함) + 마무리 20분 | 200 + 20 = **220분** |

240분 대비 1일차 +10분, 2일차 +20분. **실습 시간은 상한이지 소진해야 하는 값이 아니다** —
정해진 시각에 다 같이 끊고 못 채운 학생은 `--열기` 로 막힌 자리만 채워 다음으로 간다.
그래서 실제로는 더 남는다. (리허설이 이틀치를 11분에 도는 것이 그 증거 — 코드를 안 짜서다.)

**노션은 만들고 끝이 아니다.** 1교시에 만든 템플릿을 **1일차 마지막 20분과 2일차 마지막 20분에
실제로 채운다.** 만들어만 두면 다음에 안 쓴다. 2일차 마지막에 그 링크를 제출하는 것이 수료 조건이다.

## 절대 바꾸면 안 되는 수치

| | 값 |
|---|---|
| 드리프트 | **0.5℃/h** · 62 → 64℃ · **80℃ 를 끝까지 안 넘음** |
| 데이터 | **60,480행** (CNC 6대 × 7일 × 1분) |
| 오탐 실측 | 기준선 **0.94%** · k=3.0 **567행 = 하루 81건** · k=2.0 **6,375행 = 하루 911건(11배)** |
| 실습 결과 | 스파이크 3/3 · 드리프트 240 중 4 · 결측 0/120 ※ **강의자료에 쓰지 말 것** |
| 정비 이력 | **WO-2026-0801** 「부품 입고 지연으로 보류」 |
| 제어 도구 | `set_equipment_speed` · `ack_alarm` (자동) / `stop_equipment` · `dispatch_robot` (**승인**) |
| 감지 소요 | 배속 x120 기준 **약 100초** |
| 진단 응답 | 중앙 **4.3초** · p95 9.0초 · 평균 출력 약 970자 |

---

# 3. 지금 어디까지 됐나

## 실행으로 증명된 것

| 무엇 | 결과 |
|---|---|
| **39명 동시 진단** (2초 시차·동시성 12) | **39/39 성공 · 429 0건 · 중앙 4.3초 · p95 9.0초 · 전체 81초** |
| **전원이 같은 장면을 보는가** | **WO 인용 39/39 · 감속 39/39 · 파견 요청 39/39 · 스키마 39/39** |
| 최악 조건 (전원 동시) | 39/39 성공. 중앙 15.8초·최대 30초 → **`--stagger 2` 필수** |
| **폐루프 일곱 걸음 실주행** | 감지 → 진단(server·WO 인용) → 감속 → 승인 → AMR 파견 → **온도 하강**. 실측 예: 1788→1430rpm / 62.5℃→53.4℃, 다른 회차 1800→1442rpm. **노이즈로 회차마다 다르지만 관계는 물리식이 보장**(온도 = rpm 의 함수) |
| **`--열기` 이탈 방지** | 1일차 3회 · 2일차 2회 → 각각 완주 |
| 학생 환경(cp949) 완주 | ✅ `exit=0` |
| 실측 = 강의자료 수치 | ✅ 0.94% · 566행 · 6,374행 |
| 테넌트 | ✅ **39개뿐** — `007_drop_teams.sql` 적용 완료. 강사 콘솔 표도 39행(전엔 47행) · 학생 목록 39개 · 경고 0 |
| 문법·JSON · 옛 이름 · 비밀정보 | ✅ 오류 0 · 잔재 0 · 노출 0 (적용된 마이그레이션 주석은 기록으로 남김) |
| **2일치 무인 리허설** (`리허설.py --부하`) | ✅ **73/73 통과 · 10분 38초** — 39명 동시 진단(실제 모델 호출) 포함 · 건너뜀 0 |
| **자리 배정** (39명 동시) | ✅ **겹침 0건** · 재실행 동일 · 번호로 되찾기 · 서버 재시작 후에도 유지 |
| **학생 배포본** (`배포본만들기.py --검증`) | ✅ 학생 조건(cp949) **24/24 통과** · 비밀정보 유출 검사 포함 · 강사용 README 2개 제외 확인 |
| **학생 문서가 시키는 명령 전수 실행** | ✅ **25/25** — 학생 조건에서 역추적·인코딩 사망 0건 |
| **슬라이드 73장 전수 대조** | ✅ 옛 일차·팀·발표·옛 랩이름 **0건** · 가이드 안내 4장 · `점검.py` 3장 |
| **배포본에서 막힌 학생이 완주하는가** | ✅ clone → 설정 3줄 → `loop.py --열기 전부` → 감속 1791→1433rpm · **AMR 도착** · 63.1→53.8℃ |
| 랩 품질 게이트 3벌 | ✅ 1일차 · 도구만들기 · 폐루프(오프라인/`--live`) 전부 통과 |
| **7일치 데이터 검증** (`센서데이터/verify.py`) | ✅ **전 항목 통과** — 80℃ 미도달 · 드리프트 미탐(평균\|z\| 0.94 vs 정상 0.81) · 스파이크 3/3(\|z\|≈6.6) · k=2 오탐 19.9배 · SHA-256 재현성 |
| **엔진 자체검증** (`selftest_engine.py`) | ✅ **53/53** — 주입 만료 회귀검사 5건 신설 |
| **권한** (`check_grants.py`) | ✅ 공개 키로는 읽기만 — 쓰기 3종 전부 401 |
| **진단 창구 인증** | ✅ 토큰 없음·틀림 → 401 · 맞으면 200 (실제 호출로 확인) |
| **주입 만료** (라이브) | ✅ 종료 시각이 지난 주입이 5초 안에 「진행 중」에서 사라짐 |

## 2026-08-10 에 고친 것 — 전부 **실행해서** 나왔다

| 무엇 | 증상 |
|---|---|
| `리허설.py` | 4일치 기준. 팀 T1~T3 를 요구해 **첫 줄에서 즉사**했다. 재작성 |
| `1일차/실습/verify_lab.py` | 경로가 한 단계 어긋나 `FileNotFoundError`. `run.py` 만 고쳐져 있었다 |
| `도구만들기/정답/tool_bodies.py` | 같은 계열. MCP 서버가 기동 즉시 죽어 `Connection closed` |
| `폐루프/verify_lab.py` | 같은 계열. 1일차 정답 대조가 **조용히 건너뛰어져 통과처럼 보였다** |
| `폐루프/verify_lab.py` 정리 | **배속을 1 로 떨군 채 끝났다.** 수업 직전에 돌렸다면 39명 화면이 멈춰 보인다 |
| `도구만들기/verify_lab.py` | 돌릴 때마다 `config.json` 끝 개행을 지워 학생 파일이 바뀐 것처럼 보였다 |
| 확장미션 참조 4곳 | 폴더는 지웠는데 **학생 README 가 없는 폴더를 가리키고 있었다** |
| 두 실습가이드 첫머리 | 「`3일차` `4일차` 폴더가 보이면 됩니다」 — 학생이 첫 3분에 읽는 자리 |
| `폐루프/README.md`·`동작보장_최소경로.md` | 팀 번호 **`T3`** 가 남아 학생이 그대로 따라 치게 돼 있었다 |
| `정답/agent_bodies.py` | 필수 프롬프트 **③ 이 빠져 있었다** (39/39 를 만든 그 문장) |
| **2일차 오후에 탈출구가 없었다** | 마지막 장면 자리인데 `--열기` 가 없었다. `loop.py --열기` 신설 |
| `loop.py --열기` 가 **하나만** 받았다 | 세 자리 다 막힌 학생은 마지막 장면에 **도달할 수 없었다.** `--열기 1 2` · `--열기 전부` 로 고침 |
| **`control_mcp.py` 에 UTF-8 가드가 없었다** | 「최소 경로」가 **0번으로 시키는 명령**이다. cp949 에서 출력이 깨졌다 |
| 안 채운 `config.json` | 30초 기다린 끝에 「닿지 못했습니다」만 나와서 **학생이 주소를 고치러 갔다.** 이제 빈 줄을 바로 짚는다 |
| `control_mcp.py --check` · `--열기`(정답 없을 때) | 학생 화면에 **파이썬 역추적**이 떴다 |
| 2일차 가이드 `tenant` 기본값 | 가이드는 `"T1"` 이라는데 실제 파일은 빈 칸이었다. **찾다가 못 찾는다** |
| 학생 문서의 「4일」 잔재 | 「4일의 마지막」·「4일치가 쌓이면」·「**내일** 링크 제출」 |
| 저장소 받는 절차가 **아무 데도 없었다** | 가이드는 「내려받았는가」만 묻고 방법을 안 알려 줬다. 사전안내문은 「저장소를 **만들** 때 쓴다」고 잘못 안내 |
| `배포본만들기.py` 재실행 | `.git` 이 있으면 `PermissionError` 로 죽었다 — push 한 뒤 수정본을 뽑는 그 순간 |
| **쪽지에 「내 번호」·「서버 주소」가 없었다** | 가이드는 계속 「쪽지의 내 번호」를 요구하는데 쪽지엔 주소·키 둘뿐. **39명이 URL 에서 번호를 눈으로 뽑아 옮겨 적어야 했다** |
| 쪽지 라벨 「3·4일차 코드에」 | 옛 일차 표기 |
| `mcp_server.py` (1일차 폴더가 없을 때) | **역추적.** 저장소를 다시 받은 학생이 2일차 오전 첫 명령에서 그대로 본다 |
| **★ 슬라이드가 「3일차 폴더를 엽니다」** | 1일차 28장 · 2일차 17·29장. **39명이 화면으로 보는 것**이었다. 없는 폴더를 찾게 된다 |
| 1일차 16장 「쪽지에 주소와 키가 있다」 | 쪽지는 네 줄로 바뀌었는데 슬라이드는 둘이라고 했다 |
| **진행표에 서버 켜는 명령이 없었다** | 「당일 이 문서 하나만」이라면서 **가장 첫 동작**이 빠져 있었다 |
| 진행표가 시각(+2:20)으로 적혀 있었다 | 당일에는 시계가 아니라 **슬라이드**를 본다. 장 번호 기준으로 다시 씀 |
| **★ 학생 파일에 `--use-answers` 가 노출** | `동작보장_최소경로.md` 뒤에 강사 절이 붙어 배포본에 그대로 나갔다. **세 자리 전부 정답으로 도는 명령**이라 실습이 통째로 무의미해진다. `당일/폐루프_강사우회.md` 로 분리 |
| 슬라이드에 **가이드 안내 0건** | 학생이 50분 봐야 할 문서를 여는 말이 화면에 없었다 |
| 슬라이드에 **`점검.py` 언급 0건** | 이탈 방지 장치가 화면에 없어 강사가 말해 주지 않으면 학생은 모른다 |
| `서버운영.md` 가 없는 문서를 링크 | `수업진행.md` — 존재하지 않는다 |
| `당일/일차별안내/1일차.md` 슬라이드 구간표 | 실제 장 번호와 어긋나 있었다 (반전 11장인데 「01~17」) |
| `강사용/README.md` | 「`리허설.py` ⚠ 4일치 기준이라 재작성 필요」 — 이미 재작성했는데 옛 상태 |
| **루트 `README.md` 가 없었다** | 저장소를 처음 여는 사람이 볼 입구가 없었다 |

## 2026-08-11 전수조사에서 더 나온 것 — 전부 **돌려서** 나왔다

| 무엇 | 증상 |
|---|---|
| **★ `.env` 의 `HOST=127.0.0.1`** | 서버가 강사 PC 에만 열린다. 쪽지·슬라이드는 LAN 주소를 알려 주는데 **39명 전원 「연결 실패」**. **강사 본인 화면은 200 이라 혼자서는 절대 못 본다.** `.env.example`·배포문서는 `0.0.0.0` 인데 실제 `.env` 만 달랐다 |
| **`httpx` 가 1일차 requirements 에 없었다** | `내번호.py` 첫 명령에서 전원 `ModuleNotFoundError`. **pandas 사고의 재현** |
| **배정장부가 gitignore 안 됨** | 39명 실제 접속 키가 저장소에 올라갈 뻔했다 |
| `config.py` 의 `tenant_mode` 기본값 `both` | `.env` 가 없으면 팀 8개가 딸려 와 개인 배정이 어긋난다 |
| **DB 에 못 닿을 때** | 파이썬 역추적 벽 + 깨진 한글. 강사가 무엇을 해야 할지 알 수 없었다 |
| **포트가 이미 쓰일 때** | `winerror 10048` 만 나왔다 |
| 한글 URL 경로(`/claim/현황`) | curl·스크립트에서 **404**. 강사가 명령줄로 쓰면 막힌다 |
| UTF-8 가드 누락 3곳 | `mcp_server_answer.py` · `agent_bodies.py` · `state_updater.py` |
| 슬라이드가 「쪽지」를 말함 | 배정 도입 뒤에도 4곳이 남아 있었다 |
| **★ `.gitignore` 줄 끝 주석** | `배정장부.json  # 설명` 으로 적으면 **패턴이 통째로 무효**가 된다. 39명 실제 키가 든 장부가 커밋 대상으로 떠 있었다. **패턴 뒤에 주석을 붙이지 말 것** |
| **★ 배포본에 `.내번호` 동봉** | 검증이 만든 파일에 **진짜 접속 키**가 들어가 39명에게 배포될 뻔했다 |
| **★ 배포본 `config.json` 에 실제 키** | 검증이 채운 값이 그대로 남아 학생 전원이 남의 키를 갖게 된다. 뽑을 때 빈 칸으로 되돌린다 |
| 학생 README 가 「`.env` 에 OpenAI 키를 넣으라」 | **절대규칙 7 위반.** 현재 설계는 서버 중계인데 문서만 옛 설계였다 |
| 같은 README 의 「실패하면 **조용히** 규칙으로 대체」 | **절대규칙 6 위반**(조용한 폴백 금지) |
| `--use-answers` 가 학생 `--help` 에 노출 | 실습을 통째로 건너뛸 수 있었다. 숨기고 `--열기` 만 남겼다 |

## 2026-08-11 **통독** 에서 나온 것 — grep 으로는 안 걸리는 것들

패턴 검색이 아니라 **파일을 처음부터 끝까지 읽어서** 나온 것이다.
문서 내부 모순·실제 데이터와의 불일치·쓰지 않는 것 — 전부 여기서 나왔다.

| 무엇 | 왜 심각한가 |
|---|---|
| **★ `1일차/실습/README.md` 가 반전을 통째로 유출** | 「온도 드리프트는 아마 안 잡힐 겁니다」 + 결과 표(0/241 · 3/3 · 0/121)가 그대로. **학생이 실습 전에 열면 1일차 핵심 장면이 사라진다.** 절대규칙 5 정면 위반 |
| **힌트카드가 구현 불가능한 선택지를 제시** | 「결측 자체를 이상으로 본다」 — 뼈대의 `detect()` 가 값이 없는 자리를 무조건 「판정 안 함」으로 넘겨 그대로는 안 된다. 실제로 돌려서 확인함 |
| **인쇄 시트의 절반이 이번 특강에 없는 것** | 4개 절 중 2개가 삭제된 팀 과제(발주처 요구사항 5장 · 확장미션 5장). D-1 에 쓰지도 않을 카드 10장을 인쇄하게 된다 |
| 슬라이드 10장 「마지막 정비가 90일 전」 | 실제 데이터는 **WO-2026-0712(약 한 달 전)** + 진행 중 WO-2026-0801. 학생이 2일차에 조회하면 어긋난다 |
| 2일차 가이드 「사전에 배부된 키」 | 현재는 서버 중계라 학생이 키를 안 받는다 (절대규칙 7) |
| 가이드가 **나오지 않는 문자열**을 안내 | `backend: rules` — 실제 출력은 `원인(rules)` |
| **강사 절차 없는 우회로** 4곳 | 「`transport` 를 `http` 로」 — 강사가 MCP 공용 서버를 띄우는 절차가 어디에도 없다 |
| 1일차 가이드 「`백스테이지/센서데이터` 옆 폴더」 | 학생 배포본엔 그 폴더가 없다(`데이터/`) |
| 「3일 내내 AI에게 시키는데」 | 슬라이드 27장 + 가이드. `3일` 은 데이터 일차와 겹쳐 grep 이 못 잡는다 |
| 학생 브라우저 화면에 **`W1`** | 이틀 내내 보는 화면 제목 |
| 힌트카드 실측 「3초」 | 실측 1~2초 |
| `python3 run.py` 안내 | 윈도우엔 보통 없다. `py` 가 맞다 |

### 강사 전용 코드·문서 통독에서 더 나온 것

| 무엇 | 왜 |
|---|---|
| **제어 403 메시지가 학생에게 불가능한 것을 시킴** | 「강사 콘솔에서 개방하세요」 — **학생 화면에 뜨는 문구**다. 개방은 강사만 할 수 있다 → 「손을 드세요」로 |
| `제어개방.py` 머리말이 **「1일차 오후에 연다」** | 실제로는 **2일차 오후**(슬라이드 24장). 강사가 읽고 하루 일찍 열면 「어제까지는 못 움직였다」가 무너진다 |
| 같은 파일이 **없어진 쪽지 절차**를 안내 | 실행할 때마다 화면에 뜬다 |
| `--inject-all` 안내에 **`--stagger 2` 누락** | 핸드오프가 필수라고 못 박은 옵션인데 도구 자신은 빼고 안내했다 |
| `read.py` 주석 「정본은 학생이 Day 2 에 만드는 테이블」 | 이번 설계와 **반대**다. 공용 정비 이력이 정본이다 |
| **API 명세의 「학생 = `T1`~`T8`」** | 이어받는 사람이 학생 네임스페이스를 팀으로 오해한다 |
| 시뮬레이터 README 가 **4일 과정·팀 8개·요구사항 카드** 전제 | 16곳 |
| **최후의 대비책을 강사가 모름** | `tools/fallback/state_updater.py` + `web/fallback/` 이 이미 있는데 **어느 강사 문서에도 안내가 없었다.** 네트워크가 막히는 최악의 순간에 못 쓴다 → `서버운영.md` 7절 신설 |

| **옛 코드명 잔재 95곳** | `W1`·`W5`·`W6`·`Lab 4-1`·`Day 2` 같은 **제작 단계의 내부 이름**. 학생이 여는 자리(정답본 첫 줄·`mcp_server.py` 주석·폐루프 README)에도 있었다. 이어받는 사람도 강사도 무엇을 가리키는지 알 수 없다 → **전량을 사람 말로 치환**(`W5`→`7일치 CSV`, `W1`→`시뮬레이터`, `W6`→`폐루프`, `Day 2`→`2일차`), 남은 것 **0건** |
| 폐루프 verify 머리말에 **삭제된 확장 미션** | 「에이전트를 하나 더 붙인다」가 아직 남아 있었다 |
| **★ 강사 콘솔의 「이 팀에 시연 주입」이 영영 안 눌린다** | 대상 목록을 `tenant_type === 'team'` 으로 걸러 채우는데 이번 특강은 개인 단위라 **목록이 항상 비어 있다.** 2일차 오후에 강사가 누를 원클릭 버튼이 「팀을 고르세요」만 뱉는다. `* 전체(39명 동시)` 로 바꿈 |
| **★ 끝난 주입이 영원히 「진행 중」으로 남았다** | `expire_injections()` 를 **아무도 부르지 않았다.** 물리는 `is_live()` 가 알아서 멈추지만 표시가 안 바뀐다. 그 탓에 **「제어는 열렸는데 주입이 없습니다」 경고가 영영 안 뜬다** — 학생이 감지할 대상이 사라진 바로 그 순간에 침묵한다. 러너 적재 주기에 연결하고 `selftest_engine.py` 11절로 회귀 검사까지 걸었다 (라이브 확인: 주입 후 5초 만에 사라짐) |
| 콘솔 배속 안내가 **자기모순** | 「×120 은 팀 모드(8개)에서만 쓴다」면서 바로 위 원클릭 버튼은 39명에게 ×120 을 건다. 실측은 **39명 ×120 통과**(617행/초 · 실패 0 · p95 41ms). 부하 문서의 「실제 운용은 8개라 더 가볍다」도 반대로 적혀 있었다 |
| **학생 화면에 「넷째 날에 엽니다」** | 공장 뷰의 제어 통로 칸. 이틀 과정인데 1일차 내내 보인다 |
| 배포 절차서가 **쪽지를 주 경로로** 안내 | 「학생 쪽지는 `키배포표.py` 로 뽑는다」 · 클라우드 전환 시 「재인쇄 → 배부」. 자기배정으로 바꾼 결정과 어긋나 절차서를 따라간 강사는 쪽지를 인쇄하게 된다 |
| 강사 우회 문서의 **복구 문법이 틀렸다** | 「키를 잃어버림 → 키배포표」. 실제 복구는 학생이 `python 내번호.py S07` 을 치는 것이다 |
| **리허설이 지난 실행 찌꺼기로 실패** | 앞선 실행이 남긴 배속 120·제어 개방 상태에서 시작하면 3건이 실패한다. D-3 에 무인으로 도는 하네스인데 **강사는 그것이 진짜 고장인지 찌꺼기인지 구분할 수 없다.** 이제 출발선을 스스로 되돌리고 무엇을 되돌렸는지 말한다 |
| **★ `004_grants.sql` 은 존재하지 않는다** | `db/always/900_grants.sql` 로 옮겨졌는데 **9곳이 옛 이름을 가리켰다.** 그중 하나가 `check_grants.py` 의 **실패 안내**다 — 권한이 뚫렸을 때 강사가 없는 파일을 찾게 된다. 절차서의 「기대 출력」도 실제 마이그레이션 목록과 달랐다 |
| `.env.example` 이 **문서가 정한 값과 다름** | 문서·`.env` 는 `RETENTION_HOURS=1`(무료 500MB 에 시간당 180MB), 예시 파일만 `2`. 클라우드 VM 을 새로 올리면 **한도에 부딪힌다.** 코드 기본값(2.0)도 1.0/180초로 내렸다 — `.env` 가 없어도 안전하도록 |
| 두 문서가 **학생이 Supabase 를 만든다**고 서술 | 4일 과정 잔재. 이번엔 학생이 프로젝트를 만들지 않는다 |
| 절차서 「엔진 자체검증 48개 항목」 | 실제 53개 |
| 폴백 `state_updater.py` 가 **역추적을 뱉음** | 다 무너졌을 때 꺼내는 마지막 수단인데 DB 실패에 사람 말이 없었다. `run.py` 와 같은 안내로 통일 (실제로 확인) |
| **★ 진단 사용량 창구가 인증 없이 열려 있었다** | `GET /api/v1/diagnose/usage` — **토큰 없이 200.** 학생 누구나 39명 전원의 호출 횟수를 본다. 「쟤는 몇 번 돌렸네」가 되는 순간 개인 실습이 비교가 된다. 강사 토큰을 걸었다 (토큰 없음·틀림 → 401 확인) |
| 그 창구를 **아무도 안 불렀다** | 돈이 나가는 호출인데 강사가 확인할 방법이 어느 문서에도 없었다 → `서버운영.md` 4절에 넣음 |
| **★ 강사용 README 2개가 학생에게 배포됐다** | `당일/일차별안내/1일차.md`·`2일차.md` 는 슬라이드 번호와 「강사가 손대는 자리」가 적힌 강사 문서인데 배포본에 그대로 들어갔다. 배포본엔 `강의자료/` 가 없어 가리키는 것이 다 없고, 맨 아래에서 **없는 `../당일/진행표.md` 를 보라고 한다.** 학생 안내는 최상위 `README.md` 가 이미 한다 → 배포에서 제외 |
| `서버운영.md` §3 제목이 **「쪽지 만들기」** | 내용은 「쪽지 안 씁니다」인데 제목은 반대였다. 자기배정 중심으로 다시 씀 |
| 시뮬레이터 README 의 마이그레이션 목록 | `005` 가 두 번, `007` 누락, `db/always` 설명 없음 |

### 마지막 통독 — 데이터·진행표·힌트

| 무엇 | 왜 |
|---|---|
| **★ 데이터 검증기가 아예 안 돌았다** | `센서데이터/verify.py`·`generate.py` 의 경로가 **`강사용/강사용/시뮬레이터`** 로 겹쳐 `FileNotFoundError` 로 즉사. **7일치 CSV 가 강의 서사와 맞는지 증명하는 유일한 도구**인데 실행 자체가 불가능했다. 실습 파일에서 세 번 나온 경로 버그와 같은 계열이라 **위로 올라가며 찾는 방식**으로 바꿨다 → 이제 **전 항목 통과** (80℃ 미도달 · 드리프트 미탐 · 스파이크 3/3 · k=2 오탐 19.9배 전부 실측 확인) |
| **★ 힌트 카드가 3장인데 진행표는 1장만 안내** | 인쇄물·`힌트카드.md` 는 **15/25/35분 3장**인데 진행표에는 「15분 뒤 카드 1 공개」뿐. 강사가 그대로 따르면 **카드 2·3 이 안 나간다** — TODO 2(0으로 나누기)·TODO 3(결측)에서 막힌 학생이 그대로 남는다. 교안이 *"컴공 전공생 사이에서 만족도가 가장 크게 갈리는 지점"* 이라 못박은 자리다 |
| 진행표의 리허설 안내가 실측과 다름 | 「60항목·약 6분」 → 실제 **73항목·10분 38초**. 시점도 D-7 로 적혀 핸드오프(D-3)와 어긋났고, `--부하` 가 **실제 모델을 39번 부른다(비용)** 는 말이 없었다. `서버운영.md` 는 리허설을 **두 번** 돌리게 시켰다(`--부하` 가 상위집합인데) |
| 2일차 오전 게이트가 **서버 없이 조용히 실패** | 정비 이력·작업지시는 강사 서버에서 온다. 서버가 꺼져 있으면 3항목이 「실패」로 떠 **강사가 템플릿이 깨진 줄 안다** → 먼저 짚어 주고 켜는 법을 알려 준다 (실제로 확인) |
| `control/commands` 가 키 없이 열림 | 실수가 아니라 결정(담기는 것이 명령·rpm 뿐, 잠그면 검증 하네스 3벌이 키를 들고 다녀야 함)인데 **아무 데도 안 적혀 있었다** → 코드에 근거를 남김 |
| 기계 치환이 만든 겹말 14개 파일 | 「시뮬레이터 시뮬레이터」·「1일차 의」 등. 내가 만든 것이라 내가 지웠다 |
| **★ 누출 검사가 `.py` 를 안 봤다** | 배포본 유출 검사가 `.md/.json/.txt` 만 훑었다. **학생이 여는 것은 대부분 `.py` 인데** 거기 박힌 OpenAI 키·DB 문자열·강사 토큰은 통과했다. 「비밀정보 0건」이 사실이 아닐 수 있었다는 뜻이다 → `.py/.html/.bat/.cfg/.ini` 까지 확대하고, **가짜 키를 실제로 심어 잡히는 것을 확인**했다. 의도해서 남긴 예외(`loop.py` 의 `--use-answers`)만 근거와 함께 명시적으로 허용 |
### 학생 워크스루 — 가이드 문장만 보고 맨땅에서 따라가 본 것

배포본을 깨끗한 폴더에 받아 놓고, **아는 것을 쓰지 않고 문서에 적힌 명령만** 순서대로 쳤다.
학생 콘솔 그대로(cp949 · UTF-8 강제 없음).

| 무엇 | 왜 |
|---|---|
| **★ 첫 명령이 학생 PC 에서 실패한다** | `cd %USERPROFILE%\Desktop` — cmd 문법이라 **VS Code 기본 터미널(PowerShell)에서 안 먹는다.** 더 나쁜 것은, 이 PC 의 바탕화면 실제 경로가 **`C:\Users\...\OneDrive\바탕 화면`** 이라 `cd Desktop` 도 `cd ~\Desktop` 도 **둘 다 실패**했다. 학교 PC 에 OneDrive 가 걸려 있으면 39명 전원이 첫 줄에서 막힌다 → **`cd ~` 로 바꿈**(어느 셸·어느 PC 에서도 됨). 사전안내문 ⑦ 의 긴 경로도 `cd 1일차/실습` 으로 바꿈 |
| **★ `127.0.0.1` 을 넣으면 조용히 통과했다** | `내번호.py` 에 자기 컴퓨터 주소를 넣어도 그대로 `config.json` 에 박힌다. 그러면 **다음날 오전 진단 리포트에 원인 추정이 안 나오고 학생은 자기 코드 탓이라고 생각한다.** 가이드에 경고가 있지만 읽을 때는 이미 늦다 → 스크립트가 되묻는다(정상 주소는 조용히 통과 · 확인함) |
| 2일차 가이드가 **원인을 잘못 지목** | 「`127.0.0.1` 이면 내번호.py 를 안 돌린 것」 — 돌렸어도 주소를 잘못 넣으면 그렇게 된다. 고치는 법까지 적었다 |
| **★ 주입은 150초면 끝난다 — 순서를 틀리면 39명이 아무것도 못 본다** | 워크스루에서 주입을 먼저 하고 루프를 나중에 돌렸더니 **54회차(9분)까지 「감지 이상 없음」만** 나왔다. 드리프트가 가상 5시간(램프 4h + 유지 1h)이라 **배속 x120 에서 실제 150초면 끝나고**, 끝나면 온도가 5초 만에 돌아간다. 진행표는 「전원이 돌린 것을 확인한 뒤 주입」이라고 이미 적어 뒀지만 **왜 그래야 하는지와 창의 크기가 어디에도 없었다** → 진행표·서버운영·강사우회 세 곳에 표로 명시. 늦은 학생은 `--inject S07` 로 개별 재주입 |
| `점검.py --열기` 를 **두 번 누르면** | 「TODO 자리를 못 찾았습니다」 — 파일이 깨진 것처럼 읽힌다. 흔한 실수다 → 「이미 채워져 있습니다」로 갈라서 말함 |

**완주 확인** — 1일차는 `pip install` → `내번호.py` → 공장 화면(설비 6 · 로봇 2 · 3초 뒤 6/6 값 변동)
→ `점검.py` → `--힌트` → `--열기` → `run.py` → 임계값 3회. **가이드에 적힌 숫자와 실제 출력이 전부 일치**
(0.94% · 567행 · 하루 81번 · k=2 에서 911번 · 스파이크 3/3 · 드리프트 미탐 · 결측 0/120).
2일차 오전은 config 자동 채움 → `--check` → 도구 2개 → `WO-2026-0801` 확인까지.
`--열기` 는 파일을 고치지 않고 실행 중에만 채우므로 **두 번 눌러도 안전**하고,
기다리는 동안 10초마다 「N회차 · 감지 이상 없음」이 찍혀 멈춘 것처럼 보이지 않는다.

| **`--전부풀기` 에 잠금이 없었다** | 문서 네 곳이 「2일차 아침엔 하지 마세요」라고 경고하는데 도구엔 확인이 없었다. 2일차 아침에 누르면 **39명이 어제 공장을 잃고 다른 번호를 받는다** → 이미 나간 배정이 있으면 멈추고 `--예` 를 요구한다 (실제로 막히는 것 확인) |

**팀 네임스페이스 정리 — 적용 완료 (2026-08-11)**

`db/migrations/007_drop_teams.sql` 을 만들고 `python tools/migrate.py up` 으로 **반영했습니다.**

무엇이었나 — DB 에 팀 8개(T1~T8)가 남아 있었습니다. 학생 화면에는 안 떴지만
강사 콘솔의 네임스페이스 표에 **47행**이 뜨고, 대상 고르기 목록에 T1~T8 이 섞여
잘못 고르면 404 만 돌아왔습니다. `migrate status` 도 「individual 39개, team 8개」라고
보고해 이어받는 사람이 팀도 쓰는 과정으로 오해했습니다. 003 시드가 4일 과정 것이라
**새로 배포해도 다시 생기므로**, 003 을 고치지 않고 007 로 되돌립니다.

적용 뒤 실측 — `migrate status` 「individual 39개」 · 강사 콘솔 표 **39행** ·
학생 목록 39개(T* 0) · 서버 경고 0 · 권한 검증 3항목 통과.

팀을 다시 쓸 일이 생기면 `003_seed_tenants.sql` 을 그대로 다시 돌리면 복구됩니다.

> 참고 — `005_virtual_clock.sql` 의 주석 한 줄(없는 파일명)을 고쳐 체크섬이 어긋났고,
> SQL 본문이 동일함을 확인한 뒤 대장을 갱신했습니다. `status` 는 다시 「적용됨」입니다.

**죽은 코드는 없다** — 파이썬 전 파일을 참조 관계로 훑어 「아무도 안 부르는 것」 0건.
`fallback/` 둘은 죽은 것이 아니라 **강의장이 포트를 막았을 때의 진짜 대비책**이다.
땜빵 표시(FIXME·HACK·임시)도 0건, 조용히 예외를 삼키는 자리도 없다
(잡힌 것은 전부 UTF-8 가드 안의 정당한 처리).

**내가 만든 회귀도 통독에서 잡혔다** — 앞선 조사에서 「보존정책 때문에 600샘플은 도달 불가」라고
잘못 판단해 리허설 기준을 56샘플로 낮췄다. 실제로 재 보니 **배속 120 에서 3분에 388샘플**,
5분이면 600 을 넘는다(보존이 **가상 시각** 기준이라 배속이 높으면 사실상 안 지운다).
그 사이 리허설은 **이 하네스의 존재 이유인 「창 희석 결함」을 못 잡는 상태**였다.
기준을 되돌렸고, 못 채우면 통과가 아니라 **건너뜀**으로 처리한다.

**다시는 조용히 지나가지 않게 만든 것**

- 서버가 loopback 에 묶이면 **기동 배너** + `/status` 경고 + **리허설이 LAN 왕복까지 실제로 확인**
- 팀 네임스페이스가 살아 있으면 같은 방식으로 경고
- DB·포트 실패는 **사람이 읽을 말**로 세운다 (역추적 없음, 25초 안에 끝)
- 리허설에 **자리 배정 8항목**이 상시 검사로 들어갔다

### study 전체 실사 · 정리 (2026-08-11)

`D:\work\study` 아래 전부를 열어 보고 어긋난 것을 맞췄다.

| 무엇 | 왜 |
|---|---|
| **★ `설명대본.md` 이 4일 과정 기준이었다** | 4일차를 **팀전·발표**로 설명하는데 절대규칙은 개인 단위·발표 없음. 공장 47개(팀 8 포함), 학생이 fork 해서 자기 저장소를 만든다, 정답은 학생용에 없다, 리허설 39항목 10분 — **전부 지금과 다르다.** 이 대본으로 설명하면 틀린 말을 하게 된다 → 현재 설계로 다시 씀 (슬라이드 원고와 13항목 대조) |
| 최상위 `README.md` 가 **없는 폴더 셋을 안내** | `경남대특강_개인/` · `_lab/` · `산출물/` — 셋 다 존재하지 않는다 |
| 최상위 `배포본/` 이 중복 | `경남대특강/배포본/` 과 같은 것. 제거 |
| **★ 강의안이 슬라이드 원고와 달랐다** | 「에이전트는 넷」을 **모델·도구·관측·기억**이라 썼는데 원고는 **뇌(LLM)·도구·루프·자율성**. 「패턴 다섯」도 원고는 **프롬프트 체이닝·라우팅·병렬 처리·총괄과 담당·검증과 되짚기**. EQ-05 온도 75→74도. **원고를 안 보고 기억으로 쓴 결과다** → 원고 기준으로 전면 교정 후 **42항목 대조 전부 통과** |
| 강의안 일차 표기가 **2·3일차** | 저장소·코드·진행표가 전부 1·2일차인데 강의안만 달랐다 → 1일차·2일차로 통일 |
| **★ 진행표에 노션 1교시가 없었다** | 강사가 당일 여는 유일한 문서인데 **60분짜리 첫 세션이 통째로 빠져** 있었다 → 절 신설 |
| 강의안에 **힌트카드가 한 장만** | 진행표는 15·25·35분 세 장인데 강의안은 25분만 → 표로 맞춤 |

**슬라이드는 동기화돼 있었다** — 1일차 36장·2일차 37장을 다시 렌더링해 **픽셀 단위로 비교**했고
전부 동일했다. 원고와 PNG 가 어긋난 적은 없다.

### 정리 — 안 쓰는 것 제거 · 자리 잡기 (2026-08-11)

| 무엇 | 어떻게 |
|---|---|
| `__pycache__` 13개 · `.pyc` 30여 개 | 제거 |
| 실행 흔적 (`배정장부.json` · `1일차덱.html` · `2일차덱.html`) | 제거 — 돌리면 다시 생긴다 |
| `배포본/` 산출물 | 제거 — `배포본만들기.py` 가 다시 만든다 |
| **강의안 폴더에 md·옛 docx·남의 문서가 섞여 있었다** | **제출물 docx 2개만** 남기고 원본 md 는 `제작/강의안원본/`, 앞 세션 실라버스는 `제작/참고자료/외부자료/` 로 |
| **학생배포 최상위 README 가 강사용이었다** | 진행표·핸드오프·절대규칙·`cd 강사용` 이 들어 있었다. **학생이 clone 하면 이걸 본다** → 학생용으로 다시 씀 |
| 학생배포 안의 일차별 README 2개 | 슬라이드 번호와 「강사가 손대는 자리」가 적힌 강사 문서 → `당일/일차별안내/` 로 옮기고 새 구조·노션 반영해 다시 씀 |
| 저장소 밖에 있던 노션 pptx · 설명대본 | `당일/` · `제작/참고자료/` 로 들여옴. **study 최상위는 `경남대특강/` 과 `.env` 만** |
| 과거 기록 2건 (`가이드검증_리포트` · `00_전수조사_확인기록`) | 4일 과정 시절 것이라 **「과거 기록」 표시**를 머리에 달았다. 내용은 이력이라 안 고친다 |
| `참고자료/` 에 README 가 없었다 | 뭐가 우리 것이고 뭐가 발주처 것인지 구분이 안 됐다 → 표로 정리 + 「그때와 지금이 무엇이 다른가」 |
| 옛 경로 `cd 강사용` 5곳 | 실제로 못 쓰는 명령이었다 → `cd 백스테이지` |
| `제작/README.md` 의 산출 경로 | `../../1일차/강의자료/` → `../../당일/슬라이드/1일차/` |

**원고·설명란은 덱에서 자동 생성된다** — `emit_docs.py` 를 다시 돌려 4개 파일이
기존과 **바이트까지 동일**함을 확인했다. 덱을 고치면 따라 바뀌므로 어긋날 수 없다.

### 학생 자료 전달을 드라이브로 바꿨다 (2026-08-11)

**전에는 `git clone` 이 기본이었다.** 이제 **구글드라이브 ZIP 이 기본**이다.

**왜 바꿨나** — git 은 세 군데서 막힌다. 설치가 안 됐거나, PATH 에 안 잡히거나,
학교망이 GitHub 를 막거나. 39명 중 몇 명은 반드시 걸리고 그 자리가 **첫날 첫 20분**이다.
드라이브는 링크를 누르고 압축을 푸는 것이 전부라 갈릴 데가 없다.

**따라서 바뀐 것**

| 무엇 | 어떻게 |
|---|---|
| `k-precision-lab.zip` (0.9MB) | 저장소 최상위에 만들어 뒀다. **압축 해제 후 `run.py` 실행까지 확인함** |
| 사전안내문 | **8단계 → 6단계.** git 설치·GitHub 가입이 빠졌다 |
| 1일차 실습가이드 §준비물 | ZIP 받아 압축 풀기로 교체 |
| 진행표 「학생이 코드를 받는 법」 | ① 드라이브 ② USB 두 갈래로 (git 제거) |

> **압축을 안 푼 학생이 나온다.** ZIP 안에서 바로 열면 코드가 안 돌아간다.
> 첫 실습에서 이상하면 이것부터 물어볼 것. 세 문서에 다 적어 뒀다.

**ZIP 을 다시 만들려면 (현재 절차)** — `python 제작/검증도구/배포본만들기.py --검증` 으로 뽑은 뒤
`python 제작/빌드도구/드라이브폴더만들기.py` 를 돌린다. ZIP 은 드라이브업로드 폴더 안 하나뿐이다.
수업 중 코드를 고쳤으면 새 ZIP 을 올리고 다시 받게 한다.

### 복붙용 안내문자를 만들었다

`특강/당일/안내문/복붙용_안내문자.md` — 단톡방에 **그대로 붙여넣는** 것이다. 다섯 종.

D-7 사전 준비 · D-1 리마인드 · 당일 아침 서버 주소 · 1일차 마칠 때 당부 ·
2일차 제출 안내. 그리고 **자주 오는 질문 답변**(pip 없음 · ZIP 어디에 ·
회사 노트북 · 준비 못 함)까지 붙였다.

실제 자료와 **11개 항목을 대조**해 문구가 어긋나지 않는 것을 확인했다.
`[ ]` 안만 채우면 된다 — 남은 것은 **서버 주소·제출처 둘**이고 `특강/당일/안내문/README.md` 에 표로 적어 뒀다.

### 클라우드 배포 경로가 깨져 있었다

`docs/배포_기동_절차서.md` 의 VM 설치 명령이 `산출물/공통/시뮬레이터` 를 가리켰다.
**구조 개편 전 경로**라 그대로 치면 실패한다. 네 곳을 `경남대특강/백스테이지/시뮬레이터` 로 고쳤다.

> **제작 저장소 리모트에 올라간 것도 옛 구조다.** `origin/main` 에
> `1일차`·`2일차`·`강사용`·`제작` 이 들어 있다. **클라우드 VM 이 여기서 clone 하므로
> push 하지 않으면 배포가 성립하지 않는다.**

## 남은 일 — 사실 관계부터

### 이미 정해진 것 (헷갈리지 말 것)

| 무엇 | 확정 상태 |
|---|---|
| **진단 모델** | **OpenAI 로 확정.** 서버가 `OPENAI_API_KEY` 로 중계하고 모델은 `gpt-5.4-mini`. 학생 PC 에 키를 두지 않는다. `config.json` 의 `diagnose.backend` 기본값이 `"server"` 다 |
| **강사 저장소** | `github.com/ahnchiyoon87/knu-agentic-factory` (리모트 `origin`). 강의자료·백스테이지·정답이 다 들어 있다. **클라우드 VM 이 여기서 clone 해 서버를 돌린다. 학생에게 열면 안 된다** |
| **학생 저장소** | `github.com/ahnchiyoon87/k-precision-lab` — **현재 배포본으로 교체 push 완료.** clone 해서 학생 환경으로 완주까지 확인했다 |
| **학생 자료 전달** | **clone 과 드라이브 ZIP 둘 다.** 내용이 같다. ZIP(`경남대특강/k-precision-lab.zip` · 0.85MB)은 압축 해제 후 실행까지 확인했다 |
| **학생 AI 코딩 도구** | **안 쓴다.** 강사가 화면에서 시연하고, 학생은 막히면 `--열기` 로 채운다 |
| **문의 채널** | **카톡 단체방** — 담당자가 초대한다 |

### 실제로 남은 것

| # | 무엇 | 지금 상태 · 무엇을 하면 되나 |
|---|---|---|
| **1** | **드라이브에 파일 올리기** | 폴더와 링크는 확정됐다(`특강/당일/안내문/README.md`). **파일을 아직 안 올렸다** — `제작/산출물/드라이브업로드/` 폴더를 통째로 올리고, 단톡방에 링크를 붙인다 |
| **2** | **강사 저장소에 현재 구조를 push** | 리모트 `origin/main` 이 **옛 구조**(`1일차`·`2일차`·`강사용`·`제작`)다. **클라우드 VM 이 여기서 clone 하므로 push 해야 배포가 성립한다** |
| **3** | **★ 클라우드 VM 을 켜고 최신 코드를 받아야 한다** | **당일 서버는 클라우드로 확정.** 그런데 **지금 VM 이 안 떠 있다**(`34.64.94.16` ConnectTimeout). 켠 뒤 **반드시 `git pull` + `systemctl restart factory`** 를 해야 한다 — VM 은 저장소를 clone 해서 돌기 때문에 push 만으로는 안 바뀐다. **오늘 만든 `agent.py` 창구가 없으면 2일차 오전 22분이 통째로 빈다.** 절차와 확인 명령은 `docs/배포_기동_절차서.md` §13-3-a 에 넣었다 |
| **4** | 제출처 | 노션 링크를 어디로 받을지 |
| **5** | 강의장에서 D-7 재실측 | 지금 수치는 전부 **제작 PC + 로컬 서버** 기준이다. 클라우드로 수업하면 `--base-url` 을 붙여 다시 돌릴 것 |

> **학생 저장소에 올라가 있던 것이 4일 과정 시절 것이었다.** `1~4일차`·`W5`·`W6`·`Lab3-2`,
> 삭제된 확장미션, 실행 찌꺼기(`실행기록.jsonl`), `config.json` 의 `tenant: "T1"` 과
> `anthropic_key_env`·`model: claude-opus-5` 까지 그대로였다. `정답/` 도 `내번호.py` 도 없었다.
> **지금 배포본으로 통째로 교체했다.** 비밀정보는 없었다(전부 placeholder).

---

# 4. 핵심 설계

## 진단 중계 — 학생 PC 에 LLM 키를 두지 않는다

```
학생 PC ──(내 접속 키)──> 강사 서버 ──(OPENAI_API_KEY)──> OpenAI
```

39명에게 키를 나눠 주면 **회수가 안 된다.** 서버가 중계하면 키는 서버 `.env` 에만 있고,
특강이 끝나면 서버만 내리면 된다.

**프롬프트는 서버가 만들지 않는다.** 학생이 고친 프롬프트가 그대로 올라가야 실습이 성립한다.
서버는 중계만 하고, 응답을 스키마로 강제하며 분당 한도를 건다.

**한도** — OpenAI 한도는 **조직 단위**다. 같은 조직의 키를 여러 개 만들어도 늘지 않는다.
다만 우리 부하는 분당 13~25 요청이라 **키 1개로 충분**하다.

## 39명이 몰려도 안 터지는 장치 네 겹

1. 동시 12개로 줄 세우기 (`DIAGNOSE_CONCURRENCY=12`) — 20으로 올려도 개선 없음
2. 429 면 1·2·4초 백오프 재시도
3. **설비별 진단 1회 캐시** — 매 회차 다시 부르던 것이 호출 폭증의 주범이었다
4. 주입 시차 — `제어개방.py --inject-all --stagger 2`

## 필수 프롬프트 — 이 세 문장이 39/39 를 만든다

`diagnoser.py` 프롬프트에 반드시 들어간다. **가이드에 원문으로 주고 학생은 복사해 넣는다.**

```
① "이 변화는 지금 작아도 계속 오른다."
   → 없으면 "조금 올랐네요, 지켜보죠"라며 아무 조치도 안 낸다

② "원인이 정비 보류·지연이면 감속과 함께 로봇 파견 요청까지 낼 것.
   파견은 사람이 승인하므로 요청을 망설이지 말 것."
   → 없으면 감속만 하고 끝나서 승인 화면이 안 뜬다

③ "근거(evidence)에는 작업지시 번호를 그대로 인용할 것."
   → 없으면 WO 인용이 약 95% 로 떨어진다. 39명 중 2명이 하이라이트를 놓친다
```

~~③ 은 아직 가이드에 없다. 넣어야 한다.~~ → **세 문장 모두 2일차 실습가이드 §2-2 에 원문으로 들어 있다** (2026-08-12 정독으로 확인).

## 이탈 방지 4단계 — **세 랩 전부에 있다**

```
점검.py            내 코드를 돌려보고 어디가 왜 틀렸는지만 짚는다 (답은 안 준다)
점검.py --힌트 1    개념
점검.py --힌트 2    의사코드
점검.py --열기 N    ★ 시간이 다 됐을 때만. 막힌 함수 하나만 완성본으로
```

| 언제 | 어디서 | 마지막 수단 |
|---|---|---|
| 1일차 | `학생배포/1일차/실습` | `점검.py --열기 1·2·3` (파일을 고치고 원본은 `detect_내가짠것.py` 로) |
| 2일차 오전 | `2일차/실습/도구만들기` | `점검.py --열기 1·2` |
| **2일차 오후** | `2일차/실습/폐루프` | **`loop.py --열기 1·2·3`** (파일을 안 고친다. 옵션을 빼면 내 코드로 돌아온다) |

**2일차 오후에는 원래 탈출구가 없었다.** 마지막 장면(AMR 이 움직이는 것)을 못 보고
끝나는 자리가 거기다. `--use-answers` 는 세 자리를 통째로 덮어 학생 작업을 버리므로
1일차와 같은 규칙(하나만)으로 `loop.py --열기` 를 새로 넣었다.

일부러 흔한 실수 5종을 심어 검증했고 전부 잡았다 —
지금 값 포함 · n 으로 나눔 · abs 누락 · σ=0 미처리 · 결측 제거로 길이 변함.

## 정답본은 학생에게 **같이 나간다**

`--열기` 가 `정답/` 을 읽는다. 빼면 「완성본을 못 찾았습니다」로 끝나고 위 4단계가
3단계에서 끊긴다. 훔쳐볼 수 있다는 대가보다 **3분 막혀서 이탈하는 손해가 크다**는 판단이다.
`--열기` 는 함수 하나만 채우고, 쓴 것을 남겨 두고, "채운 함수를 한 번 읽어 보세요"까지 낸다 —
훔쳐보기 통로가 아니라 회수 장치로 설계돼 있다.

---

# 5. 함정 — 겪고 고친 것들. 다시 밟지 말 것

| 함정 | 내용 |
|---|---|
| **cp949 인코딩** | `—` `→` 출력에서 `UnicodeEncodeError` 로 실습이 **중간에 죽었다.** `리허설.py` 는 `PYTHONUTF8=1` 을 주입해 돌아 안 보였다. **학생은 그 환경변수가 없다.** 진입점 14개에 인라인 UTF-8 가드를 넣었다. **새 진입점을 만들면 반드시 같은 가드를 넣을 것** |
| **pandas 미설치** | 1일차 랩에 `requirements.txt` 가 없었다. 첫 명령에서 전원이 `ModuleNotFoundError`. 리허설은 강사 PC 에 pandas 가 있어 못 잡았다 |
| **`--열기` 후 또 막힘** | 완성본이 쓰는 `import math` 가 없거나, 함수 통째로를 함수 본문 자리에 넣어 들여쓰기가 깨졌다. **마지막 수단을 썼는데 또 막히는** 최악의 경우 |
| **문서만 읽고 산출물을 안 봄** | "개인으로 축소"라고 문서에 썼지만 `config` 의 `tenant: "T1"` 은 그대로였다. **결정을 내렸으면 파일까지 따라갈 것** |
| **읽기만 하고 실행 안 함** | 위 셋은 전부 **처음 실제로 돌려본 순간** 나왔다 |
| **조용한 폴백** | 진단 실패 시 규칙으로 조용히 대체하면 학생은 **AI 진단을 못 봤다는 것 자체를 모른다** |

---

# 6. 자주 쓰는 명령

## 당일 (강사)

```bash
cd 특강/시뮬레이터
python -m server.run                                # 서버 (시작.bat 도 가능)
python tools/제어개방.py                              # 2일차 오후 제어 열기
python tools/제어개방.py --inject-all --stagger 2     # 39명에게 주입
python tools/제어개방.py --end                        # 되돌리기
python tools/키배포표.py                              # 예비 — 자기배정이 막혔을 때만
```

## 사전 점검 (D-7 ~ D-3)

```bash
cd 제작/검증도구
python 리허설.py --token <강사토큰> --부하    이틀을 사람 없이 재생 — 75항목·약 11분
                                              (--부하 는 실제 모델을 39번 부릅니다 = 비용)
python 배포본만들기.py --검증                   학생에게 줄 저장소를 뽑고 학생 조건으로 돌려 본다 (27항목)
```

`리허설.py` 는 **학생 환경 그대로** 돌린다 — `PYTHONUTF8` 을 일부러 벗긴다.
예전 리허설이 이걸 주입해 돌아서 cp949 결함이 39/39 통과 뒤에 숨어 있었다.
**새 진입점을 만들면 UTF-8 가드를 넣고, 리허설은 절대 UTF8 을 주입하지 말 것.**

```bash
cd 특강/시뮬레이터
python tools/부하테스트_진단.py --n 39 --stagger 2
```
**39/39 성공 · 429 0건**이 나와야 한다. WO 인용률이 39/39 가 아니면 프롬프트 세 문장을 확인한다.

## 학생 실습

```bash
cd 1일차/실습
pip install -r requirements.txt
python 점검.py · python run.py · python run.py --k 2.0

cd 2일차/실습/도구만들기
python 점검.py · python mcp_server.py --check
cd ../폐루프 && python loop.py            (막히면 python loop.py --열기 1)
```

## 학생 저장소 — 손으로 만들지 말 것

```bash
cd 제작/검증도구 && python 배포본만들기.py --검증
```

한 번 손으로 만들어 뒀다가 구조가 바뀌었는데 안 따라와서 통째로 어긋난 적이 있다.
**항상 여기서 다시 뽑는다.** 강의자료 PNG·`verify_lab.py`·찌꺼기는 빠지고,
`정답/` 은 들어가고, 데이터는 `데이터/` 로 들어간다. `--검증` 은 학생 조건(cp949)으로
**27개 항목**을 **실제로 돌려** 본다.

**`.git` 은 지우지 않는다.** 한 번 push 한 뒤 다시 뽑아도 이력·리모트가 살아 있어서
`git add -A && git commit && git push` 로 동기화가 끝난다.

## 번호는 학생이 직접 받는다 — 종이 쪽지를 쓰지 않는다

```
학생 PC ──(무작위 번호표)──> 서버 ──> 아직 안 나간 공장 하나를 붙여 준다
                                     번호 · 접속 키 · 서버 주소를
                                     **2일차 config 까지 자동으로 채운다**
```

`1일차/실습/내번호.py` 한 줄이면 끝난다. 학생이 손으로 옮겨 적을 것이 없다.
**기억할 것은 화면에 뜨는 번호 하나(예: S07)뿐이다.**

| 상황 | 어떻게 되나 |
|---|---|
| 여러 번 실행 | **항상 같은 번호.** 수시로 확인해도 소모되지 않는다 |
| 자리를 옮김 · 노트북 교체 | `python 내번호.py S07` — 그 공장을 그대로 되찾는다 |
| 저장소를 다시 clone | 번호표가 사라진다 → 위와 같이 번호로 되찾는다 |
| 서버 재시작 | **배정이 그대로다** (`배정장부.json` 에 남는다) |
| 장부 파일이 깨짐 | 원본을 `배정장부.깨짐.json` 으로 남기고 새로 시작. 수업이 안 멈춘다 |
| 남는 공장이 없음 | 강사가 `python tools/자리배정.py --풀기 S07` |

**MAC 주소·컴퓨터 이름을 쓰지 않는 이유** — MAC 은 못 읽는 PC 에서 매번 다른 값이 나오고,
실습실 복제 PC 는 컴퓨터 이름이 같을 수 있다. 학생 PC 가 만든 무작위 번호표는 그런 실패가 없다.

**강사 도구** — `python tools/자리배정.py` (현황) · `--풀기 S07` · `--전부풀기 --예`
이미 나간 배정이 있으면 `--전부풀기` 는 **확인을 묻고 멈춥니다** (2일차 아침 사고 방지). `--예` 를 붙여야 실제로 지웁니다.
**1일차 아침에만** `--전부풀기` 를 한다. **2일차 아침에는 절대 하지 말 것** —
학생들이 어제 받은 번호를 그대로 써야 한다.

## 이틀을 잇는 고리 — 여기가 끊기면 2일차가 통째로 막힌다

```
전날    clone 또는 드라이브 ZIP → k-precision-lab

1일차   ① python 내번호.py — 내 공장을 받고, 그 화면의 주소로 공장을 연다
        1일차/실습 의 detect.py 빈칸 3개를 채운다
                    │
                    │  ← ★ 고리 1. 2일차 오전이 이 파일을 **그대로 import 한다**
                    ▼
2일차   오전  2일차/실습/도구만들기 — 어제 코드를 AI 도구로 내놓는다
        오후  2일차/실습/폐루프 — 에이전트 3개 → AMR 이 움직인다
                    ▲
                    │  ← ★ 고리 2. 내번호.py 가 채워 둔 config(번호·키·주소)를 그대로 쓴다
```

| 고리 | 끊기는 경우 | 지금은 |
|---|---|---|
| **1일차 폴더** | 저장소를 다시 받거나 폴더를 옮긴 학생 | `점검.py`·`mcp_server.py` 가 **역추적 대신** 어디를 봤는지·무엇을 해야 하는지 말한다 |
| **내 번호** | 캐시를 비우거나 노트북을 바꿔 오면 잃는다 | `.내번호` 파일이 저장소 안에 남고, 잃어도 `python 내번호.py` 를 다시 돌리면 같은 번호가 나온다. 노트북을 바꿨으면 `python 내번호.py S07` |

**1일차 마칠 때 두 문장을 말할 것** — "내 번호 적어 두세요", "1일차 폴더 지우지 마세요".
진행표 맨 위에 이틀 전체 흐름표가 있다.

## 학생은 저장소를 만들지 않는다 — 받아서 고치기만 한다

이틀 동안 **코드를 올리는 단계가 한 번도 없다.** 제출은 노션 링크다.
fork·개인 레포를 시키면 39명이 GitHub 계정 문제로 갈린다. 그게 제일 큰 이탈 위험이다.

| 경로 | 언제 | 무엇 |
|---|---|---|
| **① `git clone` 또는 드라이브 ZIP** | 사전안내문 ③ 으로 **미리** (내용 동일) | `k-precision-lab` |
| **② USB** (예비) | 당일, ① 이 막힌 학생 | 폴더째 복사. **강사가 USB 를 챙긴다** |

한글 폴더명이 깨지지 않는 것은 확인했다 — `git clone` · PowerShell zip ·
`git archive`(GitHub 「Download ZIP」) 셋 다 손상 0.

## 자료 고치기

```bash
cd 제작/덱빌드
python build_1일차.py && python build_2일차.py && python emit_docs.py
python render.py 1일차덱 && python render.py 2일차덱
```
PNG 는 `특강/당일/슬라이드/1일차/` · `특강/당일/슬라이드/2일차/` 로 떨어진다.
**PNG·HTML·원고·설명란을 손으로 고치지 말 것** — 전부 `build_*.py` 에서 나온다.

**슬라이드 글자도 산출물이다.** 폴더 구조나 쪽지 항목을 바꾸면 `build_*.py` 를 같이 고쳐야
한다. 실제로 한 번 놓쳤다 — 폴더를 1일차/2일차로 재편했는데 슬라이드는
「3일차 폴더의 이상감지 를 엽니다」를 39명에게 계속 띄우고 있었다.

## 당일 강사가 보는 것

`특강/당일/진행표.md` **하나다.** 슬라이드 번호로 적혀 있다 —
넘기다가 ★ 가 나오면 거기 적힌 것을 누르거나 말하면 된다.

| | 강사가 손대는 자리 (증량 후 42·44장 기준) |
|---|---|
| 1일차 | **11장**(반전에서 한 박자) · **18장**(`내번호.py` 안내) · **21장 주입** · **37장 "다 같이 엔터"** |
| 2일차 | **22·35장**(폴더 안내) · **24장 "다 같이"** · **29장 제어 개방** · **38장 주입** |

나머지 장은 넘기기만 한다. **대본은 없다** — 슬라이드 문장을 그대로 읽으면 뜻이 통한다.

## `.env`

`특강/시뮬레이터/.env` **하나뿐이다.** 루트 `.env` 는 이 프로젝트가 읽지 않는다.
필요 항목 — `SUPABASE_DB_URL` · `INSTRUCTOR_TOKEN` · `OPENAI_API_KEY` ·
`TENANT_MODE=individual` · `DIAGNOSE_CONCURRENCY=12` · `DIAGNOSE_PER_MIN=12`
**커밋 금지.**

---

# 7. 놓치면 안 되는 것

- **문서를 읽었다고 확인한 것이 아니다.** 돌려봐야 안다. 치명적 결함 셋이 전부 실행에서 나왔다
- **결정을 내렸으면 파일까지 따라가라.** "개인으로 바꾼다"고 쓰고 config 는 그대로 두면 당일 터진다
- **반전 유출 금지.** 학생이 `run.py` 결과로 발견한다
- **39명 전원이 같은 장면을 본다.** 여기서 갈리면 실습 실패다
